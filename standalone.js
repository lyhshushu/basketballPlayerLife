/* 完美球员 · 单文件版（由 tools/build_standalone.mjs 生成） */
(function () {
  window.__BL = window.__BL || {};
  var __M = window.__BL;
  window.__POOL_INLINE = {"ATL":[{"name":"Jalen Johnson","cname":"杰伦-约翰逊","pos":"PF / SF","height":"6'8'","type":"Mid-Post Playmaking Cleaner","ovr":89,"threePT":77,"MID":88,"FIN":83,"DNK":96,"HAN":80,"PAS":83,"PDEF":66,"IDEF":66,"BLK":51,"REB":89,"ATH":76,"STR":68,"CLU":90,"nbaId":1630552},{"name":"Nickeil Alexander-Walker","cname":"亚历山大-沃克","pos":"SG / PG","height":"6'5'","type":"3 & D Two Guard","ovr":83,"threePT":86,"MID":79,"FIN":78,"DNK":60,"HAN":88,"PAS":75,"PDEF":84,"IDEF":52,"BLK":55,"REB":48,"ATH":87,"STR":60,"CLU":85,"nbaId":1629638},{"name":"Dyson Daniels","cname":"戴森-丹尼尔斯","pos":"SF / SG","height":"6'7'","type":"Defensive Playmaker","ovr":83,"threePT":50,"MID":68,"FIN":90,"DNK":75,"HAN":85,"PAS":77,"PDEF":94,"IDEF":67,"BLK":51,"REB":62,"ATH":93,"STR":52,"CLU":40,"nbaId":1630700},{"name":"CJ McCollum","cname":"CJ-麦科勒姆","pos":"PG / SG","height":"6'3'","type":"Crafty Iso Sniper","ovr":83,"threePT":82,"MID":88,"FIN":96,"DNK":40,"HAN":91,"PAS":72,"PDEF":70,"IDEF":35,"BLK":52,"REB":47,"ATH":84,"STR":60,"CLU":85,"nbaId":203468},{"name":"Onyeka Okongwu","cname":"奥涅卡-奥孔古","pos":"C","height":"6'10'","type":"3-Level Scorer","ovr":82,"threePT":81,"MID":88,"FIN":92,"DNK":85,"HAN":80,"PAS":51,"PDEF":64,"IDEF":79,"BLK":72,"REB":77,"ATH":74,"STR":70,"CLU":85,"nbaId":1630168},{"name":"Jock Landale","cname":"乔克-兰代尔","pos":"C","height":"6'11'","type":"Post-Scoring Stretch Crasher","ovr":78,"threePT":82,"MID":72,"FIN":98,"DNK":65,"HAN":70,"PAS":57,"PDEF":48,"IDEF":71,"BLK":62,"REB":63,"ATH":54,"STR":64,"CLU":80,"nbaId":1629111},{"name":"Zaccharie Risacher","cname":"扎卡里-里萨谢","pos":"SF / SG","height":"6'8'","type":"Crafty 3 & D Creator","ovr":77,"threePT":81,"MID":65,"FIN":90,"DNK":70,"HAN":95,"PAS":68,"PDEF":79,"IDEF":57,"BLK":62,"REB":59,"ATH":82,"STR":56,"CLU":65,"nbaId":1642258},{"name":"Devin Carter","cname":"德文-卡特","pos":"PG / SG","height":"6'2'","type":"Blow-By High Flyer","ovr":77,"threePT":73,"MID":83,"FIN":82,"DNK":80,"HAN":95,"PAS":80,"PDEF":73,"IDEF":58,"BLK":50,"REB":63,"ATH":89,"STR":60,"CLU":75,"nbaId":1642269},{"name":"Corey Kispert","cname":"科里-基斯珀特","pos":"SG / SF","height":"6'6'","type":"Crafty Sharpshooter","ovr":76,"threePT":80,"MID":80,"FIN":88,"DNK":60,"HAN":75,"PAS":66,"PDEF":70,"IDEF":54,"BLK":45,"REB":52,"ATH":76,"STR":60,"CLU":80,"nbaId":1630557},{"name":"Buddy Hield","cname":"巴迪-希尔德","pos":"SG / SF","height":"6'4'","type":"3PT-Sniping Creator","ovr":75,"threePT":84,"MID":50,"FIN":97,"DNK":65,"HAN":94,"PAS":70,"PDEF":69,"IDEF":48,"BLK":51,"REB":53,"ATH":78,"STR":41,"CLU":70,"nbaId":1627741},{"name":"Asa Newell","cname":"阿萨-纽厄尔","pos":"PF / C","height":"6'10'","type":"Stretch Rim Wrecker","ovr":75,"threePT":86,"MID":67,"FIN":91,"DNK":85,"HAN":75,"PAS":54,"PDEF":66,"IDEF":70,"BLK":63,"REB":53,"ATH":70,"STR":67,"CLU":70,"nbaId":1642854},{"name":"Mouhamed Gueye","cname":"穆罕默德-古耶","pos":"PF / C","height":"6'11'","type":"Ball-Hawking Rim Wrecker","ovr":74,"threePT":73,"MID":54,"FIN":71,"DNK":80,"HAN":90,"PAS":60,"PDEF":48,"IDEF":60,"BLK":68,"REB":68,"ATH":67,"STR":63,"CLU":70,"nbaId":1631243},{"name":"Tony Bradley","cname":"托尼-布拉德利","pos":"C","height":"6'10'","type":"Crafty Post-Scoring Crasher","ovr":73,"threePT":26,"MID":66,"FIN":84,"DNK":60,"HAN":85,"PAS":44,"PDEF":35,"IDEF":72,"BLK":55,"REB":67,"ATH":54,"STR":82,"CLU":50},{"name":"Gabe Vincent","cname":"盖布-文森特","pos":"PG / SG","height":"6'2'","type":"Playmaking 3PT Creator","ovr":73,"threePT":76,"MID":70,"FIN":63,"DNK":45,"HAN":90,"PAS":78,"PDEF":60,"IDEF":37,"BLK":32,"REB":36,"ATH":73,"STR":40,"CLU":85,"nbaId":1629216},{"name":"Christian Koloko","cname":"克里斯蒂安-科洛科","pos":"C","height":"6'11'","type":"Disruptive Defender","ovr":73,"threePT":29,"MID":60,"FIN":64,"DNK":75,"HAN":70,"PAS":37,"PDEF":37,"IDEF":75,"BLK":92,"REB":60,"ATH":72,"STR":59,"CLU":70}],"BKN":[{"name":"Michael Porter Jr.","cname":"小迈克尔-波特","pos":"SF / PF","height":"6'10'","type":"3PT-Sniping Cleaner","ovr":86,"threePT":84,"MID":74,"FIN":89,"DNK":80,"HAN":80,"PAS":65,"PDEF":64,"IDEF":60,"BLK":42,"REB":80,"ATH":81,"STR":68,"CLU":90,"nbaId":1629008},{"name":"Julius Randle","cname":"朱利叶斯-兰德尔","pos":"PF / C","height":"6'9'","type":"Physical Post Creator","ovr":85,"threePT":73,"MID":79,"FIN":91,"DNK":80,"HAN":85,"PAS":70,"PDEF":73,"IDEF":77,"BLK":40,"REB":67,"ATH":76,"STR":87,"CLU":80,"nbaId":203944},{"name":"Ziaire Williams","cname":"宰伊尔-威廉姆斯","pos":"SF / SG","height":"6'9'","type":"Ball-Hawking Blow-By Ace","ovr":78,"threePT":77,"MID":68,"FIN":78,"DNK":80,"HAN":75,"PAS":50,"PDEF":73,"IDEF":60,"BLK":55,"REB":50,"ATH":87,"STR":40,"CLU":85,"nbaId":1630533},{"name":"Day’Ron Sharpe","cname":"戴龙-夏普","pos":"C","height":"6'9'","type":"Ball Snatcher","ovr":78,"threePT":69,"MID":74,"FIN":90,"DNK":70,"HAN":70,"PAS":62,"PDEF":60,"IDEF":69,"BLK":61,"REB":86,"ATH":61,"STR":79,"CLU":60,"nbaId":1630549},{"name":"Egor Demin","cname":"叶戈尔-德明","pos":"SG / PG","height":"6'8'","type":"Crafty Inside-Out Combo Guard","ovr":78,"threePT":82,"MID":60,"FIN":89,"DNK":65,"HAN":90,"PAS":84,"PDEF":65,"IDEF":47,"BLK":51,"REB":60,"ATH":75,"STR":60,"CLU":80,"nbaId":1642856},{"name":"Josh Minott","cname":"乔什-米诺特","pos":"PF / C","height":"6'8'","type":"Ball-Hawking Stretch Four","ovr":78,"threePT":85,"MID":85,"FIN":82,"DNK":80,"HAN":85,"PAS":62,"PDEF":67,"IDEF":59,"BLK":65,"REB":63,"ATH":76,"STR":62,"CLU":80,"nbaId":1631169},{"name":"Moritz Wagner","cname":"莫里茨-瓦格纳","pos":"C / PF","height":"6'11'","type":"Stretch Five Cleaner","ovr":77,"threePT":75,"MID":84,"FIN":80,"DNK":65,"HAN":70,"PAS":54,"PDEF":40,"IDEF":58,"BLK":32,"REB":83,"ATH":67,"STR":78,"CLU":70,"nbaId":1629021},{"name":"Danny Wolf","cname":"丹尼-沃尔夫","pos":"PF / SF","height":"6'11'","type":"Break-Starting Stretch Four","ovr":77,"threePT":75,"MID":70,"FIN":75,"DNK":70,"HAN":95,"PAS":69,"PDEF":55,"IDEF":58,"BLK":63,"REB":83,"ATH":52,"STR":60,"CLU":85,"nbaId":1642874},{"name":"Keon Ellis","cname":"基恩-埃利斯","pos":"SF / SG","height":"6'4'","type":"2-Way Crafty Finisher","ovr":77,"threePT":80,"MID":60,"FIN":95,"DNK":65,"HAN":85,"PAS":64,"PDEF":84,"IDEF":58,"BLK":70,"REB":42,"ATH":84,"STR":48,"CLU":50,"nbaId":1631165},{"name":"Nolan Traore","cname":"诺兰-特拉奥雷","pos":"PG / SG","height":"6'3'","type":"Speedy Blow-By Ace","ovr":77,"threePT":74,"MID":57,"FIN":83,"DNK":70,"HAN":81,"PAS":79,"PDEF":60,"IDEF":44,"BLK":56,"REB":45,"ATH":85,"STR":56,"CLU":80},{"name":"Noah Clowney","cname":"诺厄-克洛尼","pos":"PF / C","height":"6'10'","type":"Stretch Rim Wrecker","ovr":77,"threePT":76,"MID":43,"FIN":88,"DNK":80,"HAN":85,"PAS":57,"PDEF":57,"IDEF":68,"BLK":62,"REB":64,"ATH":64,"STR":64,"CLU":98,"nbaId":1641730},{"name":"Terance Mann","cname":"特伦斯-曼","pos":"SG / SF","height":"6'6'","type":"Crafty 3-Level Dime-Dropper","ovr":76,"threePT":80,"MID":82,"FIN":82,"DNK":75,"HAN":75,"PAS":79,"PDEF":66,"IDEF":54,"BLK":47,"REB":51,"ATH":73,"STR":50,"CLU":80,"nbaId":1629611},{"name":"Ochai Agbaji","cname":"奥查伊-阿巴吉","pos":"SF / PF","height":"6'5'","type":"Mid-Range Menace","ovr":75,"threePT":65,"MID":83,"FIN":72,"DNK":80,"HAN":78,"PAS":60,"PDEF":78,"IDEF":57,"BLK":56,"REB":53,"ATH":79,"STR":64,"CLU":60,"nbaId":1630534},{"name":"Ben Saraf","cname":"本-萨拉夫","pos":"PG / SG","height":"6'6'","type":"Point-Fading Wizard","ovr":75,"threePT":67,"MID":78,"FIN":77,"DNK":60,"HAN":80,"PAS":83,"PDEF":62,"IDEF":40,"BLK":40,"REB":47,"ATH":79,"STR":52,"CLU":85},{"name":"Jalen Wilson","cname":"杰伦-威尔逊","pos":"SF / PF","height":"6'6'","type":"Athletic Stretch Four","ovr":74,"threePT":79,"MID":79,"FIN":80,"DNK":75,"HAN":90,"PAS":64,"PDEF":57,"IDEF":35,"BLK":32,"REB":58,"ATH":79,"STR":30,"CLU":85}],"BOS":[{"name":"Jayson Tatum","cname":"杰森-塔图姆","pos":"PF / SF","height":"6'8'","type":"2-Way Crafty Slashing Cleaner","ovr":93,"threePT":85,"MID":85,"FIN":90,"DNK":89,"HAN":98,"PAS":78,"PDEF":87,"IDEF":81,"BLK":37,"REB":89,"ATH":88,"STR":71,"CLU":90,"nbaId":1628369},{"name":"Derrick White","cname":"德里克-怀特","pos":"PG / SG","height":"6'4'","type":"2-Way Middy Playmaker","ovr":85,"threePT":75,"MID":85,"FIN":82,"DNK":55,"HAN":90,"PAS":88,"PDEF":91,"IDEF":58,"BLK":80,"REB":51,"ATH":93,"STR":46,"CLU":70,"nbaId":1628401},{"name":"Paul George","cname":"保罗-乔治","pos":"PF / SF","height":"6'8'","type":"Ball-Hawking PlayShot","ovr":83,"threePT":83,"MID":90,"FIN":90,"DNK":80,"HAN":80,"PAS":78,"PDEF":79,"IDEF":71,"BLK":55,"REB":68,"ATH":84,"STR":70,"CLU":85,"nbaId":202331},{"name":"Payton Pritchard","cname":"佩顿-普里查德","pos":"PG","height":"6'1'","type":"Crafty Sharpshooter","ovr":82,"threePT":85,"MID":88,"FIN":98,"DNK":35,"HAN":75,"PAS":84,"PDEF":60,"IDEF":36,"BLK":35,"REB":51,"ATH":75,"STR":41,"CLU":90,"nbaId":1630202},{"name":"Neemias Queta","cname":"内米亚斯-克塔","pos":"C","height":"7'0'","type":"Shot-Erasing Putback Pro","ovr":81,"threePT":50,"MID":76,"FIN":98,"DNK":70,"HAN":85,"PAS":52,"PDEF":45,"IDEF":76,"BLK":86,"REB":83,"ATH":59,"STR":79,"CLU":85,"nbaId":1629674},{"name":"Mitchell Robinson","cname":"米切尔-罗宾逊","pos":"C","height":"7'0'","type":"Lob Threat Board Enforcer","ovr":80,"threePT":32,"MID":68,"FIN":90,"DNK":80,"HAN":80,"PAS":70,"PDEF":49,"IDEF":83,"BLK":88,"REB":87,"ATH":65,"STR":84,"CLU":60,"nbaId":1629011},{"name":"Luka Garza","cname":"卢卡-加尔扎","pos":"C / PF","height":"6'10'","type":"Crafty Stretch Five Crasher","ovr":78,"threePT":87,"MID":74,"FIN":98,"DNK":45,"HAN":85,"PAS":55,"PDEF":30,"IDEF":54,"BLK":65,"REB":53,"ATH":40,"STR":69,"CLU":85,"nbaId":1630568},{"name":"Sam Hauser","cname":"萨姆-豪瑟","pos":"SF / PF","height":"6'7'","type":"Crafty Sharpshooter","ovr":78,"threePT":86,"MID":79,"FIN":94,"DNK":60,"HAN":95,"PAS":50,"PDEF":55,"IDEF":48,"BLK":52,"REB":60,"ATH":62,"STR":66,"CLU":85,"nbaId":1630573},{"name":"Mike Conley","cname":"迈克-康利","pos":"PG","height":"6'1'","type":"2-Way Blow-By Ace","ovr":77,"threePT":74,"MID":70,"FIN":86,"DNK":30,"HAN":84,"PAS":82,"PDEF":83,"IDEF":31,"BLK":51,"REB":45,"ATH":84,"STR":40,"CLU":55,"nbaId":201144},{"name":"Jordan Walsh","cname":"乔丹-沃尔什","pos":"PF / SF","height":"6'6'","type":"2-Way Stretch Four","ovr":76,"threePT":83,"MID":67,"FIN":74,"DNK":80,"HAN":85,"PAS":63,"PDEF":82,"IDEF":78,"BLK":67,"REB":67,"ATH":81,"STR":63,"CLU":50,"nbaId":1641775},{"name":"Baylor Scheierman","cname":"贝勒-沙伊尔曼","pos":"SG / SF","height":"6'6'","type":"Wing-Running Sharpshooter","ovr":76,"threePT":82,"MID":94,"FIN":97,"DNK":55,"HAN":66,"PAS":70,"PDEF":56,"IDEF":47,"BLK":35,"REB":66,"ATH":64,"STR":54,"CLU":90,"nbaId":1631248},{"name":"Hugo Gonzalez","cname":"乌戈-冈萨雷斯","pos":"SF / PG","height":"6'6'","type":"2-Way Sharpshooter","ovr":75,"threePT":79,"MID":76,"FIN":84,"DNK":75,"HAN":70,"PAS":62,"PDEF":73,"IDEF":53,"BLK":60,"REB":71,"ATH":75,"STR":55,"CLU":80},{"name":"Dalano Banton","cname":"达拉诺-班顿","pos":"PG / SG","height":"6'8'","type":"Shot-Erasing Power Finisher","ovr":73,"threePT":77,"MID":71,"FIN":56,"DNK":65,"HAN":80,"PAS":78,"PDEF":68,"IDEF":40,"BLK":67,"REB":51,"ATH":76,"STR":66,"CLU":80},{"name":"Ron Harper Jr.","cname":"小罗恩-哈珀","pos":"SF / SG","height":"6'5'","type":"Iso Sniper","ovr":73,"threePT":78,"MID":72,"FIN":70,"DNK":70,"HAN":85,"PAS":59,"PDEF":60,"IDEF":42,"BLK":47,"REB":48,"ATH":69,"STR":47,"CLU":75},{"name":"Amari Williams","cname":"阿马里-威廉姆斯","pos":"C / PF","height":"6'11'","type":"Slashing Point Center","ovr":69,"threePT":55,"MID":60,"FIN":67,"DNK":80,"HAN":70,"PAS":66,"PDEF":60,"IDEF":70,"BLK":68,"REB":60,"ATH":68,"STR":69,"CLU":40}],"CHA":[{"name":"Brandon Miller","cname":"布兰登-米勒","pos":"SG / SF","height":"6'7'","type":"Inside-Out High-Flying Slasher","ovr":87,"threePT":85,"MID":71,"FIN":84,"DNK":87,"HAN":80,"PAS":75,"PDEF":73,"IDEF":50,"BLK":62,"REB":62,"ATH":82,"STR":51,"CLU":95,"nbaId":1641706},{"name":"Kon Knueppel","cname":"康-克努佩尔","pos":"SF / SG","height":"6'6'","type":"Crafty 3PT-Sniping Dimer","ovr":86,"threePT":94,"MID":72,"FIN":97,"DNK":55,"HAN":90,"PAS":75,"PDEF":70,"IDEF":48,"BLK":42,"REB":62,"ATH":70,"STR":62,"CLU":80,"nbaId":1642851},{"name":"Naz Reid","cname":"纳兹-里德","pos":"C / PF","height":"6'9'","type":"Stretch Five Layup Bully","ovr":82,"threePT":81,"MID":63,"FIN":97,"DNK":80,"HAN":94,"PAS":59,"PDEF":54,"IDEF":78,"BLK":73,"REB":78,"ATH":54,"STR":87,"CLU":50,"nbaId":1629675},{"name":"Coby White","cname":"科比-怀特","pos":"PG / SG","height":"6'4'","type":"3PT-Sniping Creator","ovr":81,"threePT":80,"MID":74,"FIN":90,"DNK":65,"HAN":95,"PAS":79,"PDEF":64,"IDEF":49,"BLK":35,"REB":59,"ATH":85,"STR":56,"CLU":70,"nbaId":1629632},{"name":"Grayson Allen","cname":"格雷森-阿伦","pos":"SG / SF","height":"6'3'","type":"Iso Sniper","ovr":81,"threePT":85,"MID":97,"FIN":80,"DNK":70,"HAN":78,"PAS":68,"PDEF":72,"IDEF":35,"BLK":45,"REB":47,"ATH":78,"STR":44,"CLU":50,"nbaId":1628960},{"name":"Moussa Diabate","cname":"穆萨-迪亚巴特","pos":"C / PF","height":"6'10'","type":"Rim-Rocking Putback Pro","ovr":80,"threePT":67,"MID":72,"FIN":85,"DNK":80,"HAN":65,"PAS":42,"PDEF":41,"IDEF":65,"BLK":73,"REB":79,"ATH":73,"STR":60,"CLU":85,"nbaId":1631217},{"name":"Ryan Kalkbrenner","cname":"瑞安-卡尔克布伦纳","pos":"C","height":"7'1'","type":"Shot-Erasing Putback Pro","ovr":80,"threePT":73,"MID":74,"FIN":98,"DNK":75,"HAN":85,"PAS":50,"PDEF":30,"IDEF":78,"BLK":89,"REB":65,"ATH":35,"STR":67,"CLU":90,"nbaId":1641750},{"name":"Royce O’Neale","cname":"罗伊斯-奥尼尔","pos":"PF / SF","height":"6'6'","type":"2-Way Stretch Four","ovr":77,"threePT":84,"MID":70,"FIN":73,"DNK":70,"HAN":75,"PAS":60,"PDEF":80,"IDEF":68,"BLK":53,"REB":66,"ATH":83,"STR":67,"CLU":75,"nbaId":1626220},{"name":"Grant Williams","cname":"格兰特-威廉姆斯","pos":"PF / C","height":"6'7'","type":"Crafty Stretch Four","ovr":76,"threePT":82,"MID":77,"FIN":88,"DNK":60,"HAN":80,"PAS":69,"PDEF":68,"IDEF":68,"BLK":58,"REB":64,"ATH":70,"STR":73,"CLU":75,"nbaId":1629684},{"name":"Tidjane Salaun","cname":"蒂贾内-萨隆","pos":"PF / SF","height":"6'10'","type":"Stretch Four Cleaner","ovr":75,"threePT":86,"MID":65,"FIN":76,"DNK":80,"HAN":80,"PAS":47,"PDEF":74,"IDEF":69,"BLK":52,"REB":84,"ATH":82,"STR":60,"CLU":70,"nbaId":1642275},{"name":"Dorian Finney-Smith","cname":"多里安-芬尼-史密斯","pos":"PF / SF","height":"6'7'","type":"2-Way Dual Finisher","ovr":75,"threePT":73,"MID":71,"FIN":80,"DNK":84,"HAN":72,"PAS":65,"PDEF":83,"IDEF":72,"BLK":47,"REB":51,"ATH":82,"STR":62,"CLU":70,"nbaId":1627827},{"name":"Sion James","cname":"锡安-詹姆斯","pos":"SG / SF","height":"6'5'","type":"Catch & Shoot 3PT Sniper","ovr":75,"threePT":80,"MID":70,"FIN":50,"DNK":70,"HAN":75,"PAS":69,"PDEF":72,"IDEF":69,"BLK":50,"REB":61,"ATH":85,"STR":82,"CLU":75},{"name":"Tre Mann","cname":"特雷-曼","pos":"PG / SG","height":"6'4'","type":"Crafty 3PT-Sniping Creator","ovr":74,"threePT":76,"MID":70,"FIN":66,"DNK":60,"HAN":85,"PAS":77,"PDEF":62,"IDEF":49,"BLK":40,"REB":56,"ATH":74,"STR":53,"CLU":65,"nbaId":1630544},{"name":"Pat Connaughton","cname":"帕特-康诺顿","pos":"SG / SF","height":"6'5'","type":"Bouncy High Flyer","ovr":73,"threePT":79,"MID":69,"FIN":90,"DNK":86,"HAN":90,"PAS":69,"PDEF":73,"IDEF":35,"BLK":57,"REB":63,"ATH":78,"STR":45,"CLU":55},{"name":"Xavier Tillman","cname":"泽维尔-蒂尔曼","pos":"C / PF","height":"6'8'","type":"2-Way Crafty Paint Finisher","ovr":72,"threePT":62,"MID":68,"FIN":86,"DNK":65,"HAN":95,"PAS":62,"PDEF":66,"IDEF":74,"BLK":57,"REB":72,"ATH":67,"STR":72,"CLU":50}],"CHI":[{"name":"Norman Powell","cname":"诺曼-鲍威尔","pos":"SG / SF","height":"6'4'","type":"Crafty 3PT Sniper","ovr":85,"threePT":82,"MID":75,"FIN":97,"DNK":80,"HAN":88,"PAS":74,"PDEF":79,"IDEF":50,"BLK":45,"REB":52,"ATH":80,"STR":46,"CLU":80,"nbaId":1626181},{"name":"Josh Giddey","cname":"乔什-吉迪","pos":"PG / SG","height":"6'8'","type":"Crafty Playmaking Cleaner","ovr":83,"threePT":80,"MID":38,"FIN":92,"DNK":65,"HAN":90,"PAS":90,"PDEF":62,"IDEF":50,"BLK":53,"REB":83,"ATH":79,"STR":60,"CLU":75,"nbaId":1630581},{"name":"Matas Buzelis","cname":"马塔斯-布泽利斯","pos":"PF / SF","height":"6'8'","type":"Shot-Erasing 3-Level Creator","ovr":82,"threePT":80,"MID":83,"FIN":85,"DNK":89,"HAN":90,"PAS":65,"PDEF":62,"IDEF":58,"BLK":82,"REB":68,"ATH":70,"STR":60,"CLU":85,"nbaId":1641824},{"name":"Nic Claxton","cname":"尼古拉斯-克拉克斯顿","pos":"C","height":"6'11'","type":"Lockdown Defender","ovr":81,"threePT":64,"MID":75,"FIN":89,"DNK":75,"HAN":80,"PAS":40,"PDEF":79,"IDEF":84,"BLK":76,"REB":75,"ATH":77,"STR":71,"CLU":70},{"name":"Tre Jones","cname":"特雷-琼斯","pos":"PG","height":"6'1'","type":"Crafty Mid-Range Menace","ovr":81,"threePT":75,"MID":87,"FIN":97,"DNK":35,"HAN":80,"PAS":83,"PDEF":73,"IDEF":34,"BLK":40,"REB":49,"ATH":80,"STR":60,"CLU":85,"nbaId":1630200},{"name":"Jalen Smith","cname":"杰伦-史密斯","pos":"C / PF","height":"6'8'","type":"Stretch Lob Threat Cleaner","ovr":79,"threePT":81,"MID":67,"FIN":71,"DNK":80,"HAN":80,"PAS":29,"PDEF":48,"IDEF":70,"BLK":76,"REB":85,"ATH":68,"STR":65,"CLU":95,"nbaId":1630188},{"name":"Zach Collins","cname":"扎克-科林斯","pos":"C","height":"6'9'","type":"Post-Scoring Stretch","ovr":77,"threePT":80,"MID":79,"FIN":89,"DNK":65,"HAN":75,"PAS":30,"PDEF":45,"IDEF":68,"BLK":58,"REB":82,"ATH":65,"STR":60,"CLU":75,"nbaId":1628380},{"name":"Isaac Okoro","cname":"艾萨克-奥科罗","pos":"SF / SG","height":"6'4'","type":"2-Way 3PT Sniper","ovr":77,"threePT":77,"MID":60,"FIN":65,"DNK":75,"HAN":80,"PAS":65,"PDEF":84,"IDEF":68,"BLK":55,"REB":40,"ATH":85,"STR":67,"CLU":80,"nbaId":1630171},{"name":"Leonard Miller","cname":"伦纳德-米勒","pos":"PF / SF","height":"6'10'","type":"Versatile Paint Finisher","ovr":76,"threePT":73,"MID":70,"FIN":95,"DNK":85,"HAN":85,"PAS":51,"PDEF":63,"IDEF":57,"BLK":56,"REB":72,"ATH":73,"STR":55,"CLU":80,"nbaId":1631159},{"name":"Patrick Williams","cname":"帕特里克-威廉姆斯","pos":"PF / SF","height":"6'6'","type":"2-Way Stretch Four","ovr":75,"threePT":77,"MID":86,"FIN":61,"DNK":65,"HAN":80,"PAS":60,"PDEF":76,"IDEF":70,"BLK":53,"REB":53,"ATH":74,"STR":64,"CLU":70,"nbaId":1630172},{"name":"Nick Richards","cname":"尼克-理查兹","pos":"C","height":"6'11'","type":"Rim-Reaping Mid-Range Scorer","ovr":75,"threePT":47,"MID":79,"FIN":69,"DNK":75,"HAN":80,"PAS":37,"PDEF":47,"IDEF":75,"BLK":80,"REB":87,"ATH":70,"STR":65,"CLU":60,"nbaId":1630208},{"name":"Rob Dillingham","cname":"罗布-迪林厄姆","pos":"PG / SG","height":"6'2'","type":"Speedy Blow-By Ace","ovr":73,"threePT":74,"MID":74,"FIN":67,"DNK":75,"HAN":90,"PAS":69,"PDEF":63,"IDEF":34,"BLK":37,"REB":53,"ATH":86,"STR":39,"CLU":60,"nbaId":1642265},{"name":"Yuki Kawamura","cname":"河村勇辉","pos":"PG","height":"5'8'","type":"Table Setter","ovr":72,"threePT":76,"MID":72,"FIN":69,"DNK":25,"HAN":80,"PAS":82,"PDEF":61,"IDEF":25,"BLK":26,"REB":56,"ATH":84,"STR":60,"CLU":60},{"name":"Noa Essengue","cname":"诺阿-埃森格","pos":"PF / SF","height":"6'8'","type":"Rim-Running Rim Rocker","ovr":72,"threePT":70,"MID":70,"FIN":71,"DNK":80,"HAN":90,"PAS":60,"PDEF":68,"IDEF":60,"BLK":45,"REB":54,"ATH":82,"STR":55,"CLU":60},{"name":"Mac McClung","cname":"麦克-麦克朗","pos":"SG / PG","height":"6'2'","type":"Slashing Combo Guard","ovr":71,"threePT":75,"MID":64,"FIN":78,"DNK":94,"HAN":85,"PAS":75,"PDEF":67,"IDEF":40,"BLK":42,"REB":67,"ATH":70,"STR":48,"CLU":70}],"CLE":[{"name":"Donovan Mitchell","cname":"多诺万-米切尔","pos":"SG / PG","height":"6'2'","type":"Mid-Range Slasher","ovr":93,"threePT":85,"MID":89,"FIN":96,"DNK":92,"HAN":90,"PAS":82,"PDEF":75,"IDEF":68,"BLK":45,"REB":56,"ATH":90,"STR":60,"CLU":90,"nbaId":1628378},{"name":"Evan Mobley","cname":"埃文-莫布利","pos":"PF / C","height":"6'11'","type":"Backline Post Scorer","ovr":87,"threePT":75,"MID":77,"FIN":88,"DNK":85,"HAN":85,"PAS":69,"PDEF":82,"IDEF":96,"BLK":85,"REB":82,"ATH":79,"STR":74,"CLU":50,"nbaId":1630596},{"name":"Jarrett Allen","cname":"贾勒特-阿伦","pos":"C","height":"6'9'","type":"2-Way Crafty Paint Prodigy","ovr":86,"threePT":63,"MID":80,"FIN":97,"DNK":85,"HAN":95,"PAS":25,"PDEF":72,"IDEF":88,"BLK":70,"REB":84,"ATH":72,"STR":84,"CLU":50,"nbaId":1628386},{"name":"Max Strus","cname":"马克斯-斯特鲁斯","pos":"SF / SG","height":"6'5'","type":"Sharpshooting Cleaner","ovr":79,"threePT":89,"MID":85,"FIN":86,"DNK":65,"HAN":85,"PAS":72,"PDEF":66,"IDEF":45,"BLK":25,"REB":81,"ATH":76,"STR":40,"CLU":50,"nbaId":1629622},{"name":"Sam Merrill","cname":"萨姆-梅里尔","pos":"SG / SF","height":"6'4'","type":"Dime-Dropping 3PT Gunner","ovr":78,"threePT":87,"MID":69,"FIN":87,"DNK":45,"HAN":80,"PAS":74,"PDEF":68,"IDEF":42,"BLK":37,"REB":46,"ATH":68,"STR":52,"CLU":95,"nbaId":1630241},{"name":"Dennis Schroder","cname":"丹尼斯-施罗德","pos":"PG / SG","height":"6'1'","type":"Speedy Blow-By Ace","ovr":77,"threePT":76,"MID":74,"FIN":85,"DNK":45,"HAN":85,"PAS":82,"PDEF":73,"IDEF":34,"BLK":40,"REB":51,"ATH":88,"STR":46,"CLU":60,"nbaId":203471},{"name":"Jaylon Tyson","cname":"杰伦-泰森","pos":"SF / PF","height":"6'6'","type":"3PT-Sniping Creator","ovr":77,"threePT":84,"MID":65,"FIN":92,"DNK":75,"HAN":80,"PAS":62,"PDEF":70,"IDEF":50,"BLK":53,"REB":59,"ATH":76,"STR":63,"CLU":60,"nbaId":1642281},{"name":"Thomas Bryant","cname":"托马斯-布莱恩特","pos":"C","height":"6'9'","type":"Crafty Stretch Five","ovr":76,"threePT":79,"MID":85,"FIN":97,"DNK":65,"HAN":78,"PAS":40,"PDEF":40,"IDEF":63,"BLK":72,"REB":80,"ATH":57,"STR":80,"CLU":70},{"name":"Craig Porter Jr.","cname":"小克雷格-波特","pos":"PG","height":"6'1'","type":"Chaos Trigger","ovr":74,"threePT":80,"MID":75,"FIN":71,"DNK":50,"HAN":85,"PAS":75,"PDEF":73,"IDEF":44,"BLK":71,"REB":55,"ATH":79,"STR":48,"CLU":90},{"name":"Nae’Qwan Tomlin","cname":"纳伊夸恩-汤姆林","pos":"PF / C","height":"6'8'","type":"Putback Dual-Finishing Eraser","ovr":74,"threePT":64,"MID":72,"FIN":75,"DNK":80,"HAN":80,"PAS":67,"PDEF":70,"IDEF":64,"BLK":75,"REB":50,"ATH":70,"STR":62,"CLU":70},{"name":"Tyrese Proctor","cname":"泰雷斯-普罗克特","pos":"SG / PG","height":"6'4'","type":"Point-Fading Wizard","ovr":73,"threePT":79,"MID":95,"FIN":70,"DNK":65,"HAN":90,"PAS":79,"PDEF":60,"IDEF":42,"BLK":27,"REB":51,"ATH":81,"STR":51,"CLU":45},{"name":"Olivier Sarr","cname":"奥利维耶-萨尔","pos":"C","height":"6'10'","type":"Shot-Erasing Mid-Paint Scorer","ovr":68,"threePT":69,"MID":74,"FIN":68,"DNK":75,"HAN":70,"PAS":50,"PDEF":40,"IDEF":58,"BLK":80,"REB":70,"ATH":63,"STR":63,"CLU":40},{"name":"Tristan Enaruna","cname":"特里斯坦-埃纳鲁纳","pos":"SF / SG","height":"6'6'","type":"Blow-By High Flyer","ovr":68,"threePT":72,"MID":72,"FIN":70,"DNK":80,"HAN":80,"PAS":68,"PDEF":70,"IDEF":30,"BLK":47,"REB":54,"ATH":80,"STR":67,"CLU":30},{"name":"Riley Minix","cname":"赖利-米尼克斯","pos":"PF / SF","height":"6'7'","type":"Ball-Hawking Stretch Four","ovr":67,"threePT":76,"MID":71,"FIN":69,"DNK":60,"HAN":70,"PAS":54,"PDEF":59,"IDEF":55,"BLK":47,"REB":62,"ATH":60,"STR":58,"CLU":60}],"DAL":[{"name":"Kyrie Irving","cname":"凯里-欧文","pos":"PG / SG","height":"6'2'","type":"Crafty Iso Savant","ovr":90,"threePT":86,"MID":98,"FIN":91,"DNK":30,"HAN":96,"PAS":80,"PDEF":67,"IDEF":32,"BLK":51,"REB":51,"ATH":85,"STR":37,"CLU":75,"nbaId":202681},{"name":"Cooper Flagg","cname":"库珀-弗拉格","pos":"SF / PG","height":"6'9'","type":"2-Way Mid-Paint Scorer","ovr":88,"threePT":71,"MID":82,"FIN":89,"DNK":80,"HAN":90,"PAS":70,"PDEF":83,"IDEF":81,"BLK":63,"REB":67,"ATH":85,"STR":65,"CLU":95,"nbaId":1642843},{"name":"Naji Marshall","cname":"纳吉-马歇尔","pos":"SF / PG","height":"6'6'","type":"Diming Crafty Layup Artist","ovr":81,"threePT":71,"MID":70,"FIN":97,"DNK":70,"HAN":95,"PAS":75,"PDEF":60,"IDEF":52,"BLK":30,"REB":60,"ATH":65,"STR":67,"CLU":75,"nbaId":1630230},{"name":"Santi Aldama","cname":"桑蒂-阿尔达马","pos":"PF / C","height":"7'0'","type":"Crafty Stretch Four","ovr":81,"threePT":78,"MID":80,"FIN":97,"DNK":65,"HAN":95,"PAS":70,"PDEF":66,"IDEF":67,"BLK":62,"REB":73,"ATH":60,"STR":70,"CLU":80,"nbaId":1630583},{"name":"P.J. Washington","cname":"PJ-华盛顿","pos":"PF / SF","height":"6'7'","type":"2-Way Rim Rocker","ovr":80,"threePT":75,"MID":73,"FIN":87,"DNK":80,"HAN":95,"PAS":55,"PDEF":79,"IDEF":78,"BLK":70,"REB":72,"ATH":80,"STR":68,"CLU":75,"nbaId":1629023},{"name":"Daniel Gafford","cname":"丹尼尔-加福德","pos":"C","height":"6'10'","type":"2-Way Rim-Running Lob Threat","ovr":80,"threePT":29,"MID":65,"FIN":91,"DNK":75,"HAN":70,"PAS":37,"PDEF":57,"IDEF":84,"BLK":83,"REB":79,"ATH":70,"STR":73,"CLU":50,"nbaId":1629655},{"name":"Dereck Lively II","cname":"德雷克-莱夫利二世","pos":"C","height":"7'1'","type":"2-Way Versatile Paint Finisher","ovr":79,"threePT":52,"MID":60,"FIN":97,"DNK":83,"HAN":90,"PAS":69,"PDEF":57,"IDEF":83,"BLK":90,"REB":80,"ATH":57,"STR":70,"CLU":50,"nbaId":1641726},{"name":"Klay Thompson","cname":"克莱-汤普森","pos":"SG / SF","height":"6'5'","type":"Physical 3 & D Sniper","ovr":78,"threePT":88,"MID":69,"FIN":85,"DNK":65,"HAN":85,"PAS":57,"PDEF":75,"IDEF":60,"BLK":51,"REB":47,"ATH":75,"STR":62,"CLU":85,"nbaId":202691},{"name":"Max Christie","cname":"马克斯-克里斯蒂","pos":"SG / SF","height":"6'5'","type":"3 & D Two Guard","ovr":77,"threePT":84,"MID":81,"FIN":75,"DNK":80,"HAN":90,"PAS":68,"PDEF":80,"IDEF":44,"BLK":50,"REB":52,"ATH":87,"STR":55,"CLU":75,"nbaId":1631108},{"name":"Caleb Martin","cname":"凯莱布-马丁","pos":"PF / SF","height":"6'5'","type":"2-Way Crafty Stretch Four","ovr":75,"threePT":78,"MID":65,"FIN":63,"DNK":70,"HAN":75,"PAS":62,"PDEF":75,"IDEF":68,"BLK":55,"REB":61,"ATH":74,"STR":58,"CLU":85},{"name":"Ryan Nembhard","cname":"瑞安-内姆哈德","pos":"PG","height":"5'11'","type":"Playmaking 3PT Creator","ovr":75,"threePT":80,"MID":79,"FIN":82,"DNK":30,"HAN":85,"PAS":89,"PDEF":66,"IDEF":31,"BLK":30,"REB":50,"ATH":79,"STR":37,"CLU":60,"nbaId":1642948},{"name":"Dwight Powell","cname":"德怀特-鲍威尔","pos":"C","height":"6'10'","type":"Crafty Post-Scoring Stretch","ovr":74,"threePT":78,"MID":69,"FIN":96,"DNK":65,"HAN":90,"PAS":55,"PDEF":45,"IDEF":74,"BLK":60,"REB":69,"ATH":58,"STR":80,"CLU":30},{"name":"Moussa Cisse","cname":"穆萨-西塞","pos":"C","height":"6'11'","type":"Putback Rim-Rocking Eraser","ovr":73,"threePT":25,"MID":44,"FIN":81,"DNK":80,"HAN":75,"PAS":40,"PDEF":44,"IDEF":75,"BLK":96,"REB":83,"ATH":57,"STR":68,"CLU":60},{"name":"John Poulakidas","cname":"约翰-普拉基达斯","pos":"SG / SF","height":"6'6'","type":"Physical Sharpshooter","ovr":71,"threePT":80,"MID":75,"FIN":67,"DNK":60,"HAN":70,"PAS":54,"PDEF":54,"IDEF":37,"BLK":25,"REB":56,"ATH":57,"STR":53,"CLU":75},{"name":"Tyler Smith","cname":"泰勒-史密斯","pos":"C / PF","height":"6'9'","type":"Rim-Rocking Stretch Five","ovr":69,"threePT":73,"MID":75,"FIN":72,"DNK":80,"HAN":70,"PAS":39,"PDEF":52,"IDEF":53,"BLK":59,"REB":73,"ATH":69,"STR":58,"CLU":40}],"DEN":[{"name":"Nikola Jokic","cname":"尼古拉-约基奇","pos":"C","height":"6'11'","type":"Triple-Double Threat","ovr":98,"threePT":84,"MID":98,"FIN":99,"DNK":75,"HAN":90,"PAS":93,"PDEF":61,"IDEF":83,"BLK":61,"REB":97,"ATH":62,"STR":96,"CLU":75,"nbaId":203999},{"name":"Jamal Murray","cname":"贾马尔-默里","pos":"PG / SG","height":"6'4'","type":"Iso Savant","ovr":89,"threePT":88,"MID":90,"FIN":89,"DNK":82,"HAN":84,"PAS":84,"PDEF":77,"IDEF":35,"BLK":50,"REB":55,"ATH":80,"STR":54,"CLU":90,"nbaId":1627750},{"name":"Aaron Gordon","cname":"阿龙-戈登","pos":"PF / C","height":"6'8'","type":"2-Way Rim-Wrecking Stretch","ovr":83,"threePT":81,"MID":74,"FIN":75,"DNK":94,"HAN":95,"PAS":65,"PDEF":84,"IDEF":80,"BLK":47,"REB":67,"ATH":84,"STR":72,"CLU":80,"nbaId":203932},{"name":"Peyton Watson","cname":"佩顿-沃特森","pos":"SF / PF","height":"6'7'","type":"Shot-Erasing Sharpshooter","ovr":82,"threePT":85,"MID":90,"FIN":84,"DNK":65,"HAN":95,"PAS":60,"PDEF":78,"IDEF":78,"BLK":74,"REB":62,"ATH":80,"STR":55,"CLU":80,"nbaId":1631212},{"name":"Christian Braun","cname":"克里斯蒂安-布劳恩","pos":"SG / SF","height":"6'6'","type":"Versatile Paint Finisher","ovr":80,"threePT":78,"MID":67,"FIN":97,"DNK":85,"HAN":90,"PAS":69,"PDEF":79,"IDEF":40,"BLK":45,"REB":53,"ATH":83,"STR":46,"CLU":85,"nbaId":1631128},{"name":"Cameron Johnson","cname":"卡梅伦-约翰逊","pos":"SF / SG","height":"6'8'","type":"3PT Layup Artist","ovr":80,"threePT":89,"MID":71,"FIN":75,"DNK":60,"HAN":57,"PAS":59,"PDEF":69,"IDEF":65,"BLK":51,"REB":51,"ATH":70,"STR":60,"CLU":75,"nbaId":1629661},{"name":"Tim Hardaway Jr.","cname":"小蒂姆-哈达威","pos":"SG / SF","height":"6'5'","type":"Crafty 3-Level Scorer","ovr":80,"threePT":86,"MID":98,"FIN":91,"DNK":78,"HAN":71,"PAS":75,"PDEF":70,"IDEF":45,"BLK":35,"REB":48,"ATH":75,"STR":55,"CLU":75,"nbaId":203501},{"name":"Marvin Bagley III","cname":"马文-巴格利三世","pos":"C / PF","height":"6'11'","type":"Stretch Five Crasher","ovr":79,"threePT":80,"MID":79,"FIN":94,"DNK":75,"HAN":70,"PAS":47,"PDEF":49,"IDEF":58,"BLK":70,"REB":75,"ATH":74,"STR":80,"CLU":70},{"name":"Julian Strawther","cname":"朱利安-斯特劳瑟","pos":"SF / SG","height":"6'6'","type":"3PT-Sniping Creator","ovr":77,"threePT":81,"MID":70,"FIN":96,"DNK":70,"HAN":95,"PAS":64,"PDEF":74,"IDEF":44,"BLK":40,"REB":57,"ATH":70,"STR":53,"CLU":85,"nbaId":1631124},{"name":"Bruce Brown","cname":"布鲁斯-布朗","pos":"SF / PG","height":"6'4'","type":"3PT-Creating Point Forward","ovr":75,"threePT":83,"MID":41,"FIN":70,"DNK":70,"HAN":95,"PAS":79,"PDEF":77,"IDEF":58,"BLK":47,"REB":55,"ATH":78,"STR":75,"CLU":60,"nbaId":1628971},{"name":"Zeke Nnaji","cname":"齐克-纳吉","pos":"C / PF","height":"6'9'","type":"Rim Runner","ovr":73,"threePT":72,"MID":76,"FIN":73,"DNK":70,"HAN":77,"PAS":33,"PDEF":54,"IDEF":72,"BLK":73,"REB":67,"ATH":77,"STR":70,"CLU":65},{"name":"Tyus Jones","cname":"泰厄斯-琼斯","pos":"PG","height":"6'1'","type":"Crafty Table Setter","ovr":73,"threePT":70,"MID":78,"FIN":88,"DNK":30,"HAN":78,"PAS":84,"PDEF":67,"IDEF":30,"BLK":35,"REB":41,"ATH":71,"STR":34,"CLU":60,"nbaId":1626145},{"name":"Jalen Pickett","cname":"杰伦-皮克特","pos":"PG / SG","height":"6'2'","type":"Physical 3PT Sniper","ovr":73,"threePT":85,"MID":70,"FIN":91,"DNK":60,"HAN":81,"PAS":72,"PDEF":62,"IDEF":47,"BLK":37,"REB":60,"ATH":72,"STR":59,"CLU":80},{"name":"DaRon Holmes II","cname":"达隆-霍姆斯二世","pos":"C / PF","height":"6'10'","type":"Athletic Stretch Five","ovr":73,"threePT":78,"MID":76,"FIN":77,"DNK":75,"HAN":80,"PAS":49,"PDEF":63,"IDEF":76,"BLK":62,"REB":55,"ATH":69,"STR":60,"CLU":65,"nbaId":1641747},{"name":"KJ Simpson","cname":"KJ-辛普森","pos":"PG","height":"6'2'","type":"Tempo Setter","ovr":73,"threePT":74,"MID":53,"FIN":76,"DNK":50,"HAN":80,"PAS":78,"PDEF":64,"IDEF":42,"BLK":25,"REB":57,"ATH":83,"STR":52,"CLU":70}],"DET":[{"name":"Cade Cunningham","cname":"凯德-坎宁安","pos":"PG / SG","height":"6'6'","type":"2-Way Crafty 3-Level Playmaker","ovr":95,"threePT":86,"MID":97,"FIN":94,"DNK":75,"HAN":98,"PAS":92,"PDEF":83,"IDEF":63,"BLK":65,"REB":63,"ATH":84,"STR":62,"CLU":98,"nbaId":1630595},{"name":"Ausar Thompson","cname":"奥萨尔-汤普森","pos":"SF / PF","height":"6'7'","type":"2-Way Blow-By High Flyer","ovr":86,"threePT":70,"MID":57,"FIN":88,"DNK":91,"HAN":80,"PAS":70,"PDEF":96,"IDEF":87,"BLK":73,"REB":65,"ATH":96,"STR":50,"CLU":60,"nbaId":1641709},{"name":"Jalen Duren","cname":"杰伦-杜伦","pos":"C","height":"6'10'","type":"Board-Enforcing Power Finisher","ovr":85,"threePT":35,"MID":70,"FIN":96,"DNK":90,"HAN":70,"PAS":59,"PDEF":62,"IDEF":88,"BLK":70,"REB":94,"ATH":68,"STR":95,"CLU":65,"nbaId":1631105},{"name":"John Collins","cname":"约翰-科林斯","pos":"PF / C","height":"6'9'","type":"Stretch Rim Wrecker","ovr":81,"threePT":83,"MID":74,"FIN":97,"DNK":90,"HAN":80,"PAS":58,"PDEF":49,"IDEF":70,"BLK":66,"REB":64,"ATH":65,"STR":75,"CLU":95,"nbaId":1628381},{"name":"Daniss Jenkins","cname":"达尼斯-詹金斯","pos":"PG / SG","height":"6'4'","type":"Iso Sniper","ovr":80,"threePT":83,"MID":84,"FIN":76,"DNK":60,"HAN":85,"PAS":80,"PDEF":69,"IDEF":32,"BLK":50,"REB":46,"ATH":75,"STR":45,"CLU":80,"nbaId":1642450},{"name":"Duncan Robinson","cname":"邓肯-罗宾逊","pos":"SG / SF","height":"6'7'","type":"Crafty Sharpshooter","ovr":80,"threePT":88,"MID":79,"FIN":97,"DNK":70,"HAN":90,"PAS":66,"PDEF":67,"IDEF":50,"BLK":51,"REB":47,"ATH":67,"STR":35,"CLU":80,"nbaId":1629130},{"name":"Paul Reed","cname":"保罗-里德","pos":"C / PF","height":"6'9'","type":"Disruptive Crafty Putback Pro","ovr":79,"threePT":76,"MID":74,"FIN":98,"DNK":75,"HAN":75,"PAS":48,"PDEF":43,"IDEF":69,"BLK":90,"REB":76,"ATH":52,"STR":84,"CLU":85,"nbaId":1630194},{"name":"Isaiah Joe","cname":"以赛亚-乔","pos":"SG / PG","height":"6'4'","type":"Crafty 3-Level Scorer","ovr":79,"threePT":85,"MID":94,"FIN":95,"DNK":60,"HAN":85,"PAS":67,"PDEF":69,"IDEF":42,"BLK":42,"REB":50,"ATH":77,"STR":47,"CLU":50,"nbaId":1630198},{"name":"Javonte Green","cname":"贾冯特-格林","pos":"SF / SG","height":"6'5'","type":"Ball-Hawking High Flyer","ovr":78,"threePT":81,"MID":70,"FIN":90,"DNK":85,"HAN":95,"PAS":60,"PDEF":77,"IDEF":74,"BLK":56,"REB":56,"ATH":74,"STR":46,"CLU":90,"nbaId":1629750},{"name":"Ronald Holland II","cname":"罗纳德-霍兰德二世","pos":"PF / SF","height":"6'8'","type":"2-Way Blow-By Laymaker","ovr":78,"threePT":70,"MID":64,"FIN":91,"DNK":80,"HAN":80,"PAS":60,"PDEF":84,"IDEF":78,"BLK":56,"REB":67,"ATH":88,"STR":69,"CLU":70,"nbaId":1641842},{"name":"Kevin Huerter","cname":"凯文-赫尔特","pos":"SG / SF","height":"6'6'","type":"Crafty Laymaker","ovr":77,"threePT":75,"MID":67,"FIN":94,"DNK":65,"HAN":90,"PAS":74,"PDEF":64,"IDEF":44,"BLK":58,"REB":61,"ATH":73,"STR":50,"CLU":80,"nbaId":1628989},{"name":"Marcus Sasser","cname":"马库斯-萨瑟","pos":"PG / SG","height":"6'1'","type":"3PT Shot Hunter","ovr":76,"threePT":87,"MID":60,"FIN":76,"DNK":50,"HAN":75,"PAS":74,"PDEF":70,"IDEF":35,"BLK":42,"REB":43,"ATH":83,"STR":47,"CLU":90},{"name":"Taurean Prince","cname":"托里恩-普林斯","pos":"SF / SG","height":"6'6'","type":"3PT-Sniping Lob Threat","ovr":76,"threePT":84,"MID":79,"FIN":73,"DNK":70,"HAN":85,"PAS":68,"PDEF":74,"IDEF":64,"BLK":47,"REB":53,"ATH":78,"STR":60,"CLU":60,"nbaId":1627752},{"name":"Gary Harris","cname":"加里-哈里斯","pos":"SG / SF","height":"6'4'","type":"3 & D Dime Dropper","ovr":73,"threePT":87,"MID":79,"FIN":85,"DNK":50,"HAN":75,"PAS":75,"PDEF":80,"IDEF":68,"BLK":50,"REB":46,"ATH":80,"STR":53,"CLU":50,"nbaId":203914},{"name":"Isaac Jones","cname":"艾萨克-琼斯","pos":"PF / C","height":"6'8'","type":"Shot-Erasing Putback Pro","ovr":71,"threePT":49,"MID":64,"FIN":78,"DNK":75,"HAN":95,"PAS":44,"PDEF":59,"IDEF":60,"BLK":81,"REB":64,"ATH":67,"STR":70,"CLU":60}],"GSW":[{"name":"Stephen Curry","cname":"斯蒂芬-库里","pos":"PG / SG","height":"6'2'","type":"Crafty Offensive Engine","ovr":95,"threePT":99,"MID":94,"FIN":98,"DNK":36,"HAN":98,"PAS":89,"PDEF":69,"IDEF":30,"BLK":51,"REB":53,"ATH":84,"STR":48,"CLU":80,"nbaId":201939},{"name":"Jimmy Butler","cname":"吉米-巴特勒","pos":"SF / PF","height":"6'6'","type":"2-Way Bullmaker","ovr":88,"threePT":81,"MID":70,"FIN":90,"DNK":75,"HAN":98,"PAS":85,"PDEF":86,"IDEF":80,"BLK":40,"REB":53,"ATH":92,"STR":76,"CLU":85,"nbaId":202710},{"name":"Kristaps Porzingis","cname":"克里斯塔普斯-波尔津吉斯","pos":"C / PF","height":"7'2'","type":"Stretch Lob Threat","ovr":83,"threePT":80,"MID":90,"FIN":93,"DNK":75,"HAN":85,"PAS":50,"PDEF":56,"IDEF":75,"BLK":84,"REB":68,"ATH":65,"STR":74,"CLU":50,"nbaId":204001},{"name":"Brandin Podziemski","cname":"布兰丁-波杰姆斯基","pos":"PG / SG","height":"6'4'","type":"Crafty 3PT Sniper","ovr":81,"threePT":81,"MID":79,"FIN":94,"DNK":60,"HAN":85,"PAS":69,"PDEF":63,"IDEF":45,"BLK":40,"REB":68,"ATH":82,"STR":54,"CLU":85,"nbaId":1641764},{"name":"Gui Santos","cname":"吉-桑托斯","pos":"SF / PF","height":"6'7'","type":"Crafty 3PT Sniper","ovr":80,"threePT":79,"MID":69,"FIN":96,"DNK":65,"HAN":80,"PAS":69,"PDEF":69,"IDEF":66,"BLK":55,"REB":65,"ATH":72,"STR":50,"CLU":85},{"name":"Draymond Green","cname":"德雷蒙德-格林","pos":"PF / C","height":"6'6'","type":"Defensive Playmaking Bully","ovr":79,"threePT":75,"MID":75,"FIN":78,"DNK":65,"HAN":80,"PAS":80,"PDEF":88,"IDEF":93,"BLK":61,"REB":74,"ATH":84,"STR":85,"CLU":35,"nbaId":203110},{"name":"De’Anthony Melton","cname":"德安东尼-梅尔顿","pos":"SG / PG","height":"6'2'","type":"2-Way Tempo Pusher","ovr":79,"threePT":70,"MID":72,"FIN":88,"DNK":70,"HAN":95,"PAS":74,"PDEF":82,"IDEF":48,"BLK":55,"REB":54,"ATH":81,"STR":44,"CLU":95,"nbaId":1629001},{"name":"Al Horford","cname":"艾尔-霍福德","pos":"C / PF","height":"6'8'","type":"Diming Pick & Popper","ovr":79,"threePT":80,"MID":79,"FIN":92,"DNK":65,"HAN":84,"PAS":79,"PDEF":68,"IDEF":82,"BLK":84,"REB":77,"ATH":67,"STR":91,"CLU":50,"nbaId":201143},{"name":"Gary Payton II","cname":"加里-佩顿二世","pos":"SF / PF","height":"6'2'","type":"2-Way Blow-By Laymaker","ovr":79,"threePT":72,"MID":75,"FIN":97,"DNK":75,"HAN":89,"PAS":60,"PDEF":83,"IDEF":79,"BLK":57,"REB":68,"ATH":83,"STR":48,"CLU":70,"nbaId":1627780},{"name":"Moses Moody","cname":"摩西-穆迪","pos":"SF / SG","height":"6'5'","type":"Crafty 3PT-Sniping Creator","ovr":79,"threePT":86,"MID":73,"FIN":82,"DNK":70,"HAN":80,"PAS":65,"PDEF":71,"IDEF":68,"BLK":60,"REB":52,"ATH":77,"STR":68,"CLU":85,"nbaId":1630541},{"name":"Will Richard","cname":"威尔-理查德","pos":"SG / SF","height":"6'3'","type":"Ball-Hawking Sharpshooter","ovr":75,"threePT":77,"MID":84,"FIN":81,"DNK":75,"HAN":70,"PAS":60,"PDEF":72,"IDEF":50,"BLK":37,"REB":46,"ATH":78,"STR":56,"CLU":75},{"name":"Seth Curry","cname":"塞思-库里","pos":"SG / PG","height":"6'1'","type":"Crafty 3-Level Scorer","ovr":74,"threePT":89,"MID":95,"FIN":70,"DNK":36,"HAN":69,"PAS":75,"PDEF":57,"IDEF":25,"BLK":42,"REB":48,"ATH":78,"STR":41,"CLU":50},{"name":"Nate Williams","cname":"内特-威廉姆斯","pos":"SF / SG","height":"6'5'","type":"Sharpshooting Crasher","ovr":74,"threePT":83,"MID":79,"FIN":64,"DNK":70,"HAN":80,"PAS":65,"PDEF":52,"IDEF":40,"BLK":25,"REB":38,"ATH":68,"STR":52,"CLU":90},{"name":"LJ Cryer","cname":"LJ-克赖尔","pos":"PG","height":"6'1'","type":"Iso Sniper","ovr":74,"threePT":83,"MID":79,"FIN":86,"DNK":30,"HAN":95,"PAS":77,"PDEF":57,"IDEF":25,"BLK":25,"REB":48,"ATH":82,"STR":32,"CLU":70},{"name":"Charles Bassey","cname":"查尔斯-巴塞","pos":"C","height":"6'10'","type":"Glass Guardian","ovr":72,"threePT":62,"MID":69,"FIN":92,"DNK":75,"HAN":70,"PAS":35,"PDEF":57,"IDEF":77,"BLK":93,"REB":89,"ATH":67,"STR":66,"CLU":35}],"HOU":[{"name":"Kevin Durant","cname":"凯文-杜兰特","pos":"SF / SG","height":"6'11'","type":"Crafty Walking Bucket","ovr":93,"threePT":91,"MID":97,"FIN":98,"DNK":85,"HAN":98,"PAS":78,"PDEF":77,"IDEF":70,"BLK":62,"REB":62,"ATH":78,"STR":63,"CLU":40,"nbaId":201142},{"name":"Alperen Sengun","cname":"阿尔佩伦-申京","pos":"C / PF","height":"6'11'","type":"Dime-Dropping Post Bully","ovr":88,"threePT":74,"MID":72,"FIN":94,"DNK":80,"HAN":95,"PAS":79,"PDEF":63,"IDEF":72,"BLK":70,"REB":74,"ATH":64,"STR":86,"CLU":90,"nbaId":1630578},{"name":"Amen Thompson","cname":"阿门-汤普森","pos":"SG / PG","height":"6'7'","type":"2-Way Blow-By High Flyer","ovr":87,"threePT":56,"MID":66,"FIN":89,"DNK":96,"HAN":95,"PAS":80,"PDEF":93,"IDEF":78,"BLK":55,"REB":61,"ATH":93,"STR":59,"CLU":75,"nbaId":1641708},{"name":"Jabari Smith Jr.","cname":"小贾巴里-史密斯","pos":"PF / SF","height":"6'10'","type":"Crafty Rim-Rocking Stretch","ovr":82,"threePT":81,"MID":79,"FIN":93,"DNK":85,"HAN":85,"PAS":65,"PDEF":69,"IDEF":70,"BLK":63,"REB":67,"ATH":76,"STR":60,"CLU":80,"nbaId":1631095},{"name":"Reed Sheppard","cname":"里德-谢泼德","pos":"PG / SG","height":"6'2'","type":"Ball-Hawking 3PT Shot Hunter","ovr":80,"threePT":83,"MID":83,"FIN":88,"DNK":70,"HAN":90,"PAS":79,"PDEF":78,"IDEF":54,"BLK":62,"REB":51,"ATH":89,"STR":56,"CLU":80,"nbaId":1642263},{"name":"Fred VanVleet","cname":"弗雷德-范弗利特","pos":"PG","height":"6'0'","type":"2-Way Blow-By Ace","ovr":80,"threePT":78,"MID":82,"FIN":75,"DNK":35,"HAN":98,"PAS":84,"PDEF":80,"IDEF":27,"BLK":51,"REB":49,"ATH":80,"STR":42,"CLU":85,"nbaId":1627832},{"name":"Marcus Smart","cname":"马库斯-斯马特","pos":"SF / SG","height":"6'3'","type":"2-Way Mid-Range Bully","ovr":80,"threePT":80,"MID":98,"FIN":82,"DNK":50,"HAN":70,"PAS":75,"PDEF":86,"IDEF":74,"BLK":51,"REB":46,"ATH":89,"STR":73,"CLU":40,"nbaId":203935},{"name":"Tari Eason","cname":"塔里-伊森","pos":"SF / SG","height":"6'8'","type":"Physical Perimeter Lock","ovr":79,"threePT":80,"MID":54,"FIN":75,"DNK":80,"HAN":80,"PAS":50,"PDEF":85,"IDEF":78,"BLK":56,"REB":69,"ATH":85,"STR":70,"CLU":40,"nbaId":1631106},{"name":"Clint Capela","cname":"克林特-卡佩拉","pos":"C","height":"6'10'","type":"Rim-Protecting Crasher","ovr":77,"threePT":25,"MID":50,"FIN":75,"DNK":75,"HAN":70,"PAS":34,"PDEF":47,"IDEF":83,"BLK":89,"REB":80,"ATH":51,"STR":82,"CLU":40,"nbaId":203991},{"name":"Steven Adams","cname":"史蒂文-亚当斯","pos":"C","height":"6'11'","type":"Paint Crasher","ovr":77,"threePT":26,"MID":65,"FIN":73,"DNK":70,"HAN":75,"PAS":69,"PDEF":55,"IDEF":80,"BLK":65,"REB":75,"ATH":63,"STR":96,"CLU":70,"nbaId":203500},{"name":"Aaron Holiday","cname":"阿龙-霍勒迪","pos":"SG / PG","height":"6'0'","type":"Shot-Creating Combo Guard","ovr":74,"threePT":82,"MID":64,"FIN":94,"DNK":60,"HAN":80,"PAS":82,"PDEF":68,"IDEF":34,"BLK":37,"REB":40,"ATH":77,"STR":49,"CLU":70},{"name":"Bogdan Bogdanovic","cname":"波格丹-波格丹诺维奇","pos":"SG / SF","height":"6'5'","type":"3PT Shot Hunter","ovr":74,"threePT":76,"MID":75,"FIN":80,"DNK":65,"HAN":85,"PAS":71,"PDEF":63,"IDEF":44,"BLK":37,"REB":58,"ATH":75,"STR":63,"CLU":50,"nbaId":203992},{"name":"Jae’Sean Tate","cname":"杰肖恩-泰特","pos":"SF / PF","height":"6'4'","type":"Crafty Laymaker","ovr":73,"threePT":72,"MID":70,"FIN":92,"DNK":75,"HAN":85,"PAS":70,"PDEF":70,"IDEF":70,"BLK":42,"REB":55,"ATH":70,"STR":65,"CLU":30},{"name":"Jeff Green","cname":"杰夫-格林","pos":"PF / C","height":"6'8'","type":"Skilled Stretch Four","ovr":73,"threePT":76,"MID":75,"FIN":88,"DNK":70,"HAN":85,"PAS":65,"PDEF":70,"IDEF":69,"BLK":62,"REB":49,"ATH":75,"STR":70,"CLU":50,"nbaId":201145},{"name":"JD Davison","cname":"JD-戴维森","pos":"PG / SG","height":"6'1'","type":"Blow-By High Flyer","ovr":69,"threePT":71,"MID":73,"FIN":70,"DNK":85,"HAN":80,"PAS":72,"PDEF":56,"IDEF":25,"BLK":61,"REB":55,"ATH":85,"STR":54,"CLU":60}],"IND":[{"name":"Tyrese Haliburton","cname":"泰雷塞-哈利伯顿","pos":"PG / SG","height":"6'5'","type":"Crafty Offensive Engine","ovr":93,"threePT":84,"MID":91,"FIN":98,"DNK":65,"HAN":95,"PAS":96,"PDEF":77,"IDEF":60,"BLK":56,"REB":49,"ATH":84,"STR":59,"CLU":98,"nbaId":1630169},{"name":"Pascal Siakam","cname":"帕斯卡尔-西亚卡姆","pos":"PF / C","height":"6'8'","type":"2-Way Inside-Out Creator","ovr":87,"threePT":80,"MID":69,"FIN":97,"DNK":80,"HAN":90,"PAS":72,"PDEF":79,"IDEF":85,"BLK":50,"REB":69,"ATH":78,"STR":70,"CLU":80,"nbaId":1627783},{"name":"Ivica Zubac","cname":"伊维察-祖巴茨","pos":"C","height":"7'0'","type":"2-Way Post Bully Cleaner","ovr":85,"threePT":26,"MID":76,"FIN":97,"DNK":80,"HAN":80,"PAS":70,"PDEF":63,"IDEF":92,"BLK":67,"REB":88,"ATH":63,"STR":90,"CLU":40,"nbaId":1627826},{"name":"Andrew Nembhard","cname":"安德鲁-内姆哈德","pos":"PG / SG","height":"6'4'","type":"2-Way Finisher","ovr":82,"threePT":80,"MID":79,"FIN":92,"DNK":60,"HAN":75,"PAS":80,"PDEF":88,"IDEF":68,"BLK":35,"REB":46,"ATH":88,"STR":46,"CLU":85,"nbaId":1629614},{"name":"T.J. McConnell","cname":"TJ-麦康奈尔","pos":"PG","height":"6'1'","type":"2-Way Crafty Mid-Paint Maestro","ovr":81,"threePT":75,"MID":97,"FIN":98,"DNK":25,"HAN":79,"PAS":92,"PDEF":82,"IDEF":49,"BLK":47,"REB":54,"ATH":82,"STR":44,"CLU":85,"nbaId":204456},{"name":"Jarace Walker","cname":"贾拉斯-沃克","pos":"SF / PF","height":"6'7'","type":"3PT-Sniping Posterizer","ovr":81,"threePT":82,"MID":74,"FIN":82,"DNK":85,"HAN":80,"PAS":69,"PDEF":62,"IDEF":67,"BLK":51,"REB":75,"ATH":81,"STR":80,"CLU":85,"nbaId":1641716},{"name":"Obi Toppin","cname":"奥比-托平","pos":"PF / C","height":"6'9'","type":"Paint-Finishing Cleaner","ovr":80,"threePT":75,"MID":53,"FIN":98,"DNK":90,"HAN":90,"PAS":55,"PDEF":48,"IDEF":66,"BLK":32,"REB":83,"ATH":69,"STR":74,"CLU":95,"nbaId":1630167},{"name":"Aaron Nesmith","cname":"阿龙-内史密斯","pos":"SF / SG","height":"6'6'","type":"Crafty 3 & D Wing","ovr":80,"threePT":82,"MID":70,"FIN":90,"DNK":55,"HAN":85,"PAS":69,"PDEF":76,"IDEF":72,"BLK":55,"REB":53,"ATH":76,"STR":52,"CLU":50,"nbaId":1630174},{"name":"Kelly Oubre Jr.","cname":"小凯利-乌布雷","pos":"SF / PF","height":"6'8'","type":"Mid-Range High Flyer","ovr":80,"threePT":80,"MID":83,"FIN":91,"DNK":84,"HAN":88,"PAS":73,"PDEF":74,"IDEF":49,"BLK":53,"REB":60,"ATH":82,"STR":58,"CLU":40,"nbaId":1626162},{"name":"Jay Huff","cname":"杰伊-赫夫","pos":"C","height":"7'1'","type":"Shot-Erasing Mid-Range Scorer","ovr":79,"threePT":74,"MID":82,"FIN":98,"DNK":65,"HAN":80,"PAS":50,"PDEF":32,"IDEF":79,"BLK":93,"REB":65,"ATH":47,"STR":62,"CLU":90,"nbaId":1630643},{"name":"Quenton Jackson","cname":"昆顿-杰克逊","pos":"SG / PG","height":"6'4'","type":"Dime-Dropping 3PT Gunner","ovr":76,"threePT":82,"MID":45,"FIN":76,"DNK":80,"HAN":80,"PAS":78,"PDEF":73,"IDEF":46,"BLK":42,"REB":52,"ATH":85,"STR":42,"CLU":80},{"name":"Kobe Brown","cname":"科比-布朗","pos":"PF / SF","height":"6'7'","type":"Crafty Stretch Four Crasher","ovr":76,"threePT":81,"MID":72,"FIN":85,"DNK":65,"HAN":80,"PAS":69,"PDEF":62,"IDEF":62,"BLK":51,"REB":60,"ATH":62,"STR":74,"CLU":70},{"name":"Ben Sheppard","cname":"本-谢泼德","pos":"SG / SF","height":"6'6'","type":"2-Way Sharpshooter","ovr":75,"threePT":81,"MID":79,"FIN":85,"DNK":60,"HAN":80,"PAS":70,"PDEF":79,"IDEF":54,"BLK":35,"REB":55,"ATH":84,"STR":54,"CLU":75,"nbaId":1641767},{"name":"Johnny Furphy","cname":"约翰尼-弗菲","pos":"SF / SG","height":"6'8'","type":"Crafty 3PT-Sniping Cleaner","ovr":74,"threePT":79,"MID":77,"FIN":89,"DNK":60,"HAN":85,"PAS":60,"PDEF":57,"IDEF":52,"BLK":50,"REB":77,"ATH":63,"STR":54,"CLU":75},{"name":"Larry Nance Jr.","cname":"小拉里-南斯","pos":"C / PF","height":"6'6'","type":"Ball-Hawking Rim Rocker","ovr":73,"threePT":71,"MID":74,"FIN":93,"DNK":80,"HAN":78,"PAS":69,"PDEF":61,"IDEF":74,"BLK":74,"REB":70,"ATH":61,"STR":69,"CLU":50,"nbaId":1626204}],"LAC":[{"name":"Brandon Ingram","cname":"布兰登-英格拉姆","pos":"SF / PF","height":"6'8'","type":"Crafty Mid-Range Menace","ovr":86,"threePT":81,"MID":96,"FIN":88,"DNK":80,"HAN":98,"PAS":75,"PDEF":69,"IDEF":54,"BLK":61,"REB":66,"ATH":74,"STR":42,"CLU":70,"nbaId":1627742},{"name":"Darius Garland","cname":"达里厄斯-加兰","pos":"PG / SG","height":"6'1'","type":"Iso Sniper","ovr":85,"threePT":88,"MID":93,"FIN":92,"DNK":40,"HAN":85,"PAS":85,"PDEF":71,"IDEF":31,"BLK":37,"REB":42,"ATH":88,"STR":40,"CLU":60,"nbaId":1629636},{"name":"Bennedict Mathurin","cname":"本内迪克特-马图林","pos":"SG / SF","height":"6'5'","type":"Slashing Combo Guard","ovr":82,"threePT":75,"MID":79,"FIN":83,"DNK":80,"HAN":90,"PAS":75,"PDEF":69,"IDEF":42,"BLK":42,"REB":66,"ATH":84,"STR":54,"CLU":75,"nbaId":1631097},{"name":"Rui Hachimura","cname":"八村垒","pos":"PF / SF","height":"6'8'","type":"Stretch Lob Threat","ovr":81,"threePT":88,"MID":98,"FIN":87,"DNK":80,"HAN":85,"PAS":51,"PDEF":54,"IDEF":60,"BLK":50,"REB":51,"ATH":74,"STR":79,"CLU":65,"nbaId":1629060},{"name":"Derrick Jones Jr.","cname":"小德里克-琼斯","pos":"PF / SF","height":"6'6'","type":"2-Way Mid-Paint Scorer","ovr":79,"threePT":79,"MID":82,"FIN":91,"DNK":94,"HAN":90,"PAS":65,"PDEF":82,"IDEF":67,"BLK":75,"REB":48,"ATH":82,"STR":59,"CLU":80,"nbaId":1627884},{"name":"Brook Lopez","cname":"布鲁克-洛佩斯","pos":"C","height":"7'1'","type":"Shot-Erasing Pick & Popper","ovr":78,"threePT":80,"MID":74,"FIN":94,"DNK":65,"HAN":85,"PAS":35,"PDEF":45,"IDEF":80,"BLK":85,"REB":64,"ATH":60,"STR":89,"CLU":85,"nbaId":201572},{"name":"Kris Dunn","cname":"克里斯-邓恩","pos":"PG / SG","height":"6'3'","type":"Clamp Thief","ovr":77,"threePT":81,"MID":71,"FIN":95,"DNK":55,"HAN":80,"PAS":72,"PDEF":87,"IDEF":40,"BLK":40,"REB":51,"ATH":87,"STR":56,"CLU":40,"nbaId":1627739},{"name":"Jordan Miller","cname":"乔丹-米勒","pos":"SF / SG","height":"6'5'","type":"Mid-Paint Scorer","ovr":77,"threePT":75,"MID":90,"FIN":94,"DNK":80,"HAN":65,"PAS":60,"PDEF":65,"IDEF":54,"BLK":42,"REB":52,"ATH":78,"STR":52,"CLU":70,"nbaId":1641757},{"name":"Isaiah Jackson","cname":"以赛亚-杰克逊","pos":"C / PF","height":"6'9'","type":"Putback Pro","ovr":77,"threePT":59,"MID":69,"FIN":97,"DNK":70,"HAN":80,"PAS":40,"PDEF":54,"IDEF":70,"BLK":83,"REB":80,"ATH":73,"STR":65,"CLU":75,"nbaId":1630543},{"name":"Bradley Beal","cname":"布拉德利-比尔","pos":"SG / SF","height":"6'4'","type":"Laymaking Two Guard","ovr":77,"threePT":80,"MID":80,"FIN":85,"DNK":75,"HAN":92,"PAS":74,"PDEF":60,"IDEF":45,"BLK":56,"REB":47,"ATH":83,"STR":62,"CLU":60,"nbaId":203078},{"name":"Gradey Dick","cname":"格雷迪-迪克","pos":"SF / SG","height":"6'7'","type":"Mid-Range Menace","ovr":76,"threePT":72,"MID":92,"FIN":66,"DNK":75,"HAN":80,"PAS":67,"PDEF":73,"IDEF":55,"BLK":40,"REB":53,"ATH":74,"STR":54,"CLU":40,"nbaId":1641711},{"name":"Kobe Sanders","cname":"科比-桑德斯","pos":"PG / SF","height":"6'8'","type":"3PT-Sniping Rim Wrecker","ovr":76,"threePT":84,"MID":77,"FIN":85,"DNK":85,"HAN":95,"PAS":70,"PDEF":57,"IDEF":48,"BLK":40,"REB":51,"ATH":75,"STR":42,"CLU":98,"nbaId":1642920},{"name":"Yanic Konan Niederhauser","cname":"亚尼克-科南-尼德豪泽","pos":"C / PF","height":"6'11'","type":"Shot-Erasing Masher","ovr":75,"threePT":40,"MID":62,"FIN":95,"DNK":80,"HAN":80,"PAS":29,"PDEF":48,"IDEF":70,"BLK":87,"REB":76,"ATH":64,"STR":64,"CLU":65},{"name":"Nicolas Batum","cname":"尼古拉-巴图姆","pos":"PF / SF","height":"6'8'","type":"2-Way Stretch Dime-Dropper","ovr":74,"threePT":83,"MID":70,"FIN":77,"DNK":60,"HAN":85,"PAS":70,"PDEF":73,"IDEF":66,"BLK":56,"REB":61,"ATH":75,"STR":65,"CLU":70,"nbaId":201587},{"name":"TyTy Washington Jr.","cname":"小泰泰-华盛顿","pos":"PG / SG","height":"6'3'","type":"Blow-By High Flyer","ovr":70,"threePT":70,"MID":77,"FIN":78,"DNK":75,"HAN":80,"PAS":68,"PDEF":66,"IDEF":55,"BLK":37,"REB":52,"ATH":80,"STR":54,"CLU":50}],"LAL":[{"name":"Luka Doncic","cname":"卢卡-东契奇","pos":"PG / SG","height":"6'8'","type":"Crafty Offensive Engine","ovr":97,"threePT":89,"MID":94,"FIN":98,"DNK":75,"HAN":98,"PAS":95,"PDEF":59,"IDEF":58,"BLK":53,"REB":82,"ATH":77,"STR":72,"CLU":95,"nbaId":1629029},{"name":"Austin Reaves","cname":"奥斯汀-里夫斯","pos":"SG / SF","height":"6'5'","type":"Crafty Iniside-Out Combo Guard","ovr":87,"threePT":82,"MID":69,"FIN":97,"DNK":70,"HAN":85,"PAS":85,"PDEF":66,"IDEF":35,"BLK":51,"REB":59,"ATH":80,"STR":44,"CLU":85,"nbaId":1630559},{"name":"Walker Kessler","cname":"沃克-凯斯勒","pos":"C","height":"7'2'","type":"Crafty Mashing Board Enforcer","ovr":83,"threePT":70,"MID":74,"FIN":97,"DNK":75,"HAN":95,"PAS":42,"PDEF":65,"IDEF":84,"BLK":93,"REB":88,"ATH":52,"STR":74,"CLU":75,"nbaId":1631117},{"name":"Collin Sexton","cname":"科林-塞克斯顿","pos":"SG / PG","height":"6'3'","type":"3PT Creator","ovr":81,"threePT":84,"MID":82,"FIN":93,"DNK":75,"HAN":75,"PAS":75,"PDEF":67,"IDEF":45,"BLK":40,"REB":43,"ATH":88,"STR":48,"CLU":70,"nbaId":1629012},{"name":"Quentin Grimes","cname":"昆廷-格兰姆斯","pos":"SG / SF","height":"6'4'","type":"Physical Perimeter Lock","ovr":79,"threePT":76,"MID":70,"FIN":89,"DNK":70,"HAN":80,"PAS":70,"PDEF":83,"IDEF":46,"BLK":51,"REB":53,"ATH":84,"STR":63,"CLU":70,"nbaId":1629656},{"name":"Sandro Mamukelashvili","cname":"桑德罗-马穆凯拉什维利","pos":"PF / C","height":"6'9'","type":"Crafty Stretch Four Cleaner","ovr":79,"threePT":82,"MID":70,"FIN":90,"DNK":65,"HAN":85,"PAS":62,"PDEF":50,"IDEF":65,"BLK":63,"REB":71,"ATH":59,"STR":73,"CLU":95,"nbaId":1630572},{"name":"Jarred Vanderbilt","cname":"贾里德-范德比尔特","pos":"PF / C","height":"6'8'","type":"Lockdown Defender","ovr":76,"threePT":72,"MID":74,"FIN":58,"DNK":75,"HAN":85,"PAS":60,"PDEF":83,"IDEF":83,"BLK":56,"REB":79,"ATH":83,"STR":70,"CLU":40,"nbaId":1629020},{"name":"Jaden Hardy","cname":"杰登-哈迪","pos":"SG / PG","height":"6'3'","type":"Iso Sniper","ovr":76,"threePT":84,"MID":94,"FIN":83,"DNK":80,"HAN":85,"PAS":75,"PDEF":55,"IDEF":34,"BLK":40,"REB":46,"ATH":83,"STR":39,"CLU":70},{"name":"Jake LaRavia","cname":"杰克-拉雷维亚","pos":"SF / SG","height":"6'7'","type":"2-Way Crafty Finisher","ovr":76,"threePT":75,"MID":55,"FIN":94,"DNK":75,"HAN":70,"PAS":69,"PDEF":78,"IDEF":78,"BLK":56,"REB":52,"ATH":78,"STR":62,"CLU":70,"nbaId":1631222},{"name":"Maxi Kleber","cname":"马克西-克勒贝尔","pos":"C / PF","height":"6'10'","type":"2-Way Table Setter","ovr":74,"threePT":67,"MID":70,"FIN":86,"DNK":75,"HAN":80,"PAS":70,"PDEF":77,"IDEF":80,"BLK":63,"REB":54,"ATH":77,"STR":79,"CLU":50},{"name":"Dalton Knecht","cname":"多尔顿-克内克特","pos":"SF / SG","height":"6'6'","type":"Mid-Paint Scorer","ovr":74,"threePT":79,"MID":91,"FIN":92,"DNK":65,"HAN":85,"PAS":64,"PDEF":59,"IDEF":50,"BLK":63,"REB":53,"ATH":59,"STR":50,"CLU":40,"nbaId":1642261},{"name":"Kevon Looney","cname":"凯文-卢尼","pos":"C","height":"6'9'","type":"Board-Hunting Stretch Five","ovr":74,"threePT":69,"MID":44,"FIN":74,"DNK":70,"HAN":66,"PAS":55,"PDEF":46,"IDEF":72,"BLK":70,"REB":77,"ATH":69,"STR":74,"CLU":40,"nbaId":1626172},{"name":"Nick Smith Jr.","cname":"小尼克-史密斯","pos":"PG / SG","height":"6'2'","type":"Inside-Out High-Flying Slasher","ovr":73,"threePT":83,"MID":70,"FIN":96,"DNK":80,"HAN":80,"PAS":70,"PDEF":63,"IDEF":33,"BLK":42,"REB":41,"ATH":84,"STR":48,"CLU":65,"nbaId":1641733},{"name":"Drew Timme","cname":"德鲁-蒂米","pos":"C / PF","height":"6'10'","type":"Crafty Stretch Five","ovr":72,"threePT":75,"MID":75,"FIN":86,"DNK":65,"HAN":80,"PAS":58,"PDEF":40,"IDEF":57,"BLK":45,"REB":60,"ATH":40,"STR":68,"CLU":75},{"name":"Bronny James","cname":"布朗尼-詹姆斯","pos":"PG / SG","height":"6'2'","type":"Blow-By High Flyer","ovr":71,"threePT":79,"MID":72,"FIN":64,"DNK":80,"HAN":85,"PAS":67,"PDEF":72,"IDEF":47,"BLK":44,"REB":50,"ATH":87,"STR":60,"CLU":80}],"MEM":[{"name":"Ty Jerome","cname":"泰-杰罗姆","pos":"PG / SG","height":"6'5'","type":"Crafty Iso Savant","ovr":83,"threePT":86,"MID":93,"FIN":95,"DNK":45,"HAN":80,"PAS":84,"PDEF":56,"IDEF":32,"BLK":42,"REB":50,"ATH":73,"STR":46,"CLU":75,"nbaId":1629660},{"name":"Zach Edey","cname":"扎克-埃迪","pos":"C","height":"7'4'","type":"Glass Guardian Crafty Finisher","ovr":82,"threePT":76,"MID":78,"FIN":98,"DNK":70,"HAN":95,"PAS":58,"PDEF":37,"IDEF":79,"BLK":94,"REB":91,"ATH":31,"STR":84,"CLU":65,"nbaId":1641744},{"name":"Jerami Grant","cname":"杰拉米-格兰特","pos":"SF / PF","height":"6'7'","type":"Crafty Inside-Out Creator","ovr":81,"threePT":82,"MID":62,"FIN":80,"DNK":80,"HAN":95,"PAS":69,"PDEF":69,"IDEF":70,"BLK":58,"REB":48,"ATH":77,"STR":69,"CLU":70,"nbaId":203924},{"name":"Cedric Coward","cname":"塞德里克-科沃德","pos":"SG / SF","height":"6'5'","type":"Wing-Running Finisher","ovr":80,"threePT":76,"MID":69,"FIN":92,"DNK":80,"HAN":80,"PAS":68,"PDEF":73,"IDEF":58,"BLK":55,"REB":73,"ATH":84,"STR":57,"CLU":98,"nbaId":1642907},{"name":"GG Jackson","cname":"GG-杰克逊","pos":"PF / SF","height":"6'9'","type":"Rim-Rocking Mid-Range Slasher","ovr":80,"threePT":76,"MID":89,"FIN":88,"DNK":85,"HAN":95,"PAS":59,"PDEF":58,"IDEF":58,"BLK":73,"REB":68,"ATH":79,"STR":62,"CLU":90},{"name":"Jaylen Wells","cname":"杰伦-威尔斯","pos":"SF / SG","height":"6'8'","type":"Mid-Paint Scorer","ovr":79,"threePT":79,"MID":90,"FIN":94,"DNK":70,"HAN":80,"PAS":62,"PDEF":80,"IDEF":51,"BLK":37,"REB":48,"ATH":80,"STR":60,"CLU":70,"nbaId":1642377},{"name":"Cam Spencer","cname":"卡姆-斯潘塞","pos":"SG / PG","height":"6'3'","type":"Diming Crafty Sharpshooter","ovr":79,"threePT":88,"MID":86,"FIN":97,"DNK":55,"HAN":85,"PAS":84,"PDEF":59,"IDEF":44,"BLK":40,"REB":47,"ATH":73,"STR":45,"CLU":75,"nbaId":1642285},{"name":"Isaiah Stewart","cname":"以赛亚-斯图尔特","pos":"C / PF","height":"6'8'","type":"2-Way Pick & Popper","ovr":78,"threePT":77,"MID":77,"FIN":92,"DNK":70,"HAN":70,"PAS":44,"PDEF":67,"IDEF":84,"BLK":91,"REB":66,"ATH":71,"STR":89,"CLU":60,"nbaId":1630191},{"name":"Olivier-Maxence Prosper","cname":"奥利维耶-马克桑斯-普罗斯珀","pos":"C / PF","height":"6'7'","type":"Rim-Rocking Stretch Five","ovr":78,"threePT":83,"MID":68,"FIN":92,"DNK":85,"HAN":80,"PAS":65,"PDEF":74,"IDEF":74,"BLK":52,"REB":62,"ATH":81,"STR":60,"CLU":90},{"name":"Javon Small","cname":"贾冯-斯莫尔","pos":"PG / SG","height":"6'1'","type":"Inside-Out High-Flying Slasher","ovr":77,"threePT":87,"MID":78,"FIN":71,"DNK":80,"HAN":85,"PAS":75,"PDEF":64,"IDEF":44,"BLK":50,"REB":56,"ATH":85,"STR":49,"CLU":95},{"name":"Scotty Pippen Jr.","cname":"小斯科蒂-皮蓬","pos":"PG / SG","height":"6'1'","type":"Chaos General","ovr":77,"threePT":75,"MID":79,"FIN":74,"DNK":65,"HAN":72,"PAS":85,"PDEF":62,"IDEF":31,"BLK":62,"REB":39,"ATH":82,"STR":39,"CLU":80,"nbaId":1630590},{"name":"D’Angelo Russell","cname":"丹吉洛-拉塞尔","pos":"PG / SG","height":"6'3'","type":"Crafty Mid-Range Menace","ovr":76,"threePT":76,"MID":92,"FIN":94,"DNK":45,"HAN":81,"PAS":82,"PDEF":57,"IDEF":40,"BLK":37,"REB":53,"ATH":65,"STR":54,"CLU":50,"nbaId":1626156},{"name":"Quinten Post","cname":"昆滕-波斯特","pos":"C","height":"7'0'","type":"Crafty Stretch Five","ovr":76,"threePT":76,"MID":89,"FIN":95,"DNK":65,"HAN":80,"PAS":59,"PDEF":33,"IDEF":62,"BLK":67,"REB":74,"ATH":33,"STR":70,"CLU":60,"nbaId":1642366},{"name":"Taylor Hendricks","cname":"泰勒-亨德里克斯","pos":"PF / SF","height":"6'9'","type":"Crafty Mid-Paint Scorer","ovr":75,"threePT":76,"MID":78,"FIN":90,"DNK":75,"HAN":85,"PAS":55,"PDEF":68,"IDEF":68,"BLK":65,"REB":64,"ATH":75,"STR":62,"CLU":80,"nbaId":1641707},{"name":"Walter Clayton Jr.","cname":"小沃尔特-克莱顿","pos":"PG / SG","height":"6'4'","type":"Crafty Mid-Range Menace","ovr":75,"threePT":71,"MID":84,"FIN":89,"DNK":60,"HAN":85,"PAS":74,"PDEF":64,"IDEF":40,"BLK":53,"REB":47,"ATH":84,"STR":47,"CLU":70,"nbaId":1642383}],"MIA":[{"name":"Giannis Antetokounmpo","cname":"扬尼斯-阿德托昆博","pos":"PF / C","height":"6'11'","type":"2-Way Crafty PlaySlash Cleaner","ovr":97,"threePT":76,"MID":59,"FIN":96,"DNK":91,"HAN":95,"PAS":84,"PDEF":87,"IDEF":90,"BLK":62,"REB":90,"ATH":86,"STR":94,"CLU":85,"nbaId":203507},{"name":"Bam Adebayo","cname":"巴姆-阿德巴约","pos":"C / PF","height":"6'9'","type":"2-Way Paint-Finishing Cleaner","ovr":89,"threePT":74,"MID":79,"FIN":87,"DNK":80,"HAN":90,"PAS":72,"PDEF":88,"IDEF":90,"BLK":57,"REB":87,"ATH":84,"STR":85,"CLU":70,"nbaId":1628389},{"name":"Andrew Wiggins","cname":"安德鲁-威金斯","pos":"PF / SF","height":"6'7'","type":"Inside-Out Rim-Rocking Slasher","ovr":81,"threePT":86,"MID":73,"FIN":88,"DNK":85,"HAN":85,"PAS":62,"PDEF":74,"IDEF":55,"BLK":71,"REB":52,"ATH":82,"STR":57,"CLU":85,"nbaId":203952},{"name":"Bobby Portis","cname":"博比-波蒂斯","pos":"C / PF","height":"6'10'","type":"Crafty Post-Scoring Stretch","ovr":80,"threePT":89,"MID":82,"FIN":96,"DNK":70,"HAN":85,"PAS":55,"PDEF":40,"IDEF":78,"BLK":45,"REB":84,"ATH":64,"STR":83,"CLU":70,"nbaId":1626171},{"name":"Pelle Larsson","cname":"佩莱-拉松","pos":"SF / SG","height":"6'5'","type":"Mid-Paint Scorer","ovr":80,"threePT":75,"MID":87,"FIN":94,"DNK":75,"HAN":80,"PAS":64,"PDEF":65,"IDEF":52,"BLK":45,"REB":49,"ATH":80,"STR":55,"CLU":85,"nbaId":1641796},{"name":"Davion Mitchell","cname":"戴维恩-米切尔","pos":"PG / SG","height":"6'0'","type":"3 & D Creator","ovr":79,"threePT":83,"MID":74,"FIN":98,"DNK":40,"HAN":95,"PAS":75,"PDEF":87,"IDEF":44,"BLK":37,"REB":45,"ATH":88,"STR":60,"CLU":65,"nbaId":1630558},{"name":"Simone Fontecchio","cname":"西莫内-丰泰基奥","pos":"SF / PF","height":"6'7'","type":"Crafty 3-Level Scorer","ovr":77,"threePT":82,"MID":83,"FIN":75,"DNK":65,"HAN":69,"PAS":55,"PDEF":65,"IDEF":51,"BLK":42,"REB":60,"ATH":66,"STR":62,"CLU":80,"nbaId":1631323},{"name":"Dru Smith","cname":"德鲁-史密斯","pos":"PG / SG","height":"6'2'","type":"2-Way Crafty Finisher","ovr":75,"threePT":71,"MID":77,"FIN":84,"DNK":45,"HAN":80,"PAS":78,"PDEF":74,"IDEF":40,"BLK":56,"REB":51,"ATH":70,"STR":43,"CLU":90,"nbaId":1630696},{"name":"Myron Gardner","cname":"迈伦-加德纳","pos":"SF / SG","height":"6'5'","type":"Board-Hunting 3PT Sniper","ovr":75,"threePT":83,"MID":72,"FIN":88,"DNK":70,"HAN":84,"PAS":69,"PDEF":69,"IDEF":45,"BLK":48,"REB":78,"ATH":78,"STR":62,"CLU":65},{"name":"Nikola Jovic","cname":"尼古拉-约维奇","pos":"C / PF","height":"6'10'","type":"Crafty Inside-Out Point Center","ovr":74,"threePT":73,"MID":70,"FIN":80,"DNK":60,"HAN":90,"PAS":69,"PDEF":62,"IDEF":59,"BLK":57,"REB":68,"ATH":68,"STR":44,"CLU":95,"nbaId":1631107},{"name":"Keshad Johnson","cname":"凯沙德-约翰逊","pos":"PF / SF","height":"6'6'","type":"Rim-Running Rim Rocker","ovr":72,"threePT":71,"MID":70,"FIN":75,"DNK":86,"HAN":80,"PAS":57,"PDEF":60,"IDEF":52,"BLK":70,"REB":66,"ATH":80,"STR":65,"CLU":60},{"name":"Trevor Keels","cname":"特雷弗-基尔斯","pos":"SG","height":"6'4'","type":"Physical Combo Guard","ovr":68,"threePT":71,"MID":68,"FIN":71,"DNK":70,"HAN":72,"PAS":74,"PDEF":60,"IDEF":37,"BLK":51,"REB":47,"ATH":74,"STR":60,"CLU":30},{"name":"Vladislav Goldin","cname":"弗拉季斯拉夫-戈尔丁","pos":"C","height":"7'0'","type":"Stretch Lob Threat","ovr":68,"threePT":66,"MID":68,"FIN":70,"DNK":65,"HAN":75,"PAS":59,"PDEF":45,"IDEF":66,"BLK":67,"REB":65,"ATH":30,"STR":70,"CLU":50},{"name":"Jahmir Young","cname":"贾米尔-扬","pos":"PG","height":"6'2'","type":"Mid-Range Menace","ovr":67,"threePT":72,"MID":81,"FIN":74,"DNK":25,"HAN":75,"PAS":75,"PDEF":64,"IDEF":27,"BLK":50,"REB":56,"ATH":78,"STR":51,"CLU":40}],"MIL":[{"name":"Tyler Herro","cname":"泰勒-希罗","pos":"SG / PG","height":"6'5'","type":"Crafty Offensive Engine","ovr":87,"threePT":83,"MID":90,"FIN":98,"DNK":60,"HAN":95,"PAS":79,"PDEF":69,"IDEF":46,"BLK":51,"REB":60,"ATH":78,"STR":59,"CLU":80,"nbaId":1629639},{"name":"Kevin Porter Jr.","cname":"小凯文-波特","pos":"PG / SG","height":"6'4'","type":"Ball-Hawking Mid-Paint Scorer","ovr":82,"threePT":76,"MID":94,"FIN":79,"DNK":80,"HAN":80,"PAS":82,"PDEF":69,"IDEF":44,"BLK":53,"REB":62,"ATH":75,"STR":52,"CLU":70,"nbaId":1629645},{"name":"Ryan Rollins","cname":"瑞安-罗林斯","pos":"PG / SG","height":"6'3'","type":"Crafty 3PT-Sniping Creator","ovr":82,"threePT":84,"MID":79,"FIN":85,"DNK":65,"HAN":85,"PAS":79,"PDEF":68,"IDEF":43,"BLK":52,"REB":60,"ATH":86,"STR":39,"CLU":85,"nbaId":1631157},{"name":"Kyle Kuzma","cname":"凯尔-库兹马","pos":"SF / PF","height":"6'9'","type":"Drive & Rise Bully","ovr":80,"threePT":78,"MID":79,"FIN":84,"DNK":75,"HAN":95,"PAS":66,"PDEF":75,"IDEF":66,"BLK":55,"REB":66,"ATH":74,"STR":74,"CLU":60,"nbaId":1628398},{"name":"Jaime Jaquez Jr.","cname":"小海梅-哈克斯","pos":"PF / SF","height":"6'6'","type":"Versatile Paint Finisher","ovr":80,"threePT":73,"MID":69,"FIN":92,"DNK":88,"HAN":80,"PAS":70,"PDEF":72,"IDEF":68,"BLK":47,"REB":61,"ATH":75,"STR":68,"CLU":90,"nbaId":1631170},{"name":"Kel’el Ware","cname":"凯尔-韦尔","pos":"C / PF","height":"7'0'","type":"Board-Hunting Stretch Dunker","ovr":80,"threePT":82,"MID":64,"FIN":77,"DNK":86,"HAN":90,"PAS":37,"PDEF":37,"IDEF":75,"BLK":82,"REB":91,"ATH":68,"STR":66,"CLU":60,"nbaId":1642276},{"name":"Caris LeVert","cname":"卡里斯-勒韦尔","pos":"SF / SG","height":"6'7'","type":"Shot-Erasing Mid-Range Menace","ovr":79,"threePT":79,"MID":84,"FIN":83,"DNK":75,"HAN":95,"PAS":75,"PDEF":74,"IDEF":45,"BLK":73,"REB":47,"ATH":77,"STR":54,"CLU":55,"nbaId":1627747},{"name":"Myles Turner","cname":"迈尔斯-特纳","pos":"C","height":"6'11'","type":"2-Way Stretch Five","ovr":78,"threePT":82,"MID":88,"FIN":85,"DNK":65,"HAN":85,"PAS":31,"PDEF":70,"IDEF":87,"BLK":87,"REB":69,"ATH":66,"STR":80,"CLU":35,"nbaId":1626167},{"name":"Ousmane Dieng","cname":"乌斯曼-迪昂","pos":"SF / PF","height":"6'9'","type":"Iso Sniper","ovr":77,"threePT":80,"MID":77,"FIN":85,"DNK":75,"HAN":90,"PAS":69,"PDEF":68,"IDEF":67,"BLK":56,"REB":62,"ATH":78,"STR":62,"CLU":80},{"name":"AJ Green","cname":"AJ-格林","pos":"SG / SF","height":"6'4'","type":"Crafty Sharpshooter","ovr":76,"threePT":85,"MID":84,"FIN":90,"DNK":40,"HAN":80,"PAS":69,"PDEF":63,"IDEF":40,"BLK":32,"REB":47,"ATH":69,"STR":42,"CLU":75,"nbaId":1631260},{"name":"Kasparas Jakucionis","cname":"卡斯帕拉斯-雅库西奥尼斯","pos":"PG / SG","height":"6'6'","type":"3PT Shot Hunter","ovr":76,"threePT":85,"MID":71,"FIN":60,"DNK":70,"HAN":80,"PAS":82,"PDEF":66,"IDEF":59,"BLK":42,"REB":51,"ATH":82,"STR":60,"CLU":75},{"name":"Gary Trent Jr.","cname":"小加里-特伦特","pos":"SG / SF","height":"6'5'","type":"Sharpshooter","ovr":75,"threePT":82,"MID":95,"FIN":73,"DNK":75,"HAN":73,"PAS":70,"PDEF":61,"IDEF":50,"BLK":30,"REB":37,"ATH":76,"STR":58,"CLU":65,"nbaId":1629018},{"name":"Jericho Sims","cname":"杰里科-西姆斯","pos":"C","height":"6'10'","type":"Rim-Running Rim Rocker","ovr":74,"threePT":26,"MID":69,"FIN":90,"DNK":89,"HAN":80,"PAS":30,"PDEF":40,"IDEF":66,"BLK":51,"REB":75,"ATH":80,"STR":65,"CLU":60},{"name":"Kam Jones","cname":"卡姆-琼斯","pos":"SG / PG","height":"6'4'","type":"Crafty Laymaking Combo Guard","ovr":74,"threePT":73,"MID":79,"FIN":88,"DNK":60,"HAN":89,"PAS":79,"PDEF":60,"IDEF":30,"BLK":25,"REB":44,"ATH":75,"STR":52,"CLU":80},{"name":"Andre Jackson Jr.","cname":"小安德烈-杰克逊","pos":"SF / SG","height":"6'6'","type":"High-Flying Wing Runner","ovr":72,"threePT":81,"MID":68,"FIN":70,"DNK":87,"HAN":80,"PAS":65,"PDEF":75,"IDEF":53,"BLK":40,"REB":53,"ATH":82,"STR":50,"CLU":30}],"MIN":[{"name":"Anthony Edwards","cname":"安东尼-爱德华兹","pos":"SG / SF","height":"6'4'","type":"2-Way Inside-Out Creator","ovr":96,"threePT":89,"MID":84,"FIN":96,"DNK":97,"HAN":98,"PAS":79,"PDEF":87,"IDEF":59,"BLK":61,"REB":60,"ATH":90,"STR":62,"CLU":95,"nbaId":1630162},{"name":"LaMelo Ball","cname":"拉梅洛-鲍尔","pos":"PG / SG","height":"6'7'","type":"Playmaking 3PT Creator","ovr":88,"threePT":84,"MID":76,"FIN":83,"DNK":75,"HAN":98,"PAS":93,"PDEF":70,"IDEF":45,"BLK":42,"REB":65,"ATH":84,"STR":50,"CLU":85,"nbaId":1630163},{"name":"Rudy Gobert","cname":"鲁迪-戈贝尔","pos":"C","height":"7'1'","type":"Lob Threat Board Enforcer","ovr":85,"threePT":25,"MID":54,"FIN":80,"DNK":75,"HAN":85,"PAS":68,"PDEF":64,"IDEF":90,"BLK":82,"REB":87,"ATH":65,"STR":87,"CLU":80,"nbaId":203497},{"name":"Jaden McDaniels","cname":"贾登-麦克丹尼尔斯","pos":"SF / PF","height":"6'9'","type":"2-Way Off-Screen Shooter","ovr":85,"threePT":85,"MID":89,"FIN":94,"DNK":75,"HAN":85,"PAS":44,"PDEF":90,"IDEF":83,"BLK":67,"REB":51,"ATH":91,"STR":60,"CLU":75,"nbaId":1630183},{"name":"Ayo Dosunmu","cname":"阿约-多孙穆","pos":"SG / PG","height":"6'4'","type":"Crafty 3-Level Scorer","ovr":83,"threePT":88,"MID":90,"FIN":89,"DNK":75,"HAN":80,"PAS":72,"PDEF":69,"IDEF":38,"BLK":47,"REB":52,"ATH":78,"STR":52,"CLU":75,"nbaId":1630245},{"name":"Donte DiVincenzo","cname":"唐特-迪温琴佐","pos":"PG / SG","height":"6'4'","type":"2-Way Off-Screen Shooter","ovr":80,"threePT":84,"MID":88,"FIN":84,"DNK":75,"HAN":70,"PAS":77,"PDEF":82,"IDEF":58,"BLK":51,"REB":54,"ATH":87,"STR":60,"CLU":75,"nbaId":1628978},{"name":"Terrence Shannon Jr.","cname":"小特伦斯-香农","pos":"SF / PF","height":"6'6'","type":"Inside-Out Slasher","ovr":77,"threePT":82,"MID":76,"FIN":60,"DNK":85,"HAN":85,"PAS":68,"PDEF":72,"IDEF":49,"BLK":30,"REB":45,"ATH":87,"STR":58,"CLU":65},{"name":"Bones Hyland","cname":"伯恩斯-海兰","pos":"PG / SG","height":"6'2'","type":"Iso Sniper","ovr":76,"threePT":82,"MID":88,"FIN":75,"DNK":60,"HAN":85,"PAS":74,"PDEF":66,"IDEF":30,"BLK":51,"REB":47,"ATH":87,"STR":32,"CLU":60,"nbaId":1630538},{"name":"Josh Green","cname":"乔什-格林","pos":"SF / SG","height":"6'6'","type":"3PT Sniper","ovr":75,"threePT":86,"MID":69,"FIN":96,"DNK":80,"HAN":95,"PAS":67,"PDEF":79,"IDEF":35,"BLK":37,"REB":41,"ATH":86,"STR":60,"CLU":70,"nbaId":1630182},{"name":"Trey Lyles","cname":"Trey Lyles","pos":"PF / C","height":"6'9\"","type":"Stretch Four Cleaner","ovr":74,"threePT":77,"MID":70,"FIN":70,"DNK":65,"HAN":70,"PAS":70,"PDEF":70,"IDEF":70,"BLK":70,"REB":70,"ATH":70,"STR":70,"CLU":70},{"name":"Jaylen Clark","cname":"杰伦-克拉克","pos":"SG / SF","height":"6'4'","type":"Clamp Thief","ovr":73,"threePT":73,"MID":67,"FIN":78,"DNK":70,"HAN":70,"PAS":62,"PDEF":81,"IDEF":52,"BLK":50,"REB":44,"ATH":86,"STR":55,"CLU":75},{"name":"Joe Ingles","cname":"乔-英格尔斯","pos":"PF / SF","height":"6'8'","type":"2-Way Stretch Four","ovr":73,"threePT":86,"MID":65,"FIN":64,"DNK":45,"HAN":70,"PAS":75,"PDEF":80,"IDEF":58,"BLK":35,"REB":56,"ATH":80,"STR":64,"CLU":65,"nbaId":204060},{"name":"Julian Phillips","cname":"朱利安-菲利普斯","pos":"SF / PF","height":"6'6'","type":"High-Flying 3PT Sniper","ovr":72,"threePT":75,"MID":68,"FIN":81,"DNK":80,"HAN":80,"PAS":60,"PDEF":68,"IDEF":55,"BLK":55,"REB":41,"ATH":78,"STR":40,"CLU":50},{"name":"Joan Beringer","cname":"若昂-贝林格","pos":"C","height":"6'11'","type":"Rim-Wrecking Putback Pro","ovr":72,"threePT":26,"MID":45,"FIN":69,"DNK":80,"HAN":80,"PAS":49,"PDEF":40,"IDEF":73,"BLK":80,"REB":70,"ATH":67,"STR":65,"CLU":50},{"name":"Enrique Freeman","cname":"恩里克-弗里曼","pos":"PF / C","height":"6'9'","type":"Bouncy Dual Finisher","ovr":69,"threePT":66,"MID":66,"FIN":74,"DNK":80,"HAN":80,"PAS":59,"PDEF":66,"IDEF":66,"BLK":56,"REB":57,"ATH":74,"STR":60,"CLU":75}],"NOP":[{"name":"Zion Williamson","cname":"锡安-威廉姆森","pos":"PF / PG","height":"6'6'","type":"Versatile Paint Bully","ovr":86,"threePT":67,"MID":79,"FIN":83,"DNK":93,"HAN":95,"PAS":65,"PDEF":55,"IDEF":59,"BLK":56,"REB":59,"ATH":81,"STR":92,"CLU":60,"nbaId":1629627},{"name":"Trey Murphy III","cname":"特雷-墨菲三世","pos":"SG / SF","height":"6'8'","type":"Crafty Inside-Out Scorer","ovr":85,"threePT":81,"MID":74,"FIN":84,"DNK":88,"HAN":80,"PAS":59,"PDEF":69,"IDEF":69,"BLK":50,"REB":62,"ATH":76,"STR":60,"CLU":90,"nbaId":1630530},{"name":"Dejounte Murray","cname":"德章泰-默里","pos":"PG / SG","height":"6'4'","type":"2-Way Mid-Range Menace","ovr":84,"threePT":74,"MID":98,"FIN":97,"DNK":75,"HAN":95,"PAS":82,"PDEF":83,"IDEF":53,"BLK":42,"REB":76,"ATH":85,"STR":40,"CLU":70,"nbaId":1627749},{"name":"Saddiq Bey","cname":"萨迪克-贝","pos":"SF / PF","height":"6'8'","type":"3PT-Sniping Layup Bully","ovr":84,"threePT":82,"MID":64,"FIN":87,"DNK":75,"HAN":95,"PAS":64,"PDEF":69,"IDEF":64,"BLK":32,"REB":60,"ATH":72,"STR":74,"CLU":80,"nbaId":1630180},{"name":"Jeremiah Fears","cname":"杰里迈亚-费尔斯","pos":"PG / SG","height":"6'3'","type":"Speedy Blow-By Ace","ovr":80,"threePT":77,"MID":75,"FIN":73,"DNK":70,"HAN":85,"PAS":78,"PDEF":62,"IDEF":38,"BLK":53,"REB":56,"ATH":88,"STR":49,"CLU":80,"nbaId":1642847},{"name":"Derik Queen","cname":"德里克-奎因","pos":"PF / C","height":"6'9'","type":"Break-Starting Crafty Finisher","ovr":80,"threePT":66,"MID":71,"FIN":85,"DNK":70,"HAN":90,"PAS":79,"PDEF":52,"IDEF":69,"BLK":73,"REB":83,"ATH":54,"STR":80,"CLU":70,"nbaId":1642852},{"name":"Herbert Jones","cname":"赫伯特-琼斯","pos":"SF / SG","height":"6'7'","type":"Lockdown Defender","ovr":79,"threePT":73,"MID":75,"FIN":65,"DNK":75,"HAN":85,"PAS":65,"PDEF":91,"IDEF":85,"BLK":56,"REB":49,"ATH":91,"STR":59,"CLU":30,"nbaId":1630529},{"name":"Yves Missi","cname":"伊夫-米西","pos":"C / PF","height":"6'11'","type":"Rim-Running Putback Eraser","ovr":78,"threePT":30,"MID":42,"FIN":78,"DNK":80,"HAN":90,"PAS":29,"PDEF":48,"IDEF":75,"BLK":92,"REB":67,"ATH":67,"STR":72,"CLU":75,"nbaId":1642274},{"name":"DeAndre Jordan","cname":"德安德烈-乔丹","pos":"C","height":"6'11'","type":"Physical Glass Guardian","ovr":76,"threePT":25,"MID":25,"FIN":75,"DNK":75,"HAN":90,"PAS":25,"PDEF":57,"IDEF":70,"BLK":85,"REB":94,"ATH":62,"STR":85,"CLU":75},{"name":"Jordan Poole","cname":"乔丹-普尔","pos":"SG / PG","height":"6'4'","type":"Combo Guard","ovr":76,"threePT":75,"MID":66,"FIN":77,"DNK":60,"HAN":80,"PAS":83,"PDEF":57,"IDEF":47,"BLK":53,"REB":45,"ATH":79,"STR":61,"CLU":60,"nbaId":1629673},{"name":"Karlo Matkovic","cname":"卡洛-马特科维奇","pos":"PF / C","height":"6'10'","type":"Shot-Erasing Stretch Riser","ovr":76,"threePT":84,"MID":75,"FIN":78,"DNK":75,"HAN":80,"PAS":50,"PDEF":41,"IDEF":73,"BLK":87,"REB":72,"ATH":51,"STR":77,"CLU":70},{"name":"Bryce McGowens","cname":"布赖斯-麦高恩斯","pos":"SF / SG","height":"6'6'","type":"3-Level Scorer","ovr":75,"threePT":84,"MID":82,"FIN":86,"DNK":80,"HAN":78,"PAS":52,"PDEF":66,"IDEF":37,"BLK":42,"REB":48,"ATH":75,"STR":48,"CLU":50},{"name":"Jordan Hawkins","cname":"乔丹-霍金斯","pos":"SG / PG","height":"6'5'","type":"3PT-Creating Combo Guard","ovr":72,"threePT":77,"MID":71,"FIN":53,"DNK":60,"HAN":80,"PAS":70,"PDEF":65,"IDEF":45,"BLK":53,"REB":50,"ATH":72,"STR":48,"CLU":75,"nbaId":1641722},{"name":"Micah Peavy","cname":"迈卡-皮维","pos":"SF / SG","height":"6'7'","type":"Slashing Point Forward","ovr":72,"threePT":69,"MID":75,"FIN":59,"DNK":80,"HAN":85,"PAS":75,"PDEF":71,"IDEF":59,"BLK":47,"REB":47,"ATH":82,"STR":53,"CLU":50},{"name":"Hunter Dickinson","cname":"亨特-迪金森","pos":"C","height":"7'1'","type":"Stretch Five Cleaner","ovr":68,"threePT":69,"MID":70,"FIN":78,"DNK":65,"HAN":66,"PAS":59,"PDEF":52,"IDEF":67,"BLK":60,"REB":79,"ATH":39,"STR":74,"CLU":40}],"NYK":[{"name":"Jalen Brunson","cname":"杰伦-布伦森","pos":"PG","height":"6'2'","type":"Crafty Offensive Engine","ovr":96,"threePT":89,"MID":97,"FIN":96,"DNK":35,"HAN":98,"PAS":96,"PDEF":73,"IDEF":55,"BLK":32,"REB":48,"ATH":84,"STR":60,"CLU":90,"nbaId":1628973},{"name":"Karl-Anthony Towns","cname":"卡尔-安东尼-唐斯","pos":"C / PF","height":"7'0'","type":"Inside-Out Break Starter","ovr":91,"threePT":83,"MID":69,"FIN":93,"DNK":75,"HAN":98,"PAS":75,"PDEF":50,"IDEF":71,"BLK":57,"REB":93,"ATH":76,"STR":87,"CLU":85,"nbaId":1626157},{"name":"OG Anunoby","cname":"OG-阿奴诺比","pos":"SF / PF","height":"6'7'","type":"2-Way 3PT-Sniping High Flyer","ovr":88,"threePT":85,"MID":76,"FIN":81,"DNK":85,"HAN":90,"PAS":58,"PDEF":90,"IDEF":81,"BLK":62,"REB":59,"ATH":91,"STR":68,"CLU":80,"nbaId":1628384},{"name":"Mikal Bridges","cname":"米卡尔-布里奇斯","pos":"SG / SF","height":"6'6'","type":"2-Way Combo Guard Creator","ovr":84,"threePT":82,"MID":89,"FIN":92,"DNK":75,"HAN":80,"PAS":78,"PDEF":88,"IDEF":69,"BLK":63,"REB":49,"ATH":87,"STR":68,"CLU":50,"nbaId":1628969},{"name":"Josh Hart","cname":"约什-哈特","pos":"PF / SF","height":"6'5'","type":"2-Way Skilled Stretch Cleaner","ovr":82,"threePT":84,"MID":82,"FIN":94,"DNK":70,"HAN":95,"PAS":73,"PDEF":86,"IDEF":78,"BLK":47,"REB":82,"ATH":86,"STR":64,"CLU":80,"nbaId":1628404},{"name":"Miles McBride","cname":"迈尔斯-麦克布莱德","pos":"SG / PG","height":"6'2'","type":"2-Way Iso Sniper","ovr":79,"threePT":86,"MID":91,"FIN":58,"DNK":65,"HAN":80,"PAS":72,"PDEF":81,"IDEF":40,"BLK":40,"REB":44,"ATH":86,"STR":44,"CLU":80,"nbaId":1630540},{"name":"Jose Alvarado","cname":"何塞-阿尔瓦拉多","pos":"PG / SG","height":"6'0'","type":"2-Way Crafty Finisher","ovr":78,"threePT":78,"MID":74,"FIN":91,"DNK":30,"HAN":85,"PAS":70,"PDEF":85,"IDEF":31,"BLK":37,"REB":53,"ATH":86,"STR":42,"CLU":60,"nbaId":1630631},{"name":"Landry Shamet","cname":"兰德里-沙梅特","pos":"SG / SF","height":"6'5'","type":"Shot-Creating Combo Guard","ovr":77,"threePT":83,"MID":97,"FIN":69,"DNK":70,"HAN":76,"PAS":79,"PDEF":60,"IDEF":25,"BLK":45,"REB":41,"ATH":85,"STR":41,"CLU":70,"nbaId":1629013},{"name":"Jordan Clarkson","cname":"乔丹-克拉克森","pos":"SG / SF","height":"6'5'","type":"Laymaking Two Guard","ovr":77,"threePT":76,"MID":63,"FIN":97,"DNK":70,"HAN":98,"PAS":73,"PDEF":58,"IDEF":43,"BLK":42,"REB":38,"ATH":75,"STR":54,"CLU":70,"nbaId":203903},{"name":"Andre Drummond","cname":"安德烈-德拉蒙德","pos":"C","height":"6'11'","type":"Board-Hunting Stretch Riser","ovr":76,"threePT":79,"MID":60,"FIN":85,"DNK":80,"HAN":85,"PAS":52,"PDEF":52,"IDEF":75,"BLK":78,"REB":91,"ATH":58,"STR":94,"CLU":40,"nbaId":203083},{"name":"Jeremy Sochan","cname":"杰里米-索汉","pos":"PF / C","height":"6'8'","type":"Crafty Laymaking Point Four","ovr":74,"threePT":70,"MID":45,"FIN":92,"DNK":80,"HAN":70,"PAS":79,"PDEF":74,"IDEF":65,"BLK":52,"REB":63,"ATH":74,"STR":60,"CLU":60,"nbaId":1631110},{"name":"Tyler Kolek","cname":"泰勒-科莱克","pos":"PG / SG","height":"6'2'","type":"Crafty 3-Level Scorer","ovr":74,"threePT":82,"MID":79,"FIN":82,"DNK":45,"HAN":85,"PAS":79,"PDEF":73,"IDEF":32,"BLK":40,"REB":58,"ATH":84,"STR":54,"CLU":75},{"name":"Mohamed Diawara","cname":"穆罕默德-迪亚瓦拉","pos":"PF / SF","height":"6'9'","type":"Rim-Rocking Stretch Four","ovr":74,"threePT":83,"MID":88,"FIN":81,"DNK":85,"HAN":85,"PAS":54,"PDEF":60,"IDEF":64,"BLK":50,"REB":61,"ATH":69,"STR":69,"CLU":60},{"name":"Trey Jemison III","cname":"特雷-杰米森三世","pos":"C","height":"6'10'","type":"Ball-Snatching Rim Rocker","ovr":71,"threePT":29,"MID":50,"FIN":83,"DNK":75,"HAN":80,"PAS":44,"PDEF":37,"IDEF":71,"BLK":76,"REB":65,"ATH":52,"STR":76,"CLU":50},{"name":"Kevin McCullar Jr.","cname":"小凯文-麦卡拉","pos":"SF / SG","height":"6'6'","type":"3-Level-Diming Crasher","ovr":71,"threePT":74,"MID":75,"FIN":76,"DNK":75,"HAN":85,"PAS":68,"PDEF":72,"IDEF":52,"BLK":63,"REB":55,"ATH":82,"STR":55,"CLU":40,"nbaId":1641755}],"OKC":[{"name":"Shai Gilgeous-Alexander","cname":"谢伊-吉尔杰斯-亚历山大","pos":"PG / SG","height":"6'6'","type":"2-Way Mid-Paint Playmaker","ovr":98,"threePT":82,"MID":99,"FIN":97,"DNK":75,"HAN":98,"PAS":97,"PDEF":86,"IDEF":69,"BLK":62,"REB":54,"ATH":86,"STR":62,"CLU":90,"nbaId":1628983},{"name":"Chet Holmgren","cname":"切特-霍姆格伦","pos":"PF / C","height":"7'1'","type":"Backline Crafty 3-Level Scorer","ovr":87,"threePT":80,"MID":87,"FIN":92,"DNK":75,"HAN":95,"PAS":60,"PDEF":75,"IDEF":90,"BLK":89,"REB":86,"ATH":75,"STR":45,"CLU":70,"nbaId":1631096},{"name":"Jalen Williams","cname":"杰伦-威廉姆斯","pos":"SF / PF","height":"6'5'","type":"2-Way Point Forward","ovr":87,"threePT":77,"MID":79,"FIN":87,"DNK":80,"HAN":87,"PAS":78,"PDEF":92,"IDEF":85,"BLK":50,"REB":61,"ATH":92,"STR":68,"CLU":60,"nbaId":1631114},{"name":"Isaiah Hartenstein","cname":"以赛亚-哈尔滕施泰因","pos":"C","height":"7'0'","type":"Crafty Mashing Board Enforcer","ovr":84,"threePT":55,"MID":79,"FIN":96,"DNK":80,"HAN":75,"PAS":50,"PDEF":41,"IDEF":83,"BLK":71,"REB":88,"ATH":62,"STR":84,"CLU":65,"nbaId":1628392},{"name":"Ajay Mitchell","cname":"阿贾伊-米切尔","pos":"PG / SG","height":"6'4'","type":"Crafty 3PT Sniper","ovr":84,"threePT":80,"MID":79,"FIN":95,"DNK":70,"HAN":80,"PAS":74,"PDEF":77,"IDEF":43,"BLK":52,"REB":52,"ATH":80,"STR":53,"CLU":80,"nbaId":1642349},{"name":"Cason Wallace","cname":"卡森-华莱士","pos":"SG / PG","height":"6'3'","type":"2-Way 3PT-Sniping Layup Artist","ovr":83,"threePT":83,"MID":73,"FIN":70,"DNK":70,"HAN":95,"PAS":75,"PDEF":91,"IDEF":72,"BLK":52,"REB":47,"ATH":91,"STR":55,"CLU":70,"nbaId":1641717},{"name":"Alex Caruso","cname":"亚历克斯-卡鲁索","pos":"SF / PG","height":"6'5'","type":"2-Way Iso Sniper","ovr":82,"threePT":82,"MID":82,"FIN":88,"DNK":75,"HAN":85,"PAS":75,"PDEF":93,"IDEF":51,"BLK":56,"REB":56,"ATH":94,"STR":31,"CLU":50,"nbaId":1627936},{"name":"Luguentz Dort","cname":"吕冈茨-多尔特","pos":"SG / SF","height":"6'4'","type":"2-Way Mid-Paint Bully","ovr":80,"threePT":77,"MID":86,"FIN":64,"DNK":85,"HAN":85,"PAS":67,"PDEF":94,"IDEF":75,"BLK":55,"REB":51,"ATH":94,"STR":70,"CLU":50,"nbaId":1629652},{"name":"Jared McCain","cname":"贾里德-麦凯恩","pos":"PG / SG","height":"6'3'","type":"Iso Sniper","ovr":80,"threePT":86,"MID":79,"FIN":84,"DNK":70,"HAN":85,"PAS":73,"PDEF":63,"IDEF":45,"BLK":35,"REB":52,"ATH":76,"STR":52,"CLU":90,"nbaId":1642272},{"name":"Jaylin Williams","cname":"杰林-威廉姆斯","pos":"C","height":"6'9'","type":"Stretch Five Cleaner","ovr":79,"threePT":82,"MID":85,"FIN":83,"DNK":75,"HAN":75,"PAS":70,"PDEF":54,"IDEF":68,"BLK":72,"REB":87,"ATH":57,"STR":60,"CLU":85,"nbaId":1631119},{"name":"Kenrich Williams","cname":"肯里奇-威廉姆斯","pos":"PF / C","height":"6'7'","type":"2-Way Stretch Four","ovr":78,"threePT":84,"MID":90,"FIN":84,"DNK":65,"HAN":90,"PAS":69,"PDEF":75,"IDEF":68,"BLK":42,"REB":63,"ATH":75,"STR":63,"CLU":85,"nbaId":1629026},{"name":"Aaron Wiggins","cname":"阿龙-威金斯","pos":"SF / PF","height":"6'5'","type":"Iso Sniper","ovr":78,"threePT":79,"MID":87,"FIN":82,"DNK":70,"HAN":76,"PAS":68,"PDEF":59,"IDEF":48,"BLK":58,"REB":54,"ATH":84,"STR":45,"CLU":40,"nbaId":1630598},{"name":"Nikola Topic","cname":"尼古拉-托皮奇","pos":"PG / SG","height":"6'6'","type":"Table Setter","ovr":73,"threePT":75,"MID":69,"FIN":81,"DNK":65,"HAN":85,"PAS":81,"PDEF":63,"IDEF":45,"BLK":47,"REB":47,"ATH":76,"STR":59,"CLU":60},{"name":"Thomas Sorber","cname":"托马斯-索伯","pos":"C / PF","height":"6'9'","type":"Slashing Point Center","ovr":71,"threePT":60,"MID":70,"FIN":75,"DNK":75,"HAN":85,"PAS":69,"PDEF":58,"IDEF":69,"BLK":75,"REB":69,"ATH":66,"STR":72,"CLU":45},{"name":"Brooks Barnhizer","cname":"布鲁克斯-巴恩希泽","pos":"SF / SG","height":"6'5'","type":"Physical Middy Maestro","ovr":69,"threePT":72,"MID":79,"FIN":74,"DNK":70,"HAN":70,"PAS":70,"PDEF":70,"IDEF":59,"BLK":50,"REB":60,"ATH":79,"STR":67,"CLU":30}],"ORL":[{"name":"Paolo Banchero","cname":"保罗-班凯罗","pos":"PF / SF","height":"6'10'","type":"Glass-Cleaning Rim Runner","ovr":88,"threePT":78,"MID":75,"FIN":88,"DNK":85,"HAN":95,"PAS":74,"PDEF":66,"IDEF":68,"BLK":55,"REB":83,"ATH":80,"STR":77,"CLU":90,"nbaId":1631094},{"name":"Franz Wagner","cname":"弗朗茨-瓦格纳","pos":"SF / PF","height":"6'10'","type":"Crafty Mid-Paint Point Forward","ovr":87,"threePT":80,"MID":90,"FIN":85,"DNK":80,"HAN":95,"PAS":80,"PDEF":73,"IDEF":56,"BLK":50,"REB":64,"ATH":73,"STR":65,"CLU":75,"nbaId":1630532},{"name":"Desmond Bane","cname":"德斯蒙德-贝恩","pos":"SG / SF","height":"6'6'","type":"Crafty 3PT Sniper","ovr":86,"threePT":86,"MID":83,"FIN":93,"DNK":75,"HAN":84,"PAS":74,"PDEF":79,"IDEF":59,"BLK":51,"REB":49,"ATH":80,"STR":56,"CLU":90,"nbaId":1630217},{"name":"Anthony Black","cname":"安东尼-布莱克","pos":"PG / SG","height":"6'7'","type":"2-Way Crafty Laymaker","ovr":82,"threePT":77,"MID":75,"FIN":82,"DNK":75,"HAN":90,"PAS":80,"PDEF":84,"IDEF":63,"BLK":60,"REB":53,"ATH":85,"STR":58,"CLU":90,"nbaId":1641710},{"name":"Jalen Suggs","cname":"杰伦-萨格斯","pos":"PG / SG","height":"6'5'","type":"2-Way Mid-Range Slasher","ovr":81,"threePT":76,"MID":90,"FIN":87,"DNK":80,"HAN":84,"PAS":75,"PDEF":91,"IDEF":55,"BLK":63,"REB":60,"ATH":92,"STR":59,"CLU":50,"nbaId":1630591},{"name":"Wendell Carter Jr.","cname":"小温德尔-卡特","pos":"C / PF","height":"6'10'","type":"Post-Scoring Stretch","ovr":80,"threePT":75,"MID":86,"FIN":86,"DNK":75,"HAN":85,"PAS":56,"PDEF":47,"IDEF":78,"BLK":58,"REB":78,"ATH":55,"STR":82,"CLU":60,"nbaId":1628976},{"name":"Nikola Vucevic","cname":"尼古拉-武切维奇","pos":"C","height":"6'9'","type":"Triple-Double Threat","ovr":79,"threePT":79,"MID":83,"FIN":97,"DNK":65,"HAN":80,"PAS":77,"PDEF":41,"IDEF":68,"BLK":60,"REB":85,"ATH":49,"STR":87,"CLU":70,"nbaId":202696},{"name":"Tristan da Silva","cname":"特里斯坦-达席尔瓦","pos":"SF / PF","height":"6'8'","type":"3PT Sniper","ovr":78,"threePT":80,"MID":49,"FIN":90,"DNK":65,"HAN":90,"PAS":64,"PDEF":64,"IDEF":64,"BLK":51,"REB":59,"ATH":67,"STR":62,"CLU":95,"nbaId":1641783},{"name":"Goga Bitadze","cname":"戈加-比塔泽","pos":"C","height":"6'11'","type":"Putback Power-Finishing Eraser","ovr":77,"threePT":69,"MID":72,"FIN":98,"DNK":65,"HAN":70,"PAS":54,"PDEF":25,"IDEF":78,"BLK":86,"REB":76,"ATH":37,"STR":88,"CLU":60,"nbaId":1629048},{"name":"Jevon Carter","cname":"杰文-卡特","pos":"PG / SG","height":"6'0'","type":"2-Way Sharpshooter","ovr":75,"threePT":79,"MID":93,"FIN":66,"DNK":35,"HAN":80,"PAS":75,"PDEF":79,"IDEF":34,"BLK":50,"REB":47,"ATH":83,"STR":60,"CLU":85,"nbaId":1628975},{"name":"Noah Penda","cname":"诺亚-彭达","pos":"PF / SF","height":"6'7'","type":"Stretch Four Crasher","ovr":75,"threePT":76,"MID":68,"FIN":63,"DNK":75,"HAN":80,"PAS":67,"PDEF":73,"IDEF":73,"BLK":62,"REB":71,"ATH":84,"STR":70,"CLU":85},{"name":"Jamal Cain","cname":"贾马尔-凯恩","pos":"SF / PF","height":"6'7'","type":"Crafty 3PT Sniper","ovr":74,"threePT":83,"MID":59,"FIN":87,"DNK":75,"HAN":75,"PAS":44,"PDEF":68,"IDEF":60,"BLK":37,"REB":53,"ATH":68,"STR":60,"CLU":80,"nbaId":1631288},{"name":"Jett Howard","cname":"杰特-霍华德","pos":"SG / SF","height":"6'8'","type":"3PT-Creating Combo Guard","ovr":74,"threePT":81,"MID":68,"FIN":81,"DNK":65,"HAN":85,"PAS":69,"PDEF":67,"IDEF":55,"BLK":53,"REB":51,"ATH":69,"STR":58,"CLU":80},{"name":"Jonathan Isaac","cname":"乔纳森-艾萨克","pos":"PF / C","height":"6'10'","type":"Lockdown Defender","ovr":73,"threePT":59,"MID":44,"FIN":89,"DNK":70,"HAN":68,"PAS":54,"PDEF":83,"IDEF":83,"BLK":91,"REB":74,"ATH":80,"STR":70,"CLU":25,"nbaId":1628371},{"name":"Jase Richardson","cname":"杰斯-理查德森","pos":"SG / PG","height":"6'1'","type":"Crafty 3-Level Scorer","ovr":73,"threePT":82,"MID":84,"FIN":93,"DNK":87,"HAN":80,"PAS":70,"PDEF":67,"IDEF":33,"BLK":37,"REB":46,"ATH":84,"STR":57,"CLU":50}],"PHI":[{"name":"LeBron James","cname":"勒布朗-詹姆斯","pos":"PF / SF","height":"6'9'","type":"PlaySlash Bully","ovr":92,"threePT":76,"MID":76,"FIN":97,"DNK":85,"HAN":84,"PAS":90,"PDEF":74,"IDEF":74,"BLK":57,"REB":69,"ATH":86,"STR":89,"CLU":90,"nbaId":2544},{"name":"Jaylen Brown","cname":"杰伦-布朗","pos":"SG / SF","height":"6'6'","type":"Physical 2-Way Middy Slasher","ovr":95,"threePT":80,"MID":90,"FIN":94,"DNK":94,"HAN":98,"PAS":78,"PDEF":89,"IDEF":78,"BLK":51,"REB":70,"ATH":88,"STR":72,"CLU":98,"nbaId":1627759},{"name":"Tyrese Maxey","cname":"泰雷塞-马克西","pos":"PG / SG","height":"6'2'","type":"Ankle Taker","ovr":93,"threePT":86,"MID":87,"FIN":84,"DNK":70,"HAN":98,"PAS":80,"PDEF":79,"IDEF":54,"BLK":60,"REB":52,"ATH":90,"STR":44,"CLU":98,"nbaId":1630178},{"name":"Joel Embiid","cname":"乔尔-恩比德","pos":"C","height":"7'0'","type":"Physical 2-Way 3-Level Cleaner","ovr":92,"threePT":79,"MID":91,"FIN":94,"DNK":80,"HAN":90,"PAS":74,"PDEF":68,"IDEF":84,"BLK":73,"REB":90,"ATH":59,"STR":96,"CLU":60,"nbaId":203954},{"name":"VJ Edgecombe","cname":"VJ-埃奇库姆","pos":"SG / PG","height":"6'4'","type":"Mid-Range High Flyer","ovr":83,"threePT":81,"MID":86,"FIN":81,"DNK":89,"HAN":85,"PAS":75,"PDEF":77,"IDEF":53,"BLK":53,"REB":56,"ATH":91,"STR":57,"CLU":75,"nbaId":1642845},{"name":"Anfernee Simons","cname":"安芬尼-西蒙斯","pos":"SG / PG","height":"6'3'","type":"Inside-Out Creator","ovr":80,"threePT":82,"MID":79,"FIN":97,"DNK":89,"HAN":95,"PAS":75,"PDEF":59,"IDEF":49,"BLK":37,"REB":45,"ATH":89,"STR":41,"CLU":60,"nbaId":1629014},{"name":"Dean Wade","cname":"迪恩-韦德","pos":"PF / SF","height":"6'9'","type":"2-Way Masher","ovr":78,"threePT":79,"MID":70,"FIN":93,"DNK":75,"HAN":70,"PAS":38,"PDEF":84,"IDEF":80,"BLK":56,"REB":66,"ATH":84,"STR":60,"CLU":75,"nbaId":1629731},{"name":"Trendon Watford","cname":"特伦登-沃特福德","pos":"PF / PG","height":"6'8'","type":"Versatile Paint Finisher","ovr":76,"threePT":68,"MID":68,"FIN":96,"DNK":75,"HAN":75,"PAS":69,"PDEF":52,"IDEF":66,"BLK":62,"REB":69,"ATH":63,"STR":79,"CLU":60,"nbaId":1630570},{"name":"Justin Edwards","cname":"贾斯汀-爱德华兹","pos":"SF / PF","height":"6'7'","type":"Ball-Hawking 3PT Shot Hunter","ovr":76,"threePT":80,"MID":79,"FIN":89,"DNK":80,"HAN":75,"PAS":65,"PDEF":68,"IDEF":50,"BLK":51,"REB":45,"ATH":82,"STR":59,"CLU":70},{"name":"Dominick Barlow","cname":"多米尼克-巴洛","pos":"PF / C","height":"6'9'","type":"Crafty Putback Pro","ovr":76,"threePT":70,"MID":44,"FIN":87,"DNK":75,"HAN":85,"PAS":51,"PDEF":55,"IDEF":59,"BLK":67,"REB":59,"ATH":68,"STR":62,"CLU":85,"nbaId":1631230},{"name":"Caleb Love","cname":"凯莱布-乐福","pos":"PG / SG","height":"6'3'","type":"Iso Sniper","ovr":75,"threePT":76,"MID":91,"FIN":75,"DNK":65,"HAN":85,"PAS":74,"PDEF":65,"IDEF":34,"BLK":35,"REB":51,"ATH":75,"STR":51,"CLU":70},{"name":"Adem Bona","cname":"阿代姆-博纳","pos":"C","height":"6'10'","type":"Shot Eraser","ovr":75,"threePT":54,"MID":57,"FIN":77,"DNK":80,"HAN":70,"PAS":40,"PDEF":37,"IDEF":70,"BLK":90,"REB":66,"ATH":68,"STR":73,"CLU":80,"nbaId":1641737},{"name":"Rayan Rupert","cname":"拉扬-吕佩尔","pos":"SF / SG","height":"6'6'","type":"Crafty 3PT-Sniping Creator","ovr":74,"threePT":77,"MID":63,"FIN":75,"DNK":70,"HAN":70,"PAS":68,"PDEF":70,"IDEF":53,"BLK":40,"REB":60,"ATH":72,"STR":52,"CLU":80},{"name":"Ariel Hukporti","cname":"阿里埃尔-胡克波尔蒂","pos":"C / PF","height":"7'0'","type":"Putback Rim-Rocking Eraser","ovr":73,"threePT":47,"MID":57,"FIN":41,"DNK":75,"HAN":75,"PAS":43,"PDEF":37,"IDEF":74,"BLK":90,"REB":75,"ATH":37,"STR":68,"CLU":85,"nbaId":1630574},{"name":"Tyrese Martin","cname":"泰雷斯-马丁","pos":"SG / PG","height":"6'6'","type":"3PT-Creating Combo Guard","ovr":73,"threePT":75,"MID":76,"FIN":68,"DNK":60,"HAN":69,"PAS":75,"PDEF":66,"IDEF":54,"BLK":37,"REB":55,"ATH":72,"STR":59,"CLU":75,"nbaId":1631213}],"PHX":[{"name":"Devin Booker","cname":"德文-布克","pos":"SG / PG","height":"6'5'","type":"Crafty Offensive Engine","ovr":93,"threePT":86,"MID":86,"FIN":96,"DNK":75,"HAN":98,"PAS":80,"PDEF":75,"IDEF":58,"BLK":45,"REB":52,"ATH":80,"STR":59,"CLU":85,"nbaId":1626164},{"name":"Dillon Brooks","cname":"狄龙-布鲁克斯","pos":"SF / PF","height":"6'7'","type":"2-Way Finisher","ovr":84,"threePT":78,"MID":80,"FIN":97,"DNK":75,"HAN":85,"PAS":59,"PDEF":90,"IDEF":77,"BLK":37,"REB":51,"ATH":88,"STR":65,"CLU":75,"nbaId":1628415},{"name":"Jalen Green","cname":"杰伦-格林","pos":"SG / SF","height":"6'4'","type":"Mid-Range High Flyer","ovr":82,"threePT":79,"MID":94,"FIN":76,"DNK":91,"HAN":95,"PAS":77,"PDEF":64,"IDEF":45,"BLK":37,"REB":61,"ATH":88,"STR":55,"CLU":60,"nbaId":1630224},{"name":"Mark Williams","cname":"马克-威廉姆斯","pos":"C","height":"7'1'","type":"Bullmashing Putback Pro","ovr":82,"threePT":36,"MID":60,"FIN":97,"DNK":75,"HAN":85,"PAS":44,"PDEF":44,"IDEF":75,"BLK":76,"REB":84,"ATH":64,"STR":86,"CLU":80,"nbaId":1631109},{"name":"Miles Bridges","cname":"迈尔斯-布里奇斯","pos":"PF / SF","height":"6'7'","type":"Versatile Post Bully","ovr":81,"threePT":76,"MID":57,"FIN":89,"DNK":88,"HAN":95,"PAS":69,"PDEF":59,"IDEF":60,"BLK":52,"REB":68,"ATH":76,"STR":84,"CLU":25,"nbaId":1628970},{"name":"Collin Gillespie","cname":"科林-吉莱斯皮","pos":"PG","height":"6'1'","type":"3PT-Sniping Creator","ovr":81,"threePT":84,"MID":76,"FIN":93,"DNK":54,"HAN":75,"PAS":82,"PDEF":62,"IDEF":33,"BLK":42,"REB":55,"ATH":82,"STR":33,"CLU":90,"nbaId":1631221},{"name":"Luke Kennard","cname":"卢克-肯纳德","pos":"SG / PG","height":"6'5'","type":"Crafty Sharpshooter","ovr":78,"threePT":93,"MID":98,"FIN":98,"DNK":60,"HAN":80,"PAS":74,"PDEF":51,"IDEF":35,"BLK":37,"REB":48,"ATH":68,"STR":40,"CLU":70,"nbaId":1628379},{"name":"Jordan Goodwin","cname":"乔丹-古德温","pos":"PG / SG","height":"6'3'","type":"3 & D Crasher","ovr":77,"threePT":79,"MID":75,"FIN":79,"DNK":35,"HAN":90,"PAS":78,"PDEF":76,"IDEF":57,"BLK":47,"REB":63,"ATH":84,"STR":49,"CLU":85,"nbaId":1630692},{"name":"Oso Ighodaro","cname":"奥索-伊戈达罗","pos":"C / PF","height":"6'11'","type":"Rim-Running Finisher","ovr":77,"threePT":35,"MID":60,"FIN":97,"DNK":80,"HAN":80,"PAS":58,"PDEF":47,"IDEF":62,"BLK":67,"REB":69,"ATH":74,"STR":64,"CLU":90,"nbaId":1642345},{"name":"Pat Spencer","cname":"帕特-斯潘塞","pos":"PG / SG","height":"6'3'","type":"High-Flying 3PT Sniper","ovr":76,"threePT":80,"MID":76,"FIN":86,"DNK":75,"HAN":85,"PAS":79,"PDEF":55,"IDEF":44,"BLK":35,"REB":50,"ATH":75,"STR":54,"CLU":90,"nbaId":1630311},{"name":"Rasheer Fleming","cname":"拉希尔-弗莱明","pos":"PF / C","height":"6'9'","type":"Skilled Stretch Crasher","ovr":76,"threePT":78,"MID":74,"FIN":71,"DNK":75,"HAN":80,"PAS":55,"PDEF":40,"IDEF":67,"BLK":71,"REB":53,"ATH":70,"STR":75,"CLU":85},{"name":"Ryan Dunn","cname":"瑞安-邓恩","pos":"PF / SF","height":"6'7'","type":"Perimeter Lock","ovr":75,"threePT":76,"MID":70,"FIN":77,"DNK":80,"HAN":80,"PAS":60,"PDEF":80,"IDEF":76,"BLK":60,"REB":66,"ATH":88,"STR":65,"CLU":70,"nbaId":1642346},{"name":"Khaman Maluach","cname":"卡曼-马卢阿奇","pos":"C","height":"7'1'","type":"Shot-Erasing Rim Wrecker","ovr":75,"threePT":50,"MID":66,"FIN":70,"DNK":80,"HAN":85,"PAS":51,"PDEF":63,"IDEF":81,"BLK":94,"REB":80,"ATH":63,"STR":70,"CLU":50},{"name":"Jamaree Bouyea","cname":"贾马里-布耶","pos":"PG / SG","height":"6'2'","type":"Disruptive Blow-By Ace","ovr":74,"threePT":72,"MID":67,"FIN":85,"DNK":45,"HAN":75,"PAS":75,"PDEF":68,"IDEF":42,"BLK":66,"REB":52,"ATH":83,"STR":41,"CLU":90,"nbaId":1631123},{"name":"Amir Coffey","cname":"阿米尔-科菲","pos":"SF / SG","height":"6'7'","type":"Crafty Mid-Range Menace","ovr":74,"threePT":74,"MID":94,"FIN":74,"DNK":75,"HAN":81,"PAS":72,"PDEF":64,"IDEF":47,"BLK":51,"REB":46,"ATH":75,"STR":52,"CLU":60,"nbaId":1629599}],"POR":[{"name":"Deni Avdija","cname":"德尼-阿夫迪亚","pos":"SF / SG","height":"6'8'","type":"Crafty Mid-Paint Maestro","ovr":88,"threePT":75,"MID":89,"FIN":85,"DNK":75,"HAN":85,"PAS":79,"PDEF":80,"IDEF":62,"BLK":56,"REB":74,"ATH":81,"STR":65,"CLU":80,"nbaId":1630166},{"name":"Damian Lillard","cname":"达米安-利拉德","pos":"PG","height":"6'2'","type":"Versatile Paint Bully","ovr":88,"threePT":86,"MID":95,"FIN":82,"DNK":85,"HAN":98,"PAS":84,"PDEF":69,"IDEF":31,"BLK":37,"REB":54,"ATH":87,"STR":63,"CLU":75,"nbaId":203081},{"name":"Ja Morant","cname":"贾-莫兰特","pos":"PG / SG","height":"6'2'","type":"Blow-By Dual Finisher","ovr":87,"threePT":72,"MID":73,"FIN":81,"DNK":96,"HAN":98,"PAS":87,"PDEF":72,"IDEF":49,"BLK":40,"REB":51,"ATH":95,"STR":52,"CLU":50,"nbaId":1629630},{"name":"Shaedon Sharpe","cname":"谢登-夏普","pos":"SG / PG","height":"6'5'","type":"Blow-By Dual Finisher","ovr":83,"threePT":76,"MID":72,"FIN":83,"DNK":95,"HAN":88,"PAS":67,"PDEF":69,"IDEF":49,"BLK":32,"REB":57,"ATH":89,"STR":50,"CLU":85,"nbaId":1631101},{"name":"Donovan Clingan","cname":"多诺万-克林根","pos":"C","height":"7'2'","type":"Glass Guardian Lob Threat","ovr":83,"threePT":75,"MID":78,"FIN":94,"DNK":75,"HAN":75,"PAS":55,"PDEF":49,"IDEF":82,"BLK":86,"REB":90,"ATH":45,"STR":78,"CLU":90,"nbaId":1642270},{"name":"Jrue Holiday","cname":"朱-霍勒迪","pos":"PG / SG","height":"6'4'","type":"Ball-Hawking 3 & D Point","ovr":82,"threePT":84,"MID":81,"FIN":91,"DNK":65,"HAN":98,"PAS":80,"PDEF":90,"IDEF":66,"BLK":32,"REB":55,"ATH":90,"STR":49,"CLU":35,"nbaId":201950},{"name":"Scoot Henderson","cname":"斯库特-亨德森","pos":"PG / SG","height":"6'3'","type":"Mid-Range High Flyer","ovr":81,"threePT":79,"MID":97,"FIN":74,"DNK":88,"HAN":95,"PAS":79,"PDEF":66,"IDEF":41,"BLK":50,"REB":49,"ATH":89,"STR":53,"CLU":85,"nbaId":1630703},{"name":"Toumani Camara","cname":"图马尼-卡马拉","pos":"PF / SF","height":"6'7'","type":"2-Way Stretch Four","ovr":81,"threePT":81,"MID":60,"FIN":86,"DNK":75,"HAN":85,"PAS":68,"PDEF":90,"IDEF":87,"BLK":51,"REB":55,"ATH":89,"STR":78,"CLU":60,"nbaId":1641739},{"name":"Robert Williams III","cname":"罗伯特-威廉姆斯三世","pos":"C / PF","height":"6'9'","type":"Paint-Finishing Board Enforcer","ovr":80,"threePT":74,"MID":69,"FIN":98,"DNK":75,"HAN":75,"PAS":57,"PDEF":40,"IDEF":83,"BLK":95,"REB":91,"ATH":64,"STR":83,"CLU":40},{"name":"Matisse Thybulle","cname":"马蒂斯-赛布尔","pos":"SG / SF","height":"6'5'","type":"Ball-Hawking 3 & D Two Guard","ovr":76,"threePT":83,"MID":48,"FIN":62,"DNK":70,"HAN":70,"PAS":55,"PDEF":85,"IDEF":60,"BLK":68,"REB":52,"ATH":85,"STR":59,"CLU":80,"nbaId":1629680},{"name":"Vit Krejci","cname":"维特-克雷伊奇","pos":"SF / PG","height":"6'8'","type":"3PT-Sniping Creator","ovr":75,"threePT":83,"MID":74,"FIN":97,"DNK":70,"HAN":75,"PAS":69,"PDEF":60,"IDEF":50,"BLK":51,"REB":47,"ATH":75,"STR":40,"CLU":60,"nbaId":1630249},{"name":"Sidy Cissoko","cname":"西迪-西索科","pos":"SF / SG","height":"6'6'","type":"Crafty Inside-Out Scorer","ovr":74,"threePT":72,"MID":67,"FIN":72,"DNK":75,"HAN":85,"PAS":61,"PDEF":66,"IDEF":59,"BLK":56,"REB":45,"ATH":75,"STR":58,"CLU":75},{"name":"Blake Wesley","cname":"布莱克-韦斯利","pos":"PG / SG","height":"6'4'","type":"Ball-Hawking High Flyer","ovr":73,"threePT":70,"MID":42,"FIN":71,"DNK":86,"HAN":80,"PAS":78,"PDEF":73,"IDEF":49,"BLK":40,"REB":48,"ATH":78,"STR":40,"CLU":80,"nbaId":1631104},{"name":"Branden Carlson","cname":"布兰登-卡尔森","pos":"C","height":"7'0'","type":"Shot-Erasing Stretch Five","ovr":73,"threePT":78,"MID":72,"FIN":82,"DNK":60,"HAN":85,"PAS":47,"PDEF":30,"IDEF":52,"BLK":82,"REB":72,"ATH":30,"STR":60,"CLU":85},{"name":"Yang Hansen","cname":"杨瀚森","pos":"C","height":"7'1'","type":"Rim-Wrecking Distributor","ovr":70,"threePT":63,"MID":66,"FIN":62,"DNK":70,"HAN":68,"PAS":67,"PDEF":40,"IDEF":69,"BLK":66,"REB":67,"ATH":37,"STR":65,"CLU":70}],"SAC":[{"name":"Domantas Sabonis","cname":"多曼塔斯-萨博尼斯","pos":"C","height":"6'10'","type":"Crafty Mid-Post Break Starter","ovr":86,"threePT":70,"MID":86,"FIN":91,"DNK":75,"HAN":98,"PAS":82,"PDEF":49,"IDEF":70,"BLK":40,"REB":93,"ATH":49,"STR":82,"CLU":70,"nbaId":1627734},{"name":"Zach LaVine","cname":"扎克-拉文","pos":"SG / SF","height":"6'5'","type":"Inside-Out Creator","ovr":83,"threePT":86,"MID":79,"FIN":97,"DNK":94,"HAN":80,"PAS":70,"PDEF":61,"IDEF":47,"BLK":42,"REB":47,"ATH":82,"STR":49,"CLU":60,"nbaId":203897},{"name":"Russell Westbrook","cname":"拉塞尔-威斯布鲁克","pos":"PG / SG","height":"6'4'","type":"Explosive Blow-By High Flyer","ovr":81,"threePT":77,"MID":70,"FIN":93,"DNK":80,"HAN":85,"PAS":80,"PDEF":74,"IDEF":42,"BLK":37,"REB":67,"ATH":89,"STR":75,"CLU":80,"nbaId":201566},{"name":"Maxime Raynaud","cname":"马克西姆-雷诺","pos":"C","height":"7'1'","type":"Mid-Range Scoring Cleaner","ovr":81,"threePT":72,"MID":85,"FIN":98,"DNK":75,"HAN":90,"PAS":50,"PDEF":32,"IDEF":59,"BLK":57,"REB":85,"ATH":52,"STR":69,"CLU":85,"nbaId":1642875},{"name":"Malik Monk","cname":"马利克-蒙克","pos":"SG / PG","height":"6'3'","type":"Inside-Out Combo Guard","ovr":80,"threePT":83,"MID":79,"FIN":83,"DNK":75,"HAN":95,"PAS":75,"PDEF":60,"IDEF":45,"BLK":55,"REB":46,"ATH":80,"STR":48,"CLU":75,"nbaId":1628370},{"name":"Keegan Murray","cname":"基根-默里","pos":"PF / SF","height":"6'8'","type":"2-Way Crafty Finisher","ovr":79,"threePT":76,"MID":73,"FIN":97,"DNK":80,"HAN":80,"PAS":65,"PDEF":78,"IDEF":68,"BLK":80,"REB":62,"ATH":79,"STR":60,"CLU":60,"nbaId":1631099},{"name":"De’Andre Hunter","cname":"德安德烈-亨特","pos":"SF / PF","height":"6'7'","type":"Crafty 2-Way Middy Scorer","ovr":79,"threePT":73,"MID":92,"FIN":94,"DNK":70,"HAN":85,"PAS":67,"PDEF":82,"IDEF":63,"BLK":37,"REB":63,"ATH":83,"STR":65,"CLU":60,"nbaId":1629631},{"name":"Precious Achiuwa","cname":"普雷舍斯-阿丘瓦","pos":"PF / C","height":"6'8'","type":"Crafty Putback Masher","ovr":79,"threePT":73,"MID":60,"FIN":94,"DNK":80,"HAN":75,"PAS":36,"PDEF":56,"IDEF":79,"BLK":66,"REB":78,"ATH":74,"STR":78,"CLU":75,"nbaId":1630173},{"name":"Nique Clifford","cname":"尼克-克利福德","pos":"SF / SG","height":"6'5'","type":"3PT-Sniping Creator","ovr":77,"threePT":77,"MID":74,"FIN":83,"DNK":70,"HAN":80,"PAS":65,"PDEF":66,"IDEF":55,"BLK":51,"REB":58,"ATH":82,"STR":62,"CLU":75},{"name":"Dylan Cardwell","cname":"迪伦-卡德韦尔","pos":"C / PF","height":"6'10'","type":"Glass Guardian Lob Threat","ovr":77,"threePT":37,"MID":60,"FIN":90,"DNK":80,"HAN":70,"PAS":58,"PDEF":44,"IDEF":79,"BLK":89,"REB":85,"ATH":62,"STR":82,"CLU":55,"nbaId":1642928},{"name":"Drew Eubanks","cname":"德鲁-尤班克斯","pos":"C","height":"6'10'","type":"Shot-Erasing Crafty Stretch","ovr":75,"threePT":77,"MID":73,"FIN":97,"DNK":70,"HAN":75,"PAS":42,"PDEF":47,"IDEF":74,"BLK":89,"REB":78,"ATH":62,"STR":68,"CLU":60},{"name":"Jonathan Mogbo","cname":"乔纳森-莫格博","pos":"PF / C","height":"6'9'","type":"Board-Hunting Rim Runner","ovr":74,"threePT":70,"MID":53,"FIN":89,"DNK":80,"HAN":64,"PAS":66,"PDEF":64,"IDEF":64,"BLK":66,"REB":78,"ATH":79,"STR":68,"CLU":70},{"name":"Daeqwon Plowden","cname":"戴克万-普洛登","pos":"SF / SG","height":"6'4'","type":"Blow-By High Flyer","ovr":73,"threePT":78,"MID":68,"FIN":85,"DNK":80,"HAN":80,"PAS":60,"PDEF":66,"IDEF":46,"BLK":37,"REB":47,"ATH":82,"STR":62,"CLU":70},{"name":"Killian Hayes","cname":"基利安-海斯","pos":"PG / SG","height":"6'5'","type":"2-Way Playmaker","ovr":72,"threePT":72,"MID":65,"FIN":65,"DNK":60,"HAN":85,"PAS":82,"PDEF":73,"IDEF":49,"BLK":51,"REB":64,"ATH":79,"STR":56,"CLU":60},{"name":"Patrick Baldwin Jr.","cname":"小帕特里克-鲍德温","pos":"PF / SF","height":"6'9'","type":"Stretch Four Cleaner","ovr":71,"threePT":77,"MID":72,"FIN":70,"DNK":70,"HAN":80,"PAS":55,"PDEF":59,"IDEF":56,"BLK":65,"REB":84,"ATH":64,"STR":69,"CLU":60}],"SAS":[{"name":"Victor Wembanyama","cname":"维克托-文班亚马","pos":"C / PF","height":"7'4'","type":"Backline Stretch Post Scorer","ovr":97,"threePT":83,"MID":78,"FIN":92,"DNK":85,"HAN":98,"PAS":72,"PDEF":80,"IDEF":96,"BLK":99,"REB":98,"ATH":69,"STR":69,"CLU":95,"nbaId":1641705},{"name":"Stephon Castle","cname":"斯蒂芬-卡斯尔","pos":"SG / PG","height":"6'6'","type":"2-Way Slashing Combo Guard","ovr":88,"threePT":79,"MID":77,"FIN":91,"DNK":88,"HAN":85,"PAS":84,"PDEF":83,"IDEF":64,"BLK":47,"REB":58,"ATH":93,"STR":59,"CLU":95,"nbaId":1642264},{"name":"De’Aaron Fox","cname":"达龙-福克斯","pos":"PG / SG","height":"6'3'","type":"2-Way Mid-Range Menace","ovr":86,"threePT":77,"MID":91,"FIN":98,"DNK":80,"HAN":90,"PAS":85,"PDEF":84,"IDEF":34,"BLK":42,"REB":52,"ATH":93,"STR":46,"CLU":70,"nbaId":1628368},{"name":"Dylan Harper","cname":"迪伦-哈珀","pos":"PG / SG","height":"6'5'","type":"Mid-Range Slasher","ovr":85,"threePT":81,"MID":93,"FIN":93,"DNK":86,"HAN":95,"PAS":75,"PDEF":70,"IDEF":52,"BLK":52,"REB":55,"ATH":89,"STR":52,"CLU":70,"nbaId":1642844},{"name":"Tobias Harris","cname":"托拜厄斯-哈里斯","pos":"PF / SF","height":"6'8'","type":"Crafty Mid-Post Point Four","ovr":83,"threePT":79,"MID":84,"FIN":97,"DNK":75,"HAN":95,"PAS":77,"PDEF":68,"IDEF":67,"BLK":55,"REB":68,"ATH":67,"STR":73,"CLU":70,"nbaId":202699},{"name":"Julian Champagnie","cname":"朱利安-尚帕尼","pos":"PF / SF","height":"6'7'","type":"2-Way Stretch Four","ovr":82,"threePT":87,"MID":79,"FIN":81,"DNK":60,"HAN":90,"PAS":66,"PDEF":75,"IDEF":45,"BLK":53,"REB":71,"ATH":75,"STR":37,"CLU":90,"nbaId":1630577},{"name":"Devin Vassell","cname":"德文-瓦塞尔","pos":"SF / SG","height":"6'5'","type":"Shot-Creating Point Forward","ovr":82,"threePT":86,"MID":97,"FIN":80,"DNK":60,"HAN":80,"PAS":76,"PDEF":80,"IDEF":62,"BLK":51,"REB":54,"ATH":80,"STR":60,"CLU":60,"nbaId":1630170},{"name":"Keldon Johnson","cname":"凯尔登-约翰逊","pos":"SG / SF","height":"6'6'","type":"Power Finisher","ovr":81,"threePT":81,"MID":83,"FIN":97,"DNK":75,"HAN":95,"PAS":74,"PDEF":73,"IDEF":67,"BLK":35,"REB":67,"ATH":78,"STR":70,"CLU":70,"nbaId":1629640},{"name":"Luke Kornet","cname":"卢克-科内特","pos":"C","height":"7'1'","type":"Shot-Erasing Post Crasher","ovr":78,"threePT":70,"MID":66,"FIN":85,"DNK":60,"HAN":85,"PAS":50,"PDEF":32,"IDEF":74,"BLK":81,"REB":68,"ATH":37,"STR":66,"CLU":65,"nbaId":1628436},{"name":"Harrison Barnes","cname":"哈里森-巴恩斯","pos":"PF / SF","height":"6'7'","type":"Skilled Stretch Four","ovr":77,"threePT":81,"MID":63,"FIN":85,"DNK":80,"HAN":75,"PAS":62,"PDEF":72,"IDEF":67,"BLK":40,"REB":45,"ATH":75,"STR":70,"CLU":85,"nbaId":203084},{"name":"Carter Bryant","cname":"卡特-布赖恩特","pos":"SF / PF","height":"6'6'","type":"High-Flying Wing Runner","ovr":77,"threePT":77,"MID":74,"FIN":60,"DNK":88,"HAN":80,"PAS":62,"PDEF":82,"IDEF":60,"BLK":62,"REB":72,"ATH":88,"STR":60,"CLU":75},{"name":"Mason Plumlee","cname":"梅森-普拉姆利","pos":"C","height":"7'0'","type":"Break-Starting Paint Finisher","ovr":76,"threePT":58,"MID":60,"FIN":85,"DNK":80,"HAN":81,"PAS":75,"PDEF":46,"IDEF":74,"BLK":75,"REB":89,"ATH":48,"STR":80,"CLU":35,"nbaId":203486},{"name":"Kelly Olynyk","cname":"凯利-奥利尼克","pos":"C / PF","height":"7'0'","type":"Crafty Stretch Five Dimer","ovr":75,"threePT":74,"MID":70,"FIN":93,"DNK":60,"HAN":78,"PAS":70,"PDEF":50,"IDEF":70,"BLK":60,"REB":71,"ATH":35,"STR":70,"CLU":40,"nbaId":203482},{"name":"Lindy Waters III","cname":"林迪-沃特斯三世","pos":"SG / SF","height":"6'5'","type":"Crafty Sharpshooter","ovr":73,"threePT":77,"MID":85,"FIN":90,"DNK":65,"HAN":70,"PAS":69,"PDEF":67,"IDEF":53,"BLK":55,"REB":60,"ATH":69,"STR":33,"CLU":60},{"name":"Jordan McLaughlin","cname":"乔丹-麦克劳克林","pos":"PG","height":"5'11'","type":"Ball-Hawking 3PT Shot Hunter","ovr":72,"threePT":86,"MID":74,"FIN":69,"DNK":65,"HAN":70,"PAS":83,"PDEF":73,"IDEF":25,"BLK":25,"REB":46,"ATH":74,"STR":52,"CLU":60}],"TOR":[{"name":"Kawhi Leonard","cname":"科怀-伦纳德","pos":"SF / SG","height":"6'7'","type":"2-Way Mid-Range Menace","ovr":95,"threePT":85,"MID":88,"FIN":98,"DNK":80,"HAN":98,"PAS":79,"PDEF":89,"IDEF":81,"BLK":52,"REB":72,"ATH":89,"STR":79,"CLU":98,"nbaId":202695},{"name":"Scottie Barnes","cname":"斯科蒂-巴恩斯","pos":"PF / PG","height":"6'8'","type":"Mid-Paint Playmaking Eraser","ovr":89,"threePT":77,"MID":87,"FIN":97,"DNK":80,"HAN":95,"PAS":82,"PDEF":80,"IDEF":80,"BLK":81,"REB":73,"ATH":82,"STR":74,"CLU":80,"nbaId":1630567},{"name":"Immanuel Quickley","cname":"伊曼纽尔-奎克利","pos":"PG / SG","height":"6'2'","type":"2-Way Mid-Range Menace","ovr":84,"threePT":81,"MID":94,"FIN":91,"DNK":55,"HAN":98,"PAS":80,"PDEF":80,"IDEF":37,"BLK":30,"REB":56,"ATH":82,"STR":54,"CLU":70,"nbaId":1630193},{"name":"RJ Barrett","cname":"RJ-巴雷特","pos":"SG / SF","height":"6'6'","type":"2-Way Bullmaker","ovr":83,"threePT":78,"MID":79,"FIN":89,"DNK":80,"HAN":85,"PAS":79,"PDEF":81,"IDEF":58,"BLK":50,"REB":67,"ATH":84,"STR":76,"CLU":75,"nbaId":1629628},{"name":"Collin Murray-Boyles","cname":"科林-默里-博伊尔斯","pos":"C / PF","height":"6'7'","type":"Ball-Hawking Blow-By Laymaker","ovr":81,"threePT":77,"MID":70,"FIN":94,"DNK":75,"HAN":85,"PAS":67,"PDEF":74,"IDEF":72,"BLK":80,"REB":58,"ATH":86,"STR":70,"CLU":95,"nbaId":1642867},{"name":"Jakob Poeltl","cname":"雅各布-珀尔特尔","pos":"C","height":"7'0'","type":"Crafty Mid-Post Scorer","ovr":80,"threePT":45,"MID":79,"FIN":97,"DNK":60,"HAN":85,"PAS":69,"PDEF":45,"IDEF":81,"BLK":67,"REB":80,"ATH":37,"STR":80,"CLU":75,"nbaId":1627751},{"name":"Ja’Kobe Walter","cname":"贾科比-沃尔特","pos":"SG / SF","height":"6'4'","type":"Ball-Hawking 3PT Sniper","ovr":78,"threePT":83,"MID":78,"FIN":84,"DNK":75,"HAN":80,"PAS":69,"PDEF":66,"IDEF":47,"BLK":47,"REB":50,"ATH":82,"STR":57,"CLU":90,"nbaId":1642266},{"name":"Jamal Shead","cname":"贾马尔-谢德","pos":"PG / SG","height":"6'1'","type":"Speedy Blow-By Ace","ovr":78,"threePT":74,"MID":51,"FIN":72,"DNK":50,"HAN":80,"PAS":84,"PDEF":77,"IDEF":34,"BLK":45,"REB":43,"ATH":94,"STR":49,"CLU":85,"nbaId":1642347},{"name":"Jamison Battle","cname":"贾米森-巴特尔","pos":"SF / PF","height":"6'7'","type":"Crafty Sharpshooter","ovr":76,"threePT":88,"MID":85,"FIN":94,"DNK":65,"HAN":80,"PAS":52,"PDEF":58,"IDEF":58,"BLK":42,"REB":65,"ATH":58,"STR":55,"CLU":55},{"name":"Kyle Anderson","cname":"凯尔-安德森","pos":"PF / C","height":"6'8'","type":"Defensive Playmaker","ovr":75,"threePT":79,"MID":48,"FIN":97,"DNK":45,"HAN":70,"PAS":78,"PDEF":79,"IDEF":60,"BLK":67,"REB":61,"ATH":80,"STR":62,"CLU":60,"nbaId":203937},{"name":"A.J. Lawson","cname":"A.J.-劳森","pos":"SF / SG","height":"6'6'","type":"Crafty 3PT-Sniping Dimer","ovr":74,"threePT":82,"MID":68,"FIN":75,"DNK":75,"HAN":75,"PAS":69,"PDEF":60,"IDEF":48,"BLK":53,"REB":68,"ATH":79,"STR":34,"CLU":50},{"name":"Kyle Lowry","cname":"凯尔-洛瑞","pos":"PG / SG","height":"6'0'","type":"2-Way Mid-Range Menace","ovr":73,"threePT":75,"MID":85,"FIN":67,"DNK":25,"HAN":95,"PAS":75,"PDEF":80,"IDEF":60,"BLK":56,"REB":48,"ATH":84,"STR":67,"CLU":40,"nbaId":200768},{"name":"Trayce Jackson-Davis","cname":"特雷斯-杰克逊-戴维斯","pos":"C / PF","height":"6'9'","type":"Rim-Running Rim Rocker","ovr":73,"threePT":44,"MID":70,"FIN":61,"DNK":85,"HAN":75,"PAS":62,"PDEF":58,"IDEF":63,"BLK":83,"REB":73,"ATH":74,"STR":63,"CLU":40},{"name":"Alijah Martin","cname":"阿利雅-马丁","pos":"SG / PG","height":"6'2'","type":"Ball-Hawking Wing Runner","ovr":71,"threePT":76,"MID":76,"FIN":77,"DNK":85,"HAN":80,"PAS":67,"PDEF":74,"IDEF":48,"BLK":56,"REB":44,"ATH":88,"STR":62,"CLU":50},{"name":"Garrett Temple","cname":"加勒特-坦普尔","pos":"SG / SF","height":"6'5'","type":"2-Way Combo Guard","ovr":70,"threePT":72,"MID":55,"FIN":82,"DNK":30,"HAN":85,"PAS":75,"PDEF":78,"IDEF":46,"BLK":47,"REB":51,"ATH":76,"STR":44,"CLU":70}],"UTA":[{"name":"Lauri Markkanen","cname":"劳里-马尔卡宁","pos":"SF / PF","height":"7'1'","type":"Crafty Post-Scoring 3PT Sniper","ovr":87,"threePT":82,"MID":72,"FIN":96,"DNK":80,"HAN":95,"PAS":67,"PDEF":65,"IDEF":66,"BLK":52,"REB":66,"ATH":65,"STR":61,"CLU":80,"nbaId":1628374},{"name":"Keyonte George","cname":"基扬特-乔治","pos":"PG / SG","height":"6'4'","type":"Crafty Laymaker","ovr":87,"threePT":81,"MID":83,"FIN":94,"DNK":65,"HAN":90,"PAS":85,"PDEF":65,"IDEF":34,"BLK":45,"REB":52,"ATH":78,"STR":55,"CLU":95,"nbaId":1641718},{"name":"Jaren Jackson Jr.","cname":"小贾伦-杰克逊","pos":"PF / C","height":"6'10'","type":"2-Way Paint Pounder","ovr":86,"threePT":79,"MID":57,"FIN":97,"DNK":80,"HAN":80,"PAS":45,"PDEF":80,"IDEF":93,"BLK":83,"REB":67,"ATH":80,"STR":82,"CLU":75,"nbaId":1628991},{"name":"Isaiah Collier","cname":"以赛亚-科利尔","pos":"PG / SG","height":"6'4'","type":"Playmaker","ovr":82,"threePT":73,"MID":68,"FIN":94,"DNK":75,"HAN":95,"PAS":90,"PDEF":62,"IDEF":30,"BLK":52,"REB":48,"ATH":88,"STR":60,"CLU":80,"nbaId":1642268},{"name":"Jusuf Nurkic","cname":"优素福-努尔基奇","pos":"C","height":"6'11'","type":"Ball-Snatching Popper","ovr":82,"threePT":79,"MID":88,"FIN":97,"DNK":65,"HAN":85,"PAS":70,"PDEF":45,"IDEF":71,"BLK":56,"REB":93,"ATH":49,"STR":90,"CLU":85,"nbaId":203994},{"name":"Ace Bailey","cname":"埃斯-贝利","pos":"SG / SF","height":"6'9'","type":"Crafty Blow-By High Flyer","ovr":81,"threePT":78,"MID":74,"FIN":97,"DNK":80,"HAN":80,"PAS":66,"PDEF":68,"IDEF":59,"BLK":62,"REB":52,"ATH":84,"STR":61,"CLU":95,"nbaId":1642846},{"name":"Brice Sensabaugh","cname":"布赖斯-森萨博","pos":"SF / SG","height":"6'6'","type":"Crafty 3PT Sniper","ovr":81,"threePT":80,"MID":79,"FIN":98,"DNK":60,"HAN":95,"PAS":65,"PDEF":55,"IDEF":50,"BLK":42,"REB":52,"ATH":67,"STR":64,"CLU":90,"nbaId":1641729},{"name":"Kyle Filipowski","cname":"凯尔-菲利波夫斯基","pos":"C / PF","height":"6'11'","type":"Glass-Cleaning Rim Runner","ovr":79,"threePT":76,"MID":70,"FIN":97,"DNK":70,"HAN":85,"PAS":60,"PDEF":55,"IDEF":60,"BLK":61,"REB":86,"ATH":57,"STR":68,"CLU":80,"nbaId":1642271},{"name":"Jaxson Hayes","cname":"贾克森-海斯","pos":"C / PF","height":"7'0'","type":"Crafty Rim Wrecker","ovr":79,"threePT":60,"MID":68,"FIN":98,"DNK":88,"HAN":90,"PAS":29,"PDEF":48,"IDEF":78,"BLK":80,"REB":67,"ATH":64,"STR":72,"CLU":70,"nbaId":1629637},{"name":"Kevin Love","cname":"凯文-乐福","pos":"C / PF","height":"6'8'","type":"Crafty Stretch Five Cleaner","ovr":76,"threePT":80,"MID":79,"FIN":98,"DNK":50,"HAN":80,"PAS":65,"PDEF":50,"IDEF":69,"BLK":57,"REB":89,"ATH":54,"STR":80,"CLU":70,"nbaId":201567},{"name":"Cody Williams","cname":"科迪-威廉姆斯","pos":"PF / SF","height":"6'8'","type":"Mid-Range Menace","ovr":76,"threePT":70,"MID":80,"FIN":97,"DNK":80,"HAN":95,"PAS":63,"PDEF":73,"IDEF":68,"BLK":56,"REB":47,"ATH":86,"STR":57,"CLU":95},{"name":"John Konchar","cname":"约翰-康查尔","pos":"SF / PF","height":"6'5'","type":"Disruptive Crafty Finisher","ovr":76,"threePT":69,"MID":73,"FIN":97,"DNK":55,"HAN":85,"PAS":74,"PDEF":62,"IDEF":54,"BLK":72,"REB":65,"ATH":69,"STR":40,"CLU":95},{"name":"Svi Mykhailiuk","cname":"斯维亚托斯拉夫-米哈伊柳克","pos":"SG / SF","height":"6'7'","type":"Crafty Sharpshooter","ovr":76,"threePT":84,"MID":83,"FIN":92,"DNK":55,"HAN":94,"PAS":69,"PDEF":55,"IDEF":37,"BLK":32,"REB":45,"ATH":82,"STR":54,"CLU":80},{"name":"Josh Okogie","cname":"乔什-奥科吉","pos":"SG / SF","height":"6'4'","type":"Catch & Shoot 3PT Sniper","ovr":75,"threePT":83,"MID":70,"FIN":78,"DNK":75,"HAN":70,"PAS":69,"PDEF":77,"IDEF":50,"BLK":50,"REB":48,"ATH":84,"STR":58,"CLU":70,"nbaId":1629006},{"name":"Oscar Tshiebwe","cname":"奥斯卡-奇布韦","pos":"C / PF","height":"6'8'","type":"Rim-Running Putback Pro","ovr":73,"threePT":37,"MID":69,"FIN":97,"DNK":75,"HAN":95,"PAS":42,"PDEF":48,"IDEF":69,"BLK":45,"REB":80,"ATH":67,"STR":69,"CLU":40}],"WAS":[{"name":"Anthony Davis","cname":"安东尼-戴维斯","pos":"PF / C","height":"6'10'","type":"Backline Post Bully","ovr":92,"threePT":71,"MID":70,"FIN":91,"DNK":85,"HAN":85,"PAS":60,"PDEF":80,"IDEF":90,"BLK":82,"REB":87,"ATH":78,"STR":92,"CLU":80,"nbaId":203076},{"name":"Trae Young","cname":"特雷-杨","pos":"PG","height":"6'2'","type":"Crafty Offensive Engine","ovr":89,"threePT":83,"MID":97,"FIN":94,"DNK":30,"HAN":80,"PAS":92,"PDEF":55,"IDEF":31,"BLK":32,"REB":43,"ATH":84,"STR":50,"CLU":75,"nbaId":1629027},{"name":"Alex Sarr","cname":"亚历克斯-萨尔","pos":"C / PF","height":"7'0'","type":"Shot-Erasing Versatile Slasher","ovr":84,"threePT":76,"MID":71,"FIN":84,"DNK":85,"HAN":80,"PAS":65,"PDEF":72,"IDEF":78,"BLK":96,"REB":77,"ATH":72,"STR":67,"CLU":95,"nbaId":1642259},{"name":"KyShawn George","cname":"凯肖恩-乔治","pos":"PF / SF","height":"6'8'","type":"Crafty 3-Level Scorer","ovr":82,"threePT":82,"MID":79,"FIN":83,"DNK":60,"HAN":95,"PAS":66,"PDEF":70,"IDEF":70,"BLK":67,"REB":65,"ATH":74,"STR":60,"CLU":95,"nbaId":1642273},{"name":"Will Riley","cname":"威尔-莱利","pos":"PF / SF","height":"6'9'","type":"Blow-By Laymaking Four","ovr":81,"threePT":76,"MID":68,"FIN":88,"DNK":70,"HAN":95,"PAS":66,"PDEF":62,"IDEF":60,"BLK":35,"REB":49,"ATH":65,"STR":45,"CLU":95},{"name":"Deandre Ayton","cname":"德安德烈-艾顿","pos":"C","height":"7'0'","type":"Physical Mid-Range Lob Threat","ovr":79,"threePT":63,"MID":85,"FIN":98,"DNK":75,"HAN":82,"PAS":53,"PDEF":49,"IDEF":76,"BLK":72,"REB":83,"ATH":65,"STR":85,"CLU":45,"nbaId":1629028},{"name":"Bilal Coulibaly","cname":"比拉勒-库利巴利","pos":"SF / SG","height":"6'7'","type":"High Flyer","ovr":79,"threePT":73,"MID":70,"FIN":65,"DNK":85,"HAN":80,"PAS":69,"PDEF":80,"IDEF":67,"BLK":71,"REB":62,"ATH":85,"STR":63,"CLU":75,"nbaId":1641731},{"name":"Justin Champagnie","cname":"贾斯廷-尚帕尼","pos":"PF / SF","height":"6'6'","type":"Crafty 3-Level Scoring Crasher","ovr":78,"threePT":75,"MID":79,"FIN":71,"DNK":75,"HAN":80,"PAS":62,"PDEF":73,"IDEF":49,"BLK":67,"REB":72,"ATH":73,"STR":40,"CLU":85,"nbaId":1630551},{"name":"Tre Johnson","cname":"特雷-约翰逊","pos":"SG / PG","height":"6'5'","type":"Mid-Range Menace","ovr":78,"threePT":80,"MID":98,"FIN":81,"DNK":75,"HAN":95,"PAS":66,"PDEF":62,"IDEF":41,"BLK":51,"REB":51,"ATH":82,"STR":54,"CLU":80,"nbaId":1642848},{"name":"Bub Carrington","cname":"巴布-卡林顿","pos":"PG / SG","height":"6'4'","type":"Iso Sniper","ovr":78,"threePT":83,"MID":91,"FIN":69,"DNK":60,"HAN":85,"PAS":79,"PDEF":55,"IDEF":40,"BLK":42,"REB":54,"ATH":72,"STR":54,"CLU":95,"nbaId":1642267},{"name":"Tristan Vukcevic","cname":"特里斯坦-武克切维奇","pos":"C / PF","height":"7'0'","type":"Crafty Stretch Five","ovr":77,"threePT":78,"MID":74,"FIN":98,"DNK":65,"HAN":78,"PAS":45,"PDEF":44,"IDEF":59,"BLK":81,"REB":72,"ATH":55,"STR":72,"CLU":75},{"name":"Julian Reese","cname":"朱利安-里斯","pos":"C / PF","height":"6'9'","type":"Board-Hunting Crafty Finisher","ovr":76,"threePT":30,"MID":66,"FIN":73,"DNK":70,"HAN":60,"PAS":47,"PDEF":59,"IDEF":67,"BLK":51,"REB":87,"ATH":64,"STR":76,"CLU":60},{"name":"Jamir Watkins","cname":"贾米尔-沃特金斯","pos":"SF / PF","height":"6'6'","type":"Ball-Hawking Slasher","ovr":76,"threePT":71,"MID":78,"FIN":80,"DNK":85,"HAN":80,"PAS":68,"PDEF":69,"IDEF":67,"BLK":62,"REB":63,"ATH":85,"STR":59,"CLU":50},{"name":"Khris Middleton","cname":"克里斯-米德尔顿","pos":"SF / PF","height":"6'7'","type":"Physical Middy Maestro","ovr":76,"threePT":78,"MID":87,"FIN":81,"DNK":65,"HAN":80,"PAS":74,"PDEF":74,"IDEF":67,"BLK":40,"REB":60,"ATH":74,"STR":73,"CLU":35,"nbaId":203114},{"name":"Cam Whitmore","cname":"卡姆-惠特莫尔","pos":"SF / PF","height":"6'6'","type":"Athletic Blow-By Ace","ovr":75,"threePT":70,"MID":76,"FIN":61,"DNK":80,"HAN":80,"PAS":68,"PDEF":67,"IDEF":57,"BLK":53,"REB":54,"ATH":84,"STR":68,"CLU":50,"nbaId":1641715}]};
  window.__HIST_INLINE = [{"name":"Bob Cousy","cname":"鲍勃-库西","pos":"SG / PG","ovr":95,"threePT":92,"MID":84,"FIN":90,"DNK":91,"HAN":94,"PAS":94,"PDEF":94,"IDEF":67,"BLK":67,"REB":74,"ATH":96,"STR":84,"CLU":90,"nbaId":"h:600003","historical":true,"tier":"hall-of-fame","era":"1957-58"},{"name":"John Havlicek","cname":"约翰-哈夫利切克","pos":"SF","ovr":95,"threePT":93,"MID":90,"FIN":92,"DNK":87,"HAN":83,"PAS":83,"PDEF":92,"IDEF":76,"BLK":76,"REB":78,"ATH":92,"STR":92,"CLU":90,"nbaId":"h:76970","historical":true,"tier":"hall-of-fame","era":"1957-58"},{"name":"Larry Bird","cname":"拉里-伯德","pos":"PF / C","ovr":98,"threePT":98,"MID":96,"FIN":94,"DNK":89,"HAN":82,"PAS":82,"PDEF":84,"IDEF":77,"BLK":77,"REB":84,"ATH":94,"STR":94,"CLU":93,"nbaId":"h:1449","historical":true,"tier":"hall-of-fame","era":"1957-58"},{"name":"Kevin McHale","cname":"凯文-麦克海尔","pos":"C","ovr":95,"threePT":78,"MID":96,"FIN":97,"DNK":88,"HAN":69,"PAS":69,"PDEF":79,"IDEF":89,"BLK":89,"REB":94,"ATH":93,"STR":99,"CLU":90,"nbaId":"h:1450","historical":true,"tier":"hall-of-fame","era":"1957-58"},{"name":"Bill Russell","cname":"比尔-拉塞尔","pos":"SF","ovr":98,"threePT":58,"MID":99,"FIN":99,"DNK":86,"HAN":79,"PAS":79,"PDEF":92,"IDEF":99,"BLK":99,"REB":99,"ATH":91,"STR":99,"CLU":93,"nbaId":"h:78049","historical":true,"tier":"hall-of-fame","era":"1957-58"},{"name":"Jason Kidd","cname":"杰森-基德","pos":"SG / PG","ovr":94,"threePT":80,"MID":84,"FIN":94,"DNK":94,"HAN":96,"PAS":96,"PDEF":96,"IDEF":74,"BLK":74,"REB":73,"ATH":99,"STR":88,"CLU":89,"nbaId":"h:467","historical":true,"tier":"hall-of-fame","era":"1957-58"},{"name":"Vince Carter","cname":"文斯-卡特","pos":"SF","ovr":91,"threePT":90,"MID":97,"FIN":99,"DNK":94,"HAN":82,"PAS":82,"PDEF":69,"IDEF":70,"BLK":70,"REB":75,"ATH":99,"STR":99,"CLU":86,"nbaId":"h:1713","historical":true,"tier":"hall-of-fame","era":"1957-58"},{"name":"Julius Erving","cname":"朱利叶斯-欧文","pos":"PF / SF","ovr":95,"threePT":84,"MID":97,"FIN":99,"DNK":94,"HAN":85,"PAS":85,"PDEF":79,"IDEF":78,"BLK":78,"REB":79,"ATH":99,"STR":99,"CLU":90,"nbaId":"h:76681","historical":true,"tier":"hall-of-fame","era":"1957-58"},{"name":"Buck Williams","cname":"巴克-威廉姆斯","pos":"C","ovr":87,"threePT":66,"MID":94,"FIN":96,"DNK":87,"HAN":65,"PAS":65,"PDEF":78,"IDEF":77,"BLK":77,"REB":96,"ATH":92,"STR":99,"CLU":82,"nbaId":"h:433","historical":true,"tier":"modern-all-star","era":"1957-58"},{"name":"Brook Lopez","cname":"布鲁克-洛佩斯","pos":"SF","ovr":85,"threePT":90,"MID":81,"FIN":83,"DNK":70,"HAN":57,"PAS":57,"PDEF":75,"IDEF":94,"BLK":94,"REB":85,"ATH":75,"STR":91,"CLU":80,"nbaId":"h:201572","historical":true,"tier":"modern-all-star","era":"2022-23"},{"name":"Walt Frazier","cname":"沃尔特-弗雷泽","pos":"SG / SF","ovr":93,"threePT":78,"MID":97,"FIN":95,"DNK":94,"HAN":92,"PAS":92,"PDEF":96,"IDEF":64,"BLK":64,"REB":62,"ATH":99,"STR":89,"CLU":88,"nbaId":"h:76750","historical":true,"tier":"hall-of-fame","era":"1957-58"},{"name":"Allan Houston","cname":"阿兰-休斯顿","pos":"SF / PF","ovr":87,"threePT":93,"MID":90,"FIN":93,"DNK":91,"HAN":77,"PAS":77,"PDEF":73,"IDEF":68,"BLK":68,"REB":70,"ATH":96,"STR":90,"CLU":82,"nbaId":"h:275","historical":true,"tier":"modern-all-star","era":"1957-58"},{"name":"Carmelo Anthony","cname":"卡梅隆-安东尼","pos":"PF / C","ovr":92,"threePT":87,"MID":94,"FIN":96,"DNK":91,"HAN":86,"PAS":86,"PDEF":75,"IDEF":65,"BLK":65,"REB":83,"ATH":96,"STR":96,"CLU":87,"nbaId":"h:2546","historical":true,"tier":"hall-of-fame","era":"1957-58"},{"name":"Dave Debusschere","cname":"戴夫-德布斯切尔","pos":"C / PF","ovr":95,"threePT":85,"MID":89,"FIN":95,"DNK":86,"HAN":64,"PAS":64,"PDEF":92,"IDEF":92,"BLK":92,"REB":93,"ATH":91,"STR":99,"CLU":90,"nbaId":"h:76545","historical":true,"tier":"hall-of-fame","era":"1957-58"},{"name":"Patrick Ewing","cname":"帕特里克-尤因","pos":"SF","ovr":93,"threePT":65,"MID":97,"FIN":98,"DNK":85,"HAN":63,"PAS":63,"PDEF":63,"IDEF":89,"BLK":89,"REB":94,"ATH":90,"STR":99,"CLU":88,"nbaId":"h:201607","historical":true,"tier":"hall-of-fame","era":"1957-58"},{"name":"Allen Iverson","cname":"阿伦-艾弗森","pos":"SG","ovr":95,"threePT":85,"MID":97,"FIN":96,"DNK":94,"HAN":94,"PAS":94,"PDEF":96,"IDEF":68,"BLK":68,"REB":67,"ATH":99,"STR":93,"CLU":90,"nbaId":"h:947","historical":true,"tier":"hall-of-fame","era":"1957-58"},{"name":"Hal Greer","cname":"哈尔-格里尔","pos":"SF","ovr":85,"threePT":91,"MID":77,"FIN":85,"DNK":86,"HAN":86,"PAS":86,"PDEF":74,"IDEF":60,"BLK":60,"REB":64,"ATH":91,"STR":79,"CLU":80,"nbaId":"h:76882","historical":true,"tier":"hall-of-fame","era":"1957-58"},{"name":"Charles Barkley","cname":"查尔斯-巴克利","pos":"C","ovr":88,"threePT":70,"MID":92,"FIN":93,"DNK":84,"HAN":73,"PAS":73,"PDEF":70,"IDEF":84,"BLK":84,"REB":90,"ATH":89,"STR":97,"CLU":83,"nbaId":"h:787","historical":true,"tier":"hall-of-fame","era":"1996-97"},{"name":"Wilt Chamberlain","cname":"威尔特-张伯伦","pos":"SF","ovr":98,"threePT":68,"MID":99,"FIN":99,"DNK":86,"HAN":69,"PAS":69,"PDEF":86,"IDEF":99,"BLK":99,"REB":99,"ATH":91,"STR":99,"CLU":93,"nbaId":"h:76375","historical":true,"tier":"hall-of-fame","era":"1957-58"},{"name":"Kyle Lowry","cname":"凯尔-洛瑞","pos":"SG / PG","ovr":85,"threePT":87,"MID":91,"FIN":90,"DNK":91,"HAN":86,"PAS":86,"PDEF":75,"IDEF":62,"BLK":62,"REB":66,"ATH":96,"STR":84,"CLU":80,"nbaId":"h:200768","historical":true,"tier":"modern-all-star","era":"1957-58"},{"name":"DeMar DeRozan","cname":"德马尔-德罗赞","pos":"SF / PF","ovr":89,"threePT":80,"MID":96,"FIN":96,"DNK":94,"HAN":79,"PAS":79,"PDEF":78,"IDEF":74,"BLK":74,"REB":73,"ATH":99,"STR":93,"CLU":84,"nbaId":"h:201942","historical":true,"tier":"modern-all-star","era":"1957-58"},{"name":"Kawhi Leonard","cname":"科怀-伦纳德","pos":"PF / C","ovr":94,"threePT":83,"MID":93,"FIN":96,"DNK":91,"HAN":83,"PAS":83,"PDEF":95,"IDEF":76,"BLK":76,"REB":83,"ATH":96,"STR":96,"CLU":89,"nbaId":"h:202695","historical":true,"tier":"modern-all-star","era":"1957-58"},{"name":"Chris Bosh","cname":"克里斯-波什","pos":"C","ovr":90,"threePT":82,"MID":90,"FIN":97,"DNK":88,"HAN":67,"PAS":67,"PDEF":69,"IDEF":78,"BLK":78,"REB":93,"ATH":93,"STR":99,"CLU":85,"nbaId":"h:2547","historical":true,"tier":"hall-of-fame","era":"1957-58"},{"name":"Marc Gasol","cname":"马克-加索尔","pos":"SF","ovr":86,"threePT":75,"MID":81,"FIN":85,"DNK":72,"HAN":76,"PAS":76,"PDEF":67,"IDEF":88,"BLK":88,"REB":84,"ATH":77,"STR":93,"CLU":81,"nbaId":"h:201188","historical":true,"tier":"modern-all-star","era":"2016-17"},{"name":"Derrick Rose","cname":"德里克-罗斯","pos":"SG / PG","ovr":95,"threePT":83,"MID":98,"FIN":98,"DNK":94,"HAN":94,"PAS":94,"PDEF":89,"IDEF":75,"BLK":75,"REB":68,"ATH":99,"STR":92,"CLU":90,"nbaId":"h:201565","historical":true,"tier":"modern-all-star","era":"1957-58"},{"name":"Michael Jordan","cname":"迈克尔-乔丹","pos":"SF / PF","ovr":99,"threePT":89,"MID":97,"FIN":99,"DNK":94,"HAN":89,"PAS":89,"PDEF":97,"IDEF":78,"BLK":78,"REB":78,"ATH":99,"STR":96,"CLU":94,"nbaId":"h:893","historical":true,"tier":"hall-of-fame","era":"1957-58"},{"name":"Scottie Pippen","cname":"斯科蒂-皮蓬","pos":"PF / SF","ovr":95,"threePT":76,"MID":94,"FIN":95,"DNK":90,"HAN":84,"PAS":84,"PDEF":99,"IDEF":71,"BLK":71,"REB":89,"ATH":95,"STR":95,"CLU":90,"nbaId":"h:937","historical":true,"tier":"hall-of-fame","era":"1957-58"},{"name":"Dennis Rodman","cname":"丹尼斯-罗德曼","pos":"C / PF","ovr":90,"threePT":63,"MID":84,"FIN":99,"DNK":90,"HAN":71,"PAS":71,"PDEF":81,"IDEF":92,"BLK":92,"REB":99,"ATH":95,"STR":99,"CLU":85,"nbaId":"h:23","historical":true,"tier":"hall-of-fame","era":"1957-58"},{"name":"Artis Gilmore","cname":"阿尔提尔-吉尔莫尔","pos":"SF","ovr":90,"threePT":64,"MID":93,"FIN":91,"DNK":78,"HAN":68,"PAS":68,"PDEF":61,"IDEF":97,"BLK":97,"REB":96,"ATH":83,"STR":99,"CLU":85,"nbaId":"h:600014","historical":true,"tier":"hall-of-fame","era":"1957-58"},{"name":"Mark Price","cname":"马克-普莱斯","pos":"SG / SF","ovr":90,"threePT":96,"MID":92,"FIN":89,"DNK":90,"HAN":90,"PAS":90,"PDEF":74,"IDEF":62,"BLK":62,"REB":67,"ATH":95,"STR":83,"CLU":85,"nbaId":"h:899","historical":true,"tier":"modern-all-star","era":"1957-58"},{"name":"Kyrie Irving","cname":"凯里-欧文","pos":"SF","ovr":90,"threePT":90,"MID":96,"FIN":94,"DNK":94,"HAN":85,"PAS":85,"PDEF":73,"IDEF":67,"BLK":67,"REB":69,"ATH":99,"STR":88,"CLU":85,"nbaId":"h:202681","historical":true,"tier":"modern-all-star","era":"1957-58"},{"name":"LeBron James","cname":"勒布朗-詹姆斯","pos":"PF / C","ovr":98,"threePT":84,"MID":99,"FIN":99,"DNK":94,"HAN":94,"PAS":94,"PDEF":84,"IDEF":78,"BLK":78,"REB":85,"ATH":99,"STR":99,"CLU":93,"nbaId":"h:2544","historical":true,"tier":"modern-all-star","era":"1957-58"},{"name":"Kevin Love","cname":"凯文-乐福","pos":"C","ovr":90,"threePT":88,"MID":94,"FIN":96,"DNK":87,"HAN":65,"PAS":65,"PDEF":74,"IDEF":69,"BLK":69,"REB":94,"ATH":92,"STR":99,"CLU":85,"nbaId":"h:201567","historical":true,"tier":"modern-all-star","era":"1957-58"},{"name":"Brad Daugherty","cname":"布拉德-多赫蒂","pos":"SF","ovr":90,"threePT":62,"MID":96,"FIN":98,"DNK":85,"HAN":65,"PAS":65,"PDEF":71,"IDEF":84,"BLK":84,"REB":93,"ATH":90,"STR":99,"CLU":85,"nbaId":"h:921","historical":true,"tier":"modern-all-star","era":"1957-58"},{"name":"Isiah Thomas","cname":"以赛亚-托马斯","pos":"SG / SF","ovr":94,"threePT":78,"MID":97,"FIN":97,"DNK":94,"HAN":99,"PAS":99,"PDEF":93,"IDEF":64,"BLK":64,"REB":68,"ATH":99,"STR":91,"CLU":89,"nbaId":"h:78318","historical":true,"tier":"hall-of-fame","era":"1957-58"},{"name":"Joe Dumars","cname":"乔-杜马斯","pos":"SF / SG","ovr":92,"threePT":90,"MID":94,"FIN":98,"DNK":94,"HAN":86,"PAS":86,"PDEF":86,"IDEF":67,"BLK":67,"REB":68,"ATH":99,"STR":95,"CLU":87,"nbaId":"h:247","historical":true,"tier":"hall-of-fame","era":"1957-58"},{"name":"Grant Hill","cname":"格兰特-希尔","pos":"PF / SF","ovr":90,"threePT":67,"MID":97,"FIN":96,"DNK":91,"HAN":86,"PAS":86,"PDEF":80,"IDEF":69,"BLK":69,"REB":78,"ATH":96,"STR":96,"CLU":85,"nbaId":"h:255","historical":true,"tier":"hall-of-fame","era":"1957-58"},{"name":"Ben Wallace","cname":"本-华莱士","pos":"SF","ovr":85,"threePT":56,"MID":81,"FIN":99,"DNK":86,"HAN":64,"PAS":64,"PDEF":77,"IDEF":95,"BLK":95,"REB":89,"ATH":91,"STR":99,"CLU":80,"nbaId":"h:1112","historical":true,"tier":"hall-of-fame","era":"1957-58"},{"name":"Mark Jackson","cname":"马克-杰克逊","pos":"SG / PG","ovr":83,"threePT":78,"MID":87,"FIN":86,"DNK":87,"HAN":91,"PAS":91,"PDEF":78,"IDEF":67,"BLK":67,"REB":64,"ATH":92,"STR":80,"CLU":78,"nbaId":"h:349","historical":true,"tier":"modern-all-star","era":"1957-58"},{"name":"Reggie Miller","cname":"雷吉-米勒","pos":"SF / PF","ovr":86,"threePT":99,"MID":82,"FIN":90,"DNK":88,"HAN":76,"PAS":76,"PDEF":80,"IDEF":71,"BLK":71,"REB":72,"ATH":93,"STR":87,"CLU":81,"nbaId":"h:397","historical":true,"tier":"hall-of-fame","era":"1996-97"},{"name":"Paul George","cname":"保罗-乔治","pos":"PF / SF","ovr":91,"threePT":84,"MID":94,"FIN":96,"DNK":91,"HAN":77,"PAS":77,"PDEF":89,"IDEF":74,"BLK":74,"REB":79,"ATH":96,"STR":96,"CLU":86,"nbaId":"h:202331","historical":true,"tier":"modern-all-star","era":"1957-58"},{"name":"Jermaine ONeal","cname":"杰梅因-奥尼尔","pos":"C","ovr":90,"threePT":68,"MID":95,"FIN":99,"DNK":90,"HAN":65,"PAS":65,"PDEF":76,"IDEF":78,"BLK":78,"REB":91,"ATH":95,"STR":99,"CLU":85,"nbaId":"h:979","historical":true,"tier":"modern-all-star","era":"1957-58"},{"name":"Mel Daniels","cname":"梅尔-丹尼尔斯","pos":"SF","ovr":90,"threePT":59,"MID":96,"FIN":99,"DNK":86,"HAN":70,"PAS":70,"PDEF":70,"IDEF":84,"BLK":84,"REB":97,"ATH":91,"STR":99,"CLU":85,"nbaId":"h:76502","historical":true,"tier":"hall-of-fame","era":"1957-58"},{"name":"Oscar Robertson","cname":"奥斯卡-罗伯特森","pos":"SG / SF","ovr":95,"threePT":82,"MID":89,"FIN":99,"DNK":94,"HAN":97,"PAS":97,"PDEF":85,"IDEF":76,"BLK":76,"REB":81,"ATH":99,"STR":93,"CLU":90,"nbaId":"h:600015","historical":true,"tier":"hall-of-fame","era":"1957-58"},{"name":"Sidney Moncrief","cname":"西德尼-蒙克利夫","pos":"SF / SG","ovr":91,"threePT":83,"MID":96,"FIN":94,"DNK":92,"HAN":85,"PAS":85,"PDEF":85,"IDEF":74,"BLK":74,"REB":75,"ATH":97,"STR":91,"CLU":86,"nbaId":"h:77626","historical":true,"tier":"hall-of-fame","era":"1957-58"},{"name":"Marques Johnson","cname":"马奎斯-约翰逊","pos":"PF / SF","ovr":90,"threePT":81,"MID":96,"FIN":95,"DNK":90,"HAN":77,"PAS":77,"PDEF":83,"IDEF":70,"BLK":70,"REB":77,"ATH":95,"STR":95,"CLU":85,"nbaId":"h:77160","historical":true,"tier":"modern-all-star","era":"1957-58"},{"name":"Giannis Antetokounmpo","cname":"扬尼斯-阿德托昆博","pos":"C / SF","ovr":95,"threePT":69,"MID":95,"FIN":99,"DNK":94,"HAN":84,"PAS":84,"PDEF":84,"IDEF":87,"BLK":87,"REB":94,"ATH":99,"STR":99,"CLU":90,"nbaId":"h:203507","historical":true,"tier":"modern-all-star","era":"1957-58"},{"name":"Kareem Abdul-Jabbar","cname":"卡里姆-阿布杜尔-贾巴尔","pos":"SF","ovr":98,"threePT":61,"MID":99,"FIN":99,"DNK":86,"HAN":69,"PAS":69,"PDEF":88,"IDEF":96,"BLK":96,"REB":99,"ATH":91,"STR":99,"CLU":93,"nbaId":"h:76003","historical":true,"tier":"hall-of-fame","era":"1957-58"},{"name":"Lenny Wilkens","cname":"兰尼-威尔肯斯","pos":"SG / PG","ovr":86,"threePT":75,"MID":88,"FIN":89,"DNK":90,"HAN":91,"PAS":91,"PDEF":86,"IDEF":59,"BLK":59,"REB":65,"ATH":95,"STR":83,"CLU":81,"nbaId":"h:78530","historical":true,"tier":"hall-of-fame","era":"1970-71"},{"name":"Pete Maravich","cname":"皮特-马拉维奇","pos":"SF / SG","ovr":90,"threePT":95,"MID":91,"FIN":90,"DNK":88,"HAN":91,"PAS":91,"PDEF":73,"IDEF":65,"BLK":65,"REB":68,"ATH":93,"STR":87,"CLU":85,"nbaId":"h:77459","historical":true,"tier":"hall-of-fame","era":"1957-58"},{"name":"Dominique Wilkins","cname":"多米尼克-威尔金斯","pos":"PF / SF","ovr":92,"threePT":95,"MID":97,"FIN":97,"DNK":92,"HAN":78,"PAS":78,"PDEF":75,"IDEF":81,"BLK":81,"REB":82,"ATH":97,"STR":97,"CLU":87,"nbaId":"h:1122","historical":true,"tier":"hall-of-fame","era":"1957-58"},{"name":"Bob Pettit","cname":"鲍勃-佩蒂特","pos":"C","ovr":92,"threePT":94,"MID":98,"FIN":90,"DNK":81,"HAN":57,"PAS":57,"PDEF":65,"IDEF":84,"BLK":84,"REB":94,"ATH":86,"STR":94,"CLU":87,"nbaId":"h:77847","historical":true,"tier":"hall-of-fame","era":"1957-58"},{"name":"Dikembe Mutombo","cname":"迪肯贝-穆托姆博","pos":"SF","ovr":90,"threePT":58,"MID":89,"FIN":99,"DNK":86,"HAN":62,"PAS":62,"PDEF":77,"IDEF":99,"BLK":99,"REB":99,"ATH":91,"STR":99,"CLU":85,"nbaId":"h:87","historical":true,"tier":"hall-of-fame","era":"1957-58"},{"name":"Kemba Walker","cname":"肯巴-沃克","pos":"SG / PG","ovr":86,"threePT":91,"MID":81,"FIN":88,"DNK":89,"HAN":87,"PAS":87,"PDEF":74,"IDEF":67,"BLK":67,"REB":65,"ATH":94,"STR":82,"CLU":81,"nbaId":"h:202689","historical":true,"tier":"modern-all-star","era":"1957-58"},{"name":"Eddie Jones","cname":"埃迪-琼斯","pos":"SF / PF","ovr":86,"threePT":85,"MID":88,"FIN":89,"DNK":87,"HAN":76,"PAS":76,"PDEF":96,"IDEF":60,"BLK":60,"REB":63,"ATH":92,"STR":86,"CLU":81,"nbaId":"h:224","historical":true,"tier":"modern-all-star","era":"1957-58"},{"name":"Glen Rice","cname":"格伦-莱斯","pos":"PF / PG","ovr":90,"threePT":97,"MID":92,"FIN":92,"DNK":87,"HAN":72,"PAS":72,"PDEF":69,"IDEF":83,"BLK":83,"REB":67,"ATH":92,"STR":92,"CLU":85,"nbaId":"h:203318","historical":true,"tier":"modern-all-star","era":"1957-58"},{"name":"Larry Johnson","cname":"拉里-约翰逊","pos":"C / PF","ovr":86,"threePT":84,"MID":91,"FIN":89,"DNK":80,"HAN":71,"PAS":71,"PDEF":67,"IDEF":73,"BLK":73,"REB":85,"ATH":85,"STR":93,"CLU":81,"nbaId":"h:77156","historical":true,"tier":"modern-all-star","era":"1957-58"},{"name":"Alonzo Mourning","cname":"阿朗佐-莫宁","pos":"SF","ovr":92,"threePT":62,"MID":99,"FIN":98,"DNK":85,"HAN":54,"PAS":54,"PDEF":68,"IDEF":97,"BLK":97,"REB":94,"ATH":90,"STR":99,"CLU":87,"nbaId":"h:297","historical":true,"tier":"hall-of-fame","era":"1957-58"},{"name":"Tim Hardaway","cname":"蒂姆-哈达威","pos":"SG / SF","ovr":90,"threePT":86,"MID":85,"FIN":88,"DNK":89,"HAN":92,"PAS":92,"PDEF":99,"IDEF":64,"BLK":64,"REB":63,"ATH":94,"STR":82,"CLU":85,"nbaId":"h:896","historical":true,"tier":"hall-of-fame","era":"1957-58"},{"name":"Dwyane Wade","cname":"德维恩-韦德","pos":"SF / PG","ovr":96,"threePT":84,"MID":97,"FIN":98,"DNK":94,"HAN":93,"PAS":93,"PDEF":81,"IDEF":86,"BLK":86,"REB":75,"ATH":99,"STR":95,"CLU":91,"nbaId":"h:2548","historical":true,"tier":"hall-of-fame","era":"1957-58"},{"name":"Shaquille O'Neal","cname":"沙奎尔-奥尼尔","pos":"SF","ovr":98,"threePT":65,"MID":99,"FIN":99,"DNK":86,"HAN":75,"PAS":75,"PDEF":89,"IDEF":99,"BLK":99,"REB":99,"ATH":91,"STR":99,"CLU":93,"nbaId":"h:406","historical":true,"tier":"hall-of-fame","era":"1957-58"},{"name":"Anfernee Hardaway","cname":"安芬尼-哈达威","pos":"SG / SF","ovr":90,"threePT":84,"MID":97,"FIN":92,"DNK":93,"HAN":93,"PAS":93,"PDEF":79,"IDEF":64,"BLK":64,"REB":65,"ATH":98,"STR":86,"CLU":85,"nbaId":"h:358","historical":true,"tier":"modern-all-star","era":"1957-58"},{"name":"Nick Anderson","cname":"尼克-安德森","pos":"SF / PF","ovr":84,"threePT":87,"MID":88,"FIN":89,"DNK":87,"HAN":72,"PAS":72,"PDEF":83,"IDEF":67,"BLK":67,"REB":72,"ATH":92,"STR":86,"CLU":79,"nbaId":"h:98","historical":true,"tier":"modern-all-star","era":"1957-58"},{"name":"Tracy McGrady","cname":"特雷西-麦克格雷迪","pos":"PF / SG","ovr":93,"threePT":90,"MID":92,"FIN":94,"DNK":92,"HAN":87,"PAS":87,"PDEF":83,"IDEF":77,"BLK":77,"REB":75,"ATH":97,"STR":91,"CLU":88,"nbaId":"h:1503","historical":true,"tier":"hall-of-fame","era":"1957-58"},{"name":"Rashard Lewis","cname":"拉沙德-刘易斯","pos":"C / PF","ovr":88,"threePT":93,"MID":90,"FIN":94,"DNK":85,"HAN":74,"PAS":74,"PDEF":80,"IDEF":68,"BLK":68,"REB":71,"ATH":90,"STR":98,"CLU":83,"nbaId":"h:1740","historical":true,"tier":"modern-all-star","era":"1957-58"},{"name":"Dwight Howard","cname":"德怀特-霍华德","pos":"SF","ovr":91,"threePT":59,"MID":99,"FIN":98,"DNK":85,"HAN":63,"PAS":63,"PDEF":71,"IDEF":96,"BLK":96,"REB":97,"ATH":90,"STR":99,"CLU":86,"nbaId":"h:2730","historical":true,"tier":"hall-of-fame","era":"1957-58"},{"name":"Gilbert Arenas","cname":"吉尔伯特-阿里纳斯","pos":"SG / SF","ovr":91,"threePT":89,"MID":95,"FIN":94,"DNK":94,"HAN":89,"PAS":89,"PDEF":76,"IDEF":67,"BLK":67,"REB":68,"ATH":99,"STR":88,"CLU":86,"nbaId":"h:2240","historical":true,"tier":"modern-all-star","era":"1957-58"},{"name":"Earl Monroe","cname":"厄尔-门罗","pos":"SF","ovr":88,"threePT":83,"MID":95,"FIN":92,"DNK":93,"HAN":86,"PAS":86,"PDEF":83,"IDEF":64,"BLK":64,"REB":65,"ATH":98,"STR":86,"CLU":83,"nbaId":"h:600006","historical":true,"tier":"hall-of-fame","era":"1957-58"},{"name":"Bernard King","cname":"伯纳德-金","pos":"PF / PG","ovr":88,"threePT":79,"MID":95,"FIN":94,"DNK":89,"HAN":78,"PAS":78,"PDEF":74,"IDEF":70,"BLK":70,"REB":72,"ATH":94,"STR":94,"CLU":83,"nbaId":"h:77264","historical":true,"tier":"hall-of-fame","era":"1957-58"},{"name":"Elvin Hayes","cname":"埃尔文-海耶斯","pos":"C","ovr":93,"threePT":72,"MID":95,"FIN":99,"DNK":90,"HAN":64,"PAS":64,"PDEF":80,"IDEF":94,"BLK":94,"REB":98,"ATH":95,"STR":99,"CLU":88,"nbaId":"h:76979","historical":true,"tier":"hall-of-fame","era":"1957-58"},{"name":"Wes Unseld","cname":"韦斯-昂塞尔德","pos":"SF","ovr":94,"threePT":78,"MID":92,"FIN":93,"DNK":80,"HAN":75,"PAS":75,"PDEF":81,"IDEF":91,"BLK":91,"REB":93,"ATH":85,"STR":99,"CLU":89,"nbaId":"h:78392","historical":true,"tier":"hall-of-fame","era":"1957-58"},{"name":"Lafayette Lever","cname":"拉斐特-利弗","pos":"SG / SF","ovr":90,"threePT":81,"MID":88,"FIN":90,"DNK":91,"HAN":92,"PAS":92,"PDEF":95,"IDEF":55,"BLK":55,"REB":73,"ATH":96,"STR":84,"CLU":85,"nbaId":"h:77376","historical":true,"tier":"modern-all-star","era":"1957-58"},{"name":"David Thompson","cname":"大卫-汤普森","pos":"SF / PF","ovr":92,"threePT":78,"MID":97,"FIN":99,"DNK":94,"HAN":85,"PAS":85,"PDEF":78,"IDEF":85,"BLK":85,"REB":79,"ATH":99,"STR":96,"CLU":87,"nbaId":"h:78326","historical":true,"tier":"hall-of-fame","era":"1957-58"},{"name":"Alex English","cname":"阿历克斯-英格利什","pos":"PF / PG","ovr":91,"threePT":78,"MID":94,"FIN":97,"DNK":92,"HAN":79,"PAS":79,"PDEF":79,"IDEF":75,"BLK":75,"REB":78,"ATH":97,"STR":97,"CLU":86,"nbaId":"h:76673","historical":true,"tier":"hall-of-fame","era":"1957-58"},{"name":"Dan Issel","cname":"丹-伊赛尔","pos":"C","ovr":90,"threePT":70,"MID":96,"FIN":98,"DNK":89,"HAN":64,"PAS":64,"PDEF":73,"IDEF":78,"BLK":78,"REB":95,"ATH":94,"STR":99,"CLU":85,"nbaId":"h:77097","historical":true,"tier":"hall-of-fame","era":"1957-58"},{"name":"Nikola Jokic","cname":"尼古拉-约基奇","pos":"SF","ovr":95,"threePT":87,"MID":94,"FIN":91,"DNK":78,"HAN":94,"PAS":94,"PDEF":78,"IDEF":78,"BLK":78,"REB":92,"ATH":83,"STR":99,"CLU":90,"nbaId":"h:203999","historical":true,"tier":"modern-all-star","era":"2025-26"},{"name":"Sam Cassell","cname":"萨姆-卡塞尔","pos":"SG / SF","ovr":86,"threePT":88,"MID":94,"FIN":90,"DNK":91,"HAN":88,"PAS":88,"PDEF":76,"IDEF":63,"BLK":63,"REB":67,"ATH":96,"STR":84,"CLU":81,"nbaId":"h:208","historical":true,"tier":"modern-all-star","era":"1957-58"},{"name":"Anthony Edwards","cname":"安东尼-爱德华兹","pos":"SF / PF","ovr":90,"threePT":90,"MID":88,"FIN":94,"DNK":92,"HAN":77,"PAS":77,"PDEF":84,"IDEF":73,"BLK":73,"REB":76,"ATH":97,"STR":91,"CLU":85,"nbaId":"h:1630162","historical":true,"tier":"modern-all-star","era":"2025-26"},{"name":"Jimmy Butler","cname":"吉米-巴特勒","pos":"PF / SF","ovr":92,"threePT":77,"MID":93,"FIN":96,"DNK":91,"HAN":84,"PAS":84,"PDEF":93,"IDEF":78,"BLK":78,"REB":78,"ATH":96,"STR":96,"CLU":87,"nbaId":"h:202710","historical":true,"tier":"modern-all-star","era":"2022-23"},{"name":"Kevin Garnett","cname":"凯文-加内特","pos":"C","ovr":95,"threePT":69,"MID":96,"FIN":99,"DNK":90,"HAN":74,"PAS":74,"PDEF":86,"IDEF":86,"BLK":86,"REB":94,"ATH":95,"STR":99,"CLU":90,"nbaId":"h:708","historical":true,"tier":"hall-of-fame","era":"1957-58"},{"name":"Karl-Anthony Towns","cname":"卡尔-安东尼-唐斯","pos":"SF","ovr":89,"threePT":87,"MID":92,"FIN":85,"DNK":72,"HAN":65,"PAS":65,"PDEF":66,"IDEF":78,"BLK":78,"REB":92,"ATH":77,"STR":93,"CLU":84,"nbaId":"h:1626157","historical":true,"tier":"modern-all-star","era":"2024-25"},{"name":"Shai Gilgeous-Alexander","cname":"谢伊-吉尔杰斯-亚历山大","pos":"SG / SF","ovr":93,"threePT":86,"MID":92,"FIN":93,"DNK":94,"HAN":86,"PAS":86,"PDEF":85,"IDEF":75,"BLK":75,"REB":75,"ATH":99,"STR":87,"CLU":88,"nbaId":"h:1628983","historical":true,"tier":"modern-all-star","era":"2025-26"},{"name":"Ray allen","cname":"雷-阿伦","pos":"SF / PF","ovr":92,"threePT":98,"MID":94,"FIN":94,"DNK":92,"HAN":82,"PAS":82,"PDEF":84,"IDEF":67,"BLK":67,"REB":67,"ATH":97,"STR":91,"CLU":87,"nbaId":"h:951","historical":true,"tier":"hall-of-fame","era":"1957-58"},{"name":"Kevin Durant","cname":"凯文-杜兰特","pos":"PF / C","ovr":97,"threePT":94,"MID":98,"FIN":99,"DNK":94,"HAN":81,"PAS":81,"PDEF":76,"IDEF":77,"BLK":77,"REB":84,"ATH":99,"STR":99,"CLU":92,"nbaId":"h:201142","historical":true,"tier":"modern-all-star","era":"1957-58"},{"name":"Shawn Kemp","cname":"肖恩-坎普","pos":"C","ovr":90,"threePT":59,"MID":96,"FIN":99,"DNK":90,"HAN":67,"PAS":67,"PDEF":79,"IDEF":87,"BLK":87,"REB":92,"ATH":95,"STR":99,"CLU":85,"nbaId":"h:431","historical":true,"tier":"modern-all-star","era":"1957-58"},{"name":"Jack Sikma","cname":"杰克-西科马","pos":"SF","ovr":87,"threePT":75,"MID":88,"FIN":91,"DNK":78,"HAN":61,"PAS":61,"PDEF":74,"IDEF":80,"BLK":80,"REB":91,"ATH":83,"STR":99,"CLU":82,"nbaId":"h:78149","historical":true,"tier":"hall-of-fame","era":"1957-58"},{"name":"Damian Lillard","cname":"达米安-利拉德","pos":"SG / PG","ovr":90,"threePT":88,"MID":96,"FIN":94,"DNK":94,"HAN":89,"PAS":89,"PDEF":78,"IDEF":65,"BLK":65,"REB":67,"ATH":99,"STR":88,"CLU":85,"nbaId":"h:203081","historical":true,"tier":"modern-all-star","era":"1957-58"},{"name":"Clyde Drexler","cname":"克莱德-德雷克斯勒","pos":"SF / PF","ovr":94,"threePT":84,"MID":98,"FIN":99,"DNK":94,"HAN":84,"PAS":84,"PDEF":84,"IDEF":69,"BLK":69,"REB":85,"ATH":99,"STR":96,"CLU":89,"nbaId":"h:17","historical":true,"tier":"hall-of-fame","era":"1957-58"},{"name":"Jerome Kersey","cname":"杰罗姆-科尔西","pos":"PF / C","ovr":85,"threePT":78,"MID":89,"FIN":96,"DNK":91,"HAN":59,"PAS":59,"PDEF":75,"IDEF":74,"BLK":74,"REB":84,"ATH":96,"STR":96,"CLU":80,"nbaId":"h:760","historical":true,"tier":"modern-all-star","era":"1957-58"},{"name":"LaMarcus Aldridge","cname":"拉马库斯-阿尔德里奇","pos":"C","ovr":85,"threePT":84,"MID":93,"FIN":90,"DNK":81,"HAN":67,"PAS":67,"PDEF":65,"IDEF":64,"BLK":64,"REB":88,"ATH":86,"STR":94,"CLU":80,"nbaId":"h:200746","historical":true,"tier":"modern-all-star","era":"2018-19"},{"name":"Bill Walton","cname":"比尔-沃顿","pos":"SF","ovr":92,"threePT":50,"MID":95,"FIN":99,"DNK":86,"HAN":70,"PAS":70,"PDEF":70,"IDEF":94,"BLK":94,"REB":98,"ATH":91,"STR":99,"CLU":87,"nbaId":"h:78450","historical":true,"tier":"hall-of-fame","era":"1957-58"},{"name":"John Stockton","cname":"约翰-斯托克顿","pos":"SG / SF","ovr":93,"threePT":93,"MID":93,"FIN":84,"DNK":85,"HAN":97,"PAS":97,"PDEF":99,"IDEF":56,"BLK":56,"REB":57,"ATH":90,"STR":78,"CLU":88,"nbaId":"h:304","historical":true,"tier":"hall-of-fame","era":"1957-58"},{"name":"Donovan Mitchell","cname":"多诺万-米切尔","pos":"SF / SG","ovr":88,"threePT":89,"MID":88,"FIN":90,"DNK":88,"HAN":84,"PAS":84,"PDEF":79,"IDEF":69,"BLK":69,"REB":70,"ATH":93,"STR":87,"CLU":83,"nbaId":"h:1628378","historical":true,"tier":"modern-all-star","era":"2023-24"},{"name":"Adrian Dantley","cname":"阿德里安-丹特利","pos":"PF / PG","ovr":88,"threePT":72,"MID":97,"FIN":92,"DNK":87,"HAN":81,"PAS":81,"PDEF":74,"IDEF":68,"BLK":68,"REB":78,"ATH":92,"STR":92,"CLU":83,"nbaId":"h:76504","historical":true,"tier":"hall-of-fame","era":"1957-58"},{"name":"Karl Malone","cname":"卡尔-马龙","pos":"C","ovr":96,"threePT":80,"MID":99,"FIN":99,"DNK":90,"HAN":78,"PAS":78,"PDEF":63,"IDEF":85,"BLK":85,"REB":96,"ATH":95,"STR":99,"CLU":91,"nbaId":"h:252","historical":true,"tier":"hall-of-fame","era":"1957-58"},{"name":"Rudy Gobert","cname":"鲁迪-戈贝尔","pos":"SF","ovr":85,"threePT":65,"MID":83,"FIN":88,"DNK":75,"HAN":67,"PAS":67,"PDEF":63,"IDEF":90,"BLK":90,"REB":97,"ATH":80,"STR":96,"CLU":80,"nbaId":"h:203497","historical":true,"tier":"modern-all-star","era":"2019-20"},{"name":"Stephen Curry","cname":"斯蒂芬-库里","pos":"SG / PG","ovr":98,"threePT":99,"MID":97,"FIN":95,"DNK":94,"HAN":94,"PAS":94,"PDEF":91,"IDEF":67,"BLK":67,"REB":66,"ATH":99,"STR":89,"CLU":93,"nbaId":"h:201939","historical":true,"tier":"modern-all-star","era":"1957-58"},{"name":"Klay Thompson","cname":"克莱-汤普森","pos":"SF / PF","ovr":90,"threePT":98,"MID":88,"FIN":94,"DNK":92,"HAN":70,"PAS":70,"PDEF":81,"IDEF":71,"BLK":71,"REB":76,"ATH":97,"STR":91,"CLU":85,"nbaId":"h:202691","historical":true,"tier":"modern-all-star","era":"1957-58"},{"name":"Draymond Green","cname":"德雷蒙德-格林","pos":"C","ovr":88,"threePT":80,"MID":84,"FIN":90,"DNK":81,"HAN":85,"PAS":85,"PDEF":82,"IDEF":84,"BLK":84,"REB":84,"ATH":86,"STR":94,"CLU":83,"nbaId":"h:203110","historical":true,"tier":"modern-all-star","era":"2016-17"},{"name":"Chris Paul","cname":"克里斯-保罗","pos":"SG / PG","ovr":95,"threePT":83,"MID":97,"FIN":96,"DNK":94,"HAN":96,"PAS":96,"PDEF":98,"IDEF":60,"BLK":60,"REB":65,"ATH":99,"STR":90,"CLU":90,"nbaId":"h:101108","historical":true,"tier":"modern-all-star","era":"1957-58"},{"name":"Blake Griffin","cname":"布雷克-格里芬","pos":"C","ovr":90,"threePT":77,"MID":96,"FIN":99,"DNK":90,"HAN":71,"PAS":71,"PDEF":69,"IDEF":78,"BLK":78,"REB":88,"ATH":95,"STR":99,"CLU":85,"nbaId":"h:201933","historical":true,"tier":"modern-all-star","era":"1957-58"},{"name":"Bob McAdoo","cname":"鲍勃-麦卡杜","pos":"SF","ovr":93,"threePT":55,"MID":97,"FIN":98,"DNK":85,"HAN":66,"PAS":66,"PDEF":68,"IDEF":94,"BLK":94,"REB":97,"ATH":90,"STR":99,"CLU":88,"nbaId":"h:77498","historical":true,"tier":"hall-of-fame","era":"1957-58"},{"name":"Earvin Johnson","cname":"埃尔文-约翰逊","pos":"SG / SF","ovr":98,"threePT":81,"MID":95,"FIN":95,"DNK":94,"HAN":96,"PAS":96,"PDEF":99,"IDEF":78,"BLK":78,"REB":82,"ATH":99,"STR":89,"CLU":93,"nbaId":"h:77142","historical":true,"tier":"hall-of-fame","era":"1957-58"},{"name":"Kobe Bryant","cname":"科比-布莱恩特","pos":"SF / PF","ovr":98,"threePT":89,"MID":96,"FIN":97,"DNK":94,"HAN":87,"PAS":87,"PDEF":96,"IDEF":77,"BLK":77,"REB":78,"ATH":99,"STR":94,"CLU":93,"nbaId":"h:977","historical":true,"tier":"hall-of-fame","era":"1957-58"},{"name":"Anthony Davis","cname":"安东尼-戴维斯","pos":"C","ovr":91,"threePT":76,"MID":94,"FIN":98,"DNK":85,"HAN":66,"PAS":66,"PDEF":76,"IDEF":88,"BLK":88,"REB":87,"ATH":90,"STR":99,"CLU":86,"nbaId":"h:203076","historical":true,"tier":"modern-all-star","era":"1957-58"},{"name":"Steve Nash","cname":"史蒂夫-纳什","pos":"SG / PG","ovr":95,"threePT":94,"MID":97,"FIN":88,"DNK":89,"HAN":99,"PAS":99,"PDEF":91,"IDEF":59,"BLK":59,"REB":64,"ATH":94,"STR":82,"CLU":90,"nbaId":"h:959","historical":true,"tier":"hall-of-fame","era":"1957-58"},{"name":"Devin Booker","cname":"德文-布克","pos":"SF / SG","ovr":88,"threePT":88,"MID":92,"FIN":94,"DNK":92,"HAN":83,"PAS":83,"PDEF":79,"IDEF":64,"BLK":64,"REB":67,"ATH":97,"STR":91,"CLU":83,"nbaId":"h:1626164","historical":true,"tier":"modern-all-star","era":"1957-58"},{"name":"Shawn Marion","cname":"肖恩-马里昂","pos":"PF / C","ovr":90,"threePT":79,"MID":95,"FIN":95,"DNK":90,"HAN":62,"PAS":62,"PDEF":87,"IDEF":75,"BLK":75,"REB":89,"ATH":95,"STR":95,"CLU":85,"nbaId":"h:1890","historical":true,"tier":"modern-all-star","era":"1957-58"},{"name":"Amar'e Stoudemire","cname":"阿玛雷-斯塔德迈尔","pos":"SF","ovr":92,"threePT":75,"MID":96,"FIN":99,"DNK":90,"HAN":63,"PAS":63,"PDEF":77,"IDEF":87,"BLK":87,"REB":89,"ATH":95,"STR":99,"CLU":87,"nbaId":"h:2405","historical":true,"tier":"hall-of-fame","era":"1957-58"},{"name":"Mitch Richmond","cname":"米奇-里奇蒙德","pos":"SF / PF","ovr":91,"threePT":92,"MID":94,"FIN":95,"DNK":93,"HAN":84,"PAS":84,"PDEF":78,"IDEF":68,"BLK":68,"REB":69,"ATH":98,"STR":92,"CLU":86,"nbaId":"h:782","historical":true,"tier":"hall-of-fame","era":"1957-58"},{"name":"Peja Stojaković","cname":"佩贾-斯托亚科维奇","pos":"PF / SF","ovr":80,"threePT":95,"MID":79,"FIN":83,"DNK":78,"HAN":64,"PAS":64,"PDEF":78,"IDEF":57,"BLK":57,"REB":77,"ATH":83,"STR":83,"CLU":75,"nbaId":"h:978","historical":true,"tier":"modern-all-star","era":"2003-04"},{"name":"Chris Webber","cname":"克里斯-韦伯","pos":"C / PG","ovr":82,"threePT":55,"MID":88,"FIN":86,"DNK":77,"HAN":76,"PAS":76,"PDEF":78,"IDEF":80,"BLK":80,"REB":85,"ATH":82,"STR":90,"CLU":77,"nbaId":"h:185","historical":true,"tier":"hall-of-fame","era":"2003-04"},{"name":"DeMarcus Cousins","cname":"德马库斯-考辛斯","pos":"SF","ovr":90,"threePT":80,"MID":95,"FIN":94,"DNK":81,"HAN":68,"PAS":68,"PDEF":66,"IDEF":80,"BLK":80,"REB":89,"ATH":86,"STR":99,"CLU":85,"nbaId":"h:202326","historical":true,"tier":"modern-all-star","era":"1957-58"},{"name":"Luka Dončić","cname":"卢卡-东契奇","pos":"SG / SF","ovr":92,"threePT":88,"MID":92,"FIN":89,"DNK":90,"HAN":93,"PAS":93,"PDEF":78,"IDEF":61,"BLK":61,"REB":85,"ATH":95,"STR":83,"CLU":87,"nbaId":"h:1629029","historical":true,"tier":"modern-all-star","era":"2022-23"},{"name":"Rolando Blackman","cname":"罗兰多-布莱克曼","pos":"SF / PG","ovr":85,"threePT":83,"MID":96,"FIN":90,"DNK":88,"HAN":79,"PAS":79,"PDEF":70,"IDEF":71,"BLK":71,"REB":70,"ATH":93,"STR":87,"CLU":80,"nbaId":"h:76176","historical":true,"tier":"modern-all-star","era":"1957-58"},{"name":"Mark Aguirre","cname":"马克-阿奎尔","pos":"PF / C","ovr":86,"threePT":77,"MID":90,"FIN":90,"DNK":85,"HAN":74,"PAS":74,"PDEF":80,"IDEF":73,"BLK":73,"REB":74,"ATH":90,"STR":90,"CLU":81,"nbaId":"h:76016","historical":true,"tier":"modern-all-star","era":"1957-58"},{"name":"Dirk Nowitzki","cname":"德克-诺维斯基","pos":"C","ovr":96,"threePT":96,"MID":98,"FIN":98,"DNK":89,"HAN":74,"PAS":74,"PDEF":70,"IDEF":78,"BLK":78,"REB":91,"ATH":94,"STR":99,"CLU":91,"nbaId":"h:1717","historical":true,"tier":"hall-of-fame","era":"1957-58"},{"name":"Tyson Chandler","cname":"泰森-钱德勒","pos":"SF","ovr":83,"threePT":56,"MID":86,"FIN":99,"DNK":86,"HAN":64,"PAS":64,"PDEF":68,"IDEF":82,"BLK":82,"REB":93,"ATH":91,"STR":99,"CLU":78,"nbaId":"h:2199","historical":true,"tier":"modern-all-star","era":"1957-58"},{"name":"Calvin Murphy","cname":"卡尔文-墨菲","pos":"SG / SF","ovr":90,"threePT":79,"MID":93,"FIN":96,"DNK":94,"HAN":92,"PAS":92,"PDEF":80,"IDEF":65,"BLK":65,"REB":65,"ATH":99,"STR":90,"CLU":85,"nbaId":"h:77669","historical":true,"tier":"hall-of-fame","era":"1957-58"},{"name":"James Harden","cname":"詹姆斯-哈登","pos":"SF / SG","ovr":95,"threePT":91,"MID":98,"FIN":95,"DNK":93,"HAN":90,"PAS":90,"PDEF":83,"IDEF":66,"BLK":66,"REB":86,"ATH":98,"STR":92,"CLU":90,"nbaId":"h:201935","historical":true,"tier":"modern-all-star","era":"1957-58"},{"name":"Hakeem Olajuwon","cname":"哈基姆-奥拉朱旺","pos":"SF","ovr":98,"threePT":71,"MID":99,"FIN":99,"DNK":86,"HAN":67,"PAS":67,"PDEF":75,"IDEF":98,"BLK":98,"REB":99,"ATH":91,"STR":99,"CLU":93,"nbaId":"h:165","historical":true,"tier":"hall-of-fame","era":"1957-58"},{"name":"Mike Conley","cname":"迈克-康利","pos":"SG / PG","ovr":86,"threePT":86,"MID":87,"FIN":88,"DNK":89,"HAN":87,"PAS":87,"PDEF":78,"IDEF":64,"BLK":64,"REB":66,"ATH":94,"STR":82,"CLU":81,"nbaId":"h:201144","historical":true,"tier":"modern-all-star","era":"1957-58"},{"name":"Tony Allen","cname":"托尼-阿伦","pos":"SF / PF","ovr":80,"threePT":72,"MID":80,"FIN":96,"DNK":94,"HAN":59,"PAS":59,"PDEF":95,"IDEF":66,"BLK":66,"REB":76,"ATH":99,"STR":93,"CLU":75,"nbaId":"h:2754","historical":true,"tier":"modern-all-star","era":"1957-58"},{"name":"Rudy Gay","cname":"鲁迪-盖伊","pos":"PF / PG","ovr":81,"threePT":81,"MID":82,"FIN":89,"DNK":84,"HAN":71,"PAS":71,"PDEF":68,"IDEF":73,"BLK":73,"REB":70,"ATH":89,"STR":89,"CLU":76,"nbaId":"h:200752","historical":true,"tier":"modern-all-star","era":"1957-58"},{"name":"Zach Randolph","cname":"扎克-兰多夫","pos":"C / PG","ovr":88,"threePT":74,"MID":97,"FIN":97,"DNK":88,"HAN":56,"PAS":56,"PDEF":70,"IDEF":77,"BLK":77,"REB":90,"ATH":93,"STR":99,"CLU":83,"nbaId":"h:2216","historical":true,"tier":"modern-all-star","era":"1957-58"},{"name":"Jrue Holiday","cname":"朱-霍乐迪","pos":"SF","ovr":87,"threePT":85,"MID":82,"FIN":84,"DNK":85,"HAN":83,"PAS":83,"PDEF":89,"IDEF":78,"BLK":78,"REB":73,"ATH":90,"STR":78,"CLU":82,"nbaId":"h:201950","historical":true,"tier":"modern-all-star","era":"2022-23"},{"name":"Brandon Ingram","cname":"布兰登-英格拉姆","pos":"PF / SF","ovr":84,"threePT":86,"MID":87,"FIN":89,"DNK":84,"HAN":83,"PAS":83,"PDEF":62,"IDEF":70,"BLK":70,"REB":78,"ATH":89,"STR":89,"CLU":79,"nbaId":"h:1627742","historical":true,"tier":"modern-all-star","era":"2021-22"},{"name":"Zion Williamson","cname":"蔡恩-威廉森","pos":"C / PG","ovr":86,"threePT":70,"MID":95,"FIN":93,"DNK":84,"HAN":72,"PAS":72,"PDEF":76,"IDEF":79,"BLK":79,"REB":83,"ATH":89,"STR":97,"CLU":81,"nbaId":"h:1629627","historical":true,"tier":"modern-all-star","era":"1957-58"},{"name":"Tony Parker","cname":"托尼-帕克","pos":"SG / PG","ovr":90,"threePT":89,"MID":97,"FIN":90,"DNK":91,"HAN":92,"PAS":92,"PDEF":85,"IDEF":55,"BLK":55,"REB":59,"ATH":96,"STR":84,"CLU":85,"nbaId":"h:2225","historical":true,"tier":"hall-of-fame","era":"1957-58"},{"name":"George Gervin","cname":"乔治-格文","pos":"SF","ovr":93,"threePT":79,"MID":99,"FIN":99,"DNK":94,"HAN":75,"PAS":75,"PDEF":81,"IDEF":76,"BLK":76,"REB":86,"ATH":99,"STR":99,"CLU":88,"nbaId":"h:76804","historical":true,"tier":"hall-of-fame","era":"1957-58"},{"name":"Tim Duncan","cname":"蒂姆-邓肯","pos":"C","ovr":98,"threePT":75,"MID":99,"FIN":99,"DNK":90,"HAN":65,"PAS":65,"PDEF":79,"IDEF":98,"BLK":98,"REB":97,"ATH":95,"STR":99,"CLU":93,"nbaId":"h:1495","historical":true,"tier":"hall-of-fame","era":"1957-58"},{"name":"David Robinson","cname":"大卫-罗宾逊","pos":"SF","ovr":95,"threePT":71,"MID":96,"FIN":98,"DNK":85,"HAN":63,"PAS":63,"PDEF":74,"IDEF":93,"BLK":93,"REB":94,"ATH":90,"STR":99,"CLU":90,"nbaId":"h:764","historical":true,"tier":"hall-of-fame","era":"1957-58"}];

  // ===== data.js (deps: none) =====
  (function () {
// ================= 完美球员 · 篮球生涯模拟器 =================

const APP_TITLE = '完美球员';
const TAGLINE = '从青训到传奇，每个决定都算数';

const MODES = {
  quick:      { label: '速通', periodLength: 3, hint: '每三个赛季一次决策，十分钟一生' },
  standard:   { label: '标准', periodLength: 2, hint: '每两个赛季一次决策，推荐', recommended: true },
  immersive:  { label: '沉浸', periodLength: 1, hint: '每个赛季一次决策，最完整的一生' },
};

const POSITIONS = {
  pg: { zh: '控球后卫', en: 'PG', weight: { pts: 0.75, reb: 0.42, ast: 1.30, stl: 1.10, blk: 0.40 }, hint: '组织全队，助攻权重最高' },
  sg: { zh: '得分后卫', en: 'SG', weight: { pts: 1.30, reb: 0.48, ast: 0.90, stl: 0.95, blk: 0.40 }, hint: '外线尖刀，得分权重最高' },
  sf: { zh: '小前锋', en: 'SF', weight: { pts: 1.10, reb: 0.80, ast: 0.80, stl: 0.80, blk: 0.70 }, hint: '全能侧翼，各项均衡' },
  pf: { zh: '大前锋', en: 'PF', weight: { pts: 0.90, reb: 1.25, ast: 0.60, stl: 0.70, blk: 1.10 }, hint: '内线肉搏，篮板盖帽权重高' },
  c:  { zh: '中锋', en: 'C',  weight: { pts: 0.95, reb: 1.40, ast: 0.50, stl: 0.50, blk: 1.40 }, hint: '禁区守护神，篮板盖帽权重最高' },
};

// 国家队：tier 1 顶级 / 2 中上 / 3 亚洲中等 / 4 弱旅
const COUNTRIES = {
  US: { zh: '美国', continent: 'americas', tier: 1, qualify: 0.98, league: 'nba', surnames: ['约翰逊','威廉姆斯','布朗','琼斯','戴维斯','威尔逊','泰勒','米勒'], flag: '🇺🇸' },
  ES: { zh: '西班牙', continent: 'europe', tier: 1, qualify: 0.92, league: 'acb', surnames: ['加西亚','费尔南德斯','洛佩斯','罗德里格斯','马丁内斯','桑切斯'], flag: '🇪🇸' },
  RS: { zh: '塞尔维亚', continent: 'europe', tier: 1, qualify: 0.90, league: 'eur', surnames: ['约维奇','彼得罗维奇','尼科利奇','马尔科维奇','斯托亚诺维奇'], flag: '🇷🇸' },
  AR: { zh: '阿根廷', continent: 'americas', tier: 1, qualify: 0.88, league: 'lna', surnames: ['冈萨雷斯','罗德里格斯','迪亚斯','洛佩斯','马丁内斯'], flag: '🇦🇷' },
  FR: { zh: '法国', continent: 'europe', tier: 1, qualify: 0.90, league: 'fra', surnames: ['杜邦','勒鲁瓦','莫罗','福尔尼耶','拉科姆'], flag: '🇫🇷' },
  SI: { zh: '斯洛文尼亚', continent: 'europe', tier: 1, qualify: 0.82, league: 'eur', surnames: ['霍瓦特','诺瓦克','维德马尔','赞帕','科瓦奇'], flag: '🇸🇮' },
  AU: { zh: '澳大利亚', continent: 'oceania', tier: 1, qualify: 0.90, league: 'nbl', surnames: ['安德森','约翰逊','格林','米切尔','威尔逊'], flag: '🇦🇺' },
  GR: { zh: '希腊', continent: 'europe', tier: 1, qualify: 0.85, league: 'gre', surnames: ['帕帕斯','帕帕多普洛斯','扬诺普洛斯','科斯塔斯'], flag: '🇬🇷' },
  CA: { zh: '加拿大', continent: 'americas', tier: 1, qualify: 0.88, league: 'nba', surnames: ['布朗','史密斯','汤普森','威尔逊','泰勒'], flag: '🇨🇦' },
  DE: { zh: '德国', continent: 'europe', tier: 2, qualify: 0.84, league: 'ger', surnames: ['穆勒','施密特','施奈德','瓦格纳','霍夫曼'], flag: '🇩🇪' },
  LT: { zh: '立陶宛', continent: 'europe', tier: 2, qualify: 0.82, league: 'eur', surnames: ['萨博尼斯','扬库纳斯','瓦兰丘纳斯','库兹明斯卡斯'], flag: '🇱🇹' },
  IT: { zh: '意大利', continent: 'europe', tier: 2, qualify: 0.82, league: 'ita', surnames: ['罗西','费拉里','科隆博','里奇','曼奇尼'], flag: '🇮🇹' },
  TR: { zh: '土耳其', continent: 'europe', tier: 2, qualify: 0.80, league: 'tur', surnames: ['耶尔马兹','德米尔','阿卡','切利克'], flag: '🇹🇷' },
  BR: { zh: '巴西', continent: 'americas', tier: 2, qualify: 0.78, league: 'lna', surnames: ['席尔瓦','桑托斯','奥利维拉','佩雷拉','科斯塔'], flag: '🇧🇷' },
  NG: { zh: '尼日利亚', continent: 'africa', tier: 2, qualify: 0.76, league: 'eur', surnames: ['奥科耶','阿德巴约','奥孔沃','埃梅卡'], flag: '🇳🇬' },
  LV: { zh: '拉脱维亚', continent: 'europe', tier: 2, qualify: 0.78, league: 'eur', surnames: ['贝尔津什','奥佐利涅','卡林斯'], flag: '🇱🇻' },
  HR: { zh: '克罗地亚', continent: 'europe', tier: 2, qualify: 0.78, league: 'eur', surnames: ['霍瓦特','科瓦奇','巴比奇','武科维奇'], flag: '🇭🇷' },
  PR: { zh: '波多黎各', continent: 'americas', tier: 2, qualify: 0.74, league: 'lna', surnames: ['里维拉','科隆','托雷斯','马丁内斯'], flag: '🇵🇷' },
  DO: { zh: '多米尼加', continent: 'americas', tier: 2, qualify: 0.70, league: 'lna', surnames: ['佩雷斯','马丁内斯','桑切斯','加西亚'], flag: '🇩🇴' },
  CZ: { zh: '捷克', continent: 'europe', tier: 3, qualify: 0.66, league: 'eur', surnames: ['诺瓦克','斯沃博达','科瓦奇','多莱扎尔'], flag: '🇨🇿' },
  PL: { zh: '波兰', continent: 'europe', tier: 3, qualify: 0.62, league: 'eur', surnames: ['科瓦尔斯基','诺瓦克','马祖尔','卡明斯基'], flag: '🇵🇱' },
  FI: { zh: '芬兰', continent: 'europe', tier: 3, qualify: 0.58, league: 'eur', surnames: ['科尔霍宁','萨林','莱赫托宁','马基宁'], flag: '🇫🇮' },
  CN: { zh: '中国', continent: 'asia', tier: 2, qualify: 0.55, league: 'cba', surnames: ['张','王','李','刘','陈','杨','赵','周','吴','徐','孙','胡'], flag: '🇨🇳' },
  JP: { zh: '日本', continent: 'asia', tier: 2, qualify: 0.58, league: 'jp', surnames: ['佐藤','铃木','高桥','田中','渡边','伊藤'], flag: '🇯🇵' },
  KR: { zh: '韩国', continent: 'asia', tier: 3, qualify: 0.45, league: 'kr', surnames: ['金','李','朴','崔','郑'], flag: '🇰🇷' },
  PH: { zh: '菲律宾', continent: 'asia', tier: 3, qualify: 0.40, league: 'pba', surnames: ['桑托斯','阿基诺','拉莫斯','加西亚'], flag: '🇵🇭' },
  IR: { zh: '伊朗', continent: 'asia', tier: 3, qualify: 0.35, league: 'asia', surnames: ['哈桑','侯赛尼','卡里米','贾法里'], flag: '🇮🇷' },
  NZ: { zh: '新西兰', continent: 'oceania', tier: 3, qualify: 0.66, league: 'nbl', surnames: ['史密斯','威廉姆斯','沃克','泰勒'], flag: '🇳🇿' },
  AO: { zh: '安哥拉', continent: 'africa', tier: 3, qualify: 0.48, league: 'eur', surnames: ['恩多','卡瓦略','费尔南德斯','戈麦斯'], flag: '🇦🇴' },
  CV: { zh: '佛得角', continent: 'africa', tier: 3, qualify: 0.55, league: 'eur', surnames: ['洛佩斯','席尔瓦','门德斯','科雷亚'], flag: '🇨🇻' },
};

// 联赛：tier 1 最强
const LEAGUES = {
  nba:  { zh: 'NBA', country: 'US', tier: 1, games: 82, champ: 'NBA总冠军', cup: null, cupName: null },
  eur:  { zh: '欧洲篮球联赛', country: 'EU', tier: 2, games: 60, champ: '欧洲篮球联赛冠军', cup: '洲际杯', cupName: '洲际俱乐部杯' },
  cba:  { zh: 'CBA', country: 'CN', tier: 3, games: 52, champ: 'CBA总冠军', cup: '全明星赛', cupName: '全明星MVP' },
  acb:  { zh: '西甲篮球联赛', country: 'ES', tier: 3, games: 44, champ: 'ACB联赛冠军', cup: '国王杯', cupName: '西班牙国王杯' },
  tur:  { zh: '土耳其篮球联赛', country: 'TR', tier: 3, games: 40, champ: '土耳其联赛冠军', cup: '土耳其杯', cupName: '土耳其杯' },
  gre:  { zh: '希腊篮球联赛', country: 'GR', tier: 3, games: 38, champ: '希腊联赛冠军', cup: '希腊杯', cupName: '希腊杯' },
  ita:  { zh: '意大利篮球联赛', country: 'IT', tier: 3, games: 40, champ: '意大利联赛冠军', cup: '意大利杯', cupName: '意大利杯' },
  ger:  { zh: '德国篮球联赛', country: 'DE', tier: 4, games: 40, champ: '德国联赛冠军', cup: '德国杯', cupName: '德国杯' },
  fra:  { zh: '法国篮球联赛', country: 'FR', tier: 4, games: 40, champ: '法国联赛冠军', cup: '法国杯', cupName: '法国杯' },
  nbl:  { zh: '澳大利亚NBL', country: 'AU', tier: 4, games: 36, champ: 'NBL总冠军', cup: null, cupName: null },
  jp:   { zh: '日本B联赛', country: 'JP', tier: 4, games: 50, champ: 'B联赛总冠军', cup: '天皇杯', cupName: '天皇杯' },
  kr:   { zh: '韩国KBL', country: 'KR', tier: 4, games: 44, champ: 'KBL总冠军', cup: 'KBL杯', cupName: 'KBL杯' },
  lna:  { zh: '美洲篮球联赛', country: 'AR', tier: 4, games: 42, champ: '美洲联赛冠军', cup: null, cupName: null },
  pba:  { zh: '菲律宾PBA', country: 'PH', tier: 4, games: 48, champ: 'PBA总冠军', cup: null, cupName: null },
  asia: { zh: '亚洲篮球联赛', country: 'IR', tier: 4, games: 40, champ: '亚洲联赛冠军', cup: null, cupName: null },
  ncaa: { zh: 'NCAA', country: 'US', tier: 2, games: 38, champ: 'NCAA总冠军', cup: null, cupName: null },
};

// 球队：strength 60-95，color 主色，abbr 缩写
const TEAMS = {
  // NBA
  lal: { zh: '洛杉矶湖人', abbr: 'LAL', color: '#552583', league: 'nba', strength: 88 },
  bos: { zh: '波士顿凯尔特人', abbr: 'BOS', color: '#007A33', league: 'nba', strength: 90 },
  gsw: { zh: '金州勇士', abbr: 'GSW', color: '#1D428A', league: 'nba', strength: 86 },
  chi: { zh: '芝加哥公牛', abbr: 'CHI', color: '#CE1141', league: 'nba', strength: 80 },
  mia: { zh: '迈阿密热火', abbr: 'MIA', color: '#98002E', league: 'nba', strength: 82 },
  den: { zh: '丹佛掘金', abbr: 'DEN', color: '#0E2240', league: 'nba', strength: 87 },
  mil: { zh: '密尔沃基雄鹿', abbr: 'MIL', color: '#00471B', league: 'nba', strength: 85 },
  okc: { zh: '俄克拉荷马城雷霆', abbr: 'OKC', color: '#007AC1', league: 'nba', strength: 86 },
  dal: { zh: '达拉斯独行侠', abbr: 'DAL', color: '#00538C', league: 'nba', strength: 84 },
  pho: { zh: '菲尼克斯太阳', abbr: 'PHX', color: '#E56020', league: 'nba', strength: 81 },
  phi: { zh: '费城76人', abbr: 'PHI', color: '#006BB6', league: 'nba', strength: 83 },
  nyk: { zh: '纽约尼克斯', abbr: 'NYK', color: '#F58426', league: 'nba', strength: 82 },
  lac: { zh: '洛杉矶快船', abbr: 'LAC', color: '#C8102E', league: 'nba', strength: 83 },
  cle: { zh: '克利夫兰骑士', abbr: 'CLE', color: '#860038', league: 'nba', strength: 82 },
  sas: { zh: '圣安东尼奥马刺', abbr: 'SAS', color: '#C4CED4', league: 'nba', strength: 81 },
  hou: { zh: '休斯顿火箭', abbr: 'HOU', color: '#CE1141', league: 'nba', strength: 80 },
  // 欧洲篮球联赛
  pan: { zh: '帕纳辛奈科斯', abbr: 'PAO', color: '#046A38', league: 'eur', strength: 88 },
  rma: { zh: '皇家马德里', abbr: 'RMB', color: '#FEBE10', league: 'eur', strength: 90 },
  bar: { zh: '巴塞罗那', abbr: 'BAR', color: '#A50044', league: 'eur', strength: 87 },
  fen: { zh: '费内巴切', abbr: 'FEN', color: '#004A8F', league: 'eur', strength: 85 },
  oly: { zh: '奥林匹亚科斯', abbr: 'OLY', color: '#E30613', league: 'eur', strength: 86 },
  mon: { zh: '摩纳哥', abbr: 'MON', color: '#E4002B', league: 'eur', strength: 84 },
  zve: { zh: '贝尔格莱德红星', abbr: 'ZVE', color: '#E4002B', league: 'eur', strength: 84 },
  mta: { zh: '特拉维夫马卡比', abbr: 'MTA', color: '#FFD500', league: 'eur', strength: 82 },
  efs: { zh: '阿纳多卢艾菲斯', abbr: 'EFS', color: '#1B3C6E', league: 'eur', strength: 83 },
  bay: { zh: '拜仁慕尼黑', abbr: 'BAY', color: '#DC052D', league: 'eur', strength: 81 },
  // CBA
  lia: { zh: '辽宁本钢', abbr: '辽宁', color: '#D40000', league: 'cba', strength: 86 },
  xin: { zh: '新疆广汇', abbr: '新疆', color: '#1D4F91', league: 'cba', strength: 82 },
  zhe: { zh: '浙江稠州金租', abbr: '浙江', color: '#C8102E', league: 'cba', strength: 82 },
  gdd: { zh: '广东宏远', abbr: '广东', color: '#005BAC', league: 'cba', strength: 84 },
  sha: { zh: '上海久事', abbr: '上海', color: '#B01E23', league: 'cba', strength: 79 },
  bjg: { zh: '北京首钢', abbr: '北京', color: '#3B5BA5', league: 'cba', strength: 80 },
  shx: { zh: '山西汾酒', abbr: '山西', color: '#F0A500', league: 'cba', strength: 78 },
  szm: { zh: '深圳马可波罗', abbr: '深圳', color: '#1C449B', league: 'cba', strength: 77 },
  gzl: { zh: '广州龙狮', abbr: '广州', color: '#D40000', league: 'cba', strength: 76 },
  zjg: { zh: '浙江广厦', abbr: '广厦', color: '#0E6F3C', league: 'cba', strength: 81 },
  qdg: { zh: '青岛国信', abbr: '青岛', color: '#1A3E8C', league: 'cba', strength: 75 },
  njt: { zh: '南京同曦', abbr: '南京', color: '#005BAC', league: 'cba', strength: 74 },
  // 西甲篮球
  bkn: { zh: '巴斯克尼亚', abbr: 'BKN', color: '#C8102E', league: 'acb', strength: 82 },
  vlc: { zh: '瓦伦西亚', abbr: 'VLC', color: '#F28E00', league: 'acb', strength: 79 },
  mlg: { zh: '马拉加', abbr: 'MLG', color: '#00A0DF', league: 'acb', strength: 77 },
  juv: { zh: '巴达洛纳尤文图特', abbr: 'JOV', color: '#1B2A4A', league: 'acb', strength: 76 },
  gcn: { zh: '大加纳利', abbr: 'GCN', color: '#FFD700', league: 'acb', strength: 75 },
  tfe: { zh: '特内里费', abbr: 'TFE', color: '#00247D', league: 'acb', strength: 75 },
  bil: { zh: '毕尔巴鄂', abbr: 'BIL', color: '#EE1C25', league: 'acb', strength: 73 },
  mrs: { zh: '曼雷萨', abbr: 'MRS', color: '#1A1A1A', league: 'acb', strength: 72 },
  // 土耳其
  gst: { zh: '加拉塔萨雷', abbr: 'GST', color: '#A32638', league: 'tur', strength: 78 },
  bjk: { zh: '贝西克塔斯', abbr: 'BJK', color: '#000000', league: 'tur', strength: 77 },
  bsa: { zh: '布尔萨体育', abbr: 'BSA', color: '#009E60', league: 'tur', strength: 72 },
  dfs: { zh: '达鲁沙法卡', abbr: 'DFS', color: '#1B3C6E', league: 'tur', strength: 72 },
  bsk: { zh: '巴赫切谢希尔', abbr: 'BSK', color: '#E4002B', league: 'tur', strength: 71 },
  tlt: { zh: '土耳其电信', abbr: 'TLT', color: '#005B9F', league: 'tur', strength: 70 },
  ptn: { zh: '佩特金', abbr: 'PTN', color: '#5B2D8E', league: 'tur', strength: 69 },
  mzf: { zh: '梅克泽芬迪', abbr: 'MZF', color: '#1A1A1A', league: 'tur', strength: 68 },
  // 希腊
  ars: { zh: '塞萨洛尼基阿里斯', abbr: 'ARS', color: '#FFD100', league: 'gre', strength: 75 },
  per: { zh: '佩里斯特里', abbr: 'PER', color: '#003DA5', league: 'gre', strength: 73 },
  lav: { zh: '拉夫里奥', abbr: 'LAV', color: '#C8102E', league: 'gre', strength: 71 },
  pro: { zh: '普罗米修斯', abbr: 'PRO', color: '#00843D', league: 'gre', strength: 72 },
  kol: { zh: '科洛索斯', abbr: 'KOL', color: '#1B2A4A', league: 'gre', strength: 69 },
  apo: { zh: '阿波罗帕特雷', abbr: 'APO', color: '#1A1A1A', league: 'gre', strength: 68 },
  // 意大利
  mila: { zh: '米兰奥林匹亚', abbr: 'MIL', color: '#C8102E', league: 'ita', strength: 82 },
  vir: { zh: '博洛尼亚维尔图斯', abbr: 'VIR', color: '#000000', league: 'ita', strength: 80 },
  bre: { zh: '布雷西亚', abbr: 'BRE', color: '#005BAC', league: 'ita', strength: 74 },
  ven: { zh: '威尼斯', abbr: 'VEN', color: '#FFA000', league: 'ita', strength: 73 },
  sas_ita: { zh: '萨萨里', abbr: 'SSR', color: '#1B3C6E', league: 'ita', strength: 72 },
  tre: { zh: '特雷维索', abbr: 'TRE', color: '#009E60', league: 'ita', strength: 71 },
  // 德国
  alb: { zh: '阿尔巴柏林', abbr: 'ALB', color: '#FFC70C', league: 'ger', strength: 79 },
  bon: { zh: '波恩', abbr: 'BON', color: '#8A2BE2', league: 'ger', strength: 76 },
  hbt: { zh: '汉堡塔', abbr: 'HBT', color: '#003DA5', league: 'ger', strength: 73 },
  ulm: { zh: '乌尔姆', abbr: 'ULM', color: '#000000', league: 'ger', strength: 72 },
  hdb: { zh: '海德堡', abbr: 'HDB', color: '#C8102E', league: 'ger', strength: 70 },
  krs: { zh: '克赖尔斯海姆', abbr: 'KRS', color: '#009E60', league: 'ger', strength: 69 },
  // 法国
  par: { zh: '巴黎篮球', abbr: 'PAR', color: '#0055A4', league: 'fra', strength: 80 },
  asv: { zh: '阿斯维尔', abbr: 'ASV', color: '#EF4135', league: 'fra', strength: 78 },
  dij: { zh: '第戎', abbr: 'DIJ', color: '#1A1A1A', league: 'fra', strength: 72 },
  lem: { zh: '勒芒', abbr: 'LEM', color: '#C8102E', league: 'fra', strength: 71 },
  ntr: { zh: '楠泰尔', abbr: 'NTR', color: '#00629B', league: 'fra', strength: 70 },
  brg: { zh: '布尔格', abbr: 'BRG', color: '#00843D', league: 'fra', strength: 69 },
  // 澳洲NBL
  syk: { zh: '悉尼国王', abbr: 'SYK', color: '#005BAC', league: 'nbl', strength: 80 },
  mel: { zh: '墨尔本联', abbr: 'MEL', color: '#1A1A1A', league: 'nbl', strength: 79 },
  perw: { zh: '珀斯野猫', abbr: 'PER', color: '#FF7F00', league: 'nbl', strength: 77 },
  adl: { zh: '阿德莱德36人', abbr: 'ADL', color: '#00843D', league: 'nbl', strength: 73 },
  bri: { zh: '布里斯班子弹', abbr: 'BRI', color: '#7C3AED', league: 'nbl', strength: 72 },
  nzb: { zh: '新西兰破坏者', abbr: 'NZB', color: '#1B2A4A', league: 'nbl', strength: 74 },
  // 日本B联赛
  uts: { zh: '宇都宫皇者', abbr: 'UTS', color: '#C8102E', league: 'jp', strength: 80 },
  cjb: { zh: '千叶喷射机', abbr: 'CJB', color: '#0072CE', league: 'jp', strength: 81 },
  ryu: { zh: '琉球黄金国王', abbr: 'RYU', color: '#FFB800', league: 'jp', strength: 79 },
  tke: { zh: '东京电击', abbr: 'TKE', color: '#1B2A4A', league: 'jp', strength: 78 },
  shm: { zh: '岛根魔术', abbr: 'SHM', color: '#00A0DF', league: 'jp', strength: 76 },
  kwa: { zh: '川崎勇者雷霆', abbr: 'KWA', color: '#005BAC', league: 'jp', strength: 75 },
  // 韩国KBL
  ssk: { zh: '首尔SK骑士', abbr: 'SSK', color: '#C8102E', league: 'kr', strength: 79 },
  any: { zh: '安养正官庄', abbr: 'ANY', color: '#00843D', league: 'kr', strength: 77 },
  wdb: { zh: '原州DB', abbr: 'WDB', color: '#003DA5', league: 'kr', strength: 75 },
  ush: { zh: '蔚山现代摩比斯', abbr: 'USH', color: '#005BAC', league: 'kr', strength: 76 },
  bsk_kr: { zh: '釜山KCC宙斯盾', abbr: 'KCC', color: '#FFD700', league: 'kr', strength: 74 },
  cgl: { zh: '昌原LG猎隼', abbr: 'CGL', color: '#00A0DF', league: 'kr', strength: 72 },
  // 美洲联赛
  boca: { zh: '博卡青年', abbr: 'BOC', color: '#003087', league: 'lna', strength: 78 },
  riv: { zh: '河床', abbr: 'RIV', color: '#E4002B', league: 'lna', strength: 77 },
  fla: { zh: '弗拉门戈', abbr: 'FLA', color: '#E30613', league: 'lna', strength: 79 },
  bsg: { zh: '巴斯克体育', abbr: 'BSG', color: '#005BAC', league: 'lna', strength: 74 },
  cap: { zh: '首都队', abbr: 'CAP', color: '#7C3AED', league: 'lna', strength: 72 },
  ssp: { zh: '圣保罗', abbr: 'SSP', color: '#1B2A4A', league: 'lna', strength: 73 },
  // 菲律宾PBA
  smb: { zh: '圣米格尔啤酒', abbr: 'SMB', color: '#C8102E', league: 'pba', strength: 78 },
  brg: { zh: '巴朗加国王', abbr: 'BRG', color: '#005BAC', league: 'pba', strength: 77 },
  tnt: { zh: 'TNT热带', abbr: 'TNT', color: '#FFD700', league: 'pba', strength: 76 },
  mgs: { zh: '麦格纳热', abbr: 'MGS', color: '#009E60', league: 'pba', strength: 73 },
  ntr_pba: { zh: '北港', abbr: 'NTP', color: '#1B2A4A', league: 'pba', strength: 71 },
  con: { zh: '汇聚', abbr: 'CON', color: '#00A0DF', league: 'pba', strength: 72 },
  // 亚洲联赛
  per_h: { zh: '波斯波利斯', abbr: 'PRS', color: '#E30613', league: 'asia', strength: 74 },
  msh: { zh: '马赫拉姆', abbr: 'MSH', color: '#1B3C6E', league: 'asia', strength: 72 },
  hik: { zh: '希克马', abbr: 'HIK', color: '#009E60', league: 'asia', strength: 72 },
  alr: { zh: '阿尔利雅得', abbr: 'ALR', color: '#005BAC', league: 'asia', strength: 73 },
  alt: { zh: '阿赫利', abbr: 'ALT', color: '#7C3AED', league: 'asia', strength: 71 },
  mnd: { zh: '曼谷城', abbr: 'MND', color: '#FFA000', league: 'asia', strength: 69 },
};

// NCAA 大学（美国大学篮球，青训后可选路径）
const NCAA_TEAMS = {
  duk: { zh: '杜克大学', abbr: 'DUK', color: '#003087', league: 'ncaa', strength: 82 },
  unc: { zh: '北卡罗来纳大学', abbr: 'UNC', color: '#7BAFD4', league: 'ncaa', strength: 81 },
  kty: { zh: '肯塔基大学', abbr: 'KTY', color: '#0033A0', league: 'ncaa', strength: 80 },
  kan: { zh: '堪萨斯大学', abbr: 'KAN', color: '#0051BA', league: 'ncaa', strength: 79 },
  ucl: { zh: '加州大学洛杉矶分校', abbr: 'UCL', color: '#536895', league: 'ncaa', strength: 79 },
  gon: { zh: '冈萨加大学', abbr: 'GON', color: '#041E42', league: 'ncaa', strength: 78 },
  ari: { zh: '亚利桑那大学', abbr: 'ARI', color: '#CC0033', league: 'ncaa', strength: 77 },
  mst: { zh: '密歇根州立大学', abbr: 'MST', color: '#18453B', league: 'ncaa', strength: 77 },
  tex: { zh: '德州大学', abbr: 'TEX', color: '#BF5700', league: 'ncaa', strength: 76 },
  bav: { zh: '贝勒大学', abbr: 'BAV', color: '#1D2F1D', league: 'ncaa', strength: 76 },
  ind: { zh: '印第安纳大学', abbr: 'IND', color: '#990000', league: 'ncaa', strength: 75 },
  mar: { zh: '马奎特大学', abbr: 'MAR', color: '#003366', league: 'ncaa', strength: 74 },
  pro: { zh: '普罗维登斯学院', abbr: 'PRO', color: '#000000', league: 'ncaa', strength: 73 },
  ksu: { zh: '堪萨斯州立大学', abbr: 'KSU', color: '#512888', league: 'ncaa', strength: 73 },
  san: { zh: '圣地亚哥州立大学', abbr: 'SAN', color: '#A6192E', league: 'ncaa', strength: 72 },
  col: { zh: '科罗拉多大学', abbr: 'COL', color: '#000000', league: 'ncaa', strength: 71 },
};

// 为数据对象补齐 id 字段（Object.values 遍历时需要）
for (const [id, team] of Object.entries(TEAMS)) team.id = id;
for (const [id, team] of Object.entries(NCAA_TEAMS)) team.id = id;
for (const [code, country] of Object.entries(COUNTRIES)) country.code = code;
for (const [id, league] of Object.entries(LEAGUES)) league.id = id;

// 称号图鉴
const TITLES = [
  { id: 'tian_zhijiaozi', name: '天之骄子', art: '👑', hint: '巅峰能力打到 96 以上' },
  { id: 'yao_ren_dx', name: '妖人兑现', art: '✨', hint: '出道不被看好，巅峰却打到 93 以上' },
  { id: 'da_qi_wan_cheng', name: '大器晚成', art: '🌙', hint: '从低起点一路练到 88 以上' },
  { id: 'shang_zhong_yong', name: '伤仲永', art: '🍂', hint: '高开低走，巅峰之后断崖下滑' },
  { id: 'yi_ren_yi_cheng', name: '一人一城', art: '🏙️', hint: '整个生涯只效力一支球队' },
  { id: 'lan_tan_liu_lang_zhe', name: '篮坛流浪者', art: '🧳', hint: '效力过 6 支以上球队' },
  { id: 'tie_ren', name: '铁人', art: '🔩', hint: '生涯出场 1200 场以上' },
  { id: 'de_fen_ji_qi', name: '得分机器', art: '🔥', hint: '生涯总得分突破 32000 分' },
  { id: 'san_shuang_ji_qi', name: '三双机器', art: '🎱', hint: '打出过场均三双的赛季' },
  { id: 'wu_mian_zhi_wang', name: '无冕之王', art: '🥀', hint: '巅峰 90+，却一座奖杯都没有' },
  { id: 'shi_jie_zhi_dian', name: '世界之巅', art: '🌍', hint: '捧起世界杯或奥运会金牌' },
  { id: 'lan_qiu_zhi_shen', name: '篮球之神', art: '🐐', hint: '世界大赛冠军 + 总决赛MVP + 巅峰 96+' },
  { id: 'jin_man_guan', name: '金满贯', art: '💎', hint: '世界杯、奥运、联赛、洲际冠军全拿过' },
  { id: 'wang_chao_ji', name: '王朝奠基人', art: '🏛️', hint: '同一支球队三连冠' },
  { id: 'guan_jun_shou_ge_ji', name: '冠军收割机', art: '🏆', hint: '生涯 12 座以上重要奖杯' },
  { id: 'lan_tan_shou_fu', name: '篮坛首富', art: '💰', hint: '生涯总收入突破 20 亿' },
  { id: 'tian_jia_he_tong', name: '天价合同', art: '📜', hint: '单赛季年薪突破 3 亿' },
  { id: 'quan_ming_xing_zhi_wang', name: '全明星之王', art: '🌟', hint: '10 次以上全明星' },
  { id: 'zui_you_jia_zhi', name: '最有价值', art: '🏅', hint: '4 次以上常规赛MVP' },
  { id: 'zong_jue_sai_zhi_wang', name: '总决赛之王', art: '🏵️', hint: '3 次以上总决赛MVP' },
  { id: 'de_fen_wang', name: '得分王', art: '🎯', hint: '5 次以上得分王' },
  { id: 'lan_ban_guai_shou', name: '篮板怪兽', art: '🦍', hint: '5 次以上篮板王或生涯篮板 15000+' },
  { id: 'zu_zhi_da_shi', name: '组织大师', art: '🎩', hint: '5 次以上助攻王或生涯助攻 10000+' },
  { id: 'fang_shou_tie_zha', name: '防守铁闸', art: '🛡️', hint: '3 次以上最佳防守球员' },
  { id: 'bu_lao_chuan_shuo', name: '不老传说', art: '⏳', hint: '38 岁还在打主力' },
  { id: 'ji_liu_yong_tui', name: '急流勇退', art: '🚀', hint: '巅峰期 32 岁前主动退役' },
  { id: 'guo_jia_dui_qi_zhi', name: '国家队旗帜', art: '🇨🇳', hint: '国家队出场 100 场以上' },
  { id: 'yuan_meng_ren', name: '圆梦人', art: '💫', hint: '为儿时主队拿过冠军' },
  { id: 'jue_sha_zhi_wang', name: '绝杀之王', art: '⏱️', hint: '关键球时刻把比赛杀死' },
  { id: 'fa_qiu_da_shi', name: '罚球大师', art: '🎯', hint: '两次以上关键罚球全部命中' },
  { id: 'yi_sheng_zhi_di', name: '一生之敌', art: '⚔️', hint: '与宿敌同场 8 个赛季以上，且双方都拿过联赛冠军' },
  { id: 'yan_zhong_ding', name: '压制宿敌', art: '🥊', hint: '生涯总得分超过宿敌，冠军也比对方多' },
];

// 更新记录
const UPDATES = [
  { version: '1.0', title: '游戏上线', items: ['选一个国籍和位置，从 16 岁打到退役', '转会、伤病、绝杀、国家队，每个决定都算数', '结束后生成一张可保存的生涯战绩卡', '从首页就能翻回过去任意一局的战绩卡'] },
  { version: '1.1', title: '新增多套事件卡面与文案', items: ['事件池上新：训练、交易、伤病、更衣室', '新增三双机器、绝杀之王等称号', '更新记录入口，玩法一直在更新'] },
  { version: '1.2', title: '历史档案与称号图鉴', items: ['每一局收场都会自动留下一条记录', '称号有了一面墙：所有称号一起排列', '拿到过的显示你当时那句话', '没拿到的只给一句线索，不告诉你怎么拿'] },
  { version: '1.3', title: '编号开局', items: ['结算页给这一局一个编号，点一下就复制', '粘贴朋友给你的编号，就能复现同一段生涯', '同一个编号可以反复打，每一遍各留一份档案', '称号照点亮'] },
  { version: '1.4', title: '洲际杯与世界大赛', items: ['世界杯、奥运会按年龄周期开打', '预选赛生死战由你来踢', '你越强，国家队走得越深', '国家队奖杯单独计数'] },
  { version: '1.5', title: '一生之敌与传奇时刻', items: ['新增强力宿敌：同一个位置、同一年出道，一辈子的比较', '每次交手都有剧情，结算页给出你们一生的对决记录', '新增全明星、扣篮大赛、交易截止日等 10 个新事件', '每个赛季会留下高光时刻，写进你的生涯总结'] },
];

// 生涯事件池
// 每个选项：label 按钮文案；hint 效果提示；outcomes 结果数组
// effects 键：overallDelta 能力永久变化；tempDelta 阶段内临时变化；
// roleShift 地位变化；injury 伤病描述；suspended 禁赛季数；trophyMult 俱乐部夺冠加成；
// nationalMult 国家队加成；money 一次性收入（万）；join/leave 转会；
// forcedOutcome 强制结果（positive/negative/neutral）；nationalTeamRetired 退出国家队

const EVENTS = {
  // ---------- 青训期 ----------
  youth_shooting: {
    key: 'youth_shooting', type: 'career_event', minAge: 16, maxAge: 17, weight: 2,
    title: '青训营：投篮专项',
    text: '青训营教练把你叫到一边，说你的投篮姿势很标准，但出手太慢。他给了你一套暑期计划。',
    options: [
      { id: 'a', label: '每天加练 500 球', hint: '出手练快了，能力上涨', outcomes: [
        { prob: 0.75, text: '三个月后，你的出手快得让教练点头。', effects: { overallDelta: 2, permanent: true } },
        { prob: 0.25, text: '姿势改了又改，反而丢了手感。', effects: { overallDelta: -1, permanent: true } },
      ]},
      { id: 'b', label: '先不练投篮，练对抗', hint: '身体先跟上，投篮以后再说', outcomes: [
        { prob: 0.8, text: '对抗上来后，突破也顺了。', effects: { overallDelta: 1, permanent: true, roleShift: 1 } },
        { prob: 0.2, text: '增重太快，膝盖开始闹脾气。', effects: { overallDelta: 0, injury: '膝伤反复' } },
      ]},
      { id: 'c', label: '按部就班', hint: '不出彩，也不冒险', outcomes: [
        { prob: 1, text: '暑假结束，一切照旧。', effects: {} },
      ]},
    ],
  },
  youth_point_guard: {
    key: 'youth_point_guard', type: 'career_event', minAge: 16, maxAge: 17, weight: 2,
    title: '青训营：组织核心',
    text: '队里最好的后卫伤退了，教练让你顶上组织位置。这场练习赛，全队都在看你。',
    options: [
      { id: 'a', label: '自己打，先得分', hint: '得分涨，组织能力暂时落后', outcomes: [
        { prob: 0.7, text: '你砍下高分，球探记下了你的名字。', effects: { overallDelta: 2, permanent: true } },
        { prob: 0.3, text: '你出手太多，队友站了一整场。', effects: { overallDelta: -1, permanent: true, roleShift: -1 } },
      ]},
      { id: 'b', label: '先传起来', hint: '组织涨，得分节奏要重新找', outcomes: [
        { prob: 0.7, text: '你的传球盘活了全队，教练笑了。', effects: { overallDelta: 2, permanent: true } },
        { prob: 0.3, text: '传球太犹豫，被断了好几个。', effects: { overallDelta: -1, permanent: true } },
      ]},
    ],
  },
  youth_late_growth: {
    key: 'youth_late_growth', type: 'career_event', minAge: 16, maxAge: 17, weight: 1,
    title: '青训营：身体晚熟',
    text: '骨龄报告说你发育偏晚，未来两年是长身体的关键期。教练给了你两个方向。',
    options: [
      { id: 'a', label: '狠练力量', hint: '上限更高，有伤病风险', outcomes: [
        { prob: 0.55, text: '你像换了个人，对抗完全不一样了。', effects: { overallDelta: 4, permanent: true, roleShift: 1 } },
        { prob: 0.45, text: '练得太猛，腰先撑不住了。', effects: { overallDelta: -1, injury: '腰伤' } },
      ]},
      { id: 'b', label: '慢慢来', hint: '稳妥发育', outcomes: [
        { prob: 1, text: '一年后你长高了 5 厘米，一切正常。', effects: { overallDelta: 2, permanent: true } },
      ]},
    ],
  },
  // ---------- 训练/休赛期 ----------
  offseason_shooting: {
    key: 'offseason_shooting', type: 'career_event', minAge: 18, maxAge: 33, weight: 3,
    title: '休赛期：投射训练',
    text: '训练师给你列了一份数据：上赛季你的三分命中率全队倒数。休赛期只有八周，你得自己定。',
    options: [
      { id: 'a', label: '每天八百球', hint: '练成能力大涨，练过头有伤', outcomes: [
        { prob: 0.65, text: '新赛季第一场，你的三分让替补席跳起来了。', effects: { overallDelta: 2, permanent: true, roleShift: 1 } },
        { prob: 0.35, text: '肩部劳损，出手姿势不得不改回去。', effects: { overallDelta: -1, injury: '肩部劳损' } },
      ]},
      { id: 'b', label: '加练罚球和终结', hint: '稳定提升，幅度小', outcomes: [
        { prob: 0.9, text: '篮下终结更稳了，罚球也稳了。', effects: { overallDelta: 1, permanent: true } },
        { prob: 0.1, text: '练得中规中矩。', effects: {} },
      ]},
      { id: 'c', label: '去度假', hint: '放松心情，不涨能力', outcomes: [
        { prob: 1, text: '阳光、沙滩，回来状态一般。', effects: { tempDelta: -1 } },
      ]},
    ],
  },
  offseason_strength: {
    key: 'offseason_strength', type: 'career_event', minAge: 18, maxAge: 33, weight: 3,
    title: '休赛期：力量训练',
    text: '体能教练建议你上一档力量。练成了对抗脱胎换骨，练过了就是一身笨重的肌肉。',
    options: [
      { id: 'a', label: '上力量', hint: '对抗上台阶，练过头变笨重', outcomes: [
        { prob: 0.6, text: '新赛季对抗脱胎换骨，低位谁都顶不动你。', effects: { overallDelta: 2, permanent: true, roleShift: 1 } },
        { prob: 0.4, text: '体重压不住了，跑不动了。', effects: { overallDelta: -1, tempDelta: -1 } },
      ]},
      { id: 'b', label: '保持现在的身材', hint: '什么都不变', outcomes: [
        { prob: 1, text: '休赛期平稳度过。', effects: {} },
      ]},
    ],
  },
  offseason_ballhandling: {
    key: 'offseason_ballhandling', type: 'career_event', minAge: 18, maxAge: 33, weight: 2,
    title: '休赛期：控球专项',
    text: '私人教练要给你改运球动作。改成了是永久提升，改废了也是永久的。',
    options: [
      { id: 'a', label: '跟着改', hint: '五五开，要么起飞要么崩', outcomes: [
        { prob: 0.5, text: '动作改顺了，防守人根本掏不到球。', effects: { overallDelta: 3, permanent: true } },
        { prob: 0.5, text: '新动作一直不习惯，运球反而别扭。', effects: { overallDelta: -2, permanent: true } },
      ]},
      { id: 'b', label: '不改，用自己的', hint: '稳定，不涨', outcomes: [
        { prob: 1, text: '你相信自己的手活。', effects: {} },
      ]},
    ],
  },
  offseason_diet: {
    key: 'offseason_diet', type: 'career_event', minAge: 18, maxAge: 35, weight: 2,
    title: '休赛期：营养师',
    text: '队医给你请了营养师，要重排你的饮食结构。身体成分变了，场上的人也会变。',
    options: [
      { id: 'a', label: '照着吃', hint: '身体变好，口味得忍', outcomes: [
        { prob: 0.75, text: '体脂降了，脚步轻了。', effects: { overallDelta: 1, permanent: true } },
        { prob: 0.25, text: '戒不掉夜宵，白搭。', effects: {} },
      ]},
      { id: 'b', label: '维持现在的吃法', hint: '不变', outcomes: [
        { prob: 1, text: '你继续快乐地干饭。', effects: {} },
      ]},
    ],
  },
  offseason_extra_work: {
    key: 'offseason_extra_work', type: 'career_event', minAge: 18, maxAge: 31, weight: 2,
    title: '休赛期：加练',
    text: '体能教练给你排了一套额外的训练量。练到位能上一个台阶，练废了就是伤。',
    options: [
      { id: 'a', label: '一天两练', hint: '练成能力大涨，练废进医院', outcomes: [
        { prob: 0.55, text: '一个夏天下来，你的运动能力肉眼可见地涨了。', effects: { overallDelta: 2, permanent: true } },
        { prob: 0.45, text: '训练量吃得太急，赛季前拉伤了。', effects: { overallDelta: -1, injury: '肌肉拉伤', tempDelta: -1 } },
      ]},
      { id: 'b', label: '减半做', hint: '一半的量，一半的收益', outcomes: [
        { prob: 0.85, text: '量刚好，稳步提升。', effects: { overallDelta: 1, permanent: true } },
        { prob: 0.15, text: '少了点，等于没练。', effects: {} },
      ]},
      { id: 'c', label: '只练一次', hint: '不涨，也不伤', outcomes: [
        { prob: 1, text: '象征性出了一身汗。', effects: {} },
      ]},
    ],
  },
  // ---------- 教练/体系 ----------
  coach_change: {
    key: 'coach_change', type: 'career_event', minAge: 18, maxAge: 34, weight: 2,
    title: '新帅上任',
    text: '俱乐部换了主教练。他嘴上说你很重要，但媒体反复分析：你的技术特点，不适合他的体系。',
    options: [
      { id: 'a', label: '主动适应新体系', hint: '练成位置更稳，练废了更难受', outcomes: [
        { prob: 0.6, text: '你交出的表现堵住了所有分析，位置没人动得了。', effects: { overallDelta: 2, permanent: true, roleShift: 1 } },
        { prob: 0.4, text: '新体系里没有你的位置，坐穿板凳。', effects: { roleShift: -2, tempDelta: -1 } },
      ]},
      { id: 'b', label: '用自己的打法打', hint: '保持打法，和新帅顶一顶', outcomes: [
        { prob: 0.45, text: '你用自己的方式打出了数据，新帅也得服。', effects: { overallDelta: 1, permanent: true } },
        { prob: 0.55, text: '新帅不买账，你的出场时间开始减少。', effects: { roleShift: -1, tempDelta: -1 } },
      ]},
      { id: 'c', label: '申请交易', hint: '换个环境，去别的队', outcomes: [
        { prob: 0.8, text: '交易完成，新环境重新开始。', effects: { transfer: true } },
        { prob: 0.2, text: '管理层没放人，还把你按在板凳上。', effects: { roleShift: -2 } },
      ]},
    ],
  },
  coach_new_system: {
    key: 'coach_new_system', type: 'career_event', minAge: 18, maxAge: 34, weight: 2,
    title: '打法要改',
    text: '主帅还是那个主帅，但俱乐部定了新方向：这套球从下赛季开始要提速打跑轰。会上放的示范录像里，你这个位置上站的是另一种球员。',
    options: [
      { id: 'a', label: '跟上节奏', hint: '适应跑轰，练成坐稳主力', outcomes: [
        { prob: 0.6, text: '你跑出来的效率让教练刮目相看。', effects: { overallDelta: 2, permanent: true, roleShift: 1 } },
        { prob: 0.4, text: '体能跟不上节奏，轮换边缘。', effects: { roleShift: -1 } },
      ]},
      { id: 'b', label: '找教练谈', hint: '争取战术地位', outcomes: [
        { prob: 0.5, text: '教练给你安排了几套专属战术。', effects: { overallDelta: 1, permanent: true } },
        { prob: 0.5, text: '谈话不欢而散，更衣室气氛微妙。', effects: { tempDelta: -1 } },
      ]},
    ],
  },
  assistant_coach: {
    key: 'assistant_coach', type: 'career_event', minAge: 16, maxAge: 28, weight: 1,
    title: '助教的建议',
    text: '助教偷偷给你看了一份球探报告，上面是你的优缺点分析。他建议你主攻一个方向。',
    options: [
      { id: 'a', label: '练短板', hint: '练成综合大涨，练废了信心受挫', outcomes: [
        { prob: 0.5, text: '短板补上了，你成了更完整的球员。', effects: { overallDelta: 3, permanent: true } },
        { prob: 0.5, text: '短板还是短板，还丢了长板的手感。', effects: { overallDelta: -1, permanent: true } },
      ]},
      { id: 'b', label: '强化长板', hint: '把强项练到极致', outcomes: [
        { prob: 0.8, text: '你的招牌动作成了防守人的噩梦。', effects: { overallDelta: 2, permanent: true } },
        { prob: 0.2, text: '练太多，身体开始抗议。', effects: { injury: '疲劳性伤病' } },
      ]},
    ],
  },
  // ---------- 竞争 ----------
  young_guns: {
    key: 'young_guns', type: 'career_event', minAge: 18, maxAge: 34, weight: 3,
    title: '队里冒出个小孩',
    text: '青训营提上来一个十七八岁的天才，天赋肉眼可见，打的正是你的位置。',
    options: [
      { id: 'a', label: '把他压下去', hint: '坐稳主力，能力永久提升，也可能被反超', outcomes: [
        { prob: 0.6, text: '你用表现告诉他：这个位置还轮不到他。', effects: { overallDelta: 1, permanent: true, roleShift: 1 } },
        { prob: 0.4, text: '他太出色了，你只能让出位置。', effects: { roleShift: -2 } },
      ]},
      { id: 'b', label: '带他成长', hint: '更衣室声望涨，上场时间可能减少', outcomes: [
        { prob: 0.7, text: '他把你当大哥，球队氛围好得发烫。', effects: { overallDelta: 1, permanent: true } },
        { prob: 0.3, text: '他成长太快，教练开始倾斜资源。', effects: { roleShift: -1 } },
      ]},
      { id: 'c', label: '申请转会', hint: '换个队重新开始', outcomes: [
        { prob: 0.8, text: '你带着天赋去了新球队。', effects: { transfer: true } },
        { prob: 0.2, text: '管理层不放人，位置还得争。', effects: {} },
      ]},
    ],
  },
  bought_rival: {
    key: 'bought_rival', type: 'career_event', minAge: 18, maxAge: 34, weight: 2,
    title: '俱乐部买了个人',
    text: '夏窗俱乐部砸下重金，买的正是你这个位置。经理找你谈：换个角色，还是坐着。',
    options: [
      { id: 'a', label: '留下证明自己', hint: '赢了地位大涨，输了坐板凳', outcomes: [
        { prob: 0.5, text: '你用表现证明了谁才是答案。', effects: { overallDelta: 2, permanent: true, roleShift: 2 } },
        { prob: 0.5, text: '新援太强，你被挤到了轮换边缘。', effects: { roleShift: -2 } },
      ]},
      { id: 'b', label: '接受替补角色', hint: '保住出场，数据下滑', outcomes: [
        { prob: 1, text: '你从替补席重新开始，机会越来越少。', effects: { roleShift: -2 } },
      ]},
      { id: 'c', label: '让经纪人找下家', hint: '换队', outcomes: [
        { prob: 0.75, text: '交易很快谈妥，你去了新球队。', effects: { transfer: true } },
        { prob: 0.25, text: '没有合适的报价，只能留下。', effects: {} },
      ]},
    ],
  },
  loan_return: {
    key: 'loan_return', type: 'career_event', minAge: 18, maxAge: 34, weight: 1,
    title: '租借归来的那个人',
    text: '两年前被租出去的那个人回来了。走的时候他排在你后面，这两年他打满了两个赛季，实力比以前强不少。',
    options: [
      { id: 'a', label: '正面竞争', hint: '赢了坐稳主力，输了让位', outcomes: [
        { prob: 0.55, text: '你用训练和比赛表现赢得了教练的信任。', effects: { overallDelta: 1, permanent: true, roleShift: 1 } },
        { prob: 0.45, text: '他太想证明自己，你被挤了下去。', effects: { roleShift: -2 } },
      ]},
      { id: 'b', label: '去别处寻找机会', hint: '申请交易', outcomes: [
        { prob: 0.7, text: '你换了一支需要你的球队。', effects: { transfer: true } },
        { prob: 0.3, text: '交易告吹，局面更尴尬。', effects: { roleShift: -1 } },
      ]},
    ],
  },
  // ---------- 伤病 ----------
  injury_minor: {
    key: 'injury_minor', type: 'career_event', minAge: 18, maxAge: 36, weight: 3,
    title: '队医的报告',
    text: '有个小伤拖了两年。休赛期队医摊开一份手术方案，你拿去问了另一位医生，两个人给的答案正好相反。',
    options: [
      { id: 'a', label: '手术根治', hint: '根治了能力永久提升，恢复期长', outcomes: [
        { prob: 0.75, text: '手术成功，伤彻底好了。', effects: { overallDelta: 1, permanent: true, injury: null, tempDelta: -1 } },
        { prob: 0.25, text: '术后感染，恢复比预期慢。', effects: { injury: '术后感染', tempDelta: -2 } },
      ]},
      { id: 'b', label: '保守治疗', hint: '不耽误赛季，伤可能反复', outcomes: [
        { prob: 0.7, text: '保守治疗撑过了整个赛季。', effects: {} },
        { prob: 0.3, text: '赛季中段还是倒下了。', effects: { injury: '旧伤复发', tempDelta: -2 } },
      ]},
    ],
  },
  injury_serious: {
    key: 'injury_serious', type: 'career_event', minAge: 18, maxAge: 34, weight: 2,
    title: '最不该受伤的时候',
    text: '新赛季开幕只剩三个月，你在训练中重伤倒地。队医摇了摇头。',
    options: [
      { id: 'a', label: '安心养伤', hint: '彻底养好，缺席大半个赛季', outcomes: [
        { prob: 0.7, text: '伤养好了，只是复出后需要时间找回节奏。', effects: { injury: '重伤恢复', tempDelta: -2 } },
        { prob: 0.3, text: '恢复期出现反复，整个赛季都跟不上。', effects: { injury: '恢复反复', tempDelta: -3 } },
      ]},
      { id: 'b', label: '强行提前复出', hint: '咬牙赶上，可能加重伤势', outcomes: [
        { prob: 0.4, text: '你奇迹般地赶上了开幕战，伤势无碍。', effects: { overallDelta: 1, permanent: true, roleShift: 1 } },
        { prob: 0.6, text: '伤势加重，能力永久受损。', effects: { overallDelta: -3, permanent: true, injury: '伤势加重' } },
      ]},
    ],
  },
  injury_big_tournament: {
    key: 'injury_big_tournament', type: 'career_event', minAge: 18, maxAge: 34, weight: 2,
    title: '大赛前的噩耗',
    text: '国家队征召来了。俱乐部队医发现，你身上的老伤复发了。打封闭上，还是养。',
    options: [
      { id: 'a', label: '打封闭上', hint: '带队赢球声望大涨，加重伤势有风险', outcomes: [
        { prob: 0.5, text: '你带伤打满大赛，回来成了英雄。', effects: { nationalMult: 1.5, injury: '伤势加重', roleShift: 1 } },
        { prob: 0.5, text: '大赛没打明白，伤反而重了。', effects: { nationalMult: 0.5, injury: '伤势加重', tempDelta: -2 } },
      ]},
      { id: 'b', label: '缺席大赛养伤', hint: '身体要紧，国家队可能不满', outcomes: [
        { prob: 0.8, text: '养好了身体，下个周期再来。', effects: { injury: null, nationalMult: 0.7 } },
        { prob: 0.2, text: '舆论开始骂你怕了。', effects: { nationalMult: 0.7, tempDelta: -1 } },
      ]},
    ],
  },
  // ---------- 国家队 ----------
  national_callup: {
    key: 'national_callup', type: 'career_event', minAge: 19, maxAge: 32, weight: 2,
    title: '国家队征召',
    text: '国家队集训名单里有你的名字。教练想让你打一个新的角色。',
    options: [
      { id: 'a', label: '接受征召，打新角色', hint: '国家队数据更好看，俱乐部体力吃紧', outcomes: [
        { prob: 0.7, text: '你在国家队打出了身价。', effects: { nationalMult: 1.2, tempDelta: -1 } },
        { prob: 0.3, text: '新角色水土不服，两头都没打好。', effects: { nationalMult: 0.8, tempDelta: -2 } },
      ]},
      { id: 'b', label: '婉拒，专注俱乐部', hint: '俱乐部全勤，国家队印象分下降', outcomes: [
        { prob: 1, text: '你选择先顾俱乐部。', effects: { nationalMult: 0.6, roleShift: 1 } },
      ]},
    ],
  },
  national_retire: {
    key: 'national_retire', type: 'career_event', minAge: 30, maxAge: 40, weight: 1,
    title: '国家队退役',
    text: '年龄摆在那里，你开始考虑从国家队退役。教练说，只要你愿意，随时回来。',
    options: [
      { id: 'a', label: '宣布退出国家队', hint: '此后不再被征召，专注俱乐部', outcomes: [
        { prob: 1, text: '你宣布退出国家队，从此专注俱乐部。', effects: { nationalTeamRetired: true, roleShift: 1 } },
      ]},
      { id: 'b', label: '只要国家需要我就打', hint: '继续为国效力', outcomes: [
        { prob: 1, text: '你选择继续为国征战。', effects: {} },
      ]},
    ],
  },
  // ---------- 转会/合同 ----------
  transfer_rumor: {
    key: 'transfer_rumor', type: 'career_event', minAge: 19, maxAge: 33, weight: 2,
    title: '转会传闻',
    text: '转会传闻被翻来覆去炒了一个冬天。你一句话没说过，看台上已经认定你身在曹营。',
    options: [
      { id: 'a', label: '公开表态留队', hint: '更衣室安心，报价降温', outcomes: [
        { prob: 0.8, text: '你公开表态，全队都松了一口气。', effects: { roleShift: 1 } },
        { prob: 0.2, text: '球迷觉得你在演戏。', effects: { tempDelta: -1 } },
      ]},
      { id: 'b', label: '不回应', hint: '专注打球', outcomes: [
        { prob: 1, text: '你用表现回应一切。', effects: {} },
      ]},
      { id: 'c', label: '让经纪人推动转会', hint: '可能去更好的球队', outcomes: [
        { prob: 0.6, text: '交易达成，你去了新球队。', effects: { transfer: true } },
        { prob: 0.4, text: '谈判破裂，两边都不痛快。', effects: { roleShift: -1 } },
      ]},
    ],
  },
  dream_offer: {
    key: 'dream_offer', type: 'career_event', minAge: 20, maxAge: 32, weight: 1,
    title: '儿时主队来敲门了',
    text: '小时候贴在墙上的那张海报，那支球队的管理层打来了电话。',
    options: [
      { id: 'a', label: '加盟儿时主队', hint: '圆梦，可能降薪', outcomes: [
        { prob: 1, text: '你穿上了那件梦寐以求的球衣。', effects: { transfer: true, dreamTeam: true } },
      ]},
      { id: 'b', label: '留队', hint: '继续现在的一切', outcomes: [
        { prob: 1, text: '你选择留下，梦留在心里。', effects: {} },
      ]},
    ],
  },
  rival_offer: {
    key: 'rival_offer', type: 'career_event', minAge: 20, maxAge: 33, weight: 1,
    title: '死敌来挖你',
    text: '死敌球队开价了。球迷会恨你，但那边的阵容真的更有机会夺冠。',
    options: [
      { id: 'a', label: '接受', hint: '去死敌，背负骂名拿冠军', outcomes: [
        { prob: 1, text: '你成了球迷口中的叛徒，也成了冠军热门。', effects: { transfer: true, roleShift: 1, money: 2000 } },
      ]},
      { id: 'b', label: '拒绝', hint: '留下，球迷爱你', outcomes: [
        { prob: 1, text: '你拒绝了死敌，主场球迷为你起立。', effects: { roleShift: 1 } },
      ]},
    ],
  },
  blockbuster: {
    key: 'blockbuster', type: 'career_event', minAge: 22, maxAge: 34, weight: 1,
    title: '天价合同',
    text: '经纪人带着一份你没见过的报价来了。数字大得离谱，代价是你得跟一堆大牌抢位置。',
    options: [
      { id: 'a', label: '签', hint: '收入暴涨，去强队抢位置', outcomes: [
        { prob: 0.7, text: '你签下了天价合同，也扛住了压力。', effects: { transfer: true, money: 15000, salaryMult: 2 } },
        { prob: 0.3, text: '合同签了，位置没了。', effects: { transfer: true, money: 15000, salaryMult: 2, roleShift: -1 } },
      ]},
      { id: 'b', label: '留在现在的队', hint: '继续做核心', outcomes: [
        { prob: 1, text: '你选择留下来，继续当这座城的核心。', effects: { roleShift: 1 } },
      ]},
    ],
  },
  home_league_offer: {
    key: 'home_league_offer', type: 'career_event', minAge: 26, maxAge: 35, weight: 1,
    title: '回国打球',
    text: '家人想让你回去。祖国的联赛开出了核心待遇，机场会有人举着你的名字。',
    options: [
      { id: 'a', label: '回国打球', hint: '当核心，联赛曝光度下降', outcomes: [
        { prob: 1, text: '你回家了，机场真的有人举着你的名字。', effects: { transfer: true, roleShift: 2, money: 3000 } },
      ]},
      { id: 'b', label: '留在国外', hint: '继续闯荡', outcomes: [
        { prob: 1, text: '你选择继续在国外证明自己。', effects: {} },
      ]},
    ],
  },
  contract_non_renewal: {
    key: 'contract_non_renewal', type: 'career_event', minAge: 30, maxAge: 40, weight: 1,
    title: '合同到期',
    text: '俱乐部决定不再续约。下一步去哪儿，或者就此结束。',
    options: [
      { id: 'a', label: '找下家', hint: '还有球队愿意要你', outcomes: [
        { prob: 0.7, text: '你找到了新东家，继续打球。', effects: { transfer: true } },
        { prob: 0.3, text: '没有球队报价，只能退役。', effects: { forceRetire: true } },
      ]},
      { id: 'b', label: '不再找下家，就此结束', hint: '直接退役', outcomes: [
        { prob: 1, text: '你把球衣挂起来，生涯到此为止。', effects: { forceRetire: true } },
      ]},
    ],
  },
  no_offers: {
    key: 'no_offers', type: 'career_event', minAge: 32, maxAge: 45, weight: 2,
    title: '无人问津',
    text: '转会窗口开了又关，电话一次都没响。经纪人把话说得很直：一份报价都没有。',
    options: [
      { id: 'a', label: '再等等', hint: '可能等到，也可能没有', outcomes: [
        { prob: 0.4, text: '窗口关上前的最后一天，电话响了。', effects: { transfer: true } },
        { prob: 0.6, text: '窗口关上了，就到这里了。', effects: { forceRetire: true } },
      ]},
      { id: 'b', label: '就此退役', hint: '结束职业生涯', outcomes: [
        { prob: 1, text: '你决定把退役消息发出去。', effects: { forceRetire: true } },
      ]},
    ],
  },
  // ---------- 场外/媒体 ----------
  social_media: {
    key: 'social_media', type: 'career_event', minAge: 18, maxAge: 35, weight: 2,
    title: '被流量盯上了',
    text: '不知道从哪来的节奏，总有自媒体靠黑你换流量。你告诉自己别看，晚上还是一条条翻。',
    options: [
      { id: 'a', label: '卸载社交媒体', hint: '心态稳了，能力微涨', outcomes: [
        { prob: 0.8, text: '眼不见心不烦，你专注打球。', effects: { overallDelta: 1, permanent: true } },
        { prob: 0.2, text: '忍不住又装回来了。', effects: {} },
      ]},
      { id: 'b', label: '开播回应', hint: '热度高，可能说错话', outcomes: [
        { prob: 0.6, text: '你在发布会上说了四十分钟，路转粉一片。', effects: { tempDelta: 1, money: 500 } },
        { prob: 0.4, text: '话说多了，又添了新节奏。', effects: { tempDelta: -1 } },
      ]},
      { id: 'c', label: '照单全收', hint: '不影响，也不反抗', outcomes: [
        { prob: 1, text: '你选择无视。', effects: {} },
      ]},
    ],
  },
  agent_issue: {
    key: 'agent_issue', type: 'career_event', minAge: 20, maxAge: 35, weight: 1,
    title: '经纪公司出问题',
    text: '经纪公司出了问题，你和俱乐部的合同被卷进一场纠纷，短期内没法专心打球。',
    options: [
      { id: 'a', label: '换经纪人', hint: '短痛，长期规范', outcomes: [
        { prob: 0.8, text: '新经纪人很快摆平了合同。', effects: {} },
        { prob: 0.2, text: '新经纪人也不省心。', effects: { tempDelta: -1 } },
      ]},
      { id: 'b', label: '自己谈', hint: '省佣金，费心神', outcomes: [
        { prob: 0.6, text: '你自己谈下了合同，省下一笔佣金。', effects: { money: 800 } },
        { prob: 0.4, text: '谈判占了你太多精力。', effects: { tempDelta: -2 } },
      ]},
    ],
  },
  supplement: {
    key: 'supplement', type: 'career_event', minAge: 18, maxAge: 33, weight: 1,
    title: '来路不明的补剂',
    text: '有人递给你一瓶没有标签的补剂，说能立刻见效。也说了：别被查到。',
    options: [
      { id: 'a', label: '喝', hint: '能力暴涨，可能东窗事发', outcomes: [
        { prob: 0.5, text: '赛季初你状态爆棚，没人怀疑。', effects: { overallDelta: 3, permanent: true, tempDelta: 2 } },
        { prob: 0.5, text: '药检呈阳性，长期禁赛，状态大幅下滑。', effects: { suspended: 2, overallDelta: -4, permanent: true } },
      ]},
      { id: 'b', label: '丢掉', hint: '干干净净', outcomes: [
        { prob: 1, text: '你把瓶子丢进了垃圾桶。', effects: {} },
      ]},
    ],
  },
  tattoo: {
    key: 'tattoo', type: 'career_event', minAge: 18, maxAge: 36, weight: 1,
    title: '纹身',
    text: '你想在背上纹一整幅图。队医皱了皱眉：恢复期会耽误训练。',
    options: [
      { id: 'a', label: '纹', hint: '帅，浪费两周', outcomes: [
        { prob: 1, text: '纹完了，真的很帅。', effects: { tempDelta: -1 } },
      ]},
      { id: 'b', label: '算了', hint: '保持专注', outcomes: [
        { prob: 1, text: '你决定先把冠军纹到心里。', effects: {} },
      ]},
    ],
  },
  family: {
    key: 'family', type: 'career_event', minAge: 20, maxAge: 33, weight: 1,
    title: '家里的事',
    text: '父母年纪大了，家里希望你回国打球，离得近一点。',
    options: [
      { id: 'a', label: '回国', hint: '家人开心，曝光度下降', outcomes: [
        { prob: 1, text: '你回了家，成了主场的骄傲。', effects: { transfer: true, roleShift: 1 } },
      ]},
      { id: 'b', label: '留在外面', hint: '继续闯荡，家人失望', outcomes: [
        { prob: 1, text: '你选择了梦想，家人选择了支持。', effects: { tempDelta: -1 } },
      ]},
    ],
  },
  education: {
    key: 'education', type: 'career_event', minAge: 16, maxAge: 24, weight: 1,
    title: '学业',
    text: '当年为了打球休了学。现在有机会一边训练一边把学历补上。',
    options: [
      { id: 'a', label: '把学业读完', hint: '分掉训练时间，出场减少', outcomes: [
        { prob: 0.7, text: '你拿到了学位，也保住了球场。', effects: { roleShift: -1, money: 0 } },
        { prob: 0.3, text: '两头忙，都耽误了。', effects: { tempDelta: -2 } },
      ]},
      { id: 'b', label: '专心打球', hint: '全力投入篮球', outcomes: [
        { prob: 0.85, text: '你把所有时间都给了球场。', effects: { overallDelta: 1, permanent: true } },
        { prob: 0.15, text: '少了退路，压力反而大了。', effects: { tempDelta: -1 } },
      ]},
    ],
  },
  trade_deadline: {
    key: 'trade_deadline', type: 'career_event', minAge: 19, maxAge: 35, weight: 2,
    title: '交易截止日',
    text: '交易截止日当天，球队上下都在等消息。你的名字出现在几份流言里，总经理约你聊了聊。',
    options: [
      { id: 'stay', label: '公开表态留队', hint: '稳定军心，位置更稳', outcomes: [
        { prob: 0.85, text: '你表了态，更衣室稳了，球迷也安心了。', effects: { roleShift: 1 } },
        { prob: 0.15, text: '管理层觉得你话里有话。', effects: { tempDelta: -1 } },
      ]},
      { id: 'push', label: '让经纪人推动交易', hint: '可能去更强的队', outcomes: [
        { prob: 0.55, text: '交易达成，你换了东家。', effects: { transfer: true } },
        { prob: 0.45, text: '没谈成，气氛变得微妙。', effects: { tempDelta: -1 } },
      ]},
    ],
  },
  allstar_game: {
    key: 'allstar_game', type: 'career_event', minAge: 20, maxAge: 35, weight: 2,
    title: '全明星正赛',
    text: '全明星周末，你入选了正赛。场边坐着你的偶像，对面站着你的宿敌。',
    options: [
      { id: 'win', label: '全力争MVP', hint: '数据好看，可能捧起全明星MVP', outcomes: [
        { prob: 0.5, text: '你砍下全场最高分，捧起全明星MVP。', effects: { award: 'allstar_mvp', tempDelta: 1 } },
        { prob: 0.5, text: '手感一般，成了全明星的配角。', effects: {} },
      ]},
      { id: 'show', label: '表演为主', hint: '秀一把，不较真', outcomes: [
        { prob: 0.8, text: '你的花式传球上了热搜，球迷都记住了。', effects: { money: 300 } },
        { prob: 0.2, text: '玩脱了，被做成了表情包。', effects: { tempDelta: -1 } },
      ]},
    ],
  },
  dunk_contest: {
    key: 'dunk_contest', type: 'career_event', minAge: 19, maxAge: 33, weight: 1,
    title: '扣篮大赛',
    text: '联盟邀请你参加扣篮大赛。你小时候的梦想就是飞越一切。',
    options: [
      { id: 'dare', label: '上高难度动作', hint: '成了封神，砸了尴尬', outcomes: [
        { prob: 0.45, text: '那个 360 度转身劈扣，评委全部给出满分。', effects: { award: 'dunk_king', tempDelta: 1, money: 200 } },
        { prob: 0.55, text: '动作没完成，全场安静了一秒。', effects: { tempDelta: -1 } },
      ]},
      { id: 'safe', label: '求稳，用招牌扣', hint: '稳进决赛，难夺冠', outcomes: [
        { prob: 0.75, text: '你进了决赛，输给了那个年轻人。', effects: {} },
        { prob: 0.25, text: '老将出手，居然拿了冠军。', effects: { award: 'dunk_king' } },
      ]},
    ],
  },
  three_point_contest: {
    key: 'three_point_contest', type: 'career_event', minAge: 19, maxAge: 34, weight: 1,
    title: '三分大赛',
    text: '三分大赛的邀请函放在你柜子里。花球点，赌一把。',
    options: [
      { id: 'flow', label: '按自己的节奏投', hint: '稳定发挥', outcomes: [
        { prob: 0.55, text: '你手感滚烫，捧起三分大赛冠军。', effects: { award: 'three_king' } },
        { prob: 0.45, text: '差一球，惜败。', effects: {} },
      ]},
      { id: 'gamble', label: '全压花球', hint: '赢就赢大的，输就输光', outcomes: [
        { prob: 0.3, text: '花球全进，全场沸腾。', effects: { award: 'three_king', tempDelta: 1, money: 200 } },
        { prob: 0.7, text: '花球点全丢，第一轮出局。', effects: { tempDelta: -1 } },
      ]},
    ],
  },
  playoff_hero: {
    key: 'playoff_hero', type: 'career_event', minAge: 19, maxAge: 35, weight: 2,
    title: '季后赛生死战',
    text: '季后赛首轮，你们被逼到悬崖边上。教练把最后一攻的战术板画给了你。',
    options: [
      { id: 'carry', label: '把球队扛肩上', hint: '进了是英雄，输了背锅', outcomes: [
        { prob: 0.5, text: '你打满全场砍下高分，系列赛起死回生。', effects: { tempDelta: 2, roleShift: 1 } },
        { prob: 0.5, text: '你太想赢了，动作变形，球队出局。', effects: { tempDelta: -2 } },
      ]},
      { id: 'team', label: '相信队友', hint: '打出团队篮球', outcomes: [
        { prob: 0.6, text: '你的分享球盘活了全队，抢回一场。', effects: { tempDelta: 1 } },
        { prob: 0.4, text: '队友没接住，遗憾出局。', effects: {} },
      ]},
    ],
  },
  locker_room: {
    key: 'locker_room', type: 'career_event', minAge: 18, maxAge: 35, weight: 1,
    title: '更衣室风波',
    text: '输球之后，更衣室里吵起来了。两个主力互相指责，你站在门口。',
    options: [
      { id: 'mediate', label: '站出来调解', hint: '队内声望大涨', outcomes: [
        { prob: 0.7, text: '你几句话把气氛压了下来，全队都服你。', effects: { roleShift: 1 } },
        { prob: 0.3, text: '你劝不动，还把自己卷了进去。', effects: { tempDelta: -1 } },
      ]},
      { id: 'silent', label: '沉默，管好自己', hint: '不掺和', outcomes: [
        { prob: 1, text: '你没说话，专注下一场。', effects: {} },
      ]},
    ],
  },
  fan_signing: {
    key: 'fan_signing', type: 'career_event', minAge: 18, maxAge: 36, weight: 1,
    title: '球迷活动',
    text: '球队安排了一场球迷见面会。队伍排了三条街，有个孩子举着你的球衣。',
    options: [
      { id: 'sign', label: '签到最后一个人', hint: '费体力，好感拉满', outcomes: [
        { prob: 0.85, text: '你签到最后一个人，那个孩子哭了。', effects: { money: 150 } },
        { prob: 0.15, text: '手签抽筋了，训练受了影响。', effects: { tempDelta: -1 } },
      ]},
      { id: 'short', label: '快速走完流程', hint: '省体力', outcomes: [
        { prob: 1, text: '你签完就回了，保存体力。', effects: {} },
      ]},
    ],
  },
  summer_league: {
    key: 'summer_league', type: 'career_event', minAge: 18, maxAge: 24, weight: 1,
    title: '夏季联赛',
    text: '夏季联赛开打，一堆年轻人想在你头上证明自己。',
    options: [
      { id: 'play', label: '上场教育他们', hint: '涨信心，费体力', outcomes: [
        { prob: 0.7, text: '你打爆了对面的新秀，教练点头。', effects: { overallDelta: 1, permanent: true, tempDelta: 1 } },
        { prob: 0.3, text: '被年轻人防住了，有点丢脸。', effects: { tempDelta: -1 } },
      ]},
      { id: 'rest', label: '休息，为新赛季备战', hint: '保留体力', outcomes: [
        { prob: 1, text: '你选择在夏天打磨自己的技术。', effects: {} },
      ]},
    ],
  },
  national_friendly: {
    key: 'national_friendly', type: 'career_event', minAge: 19, maxAge: 33, weight: 1,
    title: '国家队热身赛',
    text: '国家队安排了一轮热身赛，对手是世界排名前十的强队。',
    options: [
      { id: 'full', label: '全力以赴', hint: '磨合阵容，为大赛铺路', outcomes: [
        { prob: 0.65, text: '你打出了身价，教练把你写进首发。', effects: { nationalMult: 1.3 } },
        { prob: 0.35, text: '拼得太凶，伤了手腕。', effects: { injury: '手腕扭伤', tempDelta: -1 } },
      ]},
      { id: 'save', label: '留力', hint: '避免受伤', outcomes: [
        { prob: 1, text: '你收着打，教练有点不满。', effects: { nationalMult: 0.85 } },
      ]},
    ],
  },
  veteran_mentor: {
    key: 'veteran_mentor', type: 'career_event', minAge: 28, maxAge: 38, weight: 1,
    title: '老将带新人',
    text: '队里来了个 19 岁的新秀，教练让你带他。他看你的眼神，像你当年看偶像。',
    options: [
      { id: 'mentor', label: '认真带他', hint: '他成长，你也稳', outcomes: [
        { prob: 0.8, text: '他把你当大哥，你的更衣室地位更稳了。', effects: { roleShift: 1 } },
        { prob: 0.2, text: '他进步太快，开始抢你的时间。', effects: { roleShift: -1 } },
      ]},
      { id: 'own', label: '先管好自己', hint: '专注自己的状态', outcomes: [
        { prob: 0.85, text: '你保住了状态，新人只能自己摸索。', effects: { overallDelta: 1, permanent: true } },
        { prob: 0.15, text: '更衣室有人觉得你自私。', effects: { tempDelta: -1 } },
      ]},
    ],
  },
};

// 决胜事件（特殊屏）
const SHOWDOWNS = {
  last_shot: {
    key: 'last_shot', type: 'showdown', title: '最后一攻',
    text: '时间只剩 3.8 秒，你们落后 2 分。边线球发到你手里，全场都在等你。',
    options: [
      { id: 'three', label: '三分出手', hint: '进了绝杀，不进背锅', successText: '球进！计时器归零，全场炸了！', failText: '偏了。篮板被对方保护住，哨响。' },
      { id: 'drive', label: '突破上篮', hint: '更稳，但需要过防守人', successText: '你晃过防守人，把球放进篮筐，比赛进入加时！', failText: '上篮被盖掉，比赛结束。' },
      { id: 'pass', label: '传给空位队友', hint: '相信队友，进了是配合，不进是遗憾', successText: '队友接球就投，空心入网！', failText: '队友没接稳，时间走完。' },
    ],
  },
  free_throw: {
    key: 'free_throw', type: 'showdown', title: '关键罚球',
    text: '终场前 0.8 秒，你造成犯规，站上罚球线。落后 1 分，两罚全中才能赢。',
    options: [
      { id: 'calm', label: '深呼吸，稳稳投', hint: '按平时节奏来', successText: '两罚全中，你带走了胜利！', failText: '第一罚就偏了，全场叹气。' },
      { id: 'quick', label: '快速出手', hint: '不给对手干扰的机会', successText: '两罚都进，干净利落！', failText: '出手太急，第二罚弹框而出。' },
    ],
  },
  game7: {
    key: 'game7', type: 'showdown', title: '抢七大战',
    text: '系列赛打到第七场，第四节最后 30 秒，你们落后 1 分。球权在你手上。',
    options: [
      { id: 'iso', label: '单打', hint: '把球队扛在自己肩上', successText: '你单打命中，系列赛拿下！', failText: '单打失手，对方反击锁定胜局。' },
      { id: 'screen', label: '叫挡拆', hint: '相信战术', successText: '挡拆打成，你助攻队友命中关键球！', failText: '挡拆被识破，失误，比赛结束。' },
    ],
  },
  qualifier_showdown: {
    key: 'qualifier_showdown', type: 'showdown', title: '落选赛生死战',
    text: '世界杯预选赛最后一轮，你们只有赢下这场才能出线。最后 10 秒，你拿到球。',
    options: [
      { id: 'aggressive', label: '放手一搏', hint: '高风险高回报', successText: '你杀进内线打成 2+1，国家队出线了！', failText: '出手被干扰，出线梦碎。' },
      { id: 'steady', label: '稳扎稳打', hint: '打成功率', successText: '你冷静地制造犯规，两罚全中，出线！', failText: '裁判没吹，时间走完。' },
    ],
  },
  world_cup_showdown: {
    key: 'world_cup_showdown', type: 'showdown', title: '大赛淘汰赛',
    text: '世界杯淘汰赛，最后 5 秒，你们落后 1 分。这是你生涯最重要的一球。',
    options: [
      { id: 'hero', label: '自己来', hint: '英雄或罪人', successText: '你投进了生涯最伟大的一球！', failText: '球弹框而出，你的世界杯结束了。' },
      { id: 'team', label: '相信团队', hint: '打出战术配合', successText: '你送出致命传球，队友绝杀！', failText: '战术被识破，时间耗尽。' },
    ],
  },
};

// 告别风格
const FAREWELL_STYLES = [
  { id: 'ceremony', label: '办告别赛，跟球迷好好告别', hint: '全场为你起立' },
  { id: 'quiet', label: '不办，安静地离开', hint: '低调挂靴' },
  { id: 'social', label: '社交媒体上一句话宣布退役', hint: '全网转发' },
];

const GOODBYE_STYLES = [
  { id: 'press', label: '开个发布会，把话说完', hint: '体面收场' },
  { id: 'quiet', label: '不解释，潇洒地离开', hint: '深藏功与名' },
  { id: 'note', label: '发一条消息就够了', hint: '简单告别' },
];

const WALKAWAY_STYLES = [
  { id: 'press', label: '开个发布会，把话说完', hint: '体面收场' },
  { id: 'quiet', label: '不要仪式，不要致辞。打完这一季就走', hint: '默默离开' },
];
  __M['data.js'] = { APP_TITLE, TAGLINE, MODES, POSITIONS, COUNTRIES, LEAGUES, TEAMS, NCAA_TEAMS, TITLES, UPDATES, EVENTS, SHOWDOWNS, FAREWELL_STYLES, GOODBYE_STYLES, WALKAWAY_STYLES };
  })();

  // ===== build.js (deps: none) =====
  (function () {
// ================= 建球员模块（移植 perfect-player 的"抽属性"流程） =================
// 流程：老虎机随机球队 → 该队抽5张球员卡 → 选球员 → 锁1项属性(跨位置衰减) → 换队循环
// 13项锁满 → 揭晓 OVR/潜力/相似球员

// 13 项属性清单（顺序即锁定顺序的展示顺序）
const ATTR_LIST = [
  { key: 'threePT', zh: '三分', icon: '🎯' },
  { key: 'MID', zh: '中投', icon: '🎯' },
  { key: 'FIN', zh: '终结', icon: '🏀' },
  { key: 'DNK', zh: '扣篮', icon: '💥' },
  { key: 'HAN', zh: '手感', icon: '🤲' },
  { key: 'PAS', zh: '传球', icon: '🛣️' },
  { key: 'PDEF', zh: '外防', icon: '🛡️' },
  { key: 'IDEF', zh: '内防', icon: '🛡️' },
  { key: 'BLK', zh: '盖帽', icon: '🚫' },
  { key: 'REB', zh: '篮板', icon: '📊' },
  { key: 'ATH', zh: '运动', icon: '⚡' },
  { key: 'STR', zh: '力量', icon: '💪' },
  { key: 'CLU', zh: '关键', icon: '🔥' },
];

// 按位置的基础权重（决定 OVR 与成长倾向）
const OVR_WEIGHTS = {
  pg: { threePT: 0.10, MID: 0.10, FIN: 0.08, DNK: 0.03, HAN: 0.14, PAS: 0.14, PDEF: 0.10, IDEF: 0.05, BLK: 0.02, REB: 0.05, ATH: 0.09, STR: 0.04, CLU: 0.06 },
  sg: { threePT: 0.14, MID: 0.12, FIN: 0.10, DNK: 0.05, HAN: 0.10, PAS: 0.09, PDEF: 0.10, IDEF: 0.04, BLK: 0.02, REB: 0.05, ATH: 0.09, STR: 0.04, CLU: 0.06 },
  sf: { threePT: 0.11, MID: 0.10, FIN: 0.11, DNK: 0.08, HAN: 0.09, PAS: 0.08, PDEF: 0.10, IDEF: 0.05, BLK: 0.04, REB: 0.08, ATH: 0.09, STR: 0.05, CLU: 0.05 },
  pf: { threePT: 0.06, MID: 0.08, FIN: 0.13, DNK: 0.09, HAN: 0.06, PAS: 0.06, PDEF: 0.08, IDEF: 0.10, BLK: 0.08, REB: 0.12, ATH: 0.08, STR: 0.08, CLU: 0.04 },
  c:  { threePT: 0.02, MID: 0.06, FIN: 0.15, DNK: 0.10, HAN: 0.05, PAS: 0.05, PDEF: 0.06, IDEF: 0.14, BLK: 0.12, REB: 0.14, ATH: 0.06, STR: 0.08, CLU: 0.03 },
};

// 位置池
const POS_KEYS = ['pg', 'sg', 'sf', 'pf', 'c'];
const POS_ZH = { pg: '控球后卫', sg: '得分后卫', sf: '小前锋', pf: '大前锋', c: '中锋' };

// 加载 NBA 30 队球员池
let POOL = null;
let HIST_POOL = null;
async function loadPool() {
  if (POOL) return POOL;
  // 单文件模式：数据已内嵌在 __POOL_INLINE
  if (typeof window !== 'undefined' && window.__POOL_INLINE) {
    POOL = window.__POOL_INLINE;
    HIST_POOL = window.__HIST_INLINE || [];
    return POOL;
  }
  const res = await fetch('assets/data/playerpool.json');
  POOL = await res.json();
  try {
    const hr = await fetch('assets/data/historicalpool.json');
    HIST_POOL = await hr.json();
  } catch { HIST_POOL = []; }
  return POOL;
}

function poolTeamCount() {
  return POOL ? Object.keys(POOL).length : 30;
}

// 随机抽一支球队（老虎机目标）
function randomTeam() {
  const teams = Object.keys(POOL);
  return teams[Math.floor(Math.random() * teams.length)];
}

// 从一支球队抽 N 张球员卡，20% 概率混入 1 张历史名宿惊喜卡
function drawPlayers(teamKey, n = 5) {
  const players = POOL[teamKey] || [];
  let cards = [];
  if (players.length > 0) {
    const sorted = players.slice().sort((a, b) => b.ovr - a.ovr);
    const maxStart = Math.max(0, sorted.length - n);
    const start = Math.floor(Math.random() * (maxStart + 1));
    cards = sorted.slice(start, start + n);
  }
  // 历史名宿惊喜卡：20% 概率用一张历史球员替换某张卡
  if (HIST_POOL && HIST_POOL.length && Math.random() < 0.2) {
    const h = HIST_POOL[Math.floor(Math.random() * HIST_POOL.length)];
    const slot = Math.floor(Math.random() * cards.length);
    cards[slot] = h;
  }
  return cards;
}

// 跨位置衰减：来源位置属性均值 vs 你的位置属性均值
// 简化实现：不同位置对某些属性有自然倾向，锁定来自其他位置时轻微衰减
function posPenalty(userPos, srcPos) {
  if (userPos === srcPos) return 1;
  const map = {
    pg: { c: 0.86, pf: 0.9, sf: 0.94, sg: 0.98 },
    sg: { c: 0.86, pf: 0.9, sf: 0.95, pg: 0.98 },
    sf: { c: 0.92, pf: 0.95, pg: 0.95, sg: 0.98 },
    pf: { pg: 0.88, sg: 0.9, sf: 0.96, c: 1.0 },
    c:  { pg: 0.84, sg: 0.88, sf: 0.94, pf: 0.98 },
  };
  return (map[userPos] && map[userPos][srcPos]) || 0.92;
}

// 计算某位置下的球员 OVR（13项加权）
function calcOVR(attrs, userPos) {
  const w = OVR_WEIGHTS[userPos] || OVR_WEIGHTS.sf;
  let sum = 0;
  for (const { key } of ATTR_LIST) {
    const entry = attrs[key];
    const v = typeof entry === 'object' && entry !== null ? entry.value : entry;
    sum += (v ?? 60) * (w[key] || 0.05);
  }
  return Math.round(sum);
}

// 解析来源球员的位置（PG/SG → pg）
function srcPosKey(posStr) {
  if (!posStr) return 'sf';
  const first = String(posStr).split('/')[0].trim().toLowerCase();
  return POS_KEYS.includes(first) ? first : 'sf';
}

// 从球员池里找与锁定属性最像的球员（相似球员）
function similarPlayer(attrs, poolPlayers) {
  const all = [];
  for (const team of Object.keys(POOL)) {
    for (const p of POOL[team]) all.push(p);
  }
  let best = null, bestDist = 1e9;
  for (const p of all) {
    let dist = 0;
    for (const { key } of ATTR_LIST) {
      dist += Math.abs((p[key] ?? 60) - (attrs[key] ?? 60));
    }
    if (dist < bestDist) { bestDist = dist; best = p; }
  }
  return best;
}

// 建球员状态机
function createBuildState(userPos) {
  return {
    step: 0,            // 已锁定属性数 0-12
    userPos,
    attrs: {},          // key -> { value, fromPlayer, fromTeam }
    lockedOrder: [],
    currentTeam: null,
    drawn: [],
    selectedPlayer: null,
    rerollsLeft: 3,
    usedTeams: [],
  };
}

function buildProgress(state) {
  return Math.round((state.step / ATTR_LIST.length) * 100);
}

function nextUnlocked(state) {
  const locked = new Set(state.lockedOrder);
  for (const { key } of ATTR_LIST) if (!locked.has(key)) return key;
  return null;
}

function lockAttr(state, attrKey) {
  const p = state.selectedPlayer;
  if (!p) return null;
  const penalty = posPenalty(state.userPos, srcPosKey(p.pos));
  const val = Math.round((p[attrKey] ?? 60) * penalty);
  state.attrs[attrKey] = { value: clampV(val), fromPlayer: p.cname || p.name, fromTeam: state.currentTeam };
  state.lockedOrder.push(attrKey);
  state.step += 1;
  state.selectedPlayer = null;
  state.drawn = [];
  state.currentTeam = null;
  return state.attrs[attrKey];
}

function clampV(v) {
  return Math.max(25, Math.min(99, v));
}

// 揭晓结果
function reveal(state) {
  const ovr = calcOVR(state.attrs, state.userPos);
  const potential = Math.min(99, Math.round(ovr + 8 + Math.random() * 10));
  const similar = similarPlayer(state.attrs, null);
  const record = {};
  for (const { key } of ATTR_LIST) {
    record[key] = state.attrs[key] ? { v: state.attrs[key].value, from: state.attrs[key].fromPlayer, team: state.attrs[key].fromTeam } : null;
  }
  return {
    ovr,
    potential,
    attrs: Object.fromEntries(ATTR_LIST.map(({ key }) => [key, state.attrs[key]?.value ?? 60])),
    similar: similar ? { name: similar.cname || similar.name, team: similarTeamOf(similar) } : null,
    record,
  };
}

function similarTeamOf(p) {
  for (const [team, players] of Object.entries(POOL || {})) {
    if (players.some(x => x.name === p.name)) return team;
  }
  return '';
}
  __M['build.js'] = { ATTR_LIST, POS_KEYS, POS_ZH, loadPool, poolTeamCount, randomTeam, drawPlayers, posPenalty, calcOVR, srcPosKey, similarPlayer, createBuildState, buildProgress, nextUnlocked, lockAttr, reveal };
  })();

  // ===== simEngine.js (deps: none) =====
  (function () {
// ================= 单场模拟引擎（精简版逐回合） =================
// 移植自 NBA-Sim-Web 的核心思路：24秒进攻 → 选进攻/防守人 → 失误/犯规/盖帽/投篮判定 → 篮板 → 球权转换
// 球员属性使用 0-99 的 13 维体系（与 perfect-player 一致）

// 位置简写 → 中文
const POS_EN_ZH = { PG: '控卫', SG: '分卫', SF: '小前', PF: '大前', C: '中锋' };

function mulberry32(a) {
  return function () {
    a |= 0; a = (a + 0x6D2B79F5) | 0;
    let t = Math.imul(a ^ (a >>> 15), 1 | a);
    t = (t + Math.imul(t ^ (t >>> 7), 61 | t)) ^ t;
    return ((t ^ (t >>> 14)) >>> 0) / 4294967296;
  };
}

const clamp = (v, lo, hi) => Math.max(lo, Math.min(hi, v));
const pick = (rng, arr) => arr[Math.floor(rng() * arr.length)];

// ---------- 生成一支球队的名单 ----------
// teamLike: { abbr, zh, strength, league }
// names: 姓名池 [{zh}]；若给了 pool 球员数组则优先使用
function makeRoster(teamLike, names, pool, opts = {}) {
  const roster = [];
  const poolSize = pool ? pool.length : 0;
  const total = opts.total || 10;
  // 首发的5人用球队最强（真实球员按 ovr 排），替补可复用
  let used = new Set();
  for (let i = 0; i < total; i++) {
    const isStarter = i < 5;
    let p = null;
    if (pool && poolSize) {
      // 首发优先强球员，替补随机
      const cands = pool.slice().sort((a, b) => b.ovr - a.ovr);
      const cand = isStarter ? cands[i] : cands[Math.min(5 + Math.floor(Math.random() * 3), cands.length - 1)];
      if (cand && !used.has(cand.name)) {
        used.add(cand.name);
        p = {
          name: cand.cname || cand.name,
          en: cand.name,
          pos: mainPos(cand.pos),
          ovr: cand.ovr,
          attrs: {
            threePT: cand.threePT ?? cand.ovr - 5, MID: cand.MID ?? cand.ovr - 3,
            FIN: cand.FIN ?? cand.ovr - 2, DNK: cand.DNK ?? cand.ovr - 6,
            HAN: cand.HAN ?? cand.ovr - 2, PAS: cand.PAS ?? cand.ovr - 3,
            PDEF: cand.PDEF ?? cand.ovr - 4, IDEF: cand.IDEF ?? cand.ovr - 4,
            BLK: cand.BLK ?? cand.ovr - 8, REB: cand.REB ?? cand.ovr - 5,
            ATH: cand.ATH ?? cand.ovr - 3, STR: cand.STR ?? cand.ovr - 2,
            CLU: cand.CLU ?? cand.ovr - 2,
          },
          real: true,
        };
      }
    }
    if (!p) {
      // 虚拟球员：围绕球队 strength 生成
      const ovr = clamp(teamLike.strength + (isStarter ? 6 : -8) + Math.floor(Math.random() * 7) - 3, 55, 99);
      p = virtualPlayer(names, ovr, isStarter);
    }
    p.starter = isStarter;
    p.team = teamLike.abbr;
    roster.push(p);
  }
  return roster;
}

function virtualPlayer(names, ovr, isStarter) {
  const pos = pick(Math.random, ['PG', 'SG', 'SF', 'PF', 'C']);
  const name = names ? names[Math.floor(Math.random() * names.length)] : '球员' + Math.floor(Math.random() * 99);
  const offBase = ovr;
  return {
    name: name.zh || name,
    en: name.en || name.zh || name,
    pos,
    ovr,
    starter: isStarter,
    attrs: {
      threePT: clamp(offBase - 12 + Math.floor(Math.random() * 12), 40, 99),
      MID: clamp(offBase - 8 + Math.floor(Math.random() * 10), 40, 99),
      FIN: clamp(offBase - 6 + Math.floor(Math.random() * 10), 45, 99),
      DNK: clamp(offBase - 14 + Math.floor(Math.random() * 14), 35, 99),
      HAN: clamp(offBase - 8 + Math.floor(Math.random() * 10), 40, 99),
      PAS: clamp(offBase - 10 + Math.floor(Math.random() * 12), 40, 99),
      PDEF: clamp(offBase - 10 + Math.floor(Math.random() * 10), 40, 99),
      IDEF: clamp(offBase - 10 + Math.floor(Math.random() * 10), 40, 99),
      BLK: clamp(offBase - 18 + Math.floor(Math.random() * 14), 30, 99),
      REB: clamp(offBase - 12 + Math.floor(Math.random() * 12), 35, 99),
      ATH: clamp(offBase - 10 + Math.floor(Math.random() * 12), 40, 99),
      STR: clamp(offBase - 12 + Math.floor(Math.random() * 12), 35, 99),
      CLU: clamp(offBase - 12 + Math.floor(Math.random() * 12), 40, 99),
    },
    real: false,
  };
}

function mainPos(posStr) {
  if (!posStr) return 'SF';
  const first = String(posStr).split('/')[0].trim().toUpperCase();
  return ['PG', 'SG', 'SF', 'PF', 'C'].includes(first) ? first : 'SF';
}

// ---------- 一场比赛 ----------
// opts: { isPlayoff, seed, myPlayerIndex, label }
// myPlayer 会强制加入主队首发并作为核心
function simulateGame(homeTeam, awayTeam, homeRoster, awayRoster, opts = {}) {
  const seed = opts.seed || (Math.random() * 1e9) >>> 0;
  const rng = mulberry32(seed);
  const isPlayoff = !!opts.isPlayoff;
  const home = { team: homeTeam, roster: homeRoster.map(p => ({ ...p, box: emptyBox() })) };
  const away = { team: awayTeam, roster: awayRoster.map(p => ({ ...p, box: emptyBox() })) };

  // 强制插入我方球员到主队
  const me = opts.myPlayer;
  if (me) {
    const playerObj = {
      name: me.name, en: me.name, pos: me.pos, ovr: me.overall,
      starter: true, real: true, isMe: true,
      attrs: me.attrs,
      box: emptyBox(),
    };
    home.roster.splice(opts.myPlayerIndex || 0, 1, playerObj);
  }

  const log = [];           // play-by-play 解说
  const scoreSnap = [];     // [homeScore, awayScore] 每行对应的比分
  const homeScore = { q: [], total: 0, half: 0, tq: [] };
  const awayScore = { q: [], total: 0, half: 0, tq: [] };

  const add = (s) => {
    log.push(s);
    scoreSnap.push([homeScore.total, awayScore.total]);
  };

  const QLEN = isPlayoff ? 12 : 12;
  const homeLabel = `${home.team.abbr || home.team.zh} ${home.team.zh || ''}`;
  const awayLabel = `${away.team.abbr || away.team.zh} ${away.team.zh || ''}`;
  const hShort = home.team.abbr || home.team.zh;
  const aShort = away.team.abbr || away.team.zh;

  add(`🏀 ${isPlayoff ? '🏆 季后赛' : '⚡ 常规赛'} 开球：${awayLabel} @ ${homeLabel}`);

  // ---------- 球员工具 ----------
  const onCourt = (side) => side.roster.filter(p => p.onCourt && p.box.fouls < 6);
  const startersOf = (side) => side.roster.filter(p => p.starter);
  const benchOf = (side) => side.roster.filter(p => !p.starter);

  // 每个 possession 需要的状态
  let poss = { offense: home, defense: away, q: 1, time: QLEN * 60, hasBall: null };

  const boxOf = (p) => p.box;

  function addTo(p, key, v) {
    if (!p) return;
    const b = boxOf(p);
    if (key === 'pts') { b.pts += v; }
    else if (key === 'reb') { b.reb += v; b.totalReb += v; }
    else if (key === 'ast') { b.ast += v; }
    else if (key === 'stl') { b.stl += v; }
    else if (key === 'blk') { b.blk += v; }
    else if (key === 'min') { b.min += v; }
  }

  function pickScorer(side, opp, quarter, timeLeft) {
    // 按综合进攻属性加权；我方核心额外加成
    const players = onCourt(side);
    const weighted = players.map(p => {
      const a = p.attrs;
      const off = (a.FIN * 0.35 + a.MID * 0.25 + a.threePT * 0.2 + a.PAS * 0.1 + a.ATH * 0.1);
      let w = off + p.ovr * 0.6;
      if (p.isMe) w *= 1.9;
      return { p, w };
    });
    const total = weighted.reduce((s, x) => s + x.w, 0);
    let r = rng() * total;
    for (const x of weighted) {
      r -= x.w;
      if (r <= 0) return x.p;
    }
    return weighted[weighted.length - 1].p;
  }

  function pickRebounder(side) {
    const players = onCourt(side);
    const weighted = players.map(p => ({
      p,
      w: p.attrs.REB + (p.pos === 'C' ? 22 : p.pos === 'PF' ? 12 : p.pos === 'SF' ? 4 : 0) + (p.isMe ? 8 : 0),
    }));
    const total = weighted.reduce((s, x) => s + x.w, 0);
    let r = rng() * total;
    for (const x of weighted) { r -= x.w; if (r <= 0) return x.p; }
    return weighted[weighted.length - 1].p;
  }

  function pickDefender(side, scorer) {
    const players = onCourt(side).filter(p => !p.isMe);
    // 尽量同位置
    const same = players.filter(p => p.pos === scorer.pos);
    const pool = same.length ? same : players;
    const total = pool.reduce((s, p) => s + (p.attrs.PDEF + p.attrs.IDEF) / 2, 0);
    let r = rng() * total;
    for (const p of pool) { r -= (p.attrs.PDEF + p.attrs.IDEF) / 2; if (r <= 0) return p; }
    return pool[pool.length - 1];
  }

  function shotTypeFor(p) {
    const pos = p.pos;
    const r = rng();
    if (pos === 'C' || pos === 'PF') {
      // 内线：更多内线出手
      return r < 0.5 ? 'drive' : r < 0.7 ? 'mid' : 'three';
    }
    if (pos === 'PG' || pos === 'SG') {
      return r < 0.3 ? 'three' : r < 0.6 ? 'mid' : 'drive';
    }
    return r < 0.25 ? 'three' : r < 0.55 ? 'mid' : 'drive';
  }

  function judgeShot(offSide, defSide, scorer, defender, q, timeLeft, isCritical) {
    const a = scorer.attrs;
    const d = defender.attrs;
    const type = shotTypeFor(scorer);
    let base = 0, dist = 0;
    let pts = 0;
    if (type === 'three') {
      base = 0.36; pts = 3; dist = 24;
      base += (a.threePT - 75) * 0.002;
    } else if (type === 'mid') {
      base = 0.43; pts = 2; dist = 16;
      base += (a.MID - 72) * 0.0018;
    } else {
      base = 0.5; pts = 2; dist = 8;
      base += (a.FIN - 72) * 0.0018 + a.ATH * 0.0005;
    }
    // 防守影响
    const defP = type === 'three' ? d.PDEF : d.IDEF;
    base -= (defP - 55) * 0.0018;
    // 主客场
    base += (offSide === home ? 0.015 : -0.015);
    // 季后赛更紧张
    if (isPlayoff) base -= 0.02;
    // 关键球加成（CLU）
    if (isCritical) {
      base = base * (0.9 + a.CLU * 0.002);
    }
    base = clamp(base, 0.12, 0.6);
    const made = rng() < base;
    if (made) {
      // 助攻
      const mate = onCourt(offSide).find(p => p !== scorer && !p.isMe);
      const assist = rng() < 0.55 ? mate : null;
      addTo(scorer, 'pts', pts);
      addTo(scorer, 'min', 1);
      if (assist) { addTo(assist, 'ast', 1); }
      let t = '';
      if (type === 'three') t = `${scorer.name} 三分出手，${made ? '空心入网！' : '打铁！'}`;
      else if (type === 'mid') t = `${scorer.name} 中距离，${made ? '命中！' : '偏出！'}`;
      else t = `${scorer.name} 冲击内线，${made ? (a.DNK > 70 ? '隔人暴扣！' : '上篮得手！') : '没进！'}`;
      if (isCritical) t = '🔥 ' + t;
      add(`🎯 ${t}（${offSide.team.abbr} ${pts} 分）`);
      return { made, pts, type };
    } else {
      addTo(scorer, 'min', 1);
      // 篮板
      const offRebP = 0.25 + (a.REB - d.REB) * 0.004 - (isPlayoff ? 0.02 : 0);
      if (rng() < offRebP) {
        const rebounder = pickRebounder(offSide);
        addTo(rebounder, 'reb', 1);
        add(`💥 ${rebounder.name} 抢到进攻篮板！`);
        return { made: false, rebound: true, type };
      } else {
        const rebounder = pickRebounder(defSide);
        addTo(rebounder, 'reb', 1);
        if (type === 'three') add(`❌ ${scorer.name} 三分打铁，${rebounder.name} 收下篮板`);
        else add(`❌ ${scorer.name} 出手不中，${rebounder.name} 抓下防守篮板`);
        return { made: false, rebound: false, type };
      }
    }
  }

  // 罚球
  function freeThrow(offSide, scorer, times) {
    const a = scorer.attrs;
    // 用 CLU + MID 近似罚球
    const ftP = clamp(0.7 + (a.MID - 70) * 0.003 + (a.CLU - 70) * 0.002, 0.5, 0.92);
    let made = 0;
    for (let i = 0; i < times; i++) if (rng() < ftP) made++;
    addTo(scorer, 'pts', made);
    add(`🆓 ${scorer.name} 罚球 ${times} 罚${made}中`);
    return made;
  }

  // 失误/抢断
  function judgeTurnover(offSide, defSide, scorer) {
    const a = scorer.attrs;
    const stlP = clamp(0.03 + (a.HAN - 70) * -0.0012 + (defSide.roster.some(p => p.attrs.STL && p.attrs.STL > 85) ? 0.03 : 0), 0.02, 0.2);
    if (rng() < stlP) {
      const stealer = onCourt(defSide).find(p => p.attrs.STL && p.attrs.STL > 80) || onCourt(defSide)[0];
      addTo(stealer, 'stl', 1);
      add(`⚡ ${stealer.name} 抢断！${scorer.name} 失误了`);
      return { turnover: true, stealer };
    }
    return { turnover: false };
  }

  // 盖帽
  function judgeBlock(offSide, defSide, scorer) {
    const a = scorer.attrs, d = defSide.roster.filter(p => p.onCourt).reduce((m, p) => m + p.attrs.BLK, 0) / 5;
    const blkP = clamp(0.02 + (d - 70) * 0.0008 + (scorer.attrs.DNK > 90 ? 0.01 : 0), 0.01, 0.25);
    if (rng() < blkP) {
      const blocker = onCourt(defSide).find(p => p.attrs.BLK > 75) || onCourt(defSide)[0];
      addTo(blocker, 'blk', 1);
      add(`🚫 ${blocker.name} 送出一记大帽！`);
      return true;
    }
    return false;
  }

  // 犯规
  function judgeFoul(offSide, defSide, scorer, q) {
    const a = scorer.attrs;
    const foulP = clamp(0.04 + (a.ATH - 60) * 0.001 + (offSide === home ? 0.01 : 0.02), 0.03, 0.15);
    if (rng() < foulP) {
      const fouler = onCourt(defSide).find(p => !p.isMe) || onCourt(defSide)[0];
      boxOf(fouler).fouls++;
      add(`🟥 ${fouler.name} 对 ${scorer.name} 犯规`);
      if (boxOf(fouler).fouls >= 6) {
        fouler.onCourt = false;
        add(`⚠️ ${fouler.name} 六犯离场！`);
      }
      return fouler;
    }
    return null;
  }

  // 单回合
  function playPossession() {
    const offSide = poss.offense, defSide = poss.defense;
    const scorer = pickScorer(offSide, defSide, poss.q, poss.time);
    const defender = pickDefender(defSide, scorer);

    const timeLeft = poss.time;
    const isCritical = timeLeft <= 24 && Math.abs(homeScore.total - awayScore.total) <= 3;

    // 时间流逝（24秒进攻时钟）
    const consumed = Math.min(24, 4 + Math.floor(rng() * 18));
    poss.time -= consumed;

    // 失误
    const to = judgeTurnover(offSide, defSide, scorer);
    if (to.turnover) {
      swapBall();
      return;
    }

    // 犯规
    const fouler = judgeFoul(offSide, defSide, scorer, poss.q);
    if (fouler) {
      const isBonus = fouler.box.fouls >= 5; // 简化
      if (rng() < 0.6 || isBonus) {
        const times = isBonus ? 2 : (rng() < 0.3 ? 3 : 2);
        freeThrow(offSide, scorer, times);
        swapBall();
        return;
      } else {
        // 边线球，继续进攻
        if (rng() < 0.4) { swapBall(); }
        return;
      }
    }

    // 投篮
    const shot = judgeShot(offSide, defSide, scorer, defender, poss.q, poss.time, isCritical);
    if (shot.made) {
      const two = shot.pts === 2;
      if (two) {
        if (shot.type === 'drive' && rng() < 0.1) {
          addTo(scorer, 'pts', 1);
          add(`➕ ${scorer.name} 加罚命中`);
        }
      }
      swapBall();
      return;
    }
    if (shot.rebound) {
      // 二次进攻，重新来过（消耗较少时间）
      poss.time -= Math.min(14, 2 + Math.floor(rng() * 10));
      return;
    }
    swapBall();
  }

  function swapBall() {
    poss.offense = poss.offense === home ? away : home;
    poss.defense = poss.offense === home ? away : home;
  }

  function updateScores() {
    homeScore.total = home.roster.reduce((s, p) => s + p.box.pts, 0);
    awayScore.total = away.roster.reduce((s, p) => s + p.box.pts, 0);
  }

  function startQuarter(q) {
    poss.q = q;
    poss.time = QLEN * 60;
    if (q === 1) {
      home.roster.forEach(p => { p.onCourt = p.starter; });
      away.roster.forEach(p => { p.onCourt = p.starter; });
    }
  }

  function endQuarter(q) {
    homeScore.q.push(homeScore.total);
    awayScore.q.push(awayScore.total);
    updateScores();
    add(`⏱️ 第${q}节结束：${hShort} ${homeScore.total} : ${awayScore.total} ${aShort}`);
  }

  // 简化轮换：替补换首发
  function doSubstitutions(side) {
    side.roster.forEach(p => {
      if (p.starter) p.onCourt = true;
      else p.onCourt = false;
    });
  }

  // ---------- 主循环 ----------
  startQuarter(1);
  const maxQ = isPlayoff ? 4 : 4;
  let finalQuarter = maxQ;

  for (let q = 1; q <= maxQ; q++) {
    startQuarter(q);
    doSubstitutions(home); doSubstitutions(away);
    // 我的球员始终在场上（简化）
    if (me) home.roster.forEach(p => { if (p.isMe) p.onCourt = true; });
    let guard = 0;
    while (poss.time > 0 && guard < 400) {
      guard++;
      playPossession();
      if (poss.time <= 0) break;
    }
    endQuarter(q);
  }

  updateScores();

  // 加时
  let ot = 0;
  while (homeScore.total === awayScore.total && ot < 3) {
    ot++;
    startQuarter(4 + ot);
    doSubstitutions(home); doSubstitutions(away);
    if (me) home.roster.forEach(p => { if (p.isMe) p.onCourt = true; });
    let guard = 0;
    while (poss.time > 0 && guard < 100) {
      guard++;
      playPossession();
      if (poss.time <= 0) break;
    }
    updateScores();
    add(`⏱️ 加时${ot}结束：${hShort} ${homeScore.total} : ${awayScore.total} ${aShort}`);
  }
  finalQuarter = 4 + ot;

  const winner = homeScore.total > awayScore.total ? home : away;
  const loser = winner === home ? away : home;
  add(`🏁 终场：${hShort} ${homeScore.total} : ${awayScore.total} ${aShort}，${winner.team.abbr || winner.team.zh} 获胜！`);

  // box score
  const box = { home: boxScoreOf(home), away: boxScoreOf(away) };
  const meBox = home.roster.find(p => p.isMe)?.box || null;

  return {
    home: { abbr: home.team.abbr, zh: home.team.zh },
    away: { abbr: away.team.abbr, zh: away.team.zh },
    homeScore: homeScore.total,
    awayScore: awayScore.total,
    winner: winner === home ? 'home' : 'away',
    winnerName: winner.team.zh || winner.team.abbr,
    loserName: loser.team.zh || loser.team.abbr,
    quarters: [homeScore.q, awayScore.q],
    ot,
    log,
    scoreSnap,
    box,
    meBox,
    myPts: meBox ? meBox.pts : 0,
    myReb: meBox ? meBox.reb : 0,
    myAst: meBox ? meBox.ast : 0,
    myStl: meBox ? meBox.stl : 0,
    myBlk: meBox ? meBox.blk : 0,
  };
}

function emptyBox() {
  return { pts: 0, reb: 0, totalReb: 0, ast: 0, stl: 0, blk: 0, min: 0, fouls: 0 };
}

function boxScoreOf(side) {
  return {
    team: side.team,
    players: side.roster.map(p => ({
      name: p.name,
      isMe: !!p.isMe,
      pos: p.pos,
      pts: p.box.pts,
      reb: p.box.reb,
      ast: p.box.ast,
      stl: p.box.stl,
      blk: p.box.blk,
      fouls: p.box.fouls,
    })),
  };
}
  __M['simEngine.js'] = { POS_EN_ZH, mulberry32, makeRoster, virtualPlayer, mainPos, simulateGame };
  })();

  // ===== engine.js (deps: data.js) =====
  (function () {
const { APP_TITLE, TAGLINE, MODES, POSITIONS, COUNTRIES, LEAGUES, TEAMS, NCAA_TEAMS, EVENTS, SHOWDOWNS, TITLES, FAREWELL_STYLES, GOODBYE_STYLES, WALKAWAY_STYLES } = __BL['data.js'];
// ================= 篮球生涯模拟器 · 引擎 =================


// ---------- RNG ----------
function xmur3(str) {
  let h = 1779033703 ^ str.length;
  for (let i = 0; i < str.length; i++) {
    h = Math.imul(h ^ str.charCodeAt(i), 3432918353);
    h = (h << 13) | (h >>> 19);
  }
  return function () {
    h = Math.imul(h ^ (h >>> 16), 2246822507);
    h = Math.imul(h ^ (h >>> 13), 3266489909);
    return (h ^= h >>> 16) >>> 0;
  };
}

function mulberry32(a) {
  return function () {
    a |= 0; a = (a + 0x6D2B79F5) | 0;
    let t = Math.imul(a ^ (a >>> 15), 1 | a);
    t = (t + Math.imul(t ^ (t >>> 7), 61 | t)) ^ t;
    return ((t ^ (t >>> 14)) >>> 0) / 4294967296;
  };
}

function nextRng(state) {
  const fn = mulberry32(state);
  return { v: fn(), state: (fn() * 4294967296) >>> 0 || 12345 };
}

// 用一次性序列推进：确保每次调用都消耗两个随机数，行为稳定
function roll(rngState, n = 1) {
  let s = rngState;
  const out = [];
  for (let i = 0; i < n; i++) {
    const r = nextRng(s);
    out.push(r.v);
    s = r.state;
  }
  return { v: out, state: s };
}

function chance(rngState, p) {
  const r = roll(rngState);
  return { ok: r.v[0] < p, state: r.state };
}

function pickWeighted(rngState, items, weightFn) {
  const total = items.reduce((s, it) => s + weightFn(it), 0);
  if (total <= 0) return { item: items[0], state: rngState };
  const r = roll(rngState);
  let t = r.v[0] * total;
  for (const it of items) {
    t -= weightFn(it);
    if (t <= 0) return { item: it, state: r.state };
  }
  return { item: items[items.length - 1], state: r.state };
}

function genSeed() {
  const t = Date.now().toString(36);
  const r = Math.random().toString(36).slice(2, 8);
  return `${t}-${r}`;
}

// ---------- 格式化 ----------
function fmtMoney(v) {
  if (v == null || isNaN(v)) return '—';
  if (v >= 10000) {
    const y = v / 10000;
    return (y >= 100 ? Math.round(y) : y.toFixed(y >= 10 ? 1 : 2)) + '亿';
  }
  if (v >= 1000) return Math.round(v) + '万';
  return Math.round(v) + '万';
}

function fmtInt(n) {
  if (n == null) return '0';
  return Math.round(n).toLocaleString('zh-CN');
}

function fmtAvg(n) {
  if (n == null) return '0';
  return n.toFixed(1);
}

function percentileOf(overall) {
  if (overall >= 99) return 99;
  if (overall >= 96) return 97;
  if (overall >= 93) return 94;
  if (overall >= 90) return 88;
  if (overall >= 87) return 80;
  if (overall >= 84) return 70;
  if (overall >= 80) return 56;
  if (overall >= 76) return 40;
  if (overall >= 72) return 25;
  if (overall >= 68) return 13;
  return 5;
}

function clamp(v, lo, hi) {
  return Math.max(lo, Math.min(hi, v));
}

function teamById(id) {
  return TEAMS[id] || null;
}

function leagueById(id) {
  return LEAGUES[id] || null;
}

function countryById(code) {
  return COUNTRIES[code] || null;
}

const ROLE_NAMES = ['边缘轮换', '轮换主力', '绝对主力', '队内核心', '联盟巨星'];
const ROLE_KEYS = ['edge', 'rotation', 'starter', 'star', 'superstar'];

function roleName(role) {
  const i = ROLE_KEYS.indexOf(role);
  return i >= 0 ? ROLE_NAMES[i] : '轮换主力';
}

function roleFactor(role) {
  const map = { edge: 0.5, rotation: 0.72, starter: 0.92, star: 1.1, superstar: 1.26 };
  return map[role] ?? 0.92;
}

// ---------- 生涯创建 ----------
function tournamentSchedule(maxAge = 44) {
  const list = [];
  for (let age = 18; age <= maxAge; age++) {
    if ((age - 18) % 4 === 0) list.push({ type: 'world_cup', age, qualified: null });
    if ((age - 20) % 4 === 0) list.push({ type: 'olympics', age, qualified: null });
    if ((age - 19) % 2 === 0) list.push({ type: 'continental', age, qualified: true });
  }
  return list;
}

function newGame({ seed, mode, name, nationality, position, hand, number, domesticDreamTeamId, foreignDreamTeamId, built }) {
  const seedHash = xmur3(seed)();
  const rng = mulberry32(seedHash);
  let initial, potential;
  if (built) {
    // 建球员模式：初始能力 = 锁定属性的位置加权 OVR，潜力 = 揭晓值
    initial = clamp(Math.round(built.ovr || 60), 55, 99);
    potential = clamp(Math.round(built.potential || 85), 78, 99);
    if (potential < initial) potential = Math.min(99, initial + 6);
  } else {
    initial = clamp(58 + Math.floor(rng() * 9), 55, 66); // 16 岁初始能力
    potential = clamp(78 + Math.floor(rng() * 22) + Math.round((initial - 58) * 0.5), 78, 99);
  }
  const country = COUNTRIES[nationality];
  const pos = POSITIONS[position];
  const league = LEAGUES[country.league] || LEAGUES.eur;
  const player = {
    name: name || '未命名',
    nationality: country.zh,
    nationalityCode: nationality,
    position,
    positionZh: pos.zh,
    hand,
    number,
    age: 16,
    overall: initial,
    potential,
    debutOverall: initial,
    marketValue: marketValueOf(initial, 16, league),
    domesticDreamTeamId: domesticDreamTeamId || null,
    foreignDreamTeamId: foreignDreamTeamId || null,
    attrs: built ? { ...built.attrs } : null,
    buildRecord: built ? built.record : null,
    similarPlayer: built ? built.similar : null,
  };
  const rival = makeRival(player, rng);
  return {
    seed,
    mode,
    step: 0,
    phase: 'career',
    stage: 'youth',
    player,
    currentTeamId: null,
    contractTeamId: null,
    seasons: [],
    season: null,        // 进行中的赛季状态机（逐场模拟）
    seasonHistory: [],   // 赛季结束后的奖项/总结记录
    ncaa: {
      teamId: null,
      year: 0,          // 大一=1, 大二=2 ...
      joinedAt: null,
      stats: null,
    },
    draft: null,        // { teamId, pick, round, year } 选秀结果
    nationalTeamPeriods: [],
    totals: { apps: 0, pts: 0, reb: 0, ast: 0, stl: 0, blk: 0, salary: 0, trophies: [], awards: [] },
    hasNationalTeamCallup: false,
    nationalTeamRetiredAge: undefined,
    tournaments: tournamentSchedule(),
    period: { periodIndex: 0, remaining: 2, run: 0, modifiers: {}, youth: true },
    currentEvent: null,
    lastEventOutcome: null,
    usedEventKeys: [],
    transfers: [],
    farewell: null,
    goodbye: null,
    walkaway: null,
    retirementReason: null,
    endingBeat: null,
    showdownWins: { last_shot: 0, free_throw: 0, game7: 0, qualifier_showdown: 0, world_cup_showdown: 0 },
    legacyLines: [],
    highlights: [],
    pendingGame: null,
    playedGames: [],
    rival,
    rivalSeries: [],
    usedRivalAges: [],
    suspensionSeasonsRemaining: 0,
    suspensionRustRemaining: 0,
    pendingTransfer: null,
    pendingWorldCupUpgrade: null,
    pendingQualifier: null,
    championsFarewellOffered: false,
    noOffersOffered: false,
    lastVoluntaryOfferAge: 0,
    usedTransferOfferAges: [],
    usedUpMoveAges: [],
    rngState: seedHash,
  };
}

function randomCountry(rng) {
  const codes = Object.keys(COUNTRIES);
  return COUNTRIES[codes[Math.floor(rng() * codes.length)]];
}

function makeRival(player, rng) {
  const sameCountry = rng() < 0.45;
  const c = sameCountry ? COUNTRIES[player.nationalityCode] : randomCountry(rng);
  const name = c.surnames[Math.floor(rng() * c.surnames.length)];
  const potential = clamp(player.potential + Math.floor(rng() * 13) - 6, 72, 99);
  const overall = clamp(player.debutOverall + Math.floor(rng() * 7) - 3, 55, 70);
  return {
    name,
    nationality: c.zh,
    position: player.position,
    overall,
    potential,
    peak: overall,
    totals: { pts: 0, champs: 0, mvp: 0, apps: 0 },
  };
}

function rivalSeason(rival, age, rng) {
  if (age <= 21) rival.overall = Math.min(rival.potential, rival.overall + (rng() < 0.75 ? 2 : 1));
  else if (age <= 29) rival.overall = Math.min(rival.potential, rival.overall + (rng() < 0.35 ? 1 : 0));
  else if (age >= 33) rival.overall = Math.max(55, rival.overall - (rng() < 0.65 ? 1 : 0));
  rival.peak = Math.max(rival.peak, rival.overall);
  const w = POSITIONS[rival.position].weight;
  const scale = clamp((rival.overall - 40) / 59, 0.05, 1);
  const ageF = ageFactor(age);
  // 宿敌也按球队角色出场比赛，规则与玩家一致
  const teamStr = 72 + rng() * 14;
  const diff = rival.overall - teamStr;
  const idx = diff <= -8 ? 0 : diff <= -3 ? 1 : diff <= 3 ? 2 : diff <= 8 ? 3 : 4;
  const role = ROLE_KEYS[clamp(idx, 0, 4)];
  const rf = roleFactor(role);
  const gameRange = { edge: [28, 46], rotation: [46, 62], starter: [60, 76], star: [68, 80], superstar: [72, 82] }[role];
  const g = Math.round((gameRange[0] + rng() * (gameRange[1] - gameRange[0])) * ageF);
  const per = (9 + 23 * scale) * w.pts * rf * (0.95 + rng() * 0.1) * ageF;
  const champ = rng() < clamp((rival.overall - 72) * 0.005 + 0.03, 0.02, 0.3);
  const mvp = rival.overall >= 86 && rng() < 0.25;
  const pts = per * g;
  rival.totals.pts += pts;
  rival.totals.apps += g;
  rival.totals.champs += champ ? 1 : 0;
  rival.totals.mvp += mvp ? 1 : 0;
  return { pts, champ, mvp, g };
}

function marketValueOf(overall, age, league) {
  let v;
  if (overall < 75) v = 20 + (overall - 60) * 8;
  else if (overall < 85) v = 150 * Math.pow(1.35, overall - 75);
  else if (overall < 95) v = 3200 * Math.pow(1.30, overall - 85);
  else v = 46000 * Math.pow(1.22, overall - 95);
  const ageFactor =
    age <= 18 ? 0.45 : age <= 20 ? 0.6 : age <= 23 ? 0.82 :
    age <= 30 ? 1.0 : age <= 32 ? 0.82 : age <= 34 ? 0.62 : 0.45;
  const leagueFactor = { 1: 1.12, 2: 1.0, 3: 0.82, 4: 0.62 }[league.tier] ?? 0.8;
  return Math.round(v * ageFactor * leagueFactor);
}

function salaryOf(marketValue, role) {
  const ratio = { edge: 0.10, rotation: 0.14, starter: 0.18, star: 0.20, superstar: 0.22 }[role] ?? 0.15;
  return Math.round(marketValue * ratio);
}

// ---------- 合同系统（现实 NBA 劳资风格） ----------
// 薪资单位：万美元/年；1 美元 ≈ 7 元人民币
// 新秀合同：按选秀顺位分档，4 年递增（0-2 年保障，第 3/4 年球队选项简化处理）
function rookieSalaryByPick(pick) {
  if (pick == null) return 300;
  if (pick <= 1) return 1050;
  if (pick <= 3) return 850;
  if (pick <= 10) return 550;
  if (pick <= 15) return 380;
  if (pick <= 30) return 220;
  return 120; // 次轮
}

// 按能力签合同：顶薪/准顶薪/中产/底薪
function contractTierOf(overall) {
  if (overall >= 94) return 'max';        // 超级顶薪（35%工资帽）
  if (overall >= 90) return 'near_max';   // 准顶薪（30%）
  if (overall >= 86) return 'max_lite';   // 次顶薪（25%）
  if (overall >= 82) return 'big';        // 大合同
  if (overall >= 78) return 'starter';    // 首发级
  if (overall >= 74) return 'rotation';   // 轮换级
  if (overall >= 68) return 'role';       // 角色球员
  return 'minimum';                       // 底薪
}

// 各档次年薪（万美元），对应现实 NBA 金额
const CONTRACT_YEARLY = {
  max: 4800,        // 顶薪 ~5000 万刀
  near_max: 4100,
  max_lite: 3400,
  big: 2600,
  starter: 1800,
  rotation: 1200,
  role: 700,
  minimum: 260,     // 底薪 ~250 万刀
};

// 合同年限
function contractYearsOf(tier, age) {
  if (tier === 'minimum') return 1 + (Math.random() < 0.3 ? 1 : 0);
  if (tier === 'role' || tier === 'rotation') return 2 + Math.floor(Math.random() * 2); // 2-3
  if (tier === 'starter') return 3 + Math.floor(Math.random() * 2);                    // 3-4
  return 4 + Math.floor(Math.random() * 2);                                            // 4-5（大合同）
}

// 签约：新秀 or 自由球员/续约
function makeContract(player, team, league, opts = {}) {
  let years, annualUsd, tier;
  if (opts.draftPick != null) {
    // 新秀合同：按顺位
    tier = 'rookie';
    years = 4;
    annualUsd = rookieSalaryByPick(opts.draftPick);
  } else {
    tier = contractTierOf(player.overall);
    years = contractYearsOf(tier, player.age);
    annualUsd = CONTRACT_YEARLY[tier];
  }
  // 联赛等级影响（NBA 全额，其他联赛打折）
  const tierFactor = { 1: 1.0, 2: 0.6, 3: 0.3, 4: 0.16 }[league && league.tier] ?? 0.2;
  annualUsd = Math.round(annualUsd * tierFactor);
  return {
    teamId: team.id,
    years,
    yearsLeft: years,
    annualUsd,
    tier,
    startAge: player.age,
    extension: false,
    draftPick: opts.draftPick != null ? opts.draftPick : null,
  };
}

// 续约/新签：按当前能力给下一份合同
function renewContract(state, team, league) {
  const role = roleFor(state, team, state.player.age, {});
  const tier = contractTierOf(state.player.overall);
  const annual = CONTRACT_YEARLY[tier];
  const tierFactor = { 1: 1.0, 2: 0.6, 3: 0.3, 4: 0.16 }[league && league.tier] ?? 0.2;
  const years = contractYearsOf(tier, state.player.age);
  state.player.contract = {
    teamId: team.id,
    years,
    yearsLeft: years,
    annualUsd: Math.round(annual * tierFactor),
    tier,
    startAge: state.player.age,
    extension: true,
  };
  return state.player.contract;
}

function contractTierZh(tier) {
  const map = {
    rookie: '新秀合同', max: '超级顶薪', near_max: '顶薪', max_lite: '准顶薪',
    big: '大合同', starter: '首发级合同', rotation: '轮换级合同', role: '角色合同', minimum: '底薪',
  };
  return map[tier] || tier;
}

// ---------- 事件池 ----------
function youthEvents() {
  return Object.values(EVENTS).filter(e => e.minAge <= 17);
}

function poolForAge(state, age) {
  const pool = Object.values(EVENTS).filter(e => {
    if (e.minAge > age || e.maxAge < age) return false;
    if (state.usedEventKeys.includes(e.key)) return false;
    if (e.key === 'national_retire' && state.nationalTeamRetiredAge !== undefined) return false;
    if (e.key === 'home_league_offer' && state.player.nationalityCode === 'US') return false;
    return true;
  });
  // 年龄越大，越是"身体"主题
  const weighted = pool.map(e => ({ e, w: e.weight * (e.key.includes('injury') && age >= 32 ? 2 : 1) }));
  return weighted;
}

function pickEvent(state, age) {
  const pool = poolForAge(state, age);
  if (pool.length === 0) return null;
  const { item, state: s } = pickWeighted(state.rngState, pool, x => x.w);
  const ev = { ...item.e, options: item.e.options.map(o => ({ ...o })) };
  state.rngState = s;
  state.usedEventKeys.push(ev.key);
  if (state.usedEventKeys.length > 12) state.usedEventKeys.shift();
  ev.id = `${ev.key}-${state.step}`;
  return ev;
}

function signContractEvent(state) {
  const country = COUNTRIES[state.player.nationalityCode];
  const leagueId = country.league;
  const candidates = Object.values(TEAMS).filter(t => t.league === leagueId).sort((a, b) => b.strength - a.strength);
  const offers = candidates.slice(0, 4);
  // 若母国联赛没有球队（一般都有），退回欧洲联赛
  const list = offers.length >= 2 ? offers : Object.values(TEAMS).filter(t => t.league === 'eur').slice(0, 4);
  return {
    id: `sign-${state.step}`,
    type: 'sign_contract',
    title: '青训报价',
    text: `18 岁生日那天，${state.player.nationality}的几支球队给你发来了职业合同。你的第一步走哪儿。`,
    options: list.map(t => ({
      id: `sign-${t.id}`,
      label: `${t.zh}（${LEAGUES[t.league].zh}）`,
      hint: `球队强度 ${t.strength}，${t.strength >= 85 ? '豪门' : t.strength >= 78 ? '强队' : '成长空间大'}`,
      teamId: t.id,
    })),
  };
}

// ---------- NCAA 路径 ----------
// 19 岁：是否尝试 NCAA
function ncaaChoiceEvent(state) {
  const p = state.player;
  const overall = p.overall;
  const chance = clamp(0.18 + (overall - 60) * 0.025, 0.12, 0.92);
  return {
    id: `ncaa-choice-${state.step}`,
    type: 'ncaa_choice',
    title: 'NCAA，去还是不去',
    text: `${p.age} 岁，你打完了青训。美国的大学球探来了几趟，对你相当感兴趣——但要先通过 NCAA 的招募评估，才有大学愿意给你奖学金。`,
    offerChance: chance,
    options: [
      { id: 'try', label: '尝试 NCAA 招募', hint: `以你当前的能力，通过招募评估的概率约 ${Math.round(chance * 100)}%` },
      { id: 'pro', label: '直接签职业合同', hint: '留在国内联赛，踏踏实实开始职业生涯' },
    ],
  };
}

// NCAA 招募判定
function tryNcaaRecruit(state) {
  const chance = state.currentEvent ? (state.currentEvent.offerChance || 0.4) : 0.4;
  const rng = mulberry32(state.rngState);
  const ok = rng() < chance;
  state.rngState = (rng() * 4294967296) >>> 0 || 12345;
  if (ok) {
    // 按能力选大学：能力越高进越强的学校
    const teams = Object.values(NCAA_TEAMS).sort((a, b) => b.strength - a.strength);
    const overall = state.player.overall;
    const idx = overall >= 88 ? 0 : overall >= 84 ? 1 : overall >= 80 ? 2 : overall >= 76 ? 3 : overall >= 72 ? 5 : overall >= 68 ? 7 : 10;
    const jitter = Math.floor(rng() * 3) - 1;
    const pickIdx = clamp(idx + jitter, 0, teams.length - 1);
    const team = teams[pickIdx];
    state.stage = 'ncaa';
    state.ncaa.teamId = team.id;
    state.ncaa.year = 1;
    state.ncaa.joinedAt = state.player.age;
    state.ncaa.stats = { g: 0, pts: 0, reb: 0, ast: 0 };
    state.currentTeamId = team.id;
    state.contractTeamId = null;
    state.lastEventOutcome = { eventKey: 'ncaa_choice', optionKey: 'try', text: `你收到了${team.zh}的奖学金邀请，准备报到。`, kind: 'positive' };
    state.legacyLines.push(`你踏进了${team.zh}的训练馆。`);
  } else {
    state.lastEventOutcome = { eventKey: 'ncaa_choice', optionKey: 'try', text: '招募评估没通过，没有大学给你奖学金。', kind: 'negative' };
    // 转回职业签约
    state.stage = 'sign';
    state.currentEvent = signContractEvent(state);
    return { state, screen: 'event' };
  }
  state.step += 1;
  state.currentEvent = null;
  return { state, screen: 'career' };
}

// NCAA 赛季推进
function ncaaStep(state, age, modifiers) {
  const team = NCAA_TEAMS[state.ncaa.teamId] || Object.values(NCAA_TEAMS)[0];
  const year = state.ncaa.year || 1;
  // 赛季状态机
  if (!state.season) {
    beginSeason(state, team, age, modifiers, 'ncaa');
  }
  if (!state.season.done) {
    return { state, screen: 'season' };
  }
  const snapshot = finishSeason(state);
  if (!snapshot) {
    return { state, screen: 'season' };
  }
  state.ncaa.stats = snapshot.stats;
  state.player.age += 1;
  // 大一结束后：选秀决策
  state.currentEvent = draftChoiceEvent(state);
  state.period.run += 1;
  return { state, screen: 'banner', snapshot };
}

function simulateNcaaSeason(state, team, age, year, modifiers) {
  const rng = mulberry32(state.rngState);
  const player = state.player;
  const suspended = modifiers.suspended;
  const injury = modifiers.injury;
  const form = 1 + (modifiers.tempDelta || 0) * 0.045;

  let role = 'starter';
  const rf = roleFactor('star');
  const af = ageFactor(age);
  const scale = clamp((player.overall - 40) / 59, 0.05, 1);
  const w = POSITIONS[player.position].weight;
  const noise = () => 0.88 + rng() * 0.24;
  const injFactor = injury ? 0.72 : 1;
  const base = {
    pts: (12 + 22 * scale) * w.pts * rf * af * injFactor * form * noise(),
    reb: (3 + 9 * scale) * w.reb * rf * af * injFactor * form * noise(),
    ast: (2 + 7 * scale) * w.ast * rf * af * injFactor * form * noise(),
    stl: (0.6 + 2 * scale) * w.stl * rf * af * injFactor * form * noise(),
    blk: (0.4 + 1.8 * scale) * w.blk * rf * af * injFactor * form * noise(),
  };
  const g = suspended ? 0 : Math.round((30 + Math.floor(rng() * 6)) * (injury ? 0.62 : 1));
  const stats = {
    g,
    pts: base.pts * g,
    reb: base.reb * g,
    ast: base.ast * g,
    stl: base.stl * g,
    blk: base.blk * g,
    avg: { pts: base.pts, reb: base.reb, ast: base.ast, stl: base.stl, blk: base.blk },
  };

  // NCAA 战绩
  const teamPower = team.strength * 0.62 + player.overall * 0.38 + roleFactor(role) * 1.5;
  let champP = clamp(0.03 + (teamPower - 72) * 0.005, 0.015, 0.3);
  const r1 = rng();
  let result;
  if (r1 < champP) result = { league: 'champion' };
  else if (r1 < champP + champP * 0.5) result = { league: 'final' };
  else if (r1 < champP * 2) result = { league: 'semis' };
  else if (r1 < champP * 3) result = { league: 'quarters' };
  else if (r1 < champP * 4) result = { league: 'playoffs' };
  else result = { league: 'missed' };

  const awards = [];
  if (!suspended && g > 0) {
    if (player.overall >= 76 && rng() < 0.7) awards.push('allstar');
    if (player.overall >= 82 && rng() < 0.55) awards.push('all_team');
    if (player.overall >= 80 && rng() < 0.5) awards.push('mvp');
    if (result.league === 'champion' && player.overall >= 82 && rng() < 0.6) awards.push('fmvp');
  }
  const trophies = [];
  if (result.league === 'champion') trophies.push('ncaa:champion');

  // 能力成长（NCAA 是涨球期，成长加速）
  const dev = develop(player, age, rng);
  player.overall = dev;

  let highlight = null;
  if (!suspended && g > 0) {
    const a = stats.avg;
    if (a.pts >= 28) highlight = `NCAA 场均 ${a.pts.toFixed(1)} 分`;
    else if (result.league === 'champion' && awards.includes('fmvp')) highlight = 'NCAA总冠军 + 四强赛MVP';
    else if (awards.includes('mvp')) highlight = 'NCAA 全美最佳球员';
    else if (a.pts >= 23 && rng() < 0.2) highlight = 'NCAA 单场轰下 40+ 分';
  }

  const snapshot = {
    age,
    teamId: team.id,
    leagueId: 'ncaa',
    overall: player.overall,
    role,
    suspended: !!suspended,
    stats,
    result,
    cup: null,
    trophies,
    awards,
    salary: 0,
    marketValue: marketValueOf(player.overall, age, LEAGUES.ncaa),
    highlight,
    note: `NCAA 大一赛季`,
    ncaaYear: year,
  };
  state.rngState = (rng() * 4294967296) >>> 0 || 12345;
  return snapshot;
}

// 选秀决策事件
function draftChoiceEvent(state) {
  const p = state.player;
  const year = state.ncaa.year || 1;
  const proj = draftProjection(p.overall);
  return {
    id: `draft-choice-${state.step}`,
    type: 'draft_choice',
    title: '参选 or 留校',
    text: `${p.age} 岁，${NCAA_TEAMS[state.ncaa.teamId].zh}的这个赛季结束了。球探报告把你的选秀前景评为：${proj.zh}（预计 ${proj.pickText}）。`,
    projection: proj,
    options: [
      { id: 'declare', label: '宣布参加 NBA 选秀', hint: `预计${proj.pickText}` },
      { id: 'stay', label: '留校再打一年', hint: '提升顺位，赌一个更好的前途' },
      { id: 'transfer', label: '退出 NCAA 转职业', hint: '签本国联赛合同，开始挣钱' },
    ],
  };
}

function draftProjection(overall) {
  if (overall >= 94) return { zh: '状元热门', pickText: '前 3 顺位', min: 1, max: 3 };
  if (overall >= 89) return { zh: '乐透新秀', pickText: '乐透区（前 14）', min: 1, max: 14 };
  if (overall >= 84) return { zh: '首轮秀', pickText: '首轮中段', min: 8, max: 30 };
  if (overall >= 79) return { zh: '次轮前景', pickText: '次轮（31-45）', min: 31, max: 45 };
  if (overall >= 75) return { zh: '边缘新秀', pickText: '次轮末段', min: 40, max: 55 };
  return { zh: '落选边缘', pickText: '大概率落选', min: 50, max: 60, undraftedHigh: true };
}

function resolveDraftChoice(state, optionId) {
  if (optionId === 'stay') {
    state.ncaa.year += 1;
    if (state.ncaa.year >= 5) {
      // 大五还不走，强制转职业
      state.lastEventOutcome = { eventKey: 'draft_choice', optionKey: 'stay', text: '你在大学待了四年，决定正式进入职业。', kind: 'neutral' };
      state.stage = 'sign';
      state.currentEvent = signContractEvent(state);
      state.step += 1;
      return { state, screen: 'event' };
    }
    state.lastEventOutcome = { eventKey: 'draft_choice', optionKey: 'stay', text: `你决定再留一年，大${numToCn(state.ncaa.year)}继续打 NCAA。`, kind: 'neutral' };
    state.step += 1;
    state.currentEvent = null;
    return { state, screen: 'career' };
  }
  if (optionId === 'transfer') {
    state.lastEventOutcome = { eventKey: 'draft_choice', optionKey: 'transfer', text: '你退出 NCAA，准备签职业合同。', kind: 'neutral' };
    state.stage = 'sign';
    state.currentEvent = signContractEvent(state);
    state.step += 1;
    return { state, screen: 'event' };
  }
  // declare: 参选
  return runDraft(state);
}

function numToCn(n) {
  return ['零', '一', '二', '三', '四', '五'][n] || String(n);
}

function runDraft(state) {
  const p = state.player;
  const proj = draftProjection(p.overall);
  const rng = mulberry32(state.rngState);
  const pick = proj.min + Math.floor(rng() * Math.max(1, proj.max - proj.min + 1));
  state.rngState = (rng() * 4294967296) >>> 0 || 12345;
  const round = pick <= 30 ? 1 : 2;
  // 落选概率：能力越低越可能落选
  const undraftP = p.overall < 73 ? 0.65 : p.overall < 76 ? 0.35 : 0.12;
  const undrafted = pick > 60 || rng() < undraftP;

  if (undrafted) {
    state.draft = { pick: null, round: null, year: state.player.age, undrafted: true };
    state.lastEventOutcome = { eventKey: 'draft_choice', optionKey: 'declare', text: '选秀大会你没被念到名字，落选了。', kind: 'negative' };
    state.legacyLines.push('选秀落选，你低着头离开了小绿屋。');
    state.stage = 'sign';
    state.currentEvent = signContractEvent(state);
    state.step += 1;
    return { state, screen: 'event' };
  }

  // 被选中：顺位越高进越弱的球队（重建队更容易拿高顺位）
  const nbaTeams = Object.values(TEAMS).filter(t => t.league === 'nba').sort((a, b) => a.strength - b.strength);
  // pick 1 给最弱队，pick 末给强队
  const slot = Math.min(nbaTeams.length - 1, Math.floor((pick - 1) / 2));
  const jitter = Math.floor(rng() * 3) - 1;
  const team = nbaTeams[clamp(slot + jitter, 0, nbaTeams.length - 1)];
  state.draft = { teamId: team.id, pick, round, year: state.player.age, undrafted: false };
  state.currentTeamId = team.id;
  state.contractTeamId = team.id;
  state.stage = 'pro';
  state.pendingGame = null;
  state.player.contract = makeContract(state.player, team, LEAGUES.nba, { draftPick: pick });
  state.lastEventOutcome = {
    eventKey: 'draft_choice',
    optionKey: 'declare',
    text: `选秀大会第 ${pick} 顺位，${team.zh} 选中了你！签下新秀合同（${fmtMoney(state.player.contract.annualUsd * 7)}/年 × ${state.player.contract.years} 年）。`,
    kind: 'positive',
  };
  state.legacyLines.push(`第 ${pick} 顺位，你被 ${team.zh} 选中。`);
  state.player.marketValue = marketValueOf(state.player.overall, state.player.age, LEAGUES.nba);
  state.step += 1;
  state.currentEvent = null;
  return { state, screen: 'career' };
}

function transferChooseEvent(state) {
  const cur = state.currentTeamId ? TEAMS[state.currentTeamId] : null;
  const curTier = cur ? LEAGUES[cur.league].tier : 3;
  const candidates = Object.values(TEAMS).filter(t => {
    if (cur && t.id === cur.id) return false;
    const tier = LEAGUES[t.league].tier;
    // 逐级晋升：候选联赛限制在当前 tier 上下各一级（最低 tier1 封顶，不可从低直接跳顶级）
    return tier <= Math.min(5, curTier + 1) && tier >= Math.max(1, curTier - 1);
  });
  const picked = [];
  const rng = mulberry32(state.rngState ^ 0x9e3779b9);
  const sorted = candidates.sort((a, b) => b.strength * (LEAGUES[b.league].tier === 1 ? 1.08 : 1) - a.strength * (LEAGUES[a.league].tier === 1 ? 1.08 : 1));
  const first = sorted[0];
  picked.push(first);
  for (let i = 1; i < sorted.length && picked.length < 3; i++) {
    if (rng() < 0.35 && Math.abs(sorted[i].strength - first.strength) > 3) picked.push(sorted[i]);
  }
  if (picked.length < 2 && sorted.length > 1) picked.push(sorted[1]);
  state.rngState = (mulberry32(state.rngState)(0) * 4294967296) >>> 0 || 12345;
  return {
    id: `transfer-${state.step}`,
    type: 'transfer_choose',
    title: '新东家',
    text: '经纪人摆出几份报价，你看着那些队徽，做了一个决定。',
    options: picked.map(t => ({
      id: `tf-${t.id}`,
      label: `${t.zh}`,
      hint: `${LEAGUES[t.league].zh} · 强度 ${t.strength}${t.id === state.player.foreignDreamTeamId || t.id === state.player.domesticDreamTeamId ? ' · 你的儿时主队！' : ''}`,
      teamId: t.id,
    })),
  };
}

// ---------- 赛季模拟 ----------
function roleFor(state, team, age, modifiers) {
  const diff = state.player.overall - team.strength;
  let idx =
    diff <= -8 ? 0 : diff <= -3 ? 1 : diff <= 3 ? 2 : diff <= 8 ? 3 : 4;
  if (age <= 18) idx = Math.max(0, idx - 1);
  if (age >= 33 && idx >= 2) idx -= 1;
  if (age >= 36 && idx >= 3) idx -= 1;
  idx = clamp(idx + (modifiers.roleShift || 0), 0, 4);
  return ROLE_KEYS[idx];
}

function ageFactor(age) {
  if (age <= 18) return 0.72;
  if (age <= 21) return 0.88;
  if (age <= 29) return 1.0;
  if (age <= 32) return 0.94;
  if (age <= 34) return 0.84;
  if (age <= 36) return 0.7;
  return 0.55;
}

function simulateSeason(state, team, age, modifiers) {
  const rng = mulberry32(state.rngState);
  const player = state.player;
  const league = LEAGUES[team.league];
  const suspended = modifiers.suspended;
  const injury = modifiers.injury;
  const form = 1 + (modifiers.tempDelta || 0) * 0.045;

  let role, g, stats;
  if (suspended) {
    role = 'edge';
    g = 0;
    stats = { g: 0, pts: 0, reb: 0, ast: 0, stl: 0, blk: 0 };
  } else {
    role = roleFor(state, team, age, modifiers);
    const rf = roleFactor(role);
    const af = ageFactor(age);
    const lf = { 1: 1.0, 2: 0.96, 3: 0.9, 4: 0.84 }[league.tier] ?? 0.9;
    const scale = clamp((player.overall - 40) / 59, 0.05, 1);
    const w = POSITIONS[player.position].weight;
    const noise = () => 0.88 + rng() * 0.24;
    const injFactor = injury ? 0.72 : 1;
    const base = {
      pts: (9 + 23 * scale) * w.pts * rf * af * lf * injFactor * form * noise(),
      reb: (2.5 + 8.5 * scale) * w.reb * rf * af * lf * injFactor * form * noise(),
      ast: (1.8 + 7.2 * scale) * w.ast * rf * af * lf * injFactor * form * noise(),
      stl: (0.5 + 2.0 * scale) * w.stl * rf * af * lf * injFactor * form * noise(),
      blk: (0.35 + 1.8 * scale) * w.blk * rf * af * lf * injFactor * form * noise(),
    };
    const gameRange = { edge: [28, 46], rotation: [46, 62], starter: [60, 76], star: [68, 80], superstar: [72, 82] }[role];
    const rawG = gameRange[0] + rng() * (gameRange[1] - gameRange[0]);
    g = Math.round(rawG * (league.games / 82) * (injury ? 0.62 : 1));
    stats = {
      g,
      pts: base.pts * g,
      reb: base.reb * g,
      ast: base.ast * g,
      stl: base.stl * g,
      blk: base.blk * g,
      avg: { pts: base.pts, reb: base.reb, ast: base.ast, stl: base.stl, blk: base.blk },
    };
  }

  // 球队战绩
  const teamPower = team.strength * 0.62 + player.overall * 0.38 + roleFactor(role) * 1.5;
  let champP = clamp(0.03 + (teamPower - 74) * 0.005, 0.015, 0.34);
  champP *= { 1: 1.18, 2: 1.0, 3: 0.9, 4: 0.75 }[league.tier] ?? 0.9;
  if (modifiers.trophyMult) champP *= modifiers.trophyMult;
  if (suspended) champP *= 0.7;

  const r1 = rng();
  let result;
  if (r1 < champP) result = { league: 'champion' };
  else if (r1 < champP + champP * 0.55) result = { league: 'final' };
  else if (r1 < champP * 2.2) result = { league: 'semis' };
  else if (r1 < champP * 3.4) result = { league: 'quarters' };
  else if (r1 < champP * 4.6) result = { league: 'playoffs' };
  else result = { league: 'missed' };

  let cup = null;
  if (league.cup && !suspended) {
    if (rng() < champP * 0.8) cup = 'cup_champion';
    else if (rng() < champP * 0.6) cup = 'cup_final';
  }

  // 个人奖项
  const awards = [];
  if (!suspended && g > 0) {
    if (player.overall >= 78 && rng() < 0.75) awards.push('allstar');
    if (player.overall >= 84 && rng() < 0.65) awards.push('all_team');
    if (player.overall >= 86 && rng() < 0.4) awards.push('mvp');
    if (result.league === 'champion' && player.overall >= 84 && rng() < 0.55) awards.push('fmvp');
    if (player.overall >= 84 && rng() < 0.35 && ['pf', 'c'].includes(player.position)) awards.push('dpoy');
    if (player.overall >= 86 && rng() < 0.45 && ['sg', 'sf', 'pg'].includes(player.position)) awards.push('scoring_title');
    if (player.overall >= 85 && rng() < 0.35 && ['pf', 'c'].includes(player.position)) awards.push('rebound_title');
    if (player.overall >= 85 && rng() < 0.35 && player.position === 'pg') awards.push('assist_title');
  }

  const trophies = [];
  if (result.league === 'champion') trophies.push(`league:${team.league}`);
  if (cup === 'cup_champion') trophies.push(`cup:${team.league}`);

  const marketValue = marketValueOf(player.overall, age, league);
  // 薪资：优先用合同年薪，无合同则按能力估
  let salary;
  if (player.contract && player.contract.teamId === team.id) {
    // 合同按年限递增（新秀合同前几年低，之后涨）
    const contractYears = player.contract.years || 1;
    const progress = contractYears - (player.contract.yearsLeft || 0);
    const grow = 1 + progress * 0.08;
    salary = (player.contract.annualUsd || 1000) * 7 * grow * (modifiers.salaryMult || 1);
  } else {
    const tier = contractTierOf(player.overall);
    const tierFactor = { 1: 1.0, 2: 0.6, 3: 0.3, 4: 0.16 }[league && league.tier] ?? 0.2;
    salary = CONTRACT_YEARLY[tier] * tierFactor * 7 * (modifiers.salaryMult || 1);
  }
  salary = Math.round(salary);

  // 赛季结束后的能力成长/下滑
  const dev = develop(player, age, rng);
  player.overall = dev;

  let highlight = null;
  if (!suspended && g > 0) {
    const a = stats.avg;
    if (a.pts >= 30) highlight = `单赛季场均 ${a.pts.toFixed(1)} 分`;
    else if (a.pts >= 24 && a.reb >= 10 && a.ast >= 10) highlight = `单赛季场均 ${a.pts.toFixed(1)} 分 ${a.reb.toFixed(1)} 板 ${a.ast.toFixed(1)} 助`;
    else if (result.league === 'champion' && awards.includes('fmvp')) highlight = '总冠军 + 总决赛MVP';
    else if (awards.includes('mvp')) highlight = '荣膺常规赛MVP';
    else if (awards.includes('scoring_title')) highlight = '荣膺得分王';
    else if (awards.includes('dpoy')) highlight = '荣膺最佳防守球员';
    else if (awards.includes('allstar_mvp')) highlight = '全明星MVP';
    else if (a.pts >= 23 && rng() < 0.22) highlight = '单场轰下 50+ 分';
  }

  const snapshot = {
    age,
    teamId: team.id,
    leagueId: team.league,
    overall: player.overall,
    role,
    suspended: !!suspended,
    stats,
    result,
    cup,
    trophies,
    awards,
    salary,
    marketValue,
    highlight,
    note: null,
  };

  state.rngState = (rng() * 4294967296) >>> 0 || 12345;
  return { snapshot, rng };
}

// ---------- 属性升级 ----------
const ATTR_KEYS = ['threePT', 'MID', 'FIN', 'DNK', 'HAN', 'PAS', 'PDEF', 'IDEF', 'BLK', 'REB', 'ATH', 'STR', 'CLU'];
function upgradeAttr(state, key, points = 1) {
  const bank = state.player.growthBank || 0;
  if (bank < points) return { ok: false, reason: '点数不足' };
  if (!ATTR_KEYS.includes(key)) return { ok: false, reason: '未知属性' };
  const attrs = state.player.attrs || (state.player.attrs = fallbackAttrs(state.player.overall));
  const cur = attrs[key] || 60;
  if (cur >= 99) return { ok: false, reason: '已到上限' };
  attrs[key] = Math.min(99, cur + points);
  state.player.growthBank = bank - points;
  // 重算 overall：13 项平均 + 少量位置权重
  state.player.overall = Math.min(99, Math.round(
    ATTR_KEYS.reduce((s, k) => s + (attrs[k] || 60), 0) / ATTR_KEYS.length + 6
  ));
  return { ok: true, attrs, overall: state.player.overall, bank: state.player.growthBank };
}

function fallbackAttrs(ovr) {
  const o = ovr || 70;
  return {
    threePT: o - 8, MID: o - 5, FIN: o - 3, DNK: o - 6, HAN: o - 3,
    PAS: o - 4, PDEF: o - 5, IDEF: o - 5, BLK: o - 9, REB: o - 6,
    ATH: o - 3, STR: o - 4, CLU: o - 3,
  };
}

function recalcOverall(state) {
  const attrs = state.player.attrs || fallbackAttrs(state.player.overall);
  state.player.overall = Math.min(99, Math.round(
    ATTR_KEYS.reduce((s, k) => s + (attrs[k] || 60), 0) / ATTR_KEYS.length + 6
  ));
  return state.player.overall;
}

// 属性年成长：年轻时随机提升 2-4 项属性，35 岁后逐年随机减少（随年龄加重）
function annualAttributeGrowth(state, age) {
  const attrs = state.player.attrs || (state.player.attrs = fallbackAttrs(state.player.overall));
  const log = { age, gains: [], losses: [] };
  const rng = mulberry32((state.rngState || 12345) ^ 0xabcd);
  let n, deltaMin, deltaMax;
  if (age <= 24) { n = 3 + Math.floor(rng() * 2); deltaMin = 1; deltaMax = 3; }        // 成长爆发期
  else if (age <= 28) { n = 2 + Math.floor(rng() * 2); deltaMin = 1; deltaMax = 2; }   // 上升期
  else if (age <= 31) { n = 1 + Math.floor(rng() * 2); deltaMin = 1; deltaMax = 2; }   // 巅峰期
  else if (age <= 34) { n = 1 + Math.floor(rng() * 2); deltaMin = 0; deltaMax = 1; }   // 平稳期
  else if (age <= 36) { n = 2 + Math.floor(rng() * 2); deltaMin = -1; deltaMax = -1; } // 下滑开始
  else if (age <= 38) { n = 2 + Math.floor(rng() * 2); deltaMin = -2; deltaMax = -1; }
  else { n = 3 + Math.floor(rng() * 2); deltaMin = -3; deltaMax = -1; }                // 末期
  const keys = ATTR_KEYS.slice().sort(() => rng() - 0.5).slice(0, Math.min(n, ATTR_KEYS.length));
  for (const k of keys) {
    const cur = attrs[k] || 60;
    const delta = deltaMin + Math.floor(rng() * (deltaMax - deltaMin + 1));
    if (delta === 0) continue;
    const next = clamp(cur + delta, 35, 99);
    attrs[k] = next;
    if (delta > 0) log.gains.push(`${k} +${delta}`);
    else log.losses.push(`${k} ${delta}`);
  }
  // 重算 overall，受潜力约束
  const avg = Math.round(ATTR_KEYS.reduce((s, k) => s + (attrs[k] || 60), 0) / ATTR_KEYS.length + 6);
  const cap = Math.max(60, state.player.potential || 95);
  state.player.overall = clamp(Math.min(avg, cap), 40, 99);
  return log;
}

// ---------- 逐场赛季状态机 ----------
// beginSeason: 赛季开始，预生成赛季计划（结果/奖项/场均目标/成长），生成赛程
function beginSeason(state, team, age, modifiers, kind = 'pro') {
  const planFn = kind === 'ncaa'
    ? (s, t, a, m) => simulateNcaaSeason(s, t, a, (s.ncaa && s.ncaa.year) || 1, m)
    : simulateSeason;
  const devBefore = state.player.overall;
  const planResult = planFn(state, team, age, modifiers);
  const snapshot = planResult && planResult.snapshot ? planResult.snapshot : planResult;
  state.player.overall = devBefore; // 赛季中显示赛季初能力，成长等赛季结束再应用
  const league = LEAGUES[team.league];
  const totalGames = league ? league.games : 82;
  // 赛程：同联赛随机对手（排除自己），主客场交替
  const oppPool = kind === 'ncaa' ? Object.values(NCAA_TEAMS).filter(t => t.id !== team.id)
    : Object.values(TEAMS).filter(t => t.league === team.league && t.id !== team.id);
  const schedule = [];
  const rng = mulberry32(state.rngState ^ 0x2f2f);
  for (let i = 0; i < totalGames; i++) {
    const opp = oppPool.length ? oppPool[Math.floor(rng() * oppPool.length)]
      : (kind === 'ncaa' ? Object.values(NCAA_TEAMS)[0] : Object.values(TEAMS).find(t => t.id !== team.id));
    schedule.push({ oppId: opp ? opp.id : null, home: i % 2 === 0 });
  }
  // 为每队生成固定 roster（首次按需缓存）
  const rosters = makeSeasonRosters(state, team, schedule, kind);
  state.season = {
    kind,
    teamId: team.id,
    leagueId: snapshot.leagueId || (LEAGUES[team.league] ? team.league : null),
    totalGames,
    played: 0,
    plan: snapshot,
    baseAvg: snapshot.stats && snapshot.stats.avg ? { ...snapshot.stats.avg } : { pts: 10, reb: 5, ast: 3, stl: 1, blk: 1 },
    myStats: { g: 0, pts: 0, reb: 0, ast: 0, stl: 0, blk: 0 },
    wins: 0,
    losses: 0,
    schedule,
    rosters,
    games: [],
    done: false,
  };
  state.rngState = (rng() * 4294967296) >>> 0 || 12345;
  return state.season;
}

// 生成球队 roster（含我方球员插队 + 对手生成球员），供单场 box 展示
// 真实 NBA 球员池（由 UI 在建球员/加载 pool 时注入）
let __nbaPool = null;
function setNbaPool(pool) {
  __nbaPool = pool || null;
}
function nbaPoolTeam(key) {
  return __nbaPool ? (__nbaPool[key] || null) : null;
}

function makeSeasonRosters(state, team, schedule, kind) {
  const teams = new Map();
  const table = kind === 'ncaa' ? NCAA_TEAMS : TEAMS;
  teams.set(team.id, makeSeasonRoster(state, team, mulberry32(0x1234)));
  for (const g of schedule) {
    if (g.oppId && !teams.has(g.oppId)) {
      const opp = table[g.oppId];
      if (opp) teams.set(g.oppId, makeSeasonRoster(state, opp, mulberry32(0x5678 + g.oppId.length)));
    }
  }
  // 把上一休赛期生成的新秀注入各队 roster（NBA 联赛，每队最多 1 个新秀）
  if (kind === 'pro' && state.rookiePool && state.rookiePool.length) {
    const pool = state.rookiePool.slice();
    for (const [tid, roster] of teams) {
      if (!pool.length) break;
      if (roster.length >= 12) continue;
      const rookie = pool.shift();
      if (rookie && !roster.some(p => p.name === rookie.name)) {
        roster.push({
          name: rookie.name,
          pos: rookie.pos,
          ovr: rookie.ovr,
          starter: false,
          isMe: false,
          rookie: true,
          attrs: rookie.attrs || attrsForOvr(rookie.ovr, rookie.pos),
          g: 0, pts: 0, reb: 0, ast: 0, stl: 0, blk: 0,
        });
      }
    }
    // 已注入的从池中移除（避免每赛季重复）
    state.rookiePool = [];
  }
  return teams;
}

function makeSeasonRoster(state, team, rng) {
  const isMine = team.id === state.currentTeamId;
  const roster = [];
  // NBA 球队优先使用真实球员池
  if (__nbaPool && LEAGUES[team.league] && LEAGUES[team.league].tier === 1) {
    const real = __nbaPool[team.abbr.toUpperCase()] || [];
    if (real && real.length >= 5) {
      const sorted = real.slice().sort((a, b) => b.ovr - a.ovr);
      sorted.slice(0, 9).forEach((p, i) => {
        const pos = mainPosOf(p.pos);
        roster.push({
          name: p.cname || p.name,
          en: p.name,
          pos,
          ovr: p.ovr,
          starter: i < 5,
          isMe: false,
          nbaId: p.nbaId,
          attrs: {
            threePT: p.threePT ?? p.ovr - 5, MID: p.MID ?? p.ovr - 3, FIN: p.FIN ?? p.ovr - 2,
            DNK: p.DNK ?? p.ovr - 6, HAN: p.HAN ?? p.ovr - 2, PAS: p.PAS ?? p.ovr - 3,
            PDEF: p.PDEF ?? p.ovr - 4, IDEF: p.IDEF ?? p.ovr - 4, BLK: p.BLK ?? p.ovr - 8,
            REB: p.REB ?? p.ovr - 5, ATH: p.ATH ?? p.ovr - 3, STR: p.STR ?? p.ovr - 2,
            CLU: p.CLU ?? p.ovr - 2,
          },
          g: 0, pts: 0, reb: 0, ast: 0, stl: 0, blk: 0,
        });
      });
      if (isMine) {
        const mePos = POSITIONS[state.player.position].en;
        roster[0] = {
          name: state.player.name, en: state.player.name, pos: mePos, ovr: state.player.overall,
          starter: true, isMe: true,
          attrs: state.player.attrs || attrsForOvr(state.player.overall, mePos),
          g: 0, pts: 0, reb: 0, ast: 0, stl: 0, blk: 0,
        };
      }
      return roster;
    }
  }

  // 其他球队：生成虚拟球员
  const surnames = surnamesForTeam(team);
  const positions = ['PG', 'SG', 'SF', 'PF', 'C'];
  const usedNames = new Set();
  const n = 9; // 首发5 + 替补4
  for (let i = 0; i < n; i++) {
    const starter = i < 5;
    const base = team.strength + (starter ? 6 : -4);
    const ovr = clamp(Math.round(base + rng() * 10 - 5), 55, 99);
    // 避免同名：姓氏 + 序号（如 张·7 号）
    let name = surnames[Math.floor(rng() * surnames.length)];
    let counter = 0;
    while (usedNames.has(name)) { counter++; name = surnames[Math.floor(rng() * surnames.length)] + (counter > 0 ? `·${counter}` : ''); }
    usedNames.add(name);
    const p = {
      name, pos: positions[i % 5], ovr,
      starter,
      attrs: attrsForOvr(ovr, positions[i % 5]),
      g: 0, pts: 0, reb: 0, ast: 0, stl: 0, blk: 0,
    };
    roster.push(p);
  }
  if (isMine) {
    // 我方球员插入首发
    const mePos = POSITIONS[state.player.position].en;
    roster[0] = {
      name: state.player.name, pos: mePos, ovr: state.player.overall,
      starter: true, isMe: true,
      attrs: state.player.attrs || attrsForOvr(state.player.overall, mePos),
      g: 0, pts: 0, reb: 0, ast: 0, stl: 0, blk: 0,
    };
  }
  return roster;
}

function mainPosOf(posStr) {
  if (!posStr) return 'SF';
  const first = String(posStr).split('/')[0].trim().toUpperCase();
  return ['PG', 'SG', 'SF', 'PF', 'C'].includes(first) ? first : 'SF';
}

// 根据 OVR 和位置生成 13 项能力值
const POS_ATTR_BIAS = {
  PG: { threePT: 6, MID: 5, HAN: 8, PAS: 8, PDEF: 4, ATH: 4 },
  SG: { threePT: 7, MID: 6, FIN: 3, HAN: 4, PAS: 3 },
  SF: { threePT: 2, MID: 3, FIN: 5, DNK: 4, PDEF: 3, ATH: 4 },
  PF: { MID: 1, FIN: 5, DNK: 5, IDEF: 5, BLK: 3, REB: 6, STR: 5 },
  C: { FIN: 5, DNK: 4, IDEF: 7, BLK: 7, REB: 8, STR: 7, ATH: 2 },
};
function attrsForOvr(ovr, pos) {
  const bias = POS_ATTR_BIAS[pos] || {};
  const attrs = {};
  for (const k of ATTR_KEYS) {
    const b = bias[k] || 0;
    attrs[k] = clamp(ovr - 8 + b + Math.floor(Math.random() * 7) - 3, 40, 99);
  }
  return attrs;
}

function surnamesForTeam(team) {
  const lg = LEAGUES[team.league];
  const country = lg ? COUNTRIES[lg.country] : null;
  if (country && country.surnames) return country.surnames;
  return ['张', '王', '李', '刘', '陈', '杨', '赵', '周'];
}

// 模拟接下来的 n 场比赛（可一次多场或逐场），返回新产生的比赛记录
function simNextGames(state, n = 1) {
  const season = state.season;
  if (!season || season.done) return [];
  const out = [];
  const rng = mulberry32(state.rngState);
  const teamTable = season.kind === 'ncaa' ? NCAA_TEAMS : TEAMS;
  for (let k = 0; k < n && !season.done; k++) {
    const idx = season.played;
    const g = season.schedule[idx];
    const opp = g && g.oppId ? teamTable[g.oppId] : null;
    const homeTeam = teamTable[season.teamId];
    if (!homeTeam) { season.done = true; break; }
    const game = simOneLeagueGame(state, season, homeTeam, opp, g ? g.home : true, rng);
    season.games.push(game);
    season.played += 1;
    if (game.win) season.wins += 1; else season.losses += 1;
    season.myStats.g += 1;
    season.myStats.pts += game.my.pts;
    season.myStats.reb += game.my.reb;
    season.myStats.ast += game.my.ast;
    season.myStats.stl += game.my.stl;
    season.myStats.blk += game.my.blk;
    out.push(game);
    // 赛季中交易：低概率触发，涉及 OVR>80 才提示
    if (season.kind === 'pro' && rng() < 0.02) {
      const trade = tryGenerateTrade(state, season, teamTable, rng);
      if (trade) {
        state.pendingTrades = state.pendingTrades || [];
        state.pendingTrades.push(trade);
      }
    }
    if (season.played >= season.totalGames) season.done = true;
  }
  state.rngState = (rng() * 4294967296) >>> 0 || 12345;
  return out;
}

// 生成一笔联盟交易；若涉及能力>80 的球员则返回提示信息
function tryGenerateTrade(state, season, teamTable, rng) {
  const teams = Object.values(TEAMS).filter(t => t.league === season.leagueId);
  if (teams.length < 3) return null;
  const teamA = teams[Math.floor(rng() * teams.length)];
  let teamB = teams[Math.floor(rng() * teams.length)];
  let guard = 0;
  while (teamB.id === teamA.id && guard++ < 10) teamB = teams[Math.floor(rng() * teams.length)];
  // 从两队 roster 各取一个 OVR>75 的球员做交易
  const rosterA = season.rosters.get(teamA.id) || [];
  const rosterB = season.rosters.get(teamB.id) || [];
  const candA = rosterA.filter(p => p.ovr >= 75 && !p.isMe);
  const candB = rosterB.filter(p => p.ovr >= 75 && !p.isMe);
  if (!candA.length || !candB.length) return null;
  const pA = candA[Math.floor(rng() * candA.length)];
  const pB = candB[Math.floor(rng() * candB.length)];
  const notable = Math.max(pA.ovr, pB.ovr) >= 80;
  return {
    age: state.player.age,
    teamA: teamA.zh,
    teamB: teamB.zh,
    fromAToB: pA.name,
    fromBToA: pB.name,
    ovrA: pA.ovr,
    ovrB: pB.ovr,
    notable,
  };
}

// 玩家申请交易：按能力和球队意愿判定
function requestTrade(state) {
  const team = state.currentTeamId ? TEAMS[state.currentTeamId] : null;
  if (!team || !state.season) return { ok: false, reason: '暂无球队' };
  const season = state.season;
  const rng = mulberry32(state.rngState);
  // 能力越强、出场越多越容易达成
  const p = clamp(0.15 + (state.player.overall - 70) * 0.012 + (season.myStats.g >= 40 ? 0.1 : 0), 0.1, 0.6);
  const success = rng() < p;
  state.rngState = (rng() * 4294967296) >>> 0 || 12345;
  if (!success) {
    return { ok: false, reason: 'chance', pct: Math.round(p * 100) };
  }
  // 成功：交易到同联赛另一支球队
  const cands = Object.values(TEAMS).filter(t => t.league === team.league && t.id !== team.id);
  const newTeam = cands[Math.floor(rng() * cands.length)] || team;
  const from = team;
  state.transfers.push({ age: state.player.age, from: from.id, to: newTeam.id });
  state.currentTeamId = newTeam.id;
  state.contractTeamId = newTeam.id;
  state.contract.teamId = newTeam.id;
  state.legacyLines.push(`你申请交易，被送到了${newTeam.zh}。`);
  state.lastEventOutcome = {
    eventKey: 'request_trade',
    optionKey: 'trade',
    text: `交易达成！你被送往 ${newTeam.zh}。`,
    kind: 'positive',
  };
  return { ok: true, team: newTeam, from: from.zh };
}

// 赛季后交易/自由市场（在赛季完全结束后，进入下赛季前提示）
function seasonOffseason(state) {
  const team = state.currentTeamId ? TEAMS[state.currentTeamId] : null;
  if (!team) return null;
  // 赛季后交易窗口：生成几笔潜在交易 + 明星退役信息
  const rng = mulberry32(state.rngState ^ 0x5eed);
  const trades = [];
  const teams = Object.values(TEAMS).filter(t => t.league === team.league && t.id !== team.id);
  const poolTeams = __nbaPool ? Object.values(__nbaPool) : [];
  for (let i = 0; i < 3; i++) {
    const tA = teams[Math.floor(rng() * teams.length)];
    const tB = teams[Math.floor(rng() * teams.length)];
    if (!tA || !tB || tA.id === tB.id) continue;
    let pA = null, pB = null;
    if (poolTeams.length && rng() < 0.6) {
      const tpA = poolTeams[Math.floor(rng() * poolTeams.length)];
      const tpB = poolTeams[Math.floor(rng() * poolTeams.length)];
      if (tpA && tpA.length) pA = tpA[Math.floor(rng() * tpA.length)];
      if (tpB && tpB.length) pB = tpB[Math.floor(rng() * tpB.length)];
    }
    if (!pA || !pB) continue;
    const nameA = pA.cname || pA.name;
    const nameB = pB.cname || pB.name;
    if (Math.max(pA.ovr, pB.ovr) < 78) continue;
    trades.push({
      teamA: tA.zh, teamB: tB.zh,
      fromAToB: nameA, fromBToA: nameB,
      ovrA: pA.ovr, ovrB: pB.ovr,
    });
  }
  // 每年随机生成联盟新秀（选秀结果）
  const rookies = generateDraftClass(state, rng);
  state.rookiePool = state.rookiePool || [];
  state.rookiePool.push(...rookies);
  state.rngState = (rng() * 4294967296) >>> 0 || 12345;
  return { trades, rookies };
}

// 生成一届选秀（新秀池），供休赛期展示与下一赛季 roster 使用
function generateDraftClass(state, rng) {
  const n = 6 + Math.floor(rng() * 5); // 6-10 名新秀
  const allSurnames = [];
  Object.values(COUNTRIES).forEach(c => { (c.surnames || []).forEach(s => allSurnames.push(s)); });
  const posArr = ['PG', 'SG', 'SF', 'PF', 'C'];
  const rookies = [];
  const used = new Set();
  for (let i = 0; i < n; i++) {
    let name = allSurnames[Math.floor(rng() * allSurnames.length)] + '·' + (1 + Math.floor(rng() * 50));
    let g2 = 0;
    while (used.has(name) && g2++ < 20) name = allSurnames[Math.floor(rng() * allSurnames.length)] + '·' + (1 + Math.floor(rng() * 50));
    used.add(name);
    const pos = posArr[Math.floor(rng() * 5)];
    // 新秀能力 60-82，少数天才更高
    const ovr = Math.floor(rng() * 23) + 60;
    const isStar = rng() < 0.12;
    rookies.push({
      name,
      pos,
      ovr: isStar ? Math.min(99, ovr + 8) : ovr,
      draftAge: 19 + Math.floor(rng() * 3),
      pick: i + 1,
      attrs: attrsForOvr(ovr, pos),
      isStar,
    });
  }
  return rookies;
}

// 明星球员退役：赛季末生成退役名单（含真实球员）
function starRetirements(state) {
  if (!__nbaPool) return [];
  const rng = mulberry32(state.rngState ^ 0xdead);
  const retirements = [];
  const poolPlayers = [];
  Object.values(__nbaPool).forEach(arr => { if (Array.isArray(arr)) arr.forEach(p => poolPlayers.push(p)); });
  // 从 33+ 岁高 OVR 真实球员中选 1-3 个退役
  const candidates = poolPlayers.filter(p => p.ovr >= 80 && (p.age || 35) >= 33);
  const n = Math.min(3, 1 + Math.floor(rng() * 3));
  const picked = [];
  for (let i = 0; i < n && candidates.length; i++) {
    const p = candidates[Math.floor(rng() * candidates.length)];
    if (!picked.includes(p.name)) picked.push(p);
  }
  picked.forEach(p => {
    retirements.push({ name: p.cname || p.name, ovr: p.ovr });
  });
  state.rngState = (rng() * 4294967296) >>> 0 || 12345;
  return retirements;
}

// 单场比赛：比分 + 我方 box + 对方 box
function simOneLeagueGame(state, season, homeTeam, awayTeam, isHome, rng) {
  const teamPower = teamPowerFor(season.plan.role, homeTeam, state.player, season.plan);
  const oppPower = awayTeam ? awayTeam.strength * 0.85 + 12 : 75;
  const diff = teamPower - oppPower + (isHome ? 3 : -3) + (rng() * 16 - 8);
  const myScore = Math.max(70, Math.round(102 + diff));
  const oppScore = Math.max(70, Math.round(102 - diff));
  const win = myScore > oppScore;

  // 我的单场数据：围绕 baseAvg 波动（控制幅度，避免单场过高）
  const base = season.baseAvg;
  const boost = state.player.overall >= 85 ? 1.12 : 1;
  const per = (v, min) => Math.max(min, v * boost * (0.62 + rng() * 0.76));
  const my = {
    pts: Math.round(per(base.pts, 0)),
    reb: Math.round(per(base.reb, 1)),
    ast: Math.round(per(base.ast, 0)),
    stl: Math.round(per(base.stl, 0)),
    blk: Math.round(per(base.blk, 0)),
  };

  // 双方 box：按 roster 分配
  const homeRoster = season.rosters.get(homeTeam.id) || [];
  const awayRoster = awayTeam ? (season.rosters.get(awayTeam.id) || []) : [];
  const homeBox = buildBoxScore(homeRoster, myScore, state.player, rng, true);
  const awayBox = buildBoxScore(awayRoster, oppScore, state.player, rng, false);

  const topScorers = [...homeBox, ...awayBox].sort((a, b) => b.pts - a.pts).slice(0, 4)
    .map(p => ({ name: p.name, isMe: p.isMe, pts: p.pts }));

  return {
    g: season.played + 1,
    opp: awayTeam ? awayTeam.zh : '对手',
    oppId: awayTeam ? awayTeam.id : null,
    home: !!isHome,
    myScore,
    oppScore,
    win,
    my,
    homeBox,
    awayBox,
    topScorers,
  };
}

function teamPowerFor(role, team, player, plan) {
  const rf = roleFactor(role || 'starter');
  return team.strength * 0.62 + player.overall * 0.38 + rf * 1.5;
}

// 生成一队 box（分数按 OVR 权重分配，保证总和与比分一致），并累计到 roster 赛季数据
function buildBoxScore(roster, teamScore, me, rng, isHome) {
  if (!roster || !roster.length) return [];
  const totalOvr = roster.reduce((s, p) => s + p.ovr, 0);
  const box = roster.map(p => {
    const share = totalOvr > 0 ? p.ovr / totalOvr : 1 / roster.length;
    let pts = Math.round(teamScore * share * (0.8 + rng() * 0.4));
    if (p.isMe) pts = me.overall >= 88 ? Math.round(teamScore * 0.3) : Math.round(teamScore * 0.22);
    const ovrF = p.ovr / 99;
    const line = {
      name: p.name, pos: p.pos, ovr: p.ovr, starter: p.starter, isMe: p.isMe,
      pts,
      reb: Math.round(ovrF * 8 + rng() * 6),
      ast: Math.round(ovrF * 4 + rng() * 4),
      stl: Math.round(ovrF * 2),
      blk: Math.round(ovrF * 2),
    };
    return line;
  });
  // 修正总和到 teamScore
  const diff2 = teamScore - box.reduce((s, p) => s + p.pts, 0);
  if (box.length && Math.abs(diff2) > 0) {
    box[0].pts = Math.max(0, box[0].pts + diff2);
  }
  // 累计到 roster 赛季数据（队友/对手场均）
  box.forEach((b, i) => {
    const p = roster[i];
    if (!p) return;
    p.g += 1;
    p.pts += b.pts;
    p.reb += b.reb;
    p.ast += b.ast;
    p.stl += b.stl;
    p.blk += b.blk;
  });
  return box;
}

// ---------- 季后赛状态机 ----------
function beginPlayoffs(state, team) {
  const season = state.season;
  const winPct = season.totalGames > 0 ? season.wins / season.totalGames : 0;
  // 对手：同联赛按 strength 排名，取比我们强或接近的对手
  const league = LEAGUES[team.league];
  const opps = Object.values(TEAMS).filter(t => t.league === team.league && t.id !== team.id)
    .sort((a, b) => b.strength - a.strength);
  const opp = opps[0] || Object.values(TEAMS).find(t => t.id !== team.id);
  const rounds = [];
  // 简化：3 轮（首轮/分区决赛/总决赛），每轮对手强度递增
  for (let r = 0; r < 3; r++) {
    const idx = Math.min(r + (winPct >= 0.7 ? r : r + 1), opps.length - 1);
    rounds.push(opps[idx] ? opps[idx].id : opp.id);
  }
  state.playoffs = {
    round: 0,
    rounds,
    myWins: 0,
    oppWins: 0,
    currentOppId: rounds[0],
    games: [],
    done: false,
    eliminated: false,
  };
  return state.playoffs;
}

function simPlayoffGames(state, n = 1) {
  const po = state.playoffs;
  if (!po || po.done) return [];
  const out = [];
  const season = state.season;
  const teamTable = season && season.kind === 'ncaa' ? NCAA_TEAMS : TEAMS;
  const myTeam = teamTable[season.teamId];
  const rng = mulberry32(state.rngState ^ 0x7a7a);
  for (let k = 0; k < n && !po.done; k++) {
    const opp = teamTable[po.currentOppId];
    const game = simOneLeagueGame(state, season, myTeam, opp, k % 2 === 0, rng);
    po.games.push(game);
    if (game.win) po.myWins += 1; else po.oppWins += 1;
    out.push(game);
    if (po.myWins >= 4 || po.oppWins >= 4) {
      // 一轮结束
      if (po.myWins >= 4) {
        po.round += 1;
        po.myWins = 0;
        po.oppWins = 0;
        if (po.round >= po.rounds.length) {
          po.done = true; // 夺冠
          po.champion = true;
        } else {
          po.currentOppId = po.rounds[po.round];
        }
      } else {
        po.done = true;
        po.eliminated = true;
      }
    }
  }
  state.rngState = (rng() * 4294967296) >>> 0 || 12345;
  return out;
}

// 赛季结束：应用成长，返回最终 snapshot（stats 用实际累计场均）
// 获取我队 roster（含场均数据），供队友面板/box 展示
function getMyRoster(state) {
  const season = state.season;
  if (!season) return [];
  const teamTable = season.kind === 'ncaa' ? NCAA_TEAMS : TEAMS;
  const team = teamTable[season.teamId];
  if (!team) return [];
  const roster = season.rosters.get(season.teamId) || [];
  return roster.map(p => {
    const g = p.g || 0;
    return {
      name: p.name,
      pos: p.pos,
      ovr: p.ovr,
      starter: p.starter,
      isMe: p.isMe,
      attrs: p.attrs || {},
      g,
      avg: g > 0 ? {
        pts: (p.pts / g).toFixed(1),
        reb: (p.reb / g).toFixed(1),
        ast: (p.ast / g).toFixed(1),
        stl: (p.stl / g).toFixed(1),
        blk: (p.blk / g).toFixed(1),
      } : { pts: '0', reb: '0', ast: '0', stl: '0', blk: '0' },
    };
  });
}

function finishSeason(state) {
  const season = state.season;
  if (!season) return null;
  const plan = season.plan;
  const t = state.totals;
  const g = season.myStats.g;
  const avg = g > 0 ? {
    pts: season.myStats.pts / g,
    reb: season.myStats.reb / g,
    ast: season.myStats.ast / g,
    stl: season.myStats.stl / g,
    blk: season.myStats.blk / g,
  } : { pts: 0, reb: 0, ast: 0, stl: 0, blk: 0 };
  const stats = {
    g,
    pts: season.myStats.pts,
    reb: season.myStats.reb,
    ast: season.myStats.ast,
    stl: season.myStats.stl,
    blk: season.myStats.blk,
    avg,
  };
  // 属性年成长：年轻时随机提升，35 岁后逐年随机减少直至退役
  const growthLog = annualAttributeGrowth(state, plan.age);
  state.lastGrowthLog = growthLog;
  // 结果：季后赛打完则用真实结果；NCAA/未进季后赛保留 plan.result（或修正）
  const winPct = season.totalGames > 0 ? season.wins / season.totalGames : 0;
  let result = plan.result;
  const po = state.playoffs;
  if (po && po.done) {
    if (po.champion) result = { league: 'champion' };
    else if (po.eliminated) {
      // 在某一轮被淘汰：round 0=首轮出局, 1=半决赛, 2=总决赛
      const rnd = po.round;
      result = { league: rnd === 0 ? 'quarters' : rnd === 1 ? 'semis' : 'final' };
    }
  } else if (season.kind === 'pro' && !po) {
    // 职业未进季后赛：missed
    result = { league: 'missed' };
  }
  // 保留 NCAA 的 plan.result 不动
  // 高光：用实际场均判断
  let highlight = plan.highlight;
  if (g > 0) {
    if (avg.pts >= 30) highlight = `单赛季场均 ${avg.pts.toFixed(1)} 分`;
    else if (avg.pts >= 24 && avg.reb >= 10 && avg.ast >= 10) highlight = `单赛季场均 ${avg.pts.toFixed(1)} 分 ${avg.reb.toFixed(1)} 板 ${avg.ast.toFixed(1)} 助`;
  }
  // 冠军奖杯按结果
  const trophies = [...plan.trophies];
  if (result.league === 'champion' && !trophies.includes(`league:${plan.leagueId}`)) {
    trophies.push(`league:${plan.leagueId}`);
  }
  // 决赛冠军额外 FMVP
  const awards = [...plan.awards];
  if (result.league === 'champion' && !awards.includes('fmvp') && state.player.overall >= 84) {
    awards.push('fmvp');
  }
  // 完整奖项：基于赛季数据判定（MVP/得分王/最佳阵容等）
  const fullAwards = computeSeasonAwards(state, season, avg, result, awards);
  // 成长点数：赛季表现越好点数越多，年轻球员成长快（适量）
  const perf = g > 0 ? (avg.pts + avg.reb * 0.6 + avg.ast * 0.6) : 0;
  const ageBonus = state.player.age <= 23 ? 3 : state.player.age <= 27 ? 2 : state.player.age <= 31 ? 1 : 0;
  const awardBonus = Math.min(3, fullAwards.length);
  const winBonus = season.wins >= season.totalGames * 0.6 ? 2 : 0;
  const growthPoints = Math.max(0, 2 + Math.floor(perf / 18) + ageBonus + awardBonus + winBonus);
  state.lastGrowthPoints = growthPoints;
  state.player.growthBank = (state.player.growthBank || 0) + growthPoints;

  const snapshot = {
    ...plan,
    stats,
    overall: state.player.overall,
    highlight,
    result,
    trophies,
    awards: fullAwards,
    growthPoints,
    record: { wins: season.wins, losses: season.losses },
  };
  // 赛季总结（供 UI 展示）
  state.lastSeasonSummary = {
    age: plan.age,
    teamId: season.teamId,
    leagueId: plan.leagueId,
    kind: season.kind,
    record: `${season.wins}-${season.losses}`,
    wins: season.wins,
    losses: season.losses,
    totalGames: season.totalGames,
    stats: stats.avg,
    result,
    resultZh: resultZh(result && result.league, LEAGUES[plan.leagueId]),
    awards: fullAwards,
    awardsDetail: fullAwards,
    leagueAwards: generateLeagueAwards(state),
    nbaJump: nbaJumpInfo(state),
    highlight,
    growthPoints,
    growthBank: state.player.growthBank,
    growthLog: state.lastGrowthLog || null,
    salary: snapshot.salary,
    contractLeft: state.player.contract ? state.player.contract.yearsLeft : null,
  };
  // 记录赛季总结
  state.seasonHistory = state.seasonHistory || [];
  state.seasonHistory.push({
    age: plan.age,
    teamId: season.teamId,
    leagueId: plan.leagueId,
    record: `${season.wins}-${season.losses}`,
    stats: stats.avg,
    result,
    awards: fullAwards,
  });
  state.season = null;
  state.playoffs = null;
  return snapshot;
}

// 完整赛季奖项：基于赛季数据判定
function computeSeasonAwards(state, season, avg, result, base) {
  const awards = [];
  const pos = state.player.position;
  const ovr = state.player.overall;
  const g = season.myStats.g;
  const winPct = season.totalGames > 0 ? season.wins / season.totalGames : 0;
  const a = avg;
  const isRookie = (state.seasons || []).filter(s => !s.youth && s.teamId).length <= 1;

  // 数据王
  if (a.pts >= 26) awards.push('scoring_title');
  if (a.reb >= 11) awards.push('rebound_title');
  if (a.ast >= 9) awards.push('assist_title');
  if (a.stl >= 2.2) awards.push('steal_title');
  if (a.blk >= 2.2 && ['pf', 'c'].includes(pos)) awards.push('block_title');
  // MVP：数据 + 战绩
  if (ovr >= 86 && a.pts >= 24 && winPct >= 0.55) awards.push('mvp');
  else if (a.pts >= 28 && winPct >= 0.6) awards.push('mvp');
  // 最佳阵容：一阵/二阵/三阵（按综合表现）
  const allNbaScore = a.pts + a.reb * 0.8 + a.ast * 0.8 + a.stl * 1.5 + a.blk * 1.5;
  if (allNbaScore >= 42) awards.push('all_nba_1');
  else if (allNbaScore >= 34) awards.push('all_nba_2');
  else if (allNbaScore >= 27) awards.push('all_nba_3');
  // 最佳防守阵容
  const defScore = a.stl + a.blk + (ovr - 60) * 0.15;
  if (defScore >= 6 && g >= 40) {
    if (defScore >= 8) awards.push('all_def_1');
    else awards.push('all_def_2');
  }
  // DPOY
  if ((a.stl + a.blk) >= 4 && ovr >= 82 && g >= 40) awards.push('dpoy');
  // 最佳新秀
  if (isRookie && ovr >= 76 && a.pts >= 14) awards.push('roty');
  // 最佳第六人（替补且数据好）
  const role = season.plan.role;
  if (['edge', 'rotation'].includes(role) && a.pts >= 16) awards.push('sixth_man');
  // 进步最快（overall 增长大）
  const prev = state.player.debutOverall || ovr;
  if (ovr - prev >= 6) awards.push('mip');
  // 全明星 / 全明星MVP
  if (ovr >= 80 && a.pts >= 18) awards.push('allstar');
  if (ovr >= 90 && a.pts >= 28 && season.kind === 'pro') awards.push('allstar_mvp');
  // 总决赛相关
  if (result.league === 'champion' && ovr >= 84) awards.push('fmvp');
  if (result.league === 'champion') awards.push('champion_ring');

  // 去重：去掉 base 中已被新体系替代的旧奖项，合并其余
  const redundant = new Set(['all_team', 'allstar']);
  const merged = new Set();
  base.forEach(a => { if (!redundant.has(a)) merged.add(a); });
  awards.forEach(a => merged.add(a));
  return [...merged];
}

// 职业赛季完整结束（进季后赛流程后调用）：finishSeason + 清空季后赛，返回 snapshot 供结算
function finishCareerSeason(state, team, age, modifiers) {
  return finishSeason(state);
}

// 生成完整奖项名单：从联盟各队 roster 评选出每个奖项的获奖者（含能力值/数据），我参与竞争
function generateLeagueAwards(state) {
  const season = state.season;
  if (!season) return [];
  const myTeamId = season.teamId;
  const teamTable = season.kind === 'ncaa' ? NCAA_TEAMS : TEAMS;
  const myAvg = season.myStats.g > 0 ? {
    pts: season.myStats.pts / season.myStats.g,
    reb: season.myStats.reb / season.myStats.g,
    ast: season.myStats.ast / season.myStats.g,
    stl: season.myStats.stl / season.myStats.g,
    blk: season.myStats.blk / season.myStats.g,
  } : { pts: 0, reb: 0, ast: 0, stl: 0, blk: 0 };
  const winPct = season.totalGames > 0 ? season.wins / season.totalGames : 0;

  // 收集联盟球员池：各队 roster 中打过比赛的球员（每队最多 5 人）
  const pool = [];
  const usedNames = new Set();

  // NBA 联赛：只使用真实球员池（30 队现役真实球员），按 OVR 估算场均参与评选
  const isNba = season.kind === 'pro' && LEAGUES[season.leagueId] && LEAGUES[season.leagueId].tier === 1;
  const nbaRealUsed = isNba && !!__nbaPool;
  if (nbaRealUsed) {
    for (const [abbr, players] of Object.entries(__nbaPool)) {
      if (!Array.isArray(players) || !players.length) continue;
      const sorted = players.slice().sort((a, b) => b.ovr - a.ovr).slice(0, 5);
      for (const p of sorted) {
        if (!p || usedNames.has(p.cname || p.name)) continue;
        usedNames.add(p.cname || p.name);
        const pos = mainPosOf(p.pos);
        const ovr = p.ovr;
        const posBias = POS_ATTR_BIAS[pos] || {};
        // 按 OVR 和属性估算场均数据（贴近真实 NBA 水平）
        const isBig = ['PF', 'C'].includes(pos);
        const isPG = pos === 'PG';
        const avg = {
          pts: Math.max(4, (ovr - 55) * 0.72 + (p.FIN || posBias.FIN || 60) * 0.06 + Math.random() * 2.5),
          reb: Math.max(1.5, (isBig ? 4 : 1.5) + (p.REB || posBias.REB || 40) * 0.12 + Math.random() * 1.5),
          ast: Math.max(0.8, (isPG ? 3 : 0.8) + (p.PAS || posBias.PAS || 40) * 0.1 + Math.random() * 1.5),
          stl: Math.max(0.2, (p.PDEF || 50) * 0.018 + Math.random() * 0.7),
          blk: Math.max(0.1, (p.BLK || 40) * 0.024 + (isBig ? Math.random() * 1.2 : Math.random() * 0.3)),
        };
        pool.push({ name: p.cname || p.name, pos, ovr, teamId: null, avg, g: 55 + Math.floor(Math.random() * 20), real: true });
      }
    }
  }

  if (!nbaRealUsed) {
  for (const [tid, roster] of season.rosters) {
    if (!roster) continue;
    const teamPlayers = [];
    roster.forEach(p => {
      if (p.isMe) return;
      const gp = p.g || 1;
      const avg = {
        pts: p.pts / gp,
        reb: p.reb / gp,
        ast: p.ast / gp,
        stl: p.stl / gp,
        blk: p.blk / gp,
      };
      teamPlayers.push({ name: p.name, pos: p.pos, ovr: p.ovr, teamId: tid, avg, g: p.g });
    });
    // 每队取 OVR 最高的 5 人
    teamPlayers.sort((a, b) => b.ovr - a.ovr).slice(0, 5).forEach(p => { pool.push(p); usedNames.add(p.name); });
  }
  // 补足到 60 人：生成虚拟联盟明星（能力分布广，名字用各国姓氏）
  const allSurnames = [];
  Object.values(COUNTRIES).forEach(c => { (c.surnames || []).forEach(s => allSurnames.push(s)); });
  const posArr = ['PG', 'SG', 'SF', 'PF', 'C'];
  // 先造 6 个全明星级大牌（OVR 88-96，数据亮眼）
  const starNames = ['亚历克斯·王', '乔丹·李', '科比·张', '詹姆斯·陈', '杜兰特·刘', '库里·杨'];
  const starPos = ['SG', 'PG', 'SF', 'PF', 'C', 'PG'];
  for (let i = 0; i < starNames.length; i++) {
    const ovr = 88 + Math.floor(Math.random() * 9);
    const pos = starPos[i];
    const posBias = POS_ATTR_BIAS[pos] || {};
    pool.push({
      name: starNames[i], pos, ovr, teamId: null, g: 60 + Math.floor(Math.random() * 15),
      avg: {
        pts: ovr * 0.42 + (posBias.FIN || 0) * 0.5 + Math.random() * 4,
        reb: ovr * 0.14 + (posBias.REB || 0) * 0.6 + Math.random() * 2,
        ast: ovr * 0.1 + (posBias.PAS || 0) * 0.7 + Math.random() * 3,
        stl: 1 + Math.random() * 1.4,
        blk: 0.4 + (['PF', 'C'].includes(pos) ? Math.random() * 1.8 : Math.random() * 0.6),
      },
    });
  }
  let guard2 = 0;
  while (pool.length < 60 && guard2 < 200) {
    guard2++;
    const surname = allSurnames[Math.floor(Math.random() * allSurnames.length)] || '球员';
    const name = surname + '·' + (1 + Math.floor(Math.random() * 99));
    if (usedNames.has(name)) continue;
    usedNames.add(name);
    const ovr = 60 + Math.floor(Math.random() * 36); // 60-95
    const pos = posArr[Math.floor(Math.random() * 5)];
    const posBias = POS_ATTR_BIAS[pos] || {};
    const ptsBase = ovr * 0.28 + (posBias.FIN || 0) * 0.4;
    const rebBase = ovr * 0.12 + (posBias.REB || 0) * 0.6;
    const astBase = ovr * 0.08 + (posBias.PAS || 0) * 0.7;
    const avg = {
      pts: ptsBase + Math.random() * 5,
      reb: rebBase + Math.random() * 3,
      ast: astBase + Math.random() * 3,
      stl: 0.4 + Math.random() * 1.6,
      blk: 0.3 + (['PF', 'C'].includes(pos) ? Math.random() * 2 : Math.random() * 0.8),
    };
    pool.push({ name, pos, ovr, teamId: null, avg, g: 55 + Math.floor(Math.random() * 20) });
  }
  }

  // 我的球员加入竞争
  const me = {
    name: state.player.name, pos: POSITIONS[state.player.position].en, ovr: state.player.overall,
    teamId: myTeamId, avg: myAvg, g: season.myStats.g, isMe: true,
  };

  const pickBest = (arr, key) => {
    if (!arr.length) return null;
    return arr.sort((a, b) => b[key] - a[key])[0];
  };
  const makeLine = (p) => ({
    name: p.name, pos: p.pos, ovr: p.ovr, isMe: p.isMe,
    teamId: p.teamId, g: p.g,
    pts: p.avg.pts.toFixed(1), reb: p.avg.reb.toFixed(1), ast: p.avg.ast.toFixed(1),
    stl: p.avg.stl.toFixed(1), blk: p.avg.blk.toFixed(1),
  });

  const candidates = [...pool, me];
  const awards = [];
  const alreadyWon = new Set();

  // 数据王（取最高，一个球员最多拿一个数据王，含我）
  const statKings = [
    { key: 'scoring_title', zh: '得分王', get: p => p.avg.pts, min: 20 },
    { key: 'rebound_title', zh: '篮板王', get: p => p.avg.reb, min: 8 },
    { key: 'assist_title', zh: '助攻王', get: p => p.avg.ast, min: 6 },
    { key: 'steal_title', zh: '抢断王', get: p => p.avg.stl, min: 1.5 },
    { key: 'block_title', zh: '盖帽王', get: p => p.avg.blk, min: 1.2 },
  ];
  for (const sk of statKings) {
    const elig = candidates.filter(p => p.g >= 30 && !alreadyWon.has(p.name));
    if (!elig.length) continue;
    const best = elig.slice().sort((a, b) => sk.get(b) - sk.get(a))[0];
    if (best && sk.get(best) >= sk.min) {
      awards.push({ key: sk.key, zh: sk.zh, winner: makeLine(best) });
      alreadyWon.add(best.name);
    }
  }

  // MVP：综合数据 + 战绩
  const mvpScore = (p) => p.avg.pts + p.avg.reb * 0.7 + p.avg.ast * 0.7 + p.avg.stl + p.avg.blk + p.ovr * 0.15 + (p.isMe ? winPct * 40 : 20);
  const mvp = candidates.slice().sort((a, b) => mvpScore(b) - mvpScore(a))[0];
  awards.push({ key: 'mvp', zh: '常规赛MVP', winner: makeLine(mvp) });

  // 最佳阵容 1/2/3 阵（按综合得分取前 15，分三阵）
  const allNbaScore = (p) => p.avg.pts + p.avg.reb * 0.8 + p.avg.ast * 0.8 + p.avg.stl * 1.2 + p.avg.blk * 1.2 + p.ovr * 0.2;
  const ranked = candidates.slice().sort((a, b) => allNbaScore(b) - allNbaScore(a)).slice(0, 15);
  const teamNames = ['最佳阵容一阵', '最佳阵容二阵', '最佳阵容三阵'];
  const teamKeys = ['all_nba_1', 'all_nba_2', 'all_nba_3'];
  for (let i = 0; i < 3 && ranked.length >= (i + 1) * 5; i++) {
    const five = ranked.slice(i * 5, (i + 1) * 5);
    awards.push({ key: teamKeys[i], zh: teamNames[i], team: five.map(makeLine) });
  }

  // 最佳防守阵容（按防守数据）
  const defScore = (p) => p.avg.stl + p.avg.blk + p.ovr * 0.05;
  const defRanked = candidates.slice().sort((a, b) => defScore(b) - defScore(a)).slice(0, 10);
  awards.push({ key: 'all_def_1', zh: '最佳防守一阵', team: defRanked.slice(0, 5).map(makeLine) });
  awards.push({ key: 'all_def_2', zh: '最佳防守二阵', team: defRanked.slice(5, 10).map(makeLine) });

  // DPOY
  const dpoy = candidates.slice().sort((a, b) => (b.avg.stl + b.avg.blk) - (a.avg.stl + a.avg.blk))[0];
  awards.push({ key: 'dpoy', zh: '最佳防守球员', winner: makeLine(dpoy) });

  // 最佳新秀（生涯第一季的我，或其他低龄球员）
  const isRookie = (state.seasons || []).filter(s => !s.youth && s.teamId).length <= 1;
  if (isRookie) {
    awards.push({ key: 'roty', zh: '最佳新秀', winner: makeLine(me) });
  } else {
    const rookiePool = candidates.filter(p => !p.isMe);
    awards.push({ key: 'roty', zh: '最佳新秀', winner: makeLine(pickBest(rookiePool, p => p.ovr)) });
  }

  // 最佳第六人 / 进步最快 / FMVP（赛季结果相关）
  const champion = state.lastSeasonSummary && state.lastSeasonSummary.result && state.lastSeasonSummary.result.league === 'champion';
  if (season.kind === 'pro' && champion) {
    const fmvp = pickBest([...candidates].filter(p => p.isMe || p.ovr >= 85), p => p.avg.pts);
    awards.push({ key: 'fmvp', zh: '总决赛MVP', winner: makeLine(fmvp) });
  }

  return awards;
}

// 冲击 NBA：赛季结束后不在 NBA 时可用，按能力判定成功率
function nbaJumpInfo(state) {
  const team = state.currentTeamId ? TEAMS[state.currentTeamId] : null;
  if (!team || team.league === 'nba') return { available: false };
  const ovr = state.player.overall;
  // 能力越高成功率越高
  let p = clamp((ovr - 78) * 0.022, 0.06, 0.85);
  return { available: true, chance: p, pct: Math.round(p * 100) };
}

function tryNBAJump(state) {
  const info = nbaJumpInfo(state);
  if (!info.available) return { ok: false, reason: '你已在 NBA' };
  const rng = mulberry32(state.rngState);
  const success = rng() < info.chance;
  state.rngState = (rng() * 4294967296) >>> 0 || 12345;
  if (success) {
    const nbaTeams = Object.values(TEAMS).filter(t => t.league === 'nba').sort((a, b) => a.strength - b.strength);
    const team = nbaTeams[Math.floor(rng() * nbaTeams.length)];
    const from = TEAMS[state.currentTeamId];
    state.transfers.push({ age: state.player.age, from: from ? from.id : null, to: team.id });
    state.currentTeamId = team.id;
    state.contractTeamId = team.id;
    state.stage = 'pro';
    state.player.contract = makeContract(state.player, team, LEAGUES.nba);
    state.legacyLines.push(`你凭一己之力打进了 NBA，加盟${team.zh}。`);
    state.lastEventOutcome = { eventKey: 'nba_jump', optionKey: 'jump', text: `冲击成功！你得到了 ${team.zh} 的合同，正式登陆 NBA。`, kind: 'positive' };
    return { ok: true, team, contract: state.player.contract };
  } else {
    state.lastEventOutcome = { eventKey: 'nba_jump', optionKey: 'jump', text: '冲击 NBA 失败，各队暂时没有你的位置。继续在联赛里证明自己吧。', kind: 'negative' };
    return { ok: false, reason: 'chance_failed' };
  }
}

function develop(player, age, rng) {
  const p = player.potential;
  let target;
  if (age <= 29) target = p;
  else if (age <= 31) target = p - 1;
  else if (age <= 33) target = p - 3;
  else if (age <= 35) target = p - 6;
  else target = p - 11;
  let delta = 0;
  if (player.overall < target) {
    const speed = age <= 21 ? 2.5 : age <= 26 ? 1.35 : age <= 29 ? 0.8 : 0.4;
    delta = speed * (0.75 + rng() * 0.7);
  } else if (player.overall > target) {
    delta = -(age >= 31 ? 0.7 + rng() * 0.9 : 0.25 + rng() * 0.3);
  }
  return clamp(Math.round(player.overall + delta), 40, 99);
}

// ---------- 国家队 ----------
function nationalThreshold(country) {
  return { 1: 70, 2: 75, 3: 78, 4: 80 }[country.tier] ?? 78;
}

function qualifyProb(state, country) {
  const boost = (state.player.overall - 80) * 0.012;
  let p = country.qualify + boost;
  if (state.nationalTeamRetiredAge !== undefined) p = 0;
  return clamp(p, 0.05, 0.97);
}

function tournamentResult(state, country, tournament, modifiers, rng) {
  const overall = state.player.overall;
  const calledUp = overall >= nationalThreshold(country) && state.nationalTeamRetiredAge === undefined;
  if (!calledUp) return { called: false, result: 'not_called', stats: null };
  let q = qualifyProb(state, country);
  if (modifiers.nationalMult) q *= modifiers.nationalMult;
  if (tournament.type === 'continental') q = 1;
  if (state.pendingQualifier && state.pendingQualifier.age === tournament.age) {
    q = state.pendingQualifier.won ? 1 : 0;
  }
  const qualified = tournament.qualified === false ? false : rng() < q;
  if (!qualified) return { called: true, result: 'not_qualified', stats: null };

  // 深度
  const power = country.tier === 1 ? 78 : country.tier === 2 ? 70 : country.tier === 3 ? 62 : 56;
  const pBonus = (overall - 80) * 0.45;
  const strength = power + pBonus + (modifiers.nationalMult ? (modifiers.nationalMult - 1) * 15 : 0);
  const championP = clamp((strength - 68) * 0.006 + 0.015, 0.008, 0.32);
  const semisP = championP * 2.2;
  const quartersP = championP * 4;
  const r = rng();
  let result;
  if (r < championP) result = 'champion';
  else if (r < championP + semisP) result = 'semis';
  else if (r < championP + semisP + quartersP) result = 'quarters';
  else result = 'group';

  // 半决赛触发的决战
  if (result === 'semis' && overall >= 84 && (tournament.type === 'world_cup' || tournament.type === 'olympics')) {
    state.pendingWorldCupUpgrade = { age: tournament.age, type: tournament.type };
  }

  const depthGames = { champion: 8, semis: 7, quarters: 6, group: 4 }[result] ?? 5;
  const rf = roleFactor('superstar') * 0.95;
  const scale = clamp((overall - 40) / 59, 0.1, 1);
  const w = POSITIONS[state.player.position].weight;
  const pg = {
    pts: (10 + 24 * scale) * w.pts * rf,
    reb: (3 + 9 * scale) * w.reb * rf,
    ast: (2 + 7.5 * scale) * w.ast * rf,
  };
  const stats = {
    g: depthGames,
    pts: pg.pts * depthGames,
    reb: pg.reb * depthGames,
    ast: pg.ast * depthGames,
    avg: pg,
  };
  const awards = [];
  if (result === 'champion' && overall >= 87) awards.push('tournament_mvp');
  if ((result === 'champion' || result === 'semis') && overall >= 84) awards.push('tournament_all_team');
  return { called: true, result, stats, awards };
}

// ---------- 推进 ----------
function step(state) {
  if (state.phase !== 'career') return { state, screen: 'summary' };
  if (state.currentEvent) return { state, screen: 'event' };

  // 合同到期：优先弹出续约/自由市场事件
  if (state.pendingContractExpiry) {
    state.currentEvent = contractExpiryEvent(state);
    return { state, screen: 'event' };
  }

  const age = state.player.age;
  const modifiers = state.period.modifiers || {};

  if (state.stage === 'youth') {
    const snapshot = {
      age,
      teamId: null,
      leagueId: null,
      overall: state.player.overall,
      role: 'starter',
      stats: { g: 0, pts: 0, reb: 0, ast: 0, stl: 0, blk: 0 },
      result: { league: 'youth' },
      trophies: [],
      awards: [],
      salary: 0,
      marketValue: state.player.marketValue,
      youth: true,
    };
    state.seasons.push(snapshot);
    state.period.run += 1;
    state.player.age += 1;
    if (state.period.run === 1) {
      // 16 岁青训赛季后有一次青训决策
      state.currentEvent = pickEvent(state, 16);
    } else if (state.period.run === 2) {
      // 17 岁青训赛季后有一次青训决策
      state.currentEvent = pickEvent(state, 17);
    } else {
      // 18 岁青训结束，19 岁面临 NCAA 尝试
      state.stage = 'ncaa_choice';
      state.currentEvent = ncaaChoiceEvent(state);
      state.period = { periodIndex: 1, remaining: MODES[state.mode].periodLength, run: 0, modifiers: {} };
    }
    return { state, screen: 'banner', snapshot };
  }

  if (state.stage === 'ncaa') {
    return ncaaStep(state, age, modifiers);
  }

  // 职业赛季
  const team = TEAMS[state.currentTeamId];
  if (!team) {
    state.phase = 'summary';
    state.retirementReason = state.retirementReason || 'no_offers';
    return { state, screen: 'summary' };
  }

  const suspended = state.suspensionSeasonsRemaining > 0;
  const seasonModifiers = {
    ...modifiers,
    suspended: suspended ? '禁赛' : modifiers.injury,
    roleShift: modifiers.roleShift || (state.suspensionRustRemaining > 0 ? -1 : 0),
  };

  // 赛季状态机：赛季中返回 season 屏幕，由玩家逐场模拟
  if (!state.season) {
    beginSeason(state, team, age, seasonModifiers, 'pro');
  }
  if (!state.season.done) {
    return { state, screen: 'season' };
  }

  // 常规赛打完 → 决定是否进季后赛
  if (!state.playoffs) {
    // 胜率足够则进季后赛，否则直接结束
    const winPct = state.season.totalGames > 0 ? state.season.wins / state.season.totalGames : 0;
    const made = winPct >= 0.45 && state.season.losses < state.season.totalGames;
    if (!made) {
      // 未进季后赛，直接结算
      const snapshot = finishCareerSeason(state, team, age, seasonModifiers);
      if (!snapshot) {
        state.phase = 'summary';
        state.retirementReason = state.retirementReason || 'no_offers';
        return { state, screen: 'summary' };
      }
      return settleSeasonResult(state, snapshot, team, age, seasonModifiers, suspended);
    }
    beginPlayoffs(state, team);
    return { state, screen: 'playoffs' };
  }

  // 季后赛进行中
  if (!state.playoffs.done) {
    return { state, screen: 'playoffs' };
  }

  // 季后赛打完 → 结算赛季
  const snapshot = finishCareerSeason(state, team, age, seasonModifiers);
  if (!snapshot) {
    state.phase = 'summary';
    state.retirementReason = state.retirementReason || 'no_offers';
    return { state, screen: 'summary' };
  }
  return settleSeasonResult(state, snapshot, team, age, seasonModifiers, suspended);
}

// 赛季结算：累加 totals、宿敌、国家队、事件调度等，返回 banner
function settleSeasonResult(state, snapshot, team, age, modifiers, suspended) {
  const t = state.totals;
  t.apps += snapshot.stats.g;
  t.pts += snapshot.stats.pts;
  t.reb += snapshot.stats.reb;
  t.ast += snapshot.stats.ast;
  t.stl += snapshot.stats.stl;
  t.blk += snapshot.stats.blk;
  t.salary += snapshot.salary;
  snapshot.trophies.forEach(tid => t.trophies.push(tid));
  snapshot.awards.forEach(aid => t.awards.push(aid));
  if (snapshot.highlight) state.highlights.push({ age, text: snapshot.highlight });

  // 宿敌对位
  if (state.rival) {
    const rr = mulberry32(state.rngState);
    const rs = rivalSeason(state.rival, age, rr);
    state.rngState = (rr() * 4294967296) >>> 0 || 12345;
    state.rivalSeries.push({
      age,
      my: snapshot.stats.pts,
      rival: rs.pts,
      myChamp: snapshot.result.league === 'champion',
      rivalChamp: rs.champ,
      myMvp: snapshot.awards.includes('mvp'),
      rivalMvp: rs.mvp,
    });
  }

  if (suspended) {
    state.suspensionSeasonsRemaining -= 1;
    if (state.suspensionSeasonsRemaining === 0) state.suspensionRustRemaining = 2;
  } else if (state.suspensionRustRemaining > 0) {
    state.suspensionRustRemaining -= 1;
  }

  // 国家队征召与大赛
  const country = COUNTRIES[state.player.nationalityCode];
  const tournament = state.tournaments.find(t2 => t2.age === age);
  if (tournament && state.nationalTeamRetiredAge === undefined) {
    const tr = tournamentResult(state, country, tournament, modifiers, mulberry32(state.rngState ^ 0x51ab));
    if (tr.called) {
      state.hasNationalTeamCallup = true;
      if (tr.result !== 'not_called' && tr.result !== 'not_qualified') {
        const nat = {
          age,
          type: tournament.type,
          result: tr.result,
          stats: tr.stats,
          awards: tr.awards || [],
        };
        state.nationalTeamPeriods.push(nat);
        if (tr.stats) {
          t.apps += tr.stats.g;
          t.pts += tr.stats.pts;
          t.reb += tr.stats.reb;
          t.ast += tr.stats.ast;
        }
        if (tr.result === 'champion') {
          t.trophies.push(tournament.type === 'world_cup' ? 'world_cup' : tournament.type === 'olympics' ? 'olympics' : 'continental');
          if (tournament.type === 'world_cup') state.legacyLines.push('你把世界杯举过头顶。');
          if (tournament.type === 'olympics') state.legacyLines.push('奥运金牌挂在了你的脖子上。');
          if (tournament.type === 'continental') state.legacyLines.push(`你率${country.zh}拿下了洲际冠军。`);
        }
        tr.awards?.forEach(a => t.awards.push(a));
        snapshot.national = nat;
      } else {
        snapshot.national = { age, type: tournament.type, result: tr.result, stats: null, awards: [] };
      }
      tournament.qualified = tr.result === 'champion' || tr.result === 'semis' || tr.result === 'quarters' || tr.result === 'group';
    } else {
      snapshot.national = { age, type: tournament.type, result: 'not_called', stats: null, awards: [] };
    }
    if (state.pendingWorldCupUpgrade && state.pendingWorldCupUpgrade.age === age) {
      state.currentEvent = showdownEvent('world_cup_showdown', state, tournament);
    }
  }

  // 预选赛生死战（大赛前一年触发）
  const upcoming = state.tournaments.find(t2 => t2.age === age + 1 && (t2.type === 'world_cup' || t2.type === 'olympics') && t2.qualified === null);
  if (upcoming && state.nationalTeamRetiredAge === undefined && state.player.overall >= nationalThreshold(country) - 3) {
    const qp = qualifyProb(state, country) * (modifiers.nationalMult || 1);
    if (qp > 0.32 && qp < 0.72) {
      state.currentEvent = showdownEvent('qualifier_showdown', state, upcoming);
      state.pendingQualifier = { age: upcoming.age, won: null };
    }
  }

  state.seasons.push(snapshot);

  // 打到半决赛/决赛：可进入季后赛关键战单场模拟（玩家可模拟或跳过）
  if (!state.currentEvent && ['semis', 'final'].includes(snapshot.result.league)) {
    state.pendingGame = {
      type: 'playoff_key',
      stage: snapshot.result.league,
      seasonIndex: state.seasons.length - 1,
      opponentId: pickPlayoffOpponent(state, team, LEAGUES[team.league]),
    };
  }

  // 打到半决赛/决赛：可能触发抢七决战
  if (!state.currentEvent && ['semis', 'final'].includes(snapshot.result.league)) {
    const r = roll(state.rngState);
    state.rngState = r.state;
    if (r.v[0] < 0.45) state.currentEvent = showdownEvent('game7', state, null);
  }

  state.period.run += 1;
  state.player.age += 1;
  const league = LEAGUES[team.league];
  state.player.marketValue = marketValueOf(state.player.overall, state.player.age, league);

  const periodEnd = state.period.run >= state.period.remaining;
  if (periodEnd && !state.currentEvent) {
    // 阶段结束 → 安排下一个事件
    state.period = { ...state.period, run: 0, modifiers: {} };
    const ev = scheduleNextEvent(state);
    if (ev) state.currentEvent = ev;
  }

  // 合同推进：每赛季消耗一年，到期触发续约/自由市场事件
  if (state.player.contract) {
    state.player.contract.yearsLeft = Math.max(0, (state.player.contract.yearsLeft || 1) - 1);
    if (state.player.contract.yearsLeft <= 0) {
      // 合同到期：标记，由 step 弹出续约/自由市场事件
      state.pendingContractExpiry = {
        teamId: team.id,
        leagueId: team.league,
      };
    }
  } else if (state.player.age >= 20) {
    renewContract(state, team, LEAGUES[team.league]);
  }

  return { state, screen: 'banner', snapshot };
}

// 合同到期事件：续约 or 试水自由市场（可转会其他球队）
function contractExpiryEvent(state) {
  const team = state.currentTeamId ? TEAMS[state.currentTeamId] : null;
  const league = team ? LEAGUES[team.league] : null;
  const tier = contractTierOf(state.player.overall);
  const annual = CONTRACT_YEARLY[tier];
  const tierFactor = { 1: 1.0, 2: 0.6, 3: 0.3, 4: 0.16 }[league && league.tier] ?? 0.2;
  const years = contractYearsOf(tier, state.player.age);
  // 生成自由市场报价（其他球队）
  const offers = [];
  const cands = Object.values(TEAMS)
    .filter(t => t.league === team.league && t.id !== team.id)
    .sort((a, b) => b.strength - a.strength)
    .slice(0, 3);
  cands.forEach((t, i) => {
    const offerTier = state.player.overall >= 88 && t.strength >= 82 ? 'near_max' : tier;
    const offerAnnual = CONTRACT_YEARLY[offerTier];
    offers.push({
      id: `fa-${t.id}`,
      teamId: t.id,
      label: `${t.zh}（${LEAGUES[t.league].zh}）`,
      hint: `${contractTierZh(offerTier)} · ${fmtMoney(offerAnnual * tierFactor * 7)}/年 × ${years} 年`,
      annual: Math.round(offerAnnual * tierFactor),
      years,
    });
  });
  return {
    id: `contract-expiry-${state.step}`,
    type: 'contract_expiry',
    title: '合同到期',
    text: `赛季结束，你和${team.zh}的合同到期了。${contractTierZh(tier)}：${fmtMoney(annual * tierFactor * 7)}/年。是续约还是试试自由市场？`,
    options: [
      { id: 'renew', label: `与${team.zh}续约`, hint: `${contractTierZh(tier)} · ${fmtMoney(annual * tierFactor * 7)}/年 × ${years} 年` },
      ...offers.map(o => ({ id: `fa-${o.teamId}`, label: `自由市场 · 加盟 ${o.label}`, hint: o.hint, teamId: o.teamId })),
    ],
    offers,
    renewAnnual: Math.round(annual * tierFactor),
    renewYears: years,
    renewTier: tier,
  };
}

function resolveContractExpiry(state, optionId, offerTeamId) {
  state.pendingContractExpiry = null;
  const team = state.currentTeamId ? TEAMS[state.currentTeamId] : null;
  const league = team ? LEAGUES[team.league] : null;
  if (optionId === 'renew' || !offerTeamId) {
    renewContract(state, team, league);
    state.lastEventOutcome = {
      eventKey: 'contract_renew',
      optionKey: 'renew',
      text: `你和${team.zh}续约 ${state.player.contract.years} 年（${contractTierZh(state.player.contract.tier)} · ${fmtMoney(state.player.contract.annualUsd * 7)}/年）。`,
      kind: 'positive',
    };
  } else {
    const newTeam = TEAMS[offerTeamId];
    const from = team;
    state.transfers.push({ age: state.player.age, from: from ? from.id : null, to: newTeam.id });
    state.currentTeamId = newTeam.id;
    state.contractTeamId = newTeam.id;
    state.player.contract = makeContract(state.player, newTeam, league);
    state.legacyLines.push(`自由市场，你选择加盟${newTeam.zh}。`);
    state.lastEventOutcome = {
      eventKey: 'contract_renew',
      optionKey: 'fa',
      text: `你以自由球员身份加盟 ${newTeam.zh}，签下 ${contractTierZh(state.player.contract.tier)}（${fmtMoney(state.player.contract.annualUsd * 7)}/年 × ${state.player.contract.years} 年）。`,
      kind: 'positive',
    };
  }
  state.step += 1;
  return { state, screen: 'career' };
}

function scheduleNextEvent(state) {
  const age = state.player.age;
  // 40 岁必退
  if (age >= 40) {
    state.retirementReason = 'age';
    return retirementStyleEvent(state, 'age');
  }
  // 36 岁以上：无人问津
  if (age >= 36 && !state.noOffersOffered) {
    state.noOffersOffered = true;
    return { ...EVENTS.no_offers, id: `no-offers-${state.step}`, options: EVENTS.no_offers.options.map(o => ({ ...o })) };
  }
  // 32 岁以上：偶尔可以自己选择挂靴
  if (age >= 32 && age - state.lastVoluntaryOfferAge >= 4) {
    state.lastVoluntaryOfferAge = age;
    return {
      id: `retire-voluntary-${state.step}`,
      type: 'voluntary_retire',
      title: '退役的决定',
      text: `${age} 岁，那份合同在桌上摆了两个星期，你始终没签。剩下的只是怎么让人知道。`,
      options: [
        { id: 'keep', label: '再打两年', hint: '还想留在场上' },
        { id: 'retire', label: '就此结束职业生涯', hint: '自己决定退役' },
      ],
    };
  }
  // 决胜时刻
  {
    const r = roll(state.rngState);
    state.rngState = r.state;
    if (r.v[0] < 0.16) return showdownEvent('last_shot', state, null);
    if (r.v[0] < 0.26) return showdownEvent('free_throw', state, null);
  }
  // 22/26/30 岁左右常有转会报价
  if (state.rival && [20, 24, 28, 32].includes(age) && !state.usedRivalAges.includes(age)) {
    state.usedRivalAges.push(age);
    return rivalDuelEvent(state);
  }
  // 转会机会：
  // 1) 固定年份 22/26/30 岁（原逻辑）
  // 2) 未到顶级联赛的球员，能力超过当前联赛平均水平时，每 2 年额外增加"升一级"报价机会
  const curTeam2 = state.currentTeamId ? TEAMS[state.currentTeamId] : null;
  const curTier2 = curTeam2 ? LEAGUES[curTeam2.league].tier : 4;
  const wantMoveUp = curTier2 > 1 && state.player.overall >= curTeam2.strength - 2;
  const fixedAgeOffer = [22, 26, 30].includes(age) && !state.usedTransferOfferAges.includes(age);
  const upMoveAge = wantMoveUp && age >= 20 && age <= 33 && !state.usedUpMoveAges.includes(age);
  if (fixedAgeOffer || upMoveAge) {
    if (fixedAgeOffer) state.usedTransferOfferAges.push(age);
    if (upMoveAge) state.usedUpMoveAges.push(age);
    const r = roll(state.rngState);
    state.rngState = r.state;
    const p = fixedAgeOffer ? 0.55 : 0.5;
    if (r.v[0] < p) {
      return { ...EVENTS.transfer_rumor, id: `transfer-rumor-${state.step}`, options: EVENTS.transfer_rumor.options.map(o => ({ ...o })) };
    }
  }
  // 34 岁以上：告别赛
  if (age >= 34 && !state.championsFarewellOffered && state.currentTeamId) {
    state.championsFarewellOffered = true;
    return {
      id: `farewell-offer-${state.step}`,
      type: 'farewell_offer',
      title: '告别赛',
      text: `${age} 岁，你在训练结束后叫住了主教练。俱乐部想为你办一场告别赛，先问问你的意思。`,
      options: [
        { id: 'accept', label: '办，跟球迷好好告别', hint: '赛季末全场为你起立' },
        { id: 'decline', label: '不办，安静地离开', hint: '低调谢幕' },
        { id: 'keep', label: '再打一年', hint: '还不想走' },
      ],
    };
  }
  const ev = pickEvent(state, age);
  return ev;
}

function retirementStyleEvent(state, reason) {
  const styles = reason === 'no_offers' || reason === 'contract' ? WALKAWAY_STYLES : FAREWELL_STYLES;
  return {
    id: `retire-style-${state.step}`,
    type: reason === 'no_offers' || reason === 'contract' ? 'walkaway_style' : 'farewell_style',
    title: '谢幕',
    text: '你宣布了退役的决定。剩下的只是怎么让人知道。',
    options: styles.map(s => ({ id: s.id, label: s.label, hint: s.hint })),
  };
}

function showdownEvent(key, state, tournament) {
  const sd = SHOWDOWNS[key];
  const ev = {
    id: `${key}-${state.step}`,
    type: 'showdown',
    showdownKey: key,
    title: sd.title,
    text: sd.text,
    tournamentAge: tournament ? tournament.age : null,
    options: sd.options.map(o => ({ ...o })),
  };
  // 决胜时刻同时挂一个可进入单场模拟的对战上下文
  const stage = state.period && state.pendingGame && state.pendingGame.stage;
  const opponentId = state.pendingGame && state.pendingGame.opponentId;
  if (key === 'game7' || key === 'last_shot' || key === 'free_throw' ||
      key === 'qualifier_showdown' || key === 'world_cup_showdown') {
    state.pendingGame = {
      type: key,
      stage: stage || (key === 'game7' ? 'final' : 'playoff'),
      opponentId: opponentId || (state.currentTeamId ? pickPlayoffOpponent(state, TEAMS[state.currentTeamId], LEAGUES[TEAMS[state.currentTeamId].league]) : null),
      tournamentAge: tournament ? tournament.age : null,
    };
  }
  return ev;
}

function rivalDuelEvent(state) {
  const r = state.rival;
  const myPts = state.rivalSeries.reduce((s, x) => s + x.my, 0);
  const hisPts = state.rivalSeries.reduce((s, x) => s + x.rival, 0);
  const lead = hisPts > myPts ? `总得分上，他还压着你。` : `总得分上，你压着他。`;
  return {
    id: `rival-${state.step}`,
    type: 'career_event',
    title: '宿敌对决',
    text: `${state.player.age} 岁这年，你和${r.name}的每一次碰面都像季后赛。${lead}`,
    options: [
      {
        id: 'train', label: '赛后加练，研究他的打法', hint: '练成能力上涨，练过头有伤', outcomes: [
          { prob: 0.6, text: `你把${r.name}的进攻习惯研究透了，场上防得他难受。`, effects: { overallDelta: 1, permanent: true, legacy: `你和${r.name}的恩怨，又多了一页。` } },
          { prob: 0.4, text: '加练过猛，小腿拉伤。', effects: { injury: '小腿拉伤', tempDelta: -1 } },
        ],
      },
      {
        id: 'trash', label: '赛后当众放话', hint: '热度拉满，赢了封神输了挨骂', outcomes: [
          { prob: 0.5, text: `你在采访里点了${r.name}的名字，第二天全城都在讨论。`, effects: { tempDelta: 1, money: 200, legacy: `你向${r.name}下了战书。` } },
          { prob: 0.5, text: '话放出去了，下一场被打爆，成了笑柄。', effects: { tempDelta: -2 } },
        ],
      },
      {
        id: 'respect', label: '赛后握手致敬', hint: '英雄相惜', outcomes: [
          { prob: 1, text: `你和${r.name}交换了球衣。他说：下一场，我不会放水。`, effects: { roleShift: 1, legacy: `你和${r.name}互换了球衣。` } },
        ],
      },
    ],
  };
}

// ---------- 决策 ----------
function decide(state, optionId) {
  const ev = state.currentEvent;
  if (!ev) return { state, screen: 'event' };
  const opt = ev.options.find(o => o.id === optionId);
  if (!opt) return { state, screen: 'event' };

  if (ev.type === 'ncaa_choice') {
    if (optionId === 'pro') {
      state.lastEventOutcome = { eventKey: 'ncaa_choice', optionKey: 'pro', text: '你决定不去美国，直接签职业合同。', kind: 'neutral' };
      state.stage = 'sign';
      state.currentEvent = signContractEvent(state);
      state.step += 1;
      return { state, screen: 'event' };
    }
    return tryNcaaRecruit(state);
  }

  if (ev.type === 'draft_choice') {
    return resolveDraftChoice(state, optionId);
  }

  if (ev.type === 'contract_expiry') {
    // 续约 or 自由市场（加盟报价球队）
    if (optionId !== 'renew') {
      return resolveContractExpiry(state, optionId, opt.teamId);
    }
    return resolveContractExpiry(state, 'renew');
  }

  if (ev.type === 'sign_contract') {
    state.currentTeamId = opt.teamId;
    state.contractTeamId = opt.teamId;
    state.stage = 'pro';
    const team = TEAMS[opt.teamId];
    state.player.marketValue = marketValueOf(state.player.overall, state.player.age, LEAGUES[team.league]);
    state.player.contract = makeContract(state.player, team, LEAGUES[team.league]);
    state.lastEventOutcome = { eventKey: 'sign_contract', optionKey: opt.teamId, text: `你穿上了${team.zh}的球衣，签下 ${state.player.contract.years} 年合同。`, kind: 'positive' };
    state.step += 1;
    state.currentEvent = null;
    return { state, screen: 'career' };
  }

  if (ev.type === 'transfer_choose') {
    const from = state.currentTeamId ? TEAMS[state.currentTeamId] : null;
    const team = TEAMS[opt.teamId];
    state.transfers.push({ age: state.player.age, from: from ? from.id : null, to: team.id });
    state.currentTeamId = team.id;
    state.contractTeamId = team.id;
    const league = LEAGUES[team.league];
    state.player.marketValue = marketValueOf(state.player.overall, state.player.age, league);
    state.player.contract = makeContract(state.player, team, league);
    if (team.id === state.player.foreignDreamTeamId || team.id === state.player.domesticDreamTeamId) {
      state.legacyLines.push(`你穿上了儿时主队${team.zh}的球衣。`);
    }
    state.lastEventOutcome = { eventKey: 'transfer', optionKey: team.id, text: `你加盟了${team.zh}，签下 ${state.player.contract.years} 年合同。`, kind: 'positive' };
    state.step += 1;
    state.currentEvent = null;
    state.pendingTransfer = null;
    return { state, screen: 'career' };
  }

  if (ev.type === 'farewell_offer') {
    if (optionId === 'keep') {
      state.lastEventOutcome = { eventKey: 'farewell_offer', optionKey: 'keep', text: '你还想再打一年。', kind: 'neutral' };
      state.currentEvent = null;
      state.step += 1;
      return { state, screen: 'career' };
    }
    state.retirementReason = 'farewell';
    if (optionId === 'accept') {
      state.lastEventOutcome = { eventKey: 'farewell_offer', optionKey: 'accept', text: '赛季最后一个主场，为你办一场告别赛。', kind: 'neutral' };
      state.currentEvent = {
        id: `farewell-style-${state.step}`,
        type: 'farewell_style',
        title: '告别赛',
        text: '赛季最后一个主场，为你办一场。你想怎么告别。',
        options: FAREWELL_STYLES.map(s => ({ id: s.id, label: s.label, hint: s.hint })),
      };
    } else {
      state.lastEventOutcome = { eventKey: 'farewell_offer', optionKey: 'decline', text: '不办，安静地离开。', kind: 'neutral' };
      state.currentEvent = {
        id: `goodbye-style-${state.step}`,
        type: 'goodbye_style',
        title: '谢幕',
        text: '不办，安静地离开。',
        options: GOODBYE_STYLES.map(s => ({ id: s.id, label: s.label, hint: s.hint })),
      };
    }
    state.step += 1;
    return { state, screen: 'event' };
  }

  if (ev.type === 'farewell_style' || ev.type === 'goodbye_style' || ev.type === 'walkaway_style') {
    if (ev.type === 'farewell_style') state.farewell = optionId;
    if (ev.type === 'goodbye_style') state.goodbye = optionId;
    if (ev.type === 'walkaway_style') state.walkaway = optionId;
    state.currentEvent = null;
    return finalize(state);
  }

  if (ev.type === 'voluntary_retire') {
    if (optionId === 'keep') {
      state.lastEventOutcome = { eventKey: 'voluntary_retire', optionKey: 'keep', text: '你还想再打两年。', kind: 'neutral' };
      state.currentEvent = null;
      state.step += 1;
      return { state, screen: 'career' };
    }
    state.lastEventOutcome = { eventKey: 'voluntary_retire', optionKey: 'retire', text: '你决定把退役消息发出去。', kind: 'neutral' };
    state.currentEvent = null;
    return beginRetirement(state, 'voluntary');
  }

  if (ev.type === 'showdown') {
    return resolveShowdown(state, ev, opt);
  }

  // 普通生涯事件：掷结果
  const rng = mulberry32(state.rngState);
  const outcomes = opt.outcomes;
  let pickR = rng();
  let picked = outcomes[0];
  let acc = 0;
  for (const o of outcomes) {
    acc += o.prob;
    if (pickR < acc) { picked = o; break; }
  }
  state.rngState = (rng() * 4294967296) >>> 0 || 12345;

  const effects = picked.effects || {};
  const wasPositive = effects.overallDelta > 0 || effects.transfer || effects.money > 0 || effects.roleShift > 0;
  state.lastEventOutcome = {
    eventKey: ev.key,
    optionKey: optionId,
    text: picked.text,
    kind: wasPositive ? 'positive' : (effects.overallDelta < 0 || effects.injury || effects.suspended ? 'negative' : 'neutral'),
  };
  state.step += 1;

  // 应用效果
  const mods = state.period.modifiers || {};
  if (effects.overallDelta) state.player.overall = clamp(state.player.overall + effects.overallDelta, 40, 99);
  if (effects.roleShift) mods.roleShift = (mods.roleShift || 0) + effects.roleShift;
  if (effects.tempDelta) mods.tempDelta = (mods.tempDelta || 0) + effects.tempDelta;
  if (effects.trophyMult) mods.trophyMult = (mods.trophyMult || 1) * effects.trophyMult;
  if (effects.nationalMult) mods.nationalMult = (mods.nationalMult || 1) * effects.nationalMult;
  if (effects.salaryMult) mods.salaryMult = (mods.salaryMult || 1) * effects.salaryMult;
  if (effects.injury) mods.injury = effects.injury;
  if (effects.suspended) {
    state.suspensionSeasonsRemaining = effects.suspended;
    mods.suspended = true;
  }
  if (effects.money) state.totals.salary += effects.money;
  if (effects.nationalTeamRetired) state.nationalTeamRetiredAge = state.player.age;
  if (effects.legacy) state.legacyLines.push(effects.legacy);
  if (effects.award) state.totals.awards.push(effects.award);
  if (effects.forceRetire) {
    state.currentEvent = null;
    return beginRetirement(state, 'no_offers');
  }
  if (effects.transfer) {
    state.currentEvent = null;
    state.pendingTransfer = true;
    state.currentEvent = transferChooseEvent(state);
    return { state, screen: 'event' };
  }
  if (effects.dreamTeam) {
    const dream = state.player.domesticDreamTeamId || state.player.foreignDreamTeamId;
    if (dream) {
      state.transfers.push({ age: state.player.age, from: state.currentTeamId, to: dream });
      state.currentTeamId = dream;
      state.contractTeamId = dream;
      state.legacyLines.push(`你穿上了儿时主队${TEAMS[dream].zh}的球衣。`);
    }
  }

  state.currentEvent = null;
  return { state, screen: 'career' };
}

function resolveShowdown(state, ev, opt) {
  const key = ev.showdownKey;
  const overall = state.player.overall;
  let p = clamp(0.42 + (overall - 75) * 0.01, 0.28, 0.88);
  const mods = {
    last_shot: { three: -0.08, drive: 0.05, pass: 0.03 },
    free_throw: { calm: 0.06, quick: -0.02 },
    game7: { iso: -0.04, screen: 0.06 },
    qualifier_showdown: { aggressive: -0.06, steady: 0.07 },
    world_cup_showdown: { hero: -0.05, team: 0.05 },
  };
  p = clamp(p + (mods[key]?.[opt.id] || 0), 0.22, 0.92);
  const rng = mulberry32(state.rngState);
  const won = rng() < p;
  state.rngState = (rng() * 4294967296) >>> 0 || 12345;
  state.showdownWins[key] += won ? 1 : 0;
  const text = won ? opt.successText : opt.failText;
  state.lastEventOutcome = { eventKey: key, optionKey: opt.id, text, kind: won ? 'positive' : 'negative' };
  state.step += 1;

  if (key === 'last_shot' && won) state.legacyLines.push('终场哨响前，你把球投进了。');
  if (key === 'free_throw' && won) state.legacyLines.push('关键罚球，你稳稳两罚全中。');
  if (key === 'game7' && won) state.legacyLines.push('抢七大战，你带走了系列赛。');

  if (key === 'qualifier_showdown' && ev.tournamentAge) {
    const t = state.tournaments.find(t2 => t2.age === ev.tournamentAge);
    if (t) t.qualified = won;
    state.pendingQualifier = { age: ev.tournamentAge, won };
    if (won) state.legacyLines.push('生死战，你把国家队送进了大赛。');
  }

  if (key === 'world_cup_showdown') {
    const nat = state.nationalTeamPeriods.find(n => n.age === ev.tournamentAge);
    if (nat) {
      if (won) {
        nat.result = 'champion';
        state.totals.trophies.push(nat.type === 'world_cup' ? 'world_cup' : 'olympics');
        state.legacyLines.push(nat.type === 'world_cup' ? '你把世界杯举过头顶。' : '奥运金牌挂在了你的脖子上。');
        if (state.player.overall >= 86) state.totals.awards.push('tournament_mvp');
      } else {
        nat.result = 'semis';
      }
    }
    state.pendingWorldCupUpgrade = null;
  }

  state.currentEvent = null;
  state.pendingGame = null;
  return { state, screen: 'career' };
}

function beginRetirement(state, reason) {
  state.retirementReason = reason;
  if (reason === 'voluntary' && state.player.age <= 32) state.endingBeat = '在最好看的时候转身';
  state.currentEvent = retirementStyleEvent(state, reason);
  return { state, screen: 'event' };
}

// ---------- 单场关键战 ----------
function pickPlayoffOpponent(state, team, league) {
  // 同联赛里挑一支强度接近的球队作为对手
  const candidates = Object.values(TEAMS).filter(t => t.league === league.id && t.id !== team.id);
  if (candidates.length === 0) return Object.values(TEAMS).find(t => t.id !== team.id)?.id || null;
  const diff = Math.abs(candidates[0].strength - team.strength);
  let best = candidates[0];
  for (const t of candidates) {
    if (Math.abs(t.strength - team.strength) < diff) { best = t; }
  }
  return best.id;
}

// 为一场单场关键战生成对阵上下文
function makeGameContext(state, kind) {
  const pg = state.pendingGame;
  const team = TEAMS[state.currentTeamId];
  const homeId = state.currentTeamId;
  const awayId = pg && pg.opponentId ? pg.opponentId : pickPlayoffOpponent(state, team, LEAGUES[team.league]);
  const opp = TEAMS[awayId];
  const stage = pg && pg.stage ? pg.stage : 'final';
  const label =
    kind === 'game7' ? `抢七大战 · ${stage === 'final' ? '总决赛' : '半决赛'}决胜` :
    kind === 'qualifier_showdown' ? '国家队生死战 · 预选赛' :
    kind === 'world_cup_showdown' ? '大赛淘汰赛' :
    kind === 'last_shot' ? '最后一攻' :
    kind === 'free_throw' ? '关键罚球' :
    stage === 'final' ? '总决赛 · 生死一战' : '半决赛 · 关键一战';
  return {
    kind,
    label,
    homeId,
    homeTeam: team,
    awayId,
    awayTeam: opp,
    stage,
    seed: (state.seed.split('-').pop() || '0') + state.step,
  };
}

// 把一场单场模拟的结果写回生涯状态
function applyGameResult(state, game) {
  const won = game.winner === 'home';
  const seasonIndex = state.pendingGame && state.pendingGame.seasonIndex;
  const snap = seasonIndex != null ? state.seasons[seasonIndex] : null;
  const t = state.totals;

  // 累加我的单场数据
  const my = { pts: game.myPts || 0, reb: game.myReb || 0, ast: game.myAst || 0, stl: game.myStl || 0, blk: game.myBlk || 0 };
  if (my.pts + my.reb + my.ast > 0) {
    t.pts += my.pts; t.reb += my.reb; t.ast += my.ast; t.stl += my.stl; t.blk += my.blk; t.apps += 1;
  }

  state.playedGames = state.playedGames || [];
  state.playedGames.push({
    type: game.kind || 'key',
    label: game.label,
    won,
    homeScore: game.homeScore,
    awayScore: game.awayScore,
    opp: game.awayName || game.away.zh,
    myPts: my.pts, myReb: my.reb, myAst: my.ast,
    myStl: my.stl, myBlk: my.blk,
    age: state.player.age,
    stage: state.pendingGame?.stage,
  });

  const kind = game.kind || (state.pendingGame ? state.pendingGame.type : 'playoff_key');

  if (kind === 'playoff_key' && snap) {
    if (game.stage === 'final') {
      if (won) {
        snap.result = { league: 'champion' };
        t.trophies.push(`league:${snap.leagueId}`);
        if (state.player.overall >= 84 && !snap.awards.includes('fmvp')) snap.awards.push('fmvp');
        state.legacyLines.push('总决赛，你把总冠军带回了家。');
      } else {
        snap.result = { league: 'final' };
      }
    } else if (game.stage === 'semis') {
      if (won) {
        snap.result = { league: 'final' };
        state.legacyLines.push('半决赛生死战，你带队杀进了总决赛。');
        // 如果本场高分，再配一个决赛关键战
        if (my.pts >= 40) state.highlights.push({ age: state.player.age, text: '半决赛单场轰下 40+ 分' });
      }
    }
    if (my.pts >= 50) state.highlights.push({ age: state.player.age, text: '关键战单场轰下 50+ 分！' });
    if (my.pts >= 30) state.highlights.push({ age: state.player.age, text: '关键战单场 30+ 分' });
  }

  if (kind === 'game7') {
    state.showdownWins.game7 += won ? 1 : 0;
    if (won) {
      if (snap && snap.result.league === 'semis') { snap.result = { league: 'final' }; }
      else if (snap && snap.result.league === 'final') {
        snap.result = { league: 'champion' };
        if (snap.leagueId) t.trophies.push(`league:${snap.leagueId}`);
        if (state.player.overall >= 84 && !snap.awards.includes('fmvp')) snap.awards.push('fmvp');
      }
      state.legacyLines.push('抢七大战，你带走了系列赛。');
    } else {
      state.legacyLines.push('抢七大战，你拼到了最后，还是差了那么一点。');
    }
  }

  if (kind === 'last_shot') {
    state.showdownWins.last_shot += won ? 1 : 0;
    if (won) state.legacyLines.push('终场哨响前，你把球投进了。');
    else state.legacyLines.push('绝杀偏出，你低下了头。');
  }

  if (kind === 'free_throw') {
    state.showdownWins.free_throw += won ? 1 : 0;
    if (won) state.legacyLines.push('关键罚球，你稳稳两罚全中。');
    else state.legacyLines.push('关键时刻，罚球偏出。');
  }

  if (kind === 'qualifier_showdown') {
    const t2 = state.pendingQualifier && state.pendingQualifier.age;
    if (t2 != null) {
      const tt = state.tournaments.find(x => x.age === t2);
      if (tt) tt.qualified = won;
      state.pendingQualifier = { age: t2, won };
    }
    if (won) state.legacyLines.push('生死战，你把国家队送进了大赛。');
  }

  if (kind === 'world_cup_showdown') {
    const nat = state.nationalTeamPeriods.find(n => n.age === game.tournamentAge);
    if (nat) {
      if (won) {
        nat.result = 'champion';
        t.trophies.push(nat.type === 'world_cup' ? 'world_cup' : 'olympics');
        state.legacyLines.push(nat.type === 'world_cup' ? '你把世界杯举过头顶。' : '奥运金牌挂在了你的脖子上。');
        if (state.player.overall >= 86) t.awards.push('tournament_mvp');
      } else {
        nat.result = 'semis';
      }
    }
    state.pendingWorldCupUpgrade = null;
  }

  state.pendingGame = null;

  // 如果是决胜时刻（showdown）事件，直接通过比赛结果结算，清除当前事件
  const showdownKinds = ['game7', 'last_shot', 'free_throw', 'qualifier_showdown', 'world_cup_showdown'];
  if (showdownKinds.includes(kind) && state.currentEvent && state.currentEvent.type === 'showdown') {
    const key = kind;
    state.lastEventOutcome = {
      eventKey: key,
      optionKey: 'simulate',
      text: won ? '你在场上亲手终结了这场比赛。' : '你拼到最后一刻，还是差了一点。',
      kind: won ? 'positive' : 'negative',
    };
    state.step += 1;
    state.currentEvent = null;
  }

  return { state, screen: 'career' };
}

// ---------- 结算 ----------
function maxOverall(state) {
  let m = state.player.overall;
  for (const s of state.seasons) m = Math.max(m, s.overall);
  return m;
}

function peakSeason(state) {
  let best = null;
  for (const s of state.seasons) {
    if (!best || s.overall > best.overall) best = s;
  }
  return best;
}

function teamById2(id) {
  return TEAMS[id] || NCAA_TEAMS[id] || null;
}

function clubsOf(state) {
  const map = new Map();
  for (const s of state.seasons) {
    if (!s.teamId || s.youth) continue;
    const key = s.teamId;
    if (!map.has(key)) {
      map.set(key, { teamId: key, seasons: 0, stats: { g: 0, pts: 0, reb: 0, ast: 0 }, trophies: [], awards: [] });
    }
    const c = map.get(key);
    c.seasons += 1;
    c.stats.g += s.stats.g;
    c.stats.pts += s.stats.pts;
    c.stats.reb += s.stats.reb;
    c.stats.ast += s.stats.ast;
    s.trophies.forEach(x => c.trophies.push(x));
    s.awards.forEach(x => c.awards.push(x));
  }
  return [...map.values()];
}

function trophyCounts(trophies) {
  const counts = {};
  for (const t of trophies) counts[t] = (counts[t] || 0) + 1;
  return counts;
}

function trophyZh(id) {
  if (id === 'world_cup') return '世界杯';
  if (id === 'olympics') return '奥运会';
  if (id === 'continental') return '洲际冠军';
  if (id === 'ncaa:champion') return 'NCAA总冠军';
  if (id.startsWith('league:')) {
    const lg = LEAGUES[id.slice(7)];
    return lg ? lg.champ : '联赛冠军';
  }
  if (id.startsWith('cup:')) {
    const lg = LEAGUES[id.slice(4)];
    return lg && lg.cupName ? lg.cupName : '杯赛冠军';
  }
  return '冠军';
}

function awardZh(id) {
  const map = {
    allstar: '全明星',
    all_team: '最佳阵容',
    all_nba_1: '最佳阵容一阵',
    all_nba_2: '最佳阵容二阵',
    all_nba_3: '最佳阵容三阵',
    all_def_1: '最佳防守一阵',
    all_def_2: '最佳防守二阵',
    roty: '最佳新秀',
    mvp: '常规赛MVP',
    fmvp: '总决赛MVP',
    dpoy: '最佳防守球员',
    sixth_man: '最佳第六人',
    mip: '进步最快球员',
    champion_ring: '总冠军',
    scoring_title: '得分王',
    rebound_title: '篮板王',
    assist_title: '助攻王',
    steal_title: '抢断王',
    block_title: '盖帽王',
    tournament_mvp: '大赛MVP',
    tournament_all_team: '大赛最佳阵容',
    allstar_mvp: '全明星MVP',
    dunk_king: '扣篮大赛冠军',
    three_king: '三分大赛冠军',
  };
  return map[id] || id;
}

function tournamentZh(type) {
  return { world_cup: '世界杯', olympics: '奥运会', continental: '洲际锦标赛' }[type] || type;
}

function resultZh(result, league, type = 'club') {
  if (type === 'club') {
    const map = {
      champion: league && league.tier === 1 ? 'NBA总冠军' : '联赛冠军',
      final: '总决赛/决赛',
      semis: '四强',
      quarters: '八强',
      playoffs: '季后赛',
      missed: '无缘季后赛',
      youth: '青训营',
    };
    return map[result] || result;
  }
  const map = {
    champion: '冠军',
    semis: '四强',
    quarters: '八强',
    group: '小组赛',
    not_qualified: '未出线',
    not_called: '未入选',
  };
  return map[result] || result;
}

function epitaph(state) {
  const peak = maxOverall(state);
  const t = state.totals;
  const lines = [];
  if (t.trophies.includes('world_cup')) lines.push('一路打到世界之巅，能拿的都拿到了。');
  else if (peak >= 96) lines.push('天生的妖人胚子，真的打到了 ' + peak + '。');
  else if (peak >= 90 && t.trophies.length === 0) lines.push('强到无可争议，却始终两手空空。');
  else if (clubsOf(state).length === 1 && state.seasons.filter(s => !s.youth).length >= 8) lines.push('一辈子只穿一件球衣。');
  else if (peak >= 85 && state.player.overall <= 72) lines.push('不是每个天才都来得及长大。');
  else if (state.player.age >= 38) lines.push('同龄人都挂靴了，你还在名单里。');
  else lines.push('从青训到传奇，每个决定都算数。');
  if (state.endingBeat) lines.push(state.endingBeat);
  return lines.join('');
}

function computeTitles(state) {
  const t = state.totals;
  const peak = maxOverall(state);
  const peakAge = peakSeason(state)?.age ?? 16;
  const clubs = clubsOf(state);
  const proSeasons = state.seasons.filter(s => !s.youth && s.teamId);
  const won = (id) => t.trophies.includes(id);
  const countAward = (id) => t.awards.filter(a => a === id).length;
  const champCount = t.trophies.filter(x => x.startsWith('league:') || x.startsWith('cup:')).length;
  const titles = [];
  const unlocked = (id, quote) => titles.push({ id, quote });

  if (peak >= 96) unlocked('tian_zhijiaozi', `巅峰能力 ${peak}，真·天之骄子。`);
  if (peak >= 93 && (state.player.debutOverall || 99) <= 72) unlocked('yao_ren_dx', `出道 ${state.player.debutOverall}，巅峰 ${peak}。`);
  if (peak >= 88 && (state.player.debutOverall || 99) <= 62 && peakAge >= 28) unlocked('da_qi_wan_cheng', `晚熟型球员，硬是把自己练到了 ${peak}。`);
  if (peak >= 85 && state.player.overall <= 72) unlocked('shang_zhong_yong', `巅峰 ${peak}，退役时只有 ${state.player.overall}。`);
  if (clubs.length === 1 && proSeasons.length >= 8) unlocked('yi_ren_yi_cheng', '一生只效力一支球队。');
  if (clubs.length >= 6) unlocked('lan_tan_liu_lang_zhe', `效力过 ${clubs.length} 支球队，走到哪都是客场。`);
  if (t.apps >= 1000) unlocked('tie_ren', `生涯出场 ${fmtInt(t.apps)} 场，铁人。`);
  if (t.pts >= 30000) unlocked('de_fen_ji_qi', `生涯总得分 ${fmtInt(Math.round(t.pts))}。`);
  if (state.seasons.some(s => s.stats.avg && s.stats.avg.pts >= 10 && s.stats.avg.reb >= 10 && s.stats.avg.ast >= 10)) unlocked('san_shuang_ji_qi', '单季场均三双，只有怪物能打出来。');
  if (peak >= 90 && champCount === 0 && !won('world_cup') && !won('olympics')) unlocked('wu_mian_zhi_wang', `巅峰 ${peak}，却一冠未得。`);
  if (won('world_cup') || won('olympics')) unlocked('shi_jie_zhi_dian', '世界之巅，你站上去过。');
  if ((won('world_cup') || won('olympics')) && countAward('fmvp') >= 1 && peak >= 96) unlocked('lan_qiu_zhi_shen', '世界杯 + FMVP + 巅峰 96，篮球之神。');
  if (won('world_cup') && won('olympics') && won('continental') && champCount >= 2) unlocked('jin_man_guan', '世界杯、奥运、洲际、联赛，全拿过。');
  if (consecutiveTitles(state) >= 3) unlocked('wang_chao_ji', `同一支球队 ${consecutiveTitles(state)} 连冠，王朝。`);
  if (champCount >= 12) unlocked('guan_jun_shou_ge_ji', `生涯 ${champCount} 座奖杯。`);
  if (t.salary >= 200000) unlocked('lan_tan_shou_fu', `生涯总收入 ${fmtMoney(t.salary)}。`);
  if (state.seasons.some(s => s.salary >= 30000)) unlocked('tian_jia_he_tong', `单季年薪 ${fmtMoney(Math.max(...state.seasons.map(s => s.salary)))}。`);
  if (countAward('allstar') >= 10) unlocked('quan_ming_xing_zhi_wang', `${countAward('allstar')} 次全明星。`);
  if (countAward('mvp') >= 4) unlocked('zui_you_jia_zhi', `${countAward('mvp')} 次常规赛MVP。`);
  if (countAward('fmvp') >= 3) unlocked('zong_jue_sai_zhi_wang', `${countAward('fmvp')} 次总决赛MVP。`);
  if (countAward('scoring_title') >= 5) unlocked('de_fen_wang', `${countAward('scoring_title')} 次得分王。`);
  if (countAward('rebound_title') >= 5 || t.reb >= 15000) unlocked('lan_ban_guai_shou', '篮板就是命。');
  if (countAward('assist_title') >= 5 || t.ast >= 10000) unlocked('zu_zhi_da_shi', '把队友喂成巨星。');
  if (countAward('dpoy') >= 3) unlocked('fang_shou_tie_zha', `${countAward('dpoy')} 次最佳防守球员。`);
  if (state.player.age >= 38 && proSeasons.length >= 18) unlocked('bu_lao_chuan_shuo', `${state.player.age} 岁还在打，不老传说。`);
  if ((state.retirementReason === 'voluntary' || state.farewell) && state.player.age <= 32 && peak >= 90) unlocked('ji_liu_yong_tui', '在最好看的时候转身。');
  if (state.nationalTeamPeriods.length >= 10) unlocked('guo_jia_dui_qi_zhi', `为国出战 ${state.nationalTeamPeriods.length} 届大赛。`);
  const dreamTeam = state.player.domesticDreamTeamId || state.player.foreignDreamTeamId;
  if (dreamTeam && t.trophies.some(id => id === `league:${TEAMS[dreamTeam].league}`)) unlocked('yuan_meng_ren', '为儿时主队拿过冠军，圆梦。');
  if (state.showdownWins.last_shot > 0) unlocked('jue_sha_zhi_wang', '关键球，交给我。');
  if (state.showdownWins.free_throw >= 2) unlocked('fa_qiu_da_shi', '罚球线上，从不手软。');
  const rs = state.rivalSeries || [];
  if (state.rival && rs.length >= 8 && rs.filter(x => x.myChamp).length >= 1 && rs.filter(x => x.rivalChamp).length >= 1) {
    unlocked('yi_sheng_zhi_di', `你和${state.rival.name}斗了一辈子，谁也没服过谁。`);
  }
  if (state.rival && rs.length >= 6) {
    const myPts = rs.reduce((s, x) => s + x.my, 0);
    const hisPts = rs.reduce((s, x) => s + x.rival, 0);
    const myChamp = rs.filter(x => x.myChamp).length;
    const hisChamp = rs.filter(x => x.rivalChamp).length;
    if (myPts >= hisPts * 1.05 && myChamp > hisChamp) {
      unlocked('yan_zhong_ding', `你压了${state.rival.name}一辈子。`);
    }
  }

  return titles;
}

function consecutiveTitles(state) {
  let best = 0, cur = 0, curTeam = null;
  for (const s of state.seasons) {
    const champ = s.trophies.some(x => x.startsWith('league:'));
    if (champ && s.teamId === curTeam) cur += 1;
    else if (champ) { cur = 1; curTeam = s.teamId; }
    else { cur = 0; curTeam = null; }
    best = Math.max(best, cur);
  }
  return best;
}

function nationalLine(state) {
  const periods = state.nationalTeamPeriods;
  if (periods.length === 0) return null;
  const games = periods.reduce((s, p) => s + (p.stats ? p.stats.g : 0), 0);
  const pts = periods.reduce((s, p) => s + (p.stats ? p.stats.pts : 0), 0);
  const reb = periods.reduce((s, p) => s + (p.stats ? p.stats.reb : 0), 0);
  const ast = periods.reduce((s, p) => s + (p.stats ? p.stats.ast : 0), 0);
  const golds = periods.filter(p => p.result === 'champion').length;
  return { games, pts, reb, ast, golds };
}

function finalize(state) {
  state.phase = 'summary';
  state.currentEvent = null;
  if (!state.endingBeat) {
    if (state.retirementReason === 'voluntary' && state.player.age <= 32) state.endingBeat = '在最好看的时候转身';
    if (state.player.age >= 38) state.endingBeat = '同龄人都挂靴了，你还在名单里。';
  }
  return { state, screen: 'summary' };
}

function buildSummary(state) {
  const peak = maxOverall(state);
  const t = state.totals;
  const nat = nationalLine(state);
  const titles = computeTitles(state);
  const clubs = clubsOf(state);
  const proSeasons = state.seasons.filter(s => !s.youth && s.teamId);
  const rs = state.rivalSeries || [];
  const rival = state.rival && rs.length ? {
    name: state.rival.name,
    nationality: state.rival.nationality,
    peak: state.rival.peak,
    series: rs.length,
    myPts: Math.round(rs.reduce((s, x) => s + x.my, 0)),
    rivalPts: Math.round(rs.reduce((s, x) => s + x.rival, 0)),
    myChamps: rs.filter(x => x.myChamp).length,
    rivalChamps: rs.filter(x => x.rivalChamp).length,
    myMvp: rs.filter(x => x.myMvp).length,
    rivalMvp: rs.filter(x => x.rivalMvp).length,
  } : null;
  return {
    seed: state.seed,
    player: {
      name: state.player.name,
      nationality: state.player.nationality,
      position: state.player.positionZh,
      positionEn: POSITIONS[state.player.position].en,
      number: state.player.number,
      hand: state.player.hand,
    },
    maxOverall: peak,
    draft: state.draft ? (state.draft.undrafted ? { undrafted: true } : {
      pick: state.draft.pick,
      round: state.draft.round,
      team: state.draft.teamId ? TEAMS[state.draft.teamId]?.zh : null,
    }) : null,
    ncaaSeasons: state.seasons.filter(s => s.ncaaYear).length,
    peakAge: peakSeason(state)?.age ?? 16,
    peakValue: Math.max(...state.seasons.map(s => s.marketValue || 0), state.player.marketValue),
    totalIncome: t.salary,
    totals: {
      apps: t.apps,
      pts: Math.round(t.pts),
      reb: Math.round(t.reb),
      ast: Math.round(t.ast),
      stl: Math.round(t.stl),
      blk: Math.round(t.blk),
    },
    national: nat ? {
      games: nat.games,
      pts: Math.round(nat.pts),
      reb: Math.round(nat.reb),
      ast: Math.round(nat.ast),
      golds: nat.golds,
    } : null,
    titles,
    titleIds: titles.map(x => x.id),
    epitaph: epitaph(state),
    percentile: percentileOf(peak),
    clubs: clubs.map(c => {
      const team = TEAMS[c.teamId] || NCAA_TEAMS[c.teamId];
      return {
        teamId: c.teamId,
        abbr: team.abbr,
        name: team.zh,
        color: team.color,
        isLight: isLight(team.color),
        elite: team.strength >= 85,
        ncaa: !!NCAA_TEAMS[c.teamId],
        seasons: c.seasons,
        games: c.stats.g,
        pts: Math.round(c.stats.pts),
        reb: Math.round(c.stats.reb),
        ast: Math.round(c.stats.ast),
        trophies: c.trophies,
        awards: c.awards,
      };
    }),
    seasonsCount: proSeasons.length,
    legacyLines: state.legacyLines,
    highlights: (state.highlights || []).slice(-14),
    keyGames: (state.playedGames || []).map(g => ({
      label: g.label,
      won: g.won,
      score: `${g.homeScore} : ${g.awayScore}`,
      opp: g.opp,
      myPts: g.myPts, myReb: g.myReb, myAst: g.myAst,
      age: g.age,
    })).slice(-20),
    rival,
    farewell: state.farewell,
    goodbye: state.goodbye,
    walkaway: state.walkaway,
    retirementReason: state.retirementReason,
    endingBeat: state.endingBeat,
    savedAt: Date.now(),
  };
}

function isLight(hex) {
  const c = hex.replace('#', '');
  const r = parseInt(c.slice(0, 2), 16);
  const g = parseInt(c.slice(2, 4), 16);
  const b = parseInt(c.slice(4, 6), 16);
  return (r * 299 + g * 587 + b * 114) / 1000 > 165;
}

function endingZh(reason) {
  const map = {
    age: '挂靴',
    voluntary: '主动退役',
    no_offers: '无人问津',
    farewell: '告别赛',
    contract: '合同到期',
  };
  return map[reason] || '退役';
}

// ---------- 存档 ----------
function saveState(state) {
  try {
    localStorage.setItem(`bl-save:${state.seed}`, JSON.stringify(state));
  } catch (e) { /* ignore */ }
}

function loadState(seed) {
  try {
    const raw = localStorage.getItem(`bl-save:${seed}`);
    return raw ? JSON.parse(raw) : null;
  } catch (e) { return null; }
}

function clearState(seed) {
  try { localStorage.removeItem(`bl-save:${seed}`); } catch (e) { /* ignore */ }
}

function saveArchive(summary) {
  try {
    const key = 'bl-archive';
    const list = JSON.parse(localStorage.getItem(key) || '[]');
    list.unshift(summary);
    const dedup = [];
    const seen = new Set();
    for (const it of list) {
      if (!seen.has(it.seed)) { seen.add(it.seed); dedup.push(it); }
    }
    localStorage.setItem(key, JSON.stringify(dedup.slice(0, 50)));
  } catch (e) { /* ignore */ }
}

function loadArchive() {
  try {
    return JSON.parse(localStorage.getItem('bl-archive') || '[]');
  } catch (e) { return []; }
}

function galleryState() {
  const archive = loadArchive();
  const unlocked = new Map();
  for (const a of archive) {
    for (const t of (a.titles || [])) {
      if (!unlocked.has(t.id)) unlocked.set(t.id, t.quote);
    }
  }
  return { unlocked, total: TITLES.length };
}
  __M['engine.js'] = { xmur3, mulberry32, nextRng, roll, chance, pickWeighted, genSeed, fmtMoney, fmtInt, fmtAvg, percentileOf, clamp, teamById, leagueById, countryById, ROLE_KEYS, roleName, roleFactor, tournamentSchedule, newGame, marketValueOf, salaryOf, makeContract, renewContract, contractTierZh, upgradeAttr, recalcOverall, beginSeason, setNbaPool, nbaPoolTeam, simNextGames, requestTrade, seasonOffseason, starRetirements, beginPlayoffs, simPlayoffGames, getMyRoster, finishSeason, generateLeagueAwards, nbaJumpInfo, tryNBAJump, step, decide, makeGameContext, applyGameResult, maxOverall, peakSeason, teamById2, clubsOf, trophyCounts, trophyZh, awardZh, tournamentZh, resultZh, computeTitles, nationalLine, finalize, buildSummary, isLight, endingZh, saveState, loadState, clearState, saveArchive, loadArchive, galleryState };
  })();

  // ===== ui.js (deps: data.js,build.js,simEngine.js,engine.js) =====
  (function () {
const { APP_TITLE, TAGLINE, MODES, POSITIONS, COUNTRIES, LEAGUES, TEAMS, NCAA_TEAMS, TITLES, UPDATES } = __BL['data.js'];
const { ATTR_LIST, POS_ZH, loadPool, poolTeamCount, randomTeam, drawPlayers, posPenalty, srcPosKey, calcOVR, nextUnlocked, lockAttr, reveal, createBuildState, buildProgress } = __BL['build.js'];
const { makeRoster, simulateGame, virtualPlayer, mainPos } = __BL['simEngine.js'];
const E = __BL['engine.js'];
// ================= 篮球生涯模拟器 · UI =================





const root = document.getElementById('root');

const app = {
  view: 'home',
  mode: 'standard',
  state: null,
  seed: null,
  archived: false,
  identity: {
    name: '',
    nationality: 'CN',
    position: 'sg',
    hand: '右',
    number: String(8 + Math.floor(Math.random() * 20)),
    domesticDream: '',
    foreignDream: '',
  },
  modal: null,
  archiveDetail: null,
  shareDataUrl: null,
  invite: '',
  seasonSummary: null,
  offseason: null,
  // 建球员
  build: null,
  // 单场模拟
  game: null,
  gameView: null,   // 'pre' | 'play' | 'result'
  gameStep: 0,
  pool: null,
  poolLoaded: false,
};

// ---------- 工具 ----------
function esc(s) {
  return String(s ?? '').replace(/[&<>"']/g, c => ({
    '&': '&amp;', '<': '&lt;', '>': '&gt;', '"': '&quot;', "'": '&#39;',
  }[c]));
}

function timeAgo(ts) {
  const d = Date.now() - ts;
  if (d < 60000) return '刚刚';
  if (d < 3600000) return Math.floor(d / 60000) + ' 分钟前';
  if (d < 86400000) return Math.floor(d / 3600000) + ' 小时前';
  if (d < 604800000) return Math.floor(d / 86400000) + ' 天前';
  const date = new Date(ts);
  return `${date.getMonth() + 1}月${date.getDate()}日`;
}

// 根据球队所属联赛/国家取姓氏池
function surnamesFor(team) {
  const lg = LEAGUES[team.league];
  const country = lg ? COUNTRIES[lg.country] : null;
  if (country && country.surnames) {
    return country.surnames.map(s => ({ zh: s }));
  }
  return ['张', '王', '李', '刘', '陈', '杨'].map(s => ({ zh: s }));
}

// 玩家无 attrs 时的兜底（老存档）
function fallbackAttrs(ovr) {
  const o = ovr || 70;
  return {
    threePT: o - 8, MID: o - 5, FIN: o - 3, DNK: o - 6, HAN: o - 3,
    PAS: o - 4, PDEF: o - 5, IDEF: o - 5, BLK: o - 9, REB: o - 6,
    ATH: o - 3, STR: o - 4, CLU: o - 3,
  };
}

function toast(msg) {
  const el = document.createElement('div');
  el.className = 'toast';
  el.textContent = msg;
  document.body.appendChild(el);
  setTimeout(() => el.remove(), 1800);
}

function shell(inner) {
  return `<div class="app">${inner}</div>`;
}

function topbarHTML() {
  const s = app.state;
  if (!s) return '';
  const p = s.player;
  const team = s.currentTeamId ? (TEAMS[s.currentTeamId] || NCAA_TEAMS[s.currentTeamId] || null) : null;
  const pct = Math.round(((p.overall - 40) / 59) * 100);
  const r = 20, c = 2 * Math.PI * r;
  const place = team ? esc(team.zh) : (s.stage === 'ncaa' ? 'NCAA' : '青训营');
  return `
    <div class="topbar">
      <div class="rating-ring">
        <svg viewBox="0 0 46 46">
          <circle cx="23" cy="23" r="${r}" fill="none" stroke="#27272a" stroke-width="3.5"/>
          <circle cx="23" cy="23" r="${r}" fill="none" stroke="#10b981" stroke-width="3.5"
            stroke-linecap="round" stroke-dasharray="${c}" stroke-dashoffset="${c * (1 - pct / 100)}"/>
        </svg>
        <span class="val num">${p.overall}</span>
      </div>
      <div class="who">
        <div class="name">${esc(p.name)}</div>
        <div class="meta">${p.age} 岁 · ${place} · ${esc(E.roleName(s.seasons[s.seasons.length - 1]?.role || 'starter'))}</div>
      </div>
      <div class="money">
        <div class="v num">${E.fmtMoney(p.marketValue)}</div>
        <div class="s">身价</div>
      </div>
    </div>`;
}

// ---------- 首页 ----------
function homeHTML() {
  const archive = E.loadArchive();
  const resume = (app.seed && E.loadState(app.seed)) || latestSave()?.state;
  const modeCards = Object.entries(MODES).map(([key, m]) => `
    <button class="mode-card ${app.mode === key ? 'active' : ''}" onclick="BL.setMode('${key}')">
      ${m.recommended ? '<span class="rec">推荐</span>' : ''}
      <div class="name">${m.label}</div>
      <div class="hint">${m.hint}</div>
    </button>`).join('');
  return shell(`
    <div class="scroll">
      <div class="home-hero">
        <div class="home-ball">🏀</div>
        <h1 class="home-title">${APP_TITLE}</h1>
        <p class="home-tagline">${TAGLINE}<br/>随机球队抽取属性建球员 · 转会、伤病、绝杀、抢七、国家队，关键比赛亲手逐回合打下来。</p>
      </div>

      <div class="label">节奏</div>
      <div class="mode-grid">${modeCards}</div>

      <div class="home-actions">
        <button class="btn btn-primary btn-lg btn-block" onclick="BL.start()">开始生涯</button>
        ${resume ? `<button class="btn btn-outline btn-block" onclick="BL.resume()">继续上一局</button>` : ''}
      </div>

      <div class="home-sub">
        <button class="btn" onclick="BL.openArchive()">历史档案${archive.length ? ` · ${archive.length}` : ''}</button>
        <button class="btn" onclick="BL.openGallery()">称号图鉴</button>
      </div>

      <div class="invite-row">
        <input id="invite-input" maxlength="40" placeholder="粘贴朋友给你的编号" value="${esc(app.invite)}" oninput="BL.setInvite(this.value)"/>
        <button class="btn btn-outline" onclick="BL.useInvite()">开局</button>
      </div>

      <div class="home-foot">
        <button onclick="BL.openUpdates()">查看本期更新说明</button><br/>
        玩法一直在更新，点击关注
      </div>
    </div>
    ${app.modal ? modalHTML() : ''}
  `);
}

function latestSave() {
  try {
    const keys = Object.keys(localStorage).filter(k => k.startsWith('bl-save:'));
    if (!keys.length) return null;
    const key = keys.sort().pop();
    return { seed: key.slice(8), state: JSON.parse(localStorage.getItem(key)) };
  } catch (e) {
    return null;
  }
}

// ---------- 建档 ----------
function identityHTML() {
  const id = app.identity;
  const country = COUNTRIES[id.nationality];
  const tierHint = {
    1: '世界强队，大赛常客',
    2: '要凭真本事去挤，有机会进',
    3: '门槛不低，全靠你扛',
  }[country.tier];
  const natGrid = Object.entries(COUNTRIES).map(([code, c]) => `
    <button class="pick ${id.nationality === code ? 'active' : ''}" onclick="BL.setNationality('${code}')">
      <span class="fl">${c.flag}</span>${c.zh}
    </button>`).join('');
  const posGrid = Object.entries(POSITIONS).map(([key, p]) => `
    <button class="pick ${id.position === key ? 'active' : ''}" onclick="BL.setPosition('${key}')">
      ${p.zh}<span class="en">${p.en}</span>
    </button>`).join('');
  const domesticTeams = Object.values(TEAMS).filter(t => t.league === country.league)
    .sort((a, b) => a.zh.localeCompare(b.zh, 'zh'));
  const foreignTeams = Object.values(TEAMS).filter(t => t.league !== country.league)
    .sort((a, b) => LEAGUES[a.league].tier - LEAGUES[b.league].tier || a.zh.localeCompare(b.zh, 'zh'));
  const domOptions = `<option value="">不选</option>` + domesticTeams.map(t =>
    `<option value="${t.id}" ${id.domesticDream === t.id ? 'selected' : ''}>${esc(t.zh)}</option>`).join('');
  const forOptions = `<option value="">不选</option>` + foreignTeams.map(t =>
    `<option value="${t.id}" ${id.foreignDream === t.id ? 'selected' : ''}>${esc(LEAGUES[t.league].zh)} · ${esc(t.zh)}</option>`).join('');
  return shell(`
    <div class="page-head">
      <button class="btn btn-ghost" onclick="BL.backHome()">← 返回</button>
      <h2>建立档案</h2>
    </div>
    <div class="scroll">
      <div class="ident-section">
        <h3>给自己起个名字</h3>
        <input class="name-input" maxlength="12" placeholder="输入姓名" value="${esc(id.name)}" oninput="BL.setName(this.value)"/>
      </div>

      <div class="ident-section">
        <h3>你是哪国人 <span class="sub">国籍决定国家队的门槛和你能捧起哪座冠军</span></h3>
        <div class="grid-3">${natGrid}</div>
        <div class="hint-bar">${esc(country.zh)} · ${tierHint}</div>
      </div>

      <div class="ident-section">
        <h3>打哪个位置 <span class="sub">位置决定你的得分权重，也决定你拿什么奖</span></h3>
        <div class="grid-5">${posGrid}</div>
        <div class="hint-bar">${esc(POSITIONS[id.position].hint)}</div>
      </div>

      <div class="ident-section">
        <h3>惯用手</h3>
        <div class="seg">
          <button class="pick ${id.hand === '右' ? 'active' : ''}" onclick="BL.setHand('右')">右手</button>
          <button class="pick ${id.hand === '左' ? 'active' : ''}" onclick="BL.setHand('左')">左手</button>
        </div>
      </div>

      <div class="ident-section">
        <h3>球衣号码</h3>
        <div class="num-row">
          <input type="number" min="0" max="99" value="${id.number}" oninput="BL.setNumber(this.value)"/>
          <span class="muted">号球衣</span>
        </div>
      </div>

      <div class="ident-section">
        <h3>儿时主队 <span class="sub">主队不影响能力发育，但你有圆梦的机会</span></h3>
        <div class="muted-2" style="margin-bottom:6px">国内主队</div>
        <select class="team-select" onchange="BL.setDream('domestic', this.value)">${domOptions}</select>
        <div class="muted-2" style="margin:10px 0 6px">海外主队</div>
        <select class="team-select" onchange="BL.setDream('foreign', this.value)">${forOptions}</select>
      </div>

      <div style="height:14px"></div>
      <button class="btn btn-primary btn-lg btn-block" onclick="BL.confirmIdentity()">开始生涯</button>
      <div style="height:24px"></div>
    </div>
  `);
}

// ---------- 生涯 ----------
function careerHTML() {
  const s = app.state;
  let body = '';
  if (s.phase === 'summary') {
    return summaryHTML();
  }
  // 赛季进行中：逐场模拟屏幕
  if (s.season && !s.season.done) {
    return seasonHTML(s);
  }
  // 季后赛进行中
  if (s.playoffs && !s.playoffs.done) {
    return playoffsHTML(s);
  }
  // 赛季总结
  if (app.seasonSummary) {
    body = seasonSummaryHTML(app.seasonSummary);
    return shell(`
      ${topbarHTML()}
      <div class="scroll" style="padding-top:6px">
        ${body}
      </div>
      ${app.modal ? modalHTML() : ''}
    `);
  }
  if (app.receipt) {
    // 决策回执优先：显示结果，点一下继续
    body = receiptHTML(s) + (s.currentEvent ? `<div class="banner-tip" style="margin-top:8px">轻触继续</div>` : '');
  } else if (app.pendingBanner) {
    // 先展示刚结束的赛季横幅，再进入事件
    body = bannerHTML(app.lastBanner);
  } else {
    body = s.currentEvent ? eventHTML(s.currentEvent) : bannerHTML(app.lastBanner);
  }
  return shell(`
    ${topbarHTML()}
    <div class="scroll" style="padding-top:6px">
      ${body}
    </div>
    ${app.modal ? modalHTML() : ''}
  `);
}

// ---------- 逐场赛季 ----------
function seasonHTML(s) {
  const season = s.season;
  const team = TEAMS[season.teamId] || NCAA_TEAMS[season.teamId] || null;
  const lg = LEAGUES[season.leagueId] || (season.kind === 'ncaa' ? LEAGUES.ncaa : null);
  const pct = Math.round((season.played / season.totalGames) * 100);
  const myAvg = season.myStats.g > 0 ? {
    pts: (season.myStats.pts / season.myStats.g).toFixed(1),
    reb: (season.myStats.reb / season.myStats.g).toFixed(1),
    ast: (season.myStats.ast / season.myStats.g).toFixed(1),
    stl: (season.myStats.stl / season.myStats.g).toFixed(1),
    blk: (season.myStats.blk / season.myStats.g).toFixed(1),
  } : { pts: '0', reb: '0', ast: '0', stl: '0', blk: '0' };
  // 最近比赛（可点击查看双方 box）
  const recent = season.games.slice(-8).reverse().map((g, i) => `
    <button class="season-game ${g.win ? 'win' : 'lose'}" onclick="BL.viewGameBox(${season.played - i - 1})">
      <span class="sg-r">${g.home ? '主' : '客'}${g.win ? ' W' : ' L'}</span>
      <span class="sg-opp">${esc(g.opp)}</span>
      <span class="sg-score num">${g.myScore} : ${g.oppScore}</span>
      <span class="sg-me num">我 ${g.my.pts}分</span>
    </button>`).join('') || '<div class="empty">还没开打</div>';
  // 队友列表（点击查看能力值/场均）
  const mates = E.getMyRoster(s).filter(p => !p.isMe);
  const matesHTML = mates.map((p, i) => `
    <button class="mate-row" onclick="BL.viewMate(${i})">
      <span class="mt-pos">${p.pos}</span>
      <span class="mt-name">${esc(p.name)}</span>
      <span class="mt-ovr num">${p.ovr}</span>
      <span class="mt-avg num">${p.avg.pts}分 ${p.avg.reb}板</span>
    </button>`).join('');

  return shell(`
    ${topbarHTML()}
    <div class="scroll" style="padding-top:6px">
      <div class="season-top">
        <div class="season-head">
          <div class="age">${s.player.age} <small>岁</small></div>
          <div class="team">${team ? esc(team.zh) : ''}${lg ? ` · ${esc(lg.zh)}` : ''}${season.kind === 'ncaa' ? ' · 大学赛季' : ' · 常规赛'}</div>
        </div>
        <div class="season-progress"><div class="sp-bar" style="width:${pct}%"></div><span class="sp-txt">${season.played}/${season.totalGames}</span></div>
        <div class="season-record"><span class="w">${season.wins}胜</span><span class="l">${season.losses}负</span></div>
      </div>

      ${(s.pendingTrades || []).length ? `
      <div class="label" style="margin-top:12px">📢 联盟交易</div>
      <div class="trade-list">
        ${s.pendingTrades.slice(-3).map(t => `
          <div class="trade-item ${t.notable ? 'notable' : ''}">
            <span class="ti-text">${esc(t.teamA)} ↔ ${esc(t.teamB)}</span>
            <span class="ti-detail">${esc(t.fromAToB)}（${t.ovrA}）↔ ${esc(t.fromBToA)}（${t.ovrB}）${t.notable ? ' · 重磅' : ''}</span>
          </div>`).join('')}
      </div>` : ''}

      <div class="label" style="margin-top:12px">我的场均</div>
      <div class="season-avg">
        <div><div class="num">${myAvg.pts}</div><div class="lab">得分</div></div>
        <div><div class="num">${myAvg.reb}</div><div class="lab">篮板</div></div>
        <div><div class="num">${myAvg.ast}</div><div class="lab">助攻</div></div>
        <div><div class="num">${myAvg.stl}</div><div class="lab">抢断</div></div>
        <div><div class="num">${myAvg.blk}</div><div class="lab">盖帽</div></div>
      </div>

      <div class="label" style="margin-top:12px">最近比赛 <span class="muted-2">（点开看双方技术统计）</span></div>
      <div class="season-games">${recent}</div>

      <div class="label" style="margin-top:12px">队友 <span class="muted-2">（点开看能力值/场均）</span></div>
      ${matesHTML ? `<div class="mate-list">${matesHTML}</div>` : '<div class="empty">暂无队友数据</div>'}

      <div class="season-actions">
        <button class="btn btn-primary" onclick="BL.simGames(1)">▶ 模拟 1 场</button>
        <button class="btn btn-primary" onclick="BL.simGames(5)">⏩ 模拟 5 场</button>
        <button class="btn btn-outline" onclick="BL.simAll()">快进整个赛季</button>
        ${season.kind === 'pro' ? `<button class="btn btn-outline" onclick="BL.requestTrade()">🔄 申请交易</button>` : ''}
      </div>
      <div style="height:24px"></div>
    </div>
    ${app.modal ? modalHTML() : ''}
  `);
}

// 单场技术统计（modal）
function gameBoxHTML(game, teamZh) {
  const rows = (box) => box.map(p => `
    <tr class="${p.isMe ? 'me' : ''}">
      <td>${p.isMe ? '⭐ ' : ''}${esc(p.name)}</td>
      <td>${p.pos}</td>
      <td>${p.pts}</td><td>${p.reb}</td><td>${p.ast}</td><td>${p.stl}</td><td>${p.blk}</td>
    </tr>`).join('');
  return `
    <div class="modal-mask" onclick="if(event.target===this)BL.closeModal()">
      <div class="modal modal-wide">
        <div class="close-row"><button class="btn btn-ghost" onclick="BL.closeModal()">✕</button></div>
        <div class="gb-head">
          <span class="gb-team">${esc(teamZh)}</span>
          <span class="gb-score num">${game.myScore} : ${game.oppScore}</span>
          <span class="gb-team">${esc(game.opp)}</span>
          <div class="gb-result ${game.win ? 'win' : 'lose'}">${game.win ? '🏆 胜' : '💔 负'} · 第${game.g}场</div>
        </div>
        <div class="label">${esc(teamZh)} 技术统计</div>
        <table class="box-table"><thead><tr><th>球员</th><th>位</th><th>分</th><th>板</th><th>助</th><th>抢</th><th>帽</th></tr></thead><tbody>${rows(game.homeBox)}</tbody></table>
        <div class="label" style="margin-top:10px">${esc(game.opp)} 技术统计</div>
        <table class="box-table"><thead><tr><th>球员</th><th>位</th><th>分</th><th>板</th><th>助</th><th>抢</th><th>帽</th></tr></thead><tbody>${rows(game.awayBox)}</tbody></table>
      </div>
    </div>`;
}

// 队友详情（modal）
function mateDetailHTML(mate) {
  const attrHTML = ATTR_LIST.map(({ key, zh, icon }) => `
    <div class="attr-cell">
      <div class="a-icon">${icon}</div>
      <div class="a-zh">${zh}</div>
      <div class="a-val num">${mate.attrs[key] || '—'}</div>
    </div>`).join('');
  return `
    <div class="modal-mask" onclick="if(event.target===this)BL.closeModal()">
      <div class="modal modal-wide">
        <div class="close-row"><button class="btn btn-ghost" onclick="BL.closeModal()">✕</button></div>
        <div class="mate-head">
          <div class="mt-pos">${mate.pos}</div>
          <div class="mt-name">${esc(mate.name)}</div>
          <div class="mt-ovr num">OVR ${mate.ovr}</div>
          <div class="mt-starter">${mate.starter ? '首发' : '替补'}</div>
        </div>
        <div class="label" style="margin-top:12px">本季场均</div>
        <div class="season-avg">
          <div><div class="num">${mate.avg.pts}</div><div class="lab">得分</div></div>
          <div><div class="num">${mate.avg.reb}</div><div class="lab">篮板</div></div>
          <div><div class="num">${mate.avg.ast}</div><div class="lab">助攻</div></div>
          <div><div class="num">${mate.avg.stl}</div><div class="lab">抢断</div></div>
          <div><div class="num">${mate.avg.blk}</div><div class="lab">盖帽</div></div>
        </div>
        <div class="label" style="margin-top:12px">能力值</div>
        <div class="attr-grid">${attrHTML}</div>
      </div>
    </div>`;
}

// ---------- 赛季总结 ----------
function leagueAwardsHTML(list) {
  if (!list || !list.length) return '';
  return `<div class="label" style="margin-top:14px">🏆 联盟奖项</div>
    <div class="ss-awards-list">
    ${list.map(a => {
      const who = a.winner
        ? `<div class="al-winner ${a.winner.isMe ? 'me' : ''}">${a.winner.isMe ? '⭐ ' : ''}${esc(a.winner.name)} <span class="al-stat">OVR ${a.winner.ovr} · ${a.winner.pts}分 ${a.winner.reb}板 ${a.winner.ast}助</span></div>`
        : (a.team ? `<div class="al-team">${a.team.map(p => `<span class="al-person ${p.isMe ? 'me' : ''}">${p.isMe ? '⭐ ' : ''}${esc(p.name)}<em>OVR ${p.ovr} · ${p.pts}/${p.reb}/${p.ast}</em></span>`).join('')}</div>` : '');
      return `<div class="al-item"><div class="al-title">${esc(a.zh)}</div>${who}</div>`;
    }).join('')}
    </div>`;
}

function seasonSummaryHTML(sum) {
  const team = TEAMS[sum.teamId] || NCAA_TEAMS[sum.teamId] || null;
  const lg = LEAGUES[sum.leagueId];
  const awardChips = (sum.awards || []).map(a => `<span class="chip chip-green">🏅 ${E.awardZh(a)}</span>`).join('') || '<span class="muted-2">本赛季没有个人奖项</span>';
  const nbaJump = sum.nbaJump;
  return `
    <div class="season-summary">
      <div class="ss-head">
        <div class="age">${sum.age} <small>岁</small></div>
        <div class="team">${team ? esc(team.zh) : ''}${lg ? ` · ${esc(lg.zh)}` : ''}${sum.kind === 'ncaa' ? ' · 大学赛季' : ''}</div>
      </div>
      <div class="ss-result">
        <div class="ss-rec num">${sum.record}</div>
        <div class="ss-zh">${esc(sum.resultZh)}</div>
      </div>
      <div class="label" style="margin-top:12px">赛季场均</div>
      <div class="season-avg">
        <div><div class="num">${sum.stats ? E.fmtAvg(sum.stats.pts) : '0'}</div><div class="lab">得分</div></div>
        <div><div class="num">${sum.stats ? E.fmtAvg(sum.stats.reb) : '0'}</div><div class="lab">篮板</div></div>
        <div><div class="num">${sum.stats ? E.fmtAvg(sum.stats.ast) : '0'}</div><div class="lab">助攻</div></div>
        <div><div class="num">${sum.stats ? E.fmtAvg(sum.stats.stl) : '0'}</div><div class="lab">抢断</div></div>
        <div><div class="num">${sum.stats ? E.fmtAvg(sum.stats.blk) : '0'}</div><div class="lab">盖帽</div></div>
      </div>
      <div class="label" style="margin-top:12px">个人奖项</div>
      <div class="ss-awards">${awardChips}</div>
      ${sum.highlight ? `<div class="ss-highlight">🔥 ${esc(sum.highlight)}</div>` : ''}
      ${sum.growthPoints ? `<div class="ss-growth">📈 本季成长点数 <b>+${sum.growthPoints}</b>（累计 ${sum.growthBank || 0}）</div>` : ''}
      ${sum.growthLog ? (() => {
        const g = sum.growthLog;
        const lines = [];
        if (g.gains && g.gains.length) lines.push(...g.gains.map(x => `<span class="chip chip-green">▲ ${x}</span>`));
        if (g.losses && g.losses.length) lines.push(...g.losses.map(x => `<span class="chip chip-red">▼ ${x}</span>`));
        return `<div class="ss-growth">能力变化：${lines.join(' ') || '—'}</div>`;
      })() : ''}
      ${sum.salary ? `<div class="ss-growth">💰 本季年薪 <b>${E.fmtMoney(sum.salary)}</b>${sum.contractLeft ? ` · 合同剩 ${sum.contractLeft} 年` : ''}</div>` : ''}
      ${leagueAwardsHTML(sum.leagueAwards)}
      <div style="height:14px"></div>
      ${nbaJump && nbaJump.available ? `<button class="btn btn-amber btn-lg btn-block" style="margin-bottom:8px" onclick="BL.tryNBAJump()">🚀 冲击 NBA（成功率约 ${nbaJump.pct}%）</button>` : ''}
      ${sum.growthPoints ? `<button class="btn btn-primary btn-lg btn-block" style="margin-bottom:8px" onclick="BL.openUpgrade()">📈 升级属性（${sum.growthBank || 0} 点）</button>` : ''}
      <button class="btn btn-outline btn-lg btn-block" onclick="BL.dismissSeasonSummary()">继续 →</button>
      <div style="height:24px"></div>
    </div>
  `;
}

// ---------- 属性升级 ----------
function upgradeHTML() {
  const s = app.state;
  if (!s) return '';
  const points = s.player.growthBank || 0;
  // 确保 attrs 存在
  const attrs = s.player.attrs || fallbackAttrs(s.player.overall);
  const rows = ATTR_LIST.map(({ key, zh, icon }) => {
    const val = attrs[key] || 60;
    return `<div class="upgrade-row">
      <span class="up-icon">${icon}</span>
      <span class="up-name">${zh}</span>
      <span class="up-val num">${val}</span>
      <button class="btn btn-primary btn-sm" onclick="BL.upgradeAttr('${key}')">+</button>
    </div>`;
  }).join('');
  return shell(`
    <div class="page-head">
      <button class="btn btn-ghost" onclick="BL.closeUpgrade()">← 返回</button>
      <h2>成长升级</h2>
      <span class="count">${points} 点</span>
    </div>
    <div class="scroll">
      <div class="upgrade-hint">每赛季获得的成长点数可分配给你的属性，属性影响比赛表现。</div>
      <div class="upgrade-list">${rows}</div>
      <div style="height:14px"></div>
      <button class="btn btn-primary btn-lg btn-block" onclick="BL.closeUpgrade()">完成升级 →</button>
      <div style="height:24px"></div>
    </div>
  `);
}

// ---------- 季后赛 ----------
function playoffsHTML(s) {
  const po = s.playoffs;
  const team = TEAMS[s.currentTeamId] || NCAA_TEAMS[s.currentTeamId] || null;
  const opp = TEAMS[po.currentOppId] || NCAA_TEAMS[po.currentOppId] || null;
  const roundNames = ['首轮', '分区半决赛', '总决赛'];
  const roundName = roundNames[po.round] || `第${po.round + 1}轮`;
  const recent = po.games.slice(-6).reverse().map(g => `
    <div class="season-game ${g.win ? 'win' : 'lose'}">
      <span class="sg-r">${g.home ? '主' : '客'}${g.win ? ' W' : ' L'}</span>
      <span class="sg-opp">${esc(g.opp)}</span>
      <span class="sg-score num">${g.myScore} : ${g.oppScore}</span>
      <span class="sg-me num">我 ${g.my.pts}分</span>
    </div>`).join('') || '<div class="empty">系列赛还没开打</div>';
  return shell(`
    ${topbarHTML()}
    <div class="scroll" style="padding-top:6px">
      <div class="playoffs-top">
        <div class="po-title">🏆 季后赛 · ${roundName}</div>
        <div class="po-series">
          <div class="po-team">${esc(team ? team.zh : '')}</div>
          <div class="po-score num">${po.myWins} : ${po.oppWins}</div>
          <div class="po-team">${esc(opp ? opp.zh : '对手')}</div>
        </div>
        <div class="po-best">BO7 · 先赢 4 场晋级</div>
      </div>

      <div class="label" style="margin-top:12px">系列赛比分</div>
      <div class="season-games">${recent}</div>

      <div class="season-actions">
        <button class="btn btn-primary" onclick="BL.simPlayoff(1)">▶ 模拟 1 场</button>
        <button class="btn btn-primary" onclick="BL.simPlayoff(3)">⏩ 模拟 3 场</button>
        <button class="btn btn-outline" onclick="BL.simPlayoffAll()">快进本轮</button>
      </div>
      <div style="height:24px"></div>
    </div>
    ${app.modal ? modalHTML() : ''}
  `);
}

function bannerHTML(snapshot) {
  if (!snapshot) return `<div class="empty" onclick="BL.next()" style="cursor:pointer">轻触继续</div>`;
  const team = snapshot.teamId ? (TEAMS[snapshot.teamId] || NCAA_TEAMS[snapshot.teamId] || null) : null;
  const lg = snapshot.leagueId ? LEAGUES[snapshot.leagueId] : null;
  const st = snapshot.stats || {};
  const avg = st.avg || {};
  const resultKey = snapshot.result.league;
  const resultText = E.resultZh(resultKey, lg);
  const nat = snapshot.national;
  const natText = nat ? nationalBannerText(nat) : null;
  return `
    <div class="banner" onclick="BL.next()" style="cursor:pointer">
      <div class="banner-head">
        <div class="age">${snapshot.age} <small>岁</small></div>
        <div class="team">${snapshot.youth ? '青训营' : team ? esc(team.zh) : ''}${lg ? ` · ${esc(lg.zh)}` : ''}${snapshot.ncaaYear ? ` · 大${['零','一','二','三','四','五'][snapshot.ncaaYear] || snapshot.ncaaYear}` : ''}</div>
      </div>
      <div class="banner-body">
        <div class="banner-row">
          <span class="k">能力</span>
          <span class="v num">${snapshot.overall} ${snapshot.suspended ? '<span class="txt-red">· 禁赛</span>' : ''}</span>
        </div>
        <div class="banner-row">
          <span class="k">角色</span>
          <span class="v">${snapshot.suspended ? '停赛' : E.roleName(snapshot.role)}</span>
        </div>
        ${snapshot.youth ? '' : `<div class="banner-row">
          <span class="k">赛季战绩</span>
          <span class="v ${resultKey === 'champion' ? 'hl' : ''}">${resultText}</span>
        </div>`}
        ${snapshot.cup ? `<div class="banner-row">
          <span class="k">杯赛</span>
          <span class="v ${snapshot.cup === 'cup_champion' ? 'hl' : ''}">${snapshot.cup === 'cup_champion' ? '冠军 🏆' : '决赛'}</span>
        </div>` : ''}
        ${natText ? `<div class="banner-row"><span class="k">国家队</span><span class="v">${natText}</span></div>` : ''}
        ${snapshot.trophies.length ? `<div class="banner-row"><span class="k">新奖杯</span><span class="v hl">${snapshot.trophies.map(E.trophyZh).join(' · ')}</span></div>` : ''}
        ${snapshot.awards.length ? `<div class="banner-row"><span class="k">个人荣誉</span><span class="v pos">${snapshot.awards.map(E.awardZh).join(' · ')}</span></div>` : ''}
        ${snapshot.highlight ? `<div class="banner-row"><span class="k">本赛季高光</span><span class="v hl">${esc(snapshot.highlight)}</span></div>` : ''}
        ${snapshot.youth || snapshot.ncaaYear ? '' : `<div class="banner-row">
          <span class="k">年薪</span>
          <span class="v num">${E.fmtMoney(snapshot.salary)}</span>
        </div>`}
      </div>
      ${snapshot.youth ? '' : `
      <div class="banner-stats" style="padding:10px 14px 12px">
        <div><div class="num">${st.g}</div><div class="lab">出场</div></div>
        <div><div class="num">${E.fmtAvg(avg.pts)}</div><div class="lab">得分</div></div>
        <div><div class="num">${E.fmtAvg(avg.reb)}</div><div class="lab">篮板</div></div>
        <div><div class="num">${E.fmtAvg(avg.ast)}</div><div class="lab">助攻</div></div>
        <div><div class="num">${st.pts ? Math.round(st.pts) : 0}</div><div class="lab">总得分</div></div>
      </div>`}
    </div>
    <div class="banner-tip">轻触继续</div>
    ${app.state && app.state.pendingGame && app.state.pendingGame.type === 'playoff_key' ? `
    <button class="btn btn-primary btn-block" style="margin-top:10px" onclick="event.stopPropagation();BL.enterGame()">🎮 模拟${app.state.pendingGame.stage === 'final' ? '总决赛' : '半决赛'}关键战</button>
    <div class="banner-tip" style="margin-top:6px">也可以轻触赛季卡继续，跳过这场比赛</div>` : ''}`;
}

function nationalBannerText(nat) {
  const zh = E.tournamentZh(nat.type);
  if (nat.result === 'not_called') return `${zh} · 未入选`;
  if (nat.result === 'not_qualified') return `${zh} · 未出线`;
  const stats = nat.stats;
  const statStr = stats ? ` · ${stats.g} 场 ${Math.round(stats.pts)} 分` : '';
  return `${zh} · ${E.resultZh(nat.result, null, 'national')}${statStr}`;
}

function eventHTML(ev) {
  const isShowdown = ev.type === 'showdown';
  const tag = isShowdown ? '决胜时刻' : ev.type === 'farewell_offer' || ev.type === 'voluntary_retire' || ev.type === 'farewell_style' ? '谢幕' : '决策';
  const canSim = isShowdown && app.state && app.state.pendingGame;
  return `
    <div class="event-card">
      <span class="event-tag">${tag}</span>
      <h2 class="event-title">${esc(ev.title)}</h2>
      <p class="event-text">${esc(ev.text)}</p>
      ${canSim ? `<button class="btn btn-primary btn-block sim-entry" onclick="BL.enterGame()">🎮 进入比赛模拟</button>` : ''}
      <div class="option-list">
        ${ev.options.map(o => `
          <button class="option" onclick="BL.choose('${o.id.replace(/'/g, '')}')">
            <div class="o-label">${esc(o.label)}</div>
            ${o.hint ? `<div class="o-hint">${esc(o.hint)}</div>` : ''}
          </button>`).join('')}
      </div>
    </div>`;
}

function receiptHTML(s) {
  const r = s.lastEventOutcome;
  const kind = r ? r.kind : 'neutral';
  const cls = kind === 'positive' ? '' : kind === 'negative' ? ' neg' : '';
  const title = kind === 'positive' ? '成了' : kind === 'negative' ? '砸了' : '回执';
  const text = r ? r.text : '你做了一个决定。';
  return `
    <div class="receipt${cls}" onclick="BL.next()" style="cursor:pointer">
      <div class="r-title">${title}</div>
      <div class="r-text">${esc(text)}</div>
    </div>`;
}

// ---------- 结算 ----------
function summaryHTML() {
  const s = app.state;
  const sum = E.buildSummary(s);
  if (!app.archived) {
    E.saveArchive(sum);
    app.archived = true;
  }
  const t = sum.totals;
  const nat = sum.national;
  const trophyCounts = E.trophyCounts(s.totals.trophies);
  const trophyEntries = Object.entries(trophyCounts).slice(0, 10);
  const titleChips = sum.titles.map(x => `<span class="sum-title">${TITLES.find(t => t.id === x.id)?.art || '🏅'} ${esc(TITLES.find(t => t.id === x.id)?.name || '')}</span>`).join('');
  return shell(`
    <div class="scroll summary-scroll">
      <div class="sum-hero">
        <div class="sum-hero-top">
          <div class="rating-ring" style="width:58px;height:58px">
            <svg viewBox="0 0 46 46">
              <circle cx="23" cy="23" r="20" fill="none" stroke="#27272a" stroke-width="3.5"/>
              <circle cx="23" cy="23" r="20" fill="none" stroke="#fbbf24" stroke-width="3.5"
                stroke-linecap="round" stroke-dasharray="125.66" stroke-dashoffset="${125.66 * (1 - sum.maxOverall / 99)}"/>
            </svg>
            <span class="val" style="font-size:16px">${sum.maxOverall}</span>
          </div>
          <div>
            <div class="name">${esc(sum.player.name)}</div>
            <div class="tags">
              <span class="chip chip-zinc">${esc(sum.player.nationality)}</span>
              <span class="chip chip-green">#${sum.player.number} ${esc(sum.player.positionEn)} ${esc(sum.player.position)}</span>
              <span class="chip chip-zinc">${esc(sum.player.hand)}手</span>
              ${sum.draft ? (sum.draft.undrafted
                ? `<span class="chip chip-red">选秀落选</span>`
                : `<span class="chip chip-amber">第${sum.draft.round}轮 · 第${sum.draft.pick}顺位${sum.draft.team ? ' · ' + esc(sum.draft.team) : ''}</span>`) : ''}
              ${sum.ncaaSeasons ? `<span class="chip chip-blue">NCAA ${sum.ncaaSeasons} 季</span>` : ''}
            </div>
          </div>
          <div class="sum-value">
            <div class="k">巅峰身价</div>
            <div class="v num">${E.fmtMoney(sum.peakValue)}</div>
            <div class="k" style="margin-top:8px">生涯总收入</div>
            <div class="v sub num">${E.fmtMoney(sum.totalIncome)}</div>
          </div>
        </div>
        <div class="sum-ending">
          <div class="k">生涯结局</div>
          <div class="t">${esc(sum.titles[0] ? TITLES.find(x => x.id === sum.titles[0].id)?.name || '传奇' : '完整的一生')}</div>
          <div class="p">超过了 ${sum.percentile}% 的球员</div>
          <div class="e">${esc(sum.epitaph)}</div>
        </div>
      </div>

      <div class="sum-block">
        <div class="sum-stats">
          <div><div class="k">出场</div><div class="v num">${E.fmtInt(t.apps)}</div></div>
          <div><div class="k">总得分</div><div class="v num">${E.fmtInt(t.pts)}</div></div>
          <div><div class="k">总篮板</div><div class="v num">${E.fmtInt(t.reb)}</div></div>
          <div><div class="k">总助攻</div><div class="v num">${E.fmtInt(t.ast)}</div></div>
          <div><div class="k">抢断</div><div class="v num">${E.fmtInt(t.stl)}</div></div>
          <div><div class="k">盖帽</div><div class="v num">${E.fmtInt(t.blk)}</div></div>
        </div>
      </div>

      ${nat ? `
      <div class="sum-block">
        <h4>国家队</h4>
        <div class="card" style="padding:12px;text-align:center">
          <div class="muted-2" style="font-weight:800">${esc(sum.player.nationality)} · 大赛 ${nat.games} 场 · ${nat.pts} 分 · ${nat.reb} 板 · ${nat.ast} 助 · 冠军 ${nat.golds} 次</div>
        </div>
      </div>` : ''}

      ${sum.highlights?.length ? `
      <div class="sum-block">
        <h4>生涯高光</h4>
        <div class="card" style="padding:14px">
          ${sum.highlights.map(h => `<div class="sum-legacy" style="margin-top:6px">· ${h.age}岁 ${esc(h.text)}</div>`).join('')}
        </div>
      </div>` : ''}

      ${sum.keyGames?.length ? `
      <div class="sum-block">
        <h4>关键之战</h4>
        <div class="card" style="padding:14px">
          ${sum.keyGames.map(g => `<div class="sum-legacy" style="margin-top:6px">· ${g.age}岁 ${esc(g.label)} ${g.won ? '🏆 胜' : '💔 负'} ${g.score} vs ${esc(g.opp)}（我 ${g.myPts}分 ${g.myReb}板 ${g.myAst}助）</div>`).join('')}
        </div>
      </div>` : ''}

      ${sum.rival ? `
      <div class="sum-block">
        <h4>一生之敌</h4>
        <div class="card" style="padding:14px">
          <div style="text-align:center;font-weight:900;font-size:15px">${esc(sum.rival.name)} <span class="muted-2" style="font-weight:700">· ${esc(sum.rival.nationality)} · 巅峰 ${sum.rival.peak}</span></div>
          <div style="margin-top:10px">
            <div class="banner-row"><span class="k">交手赛季</span><span class="v num">${sum.rival.series}</span></div>
            <div class="banner-row"><span class="k">总得分</span><span class="v num">你 ${E.fmtInt(sum.rival.myPts)} : ${E.fmtInt(sum.rival.rivalPts)} 他</span></div>
            <div class="banner-row"><span class="k">联赛冠军</span><span class="v num">你 ${sum.rival.myChamps} : ${sum.rival.rivalChamps} 他</span></div>
            <div class="banner-row"><span class="k">常规赛MVP</span><span class="v num">你 ${sum.rival.myMvp} : ${sum.rival.rivalMvp} 他</span></div>
          </div>
          <div style="margin-top:10px;text-align:center;font-size:12px;color:var(--amber);font-weight:800">
            ${sum.rival.myPts >= sum.rival.rivalPts ? `总得分，你赢了。` : `总得分，他赢了。`}
            ${sum.rival.myChamps > sum.rival.rivalChamps ? '冠军数，你也赢了。' : sum.rival.myChamps === sum.rival.rivalChamps ? '冠军数，打了个平手。' : '冠军数，他赢了。'}
          </div>
        </div>
      </div>` : ''}

      <div class="sum-block">
        <h4>荣誉室</h4>
        <div class="card" style="padding:12px 8px">
          ${trophyEntries.length ? `
          <div class="sum-trophies">
            ${trophyEntries.map(([id, n]) => {
              const meta = trophyMeta(id);
              return `<div class="trophy-item"><div class="art">${meta.art}</div><div class="name">${meta.name}</div>${n > 1 ? `<div class="count">×${n}</div>` : ''}</div>`;
            }).join('')}
          </div>` : `<div class="muted" style="text-align:center;padding:14px">还没有奖杯</div>`}
        </div>
      </div>

      ${sum.titles.length ? `
      <div class="sum-block">
        <h4>称号</h4>
        <div class="sum-titles">${titleChips}</div>
      </div>` : ''}

      ${sum.legacyLines.length ? `
      <div class="sum-block">
        <h4>那些时刻</h4>
        <div class="card" style="padding:14px">
          ${sum.legacyLines.map(l => `<div class="sum-legacy" style="margin-top:6px">· ${esc(l)}</div>`).join('')}
        </div>
      </div>` : ''}

      <div class="sum-block">
        <h4>效力过的球队</h4>
        ${sum.clubs.map(c => `
          <div class="sum-club">
            <div class="crest" style="background:${c.color};color:${c.isLight ? '#09090b' : '#fff'}">${esc(c.abbr)}</div>
            <div class="info">
              <div class="n">${esc(c.name)}</div>
              <div class="s">${c.seasons} 个赛季 · ${c.games} 场 · ${c.trophies.length ? c.trophies.length + ' 座奖杯' : '无冠'}</div>
            </div>
            <div class="stat"><b class="num">${c.pts}</b>分</div>
          </div>`).join('')}
      </div>

      <div class="sum-foot">
        ${APP_TITLE} · ${sum.seasonsCount} 个赛季<br/>
        本局编号 <span class="code" onclick="BL.copyCode()" style="cursor:pointer">${esc(sum.seed)}</span> · 点一下复制<br/>
        长按上方图片保存，或直接发给朋友
      </div>
    </div>
    <div class="bottom-actions">
      <div class="row2">
        <button class="btn btn-outline" onclick="BL.replay()">再来一局</button>
        <button class="btn btn-primary" onclick="BL.openShare()">分享战绩卡</button>
      </div>
      <button class="btn btn-ghost" onclick="BL.openArchive()">返回历史档案</button>
    </div>
    ${app.modal ? modalHTML() : ''}
  `);
}

function trophyMeta(id) {
  if (id === 'world_cup') return { art: '🏆', name: '世界杯' };
  if (id === 'olympics') return { art: '🥇', name: '奥运会' };
  if (id === 'continental') return { art: '🌍', name: '洲际冠军' };
  if (id.startsWith('league:')) return { art: '🏀', name: LEAGUES[id.slice(7)]?.champ?.replace('总冠军', '总冠军') || '联赛冠军' };
  if (id.startsWith('cup:')) return { art: '🏅', name: LEAGUES[id.slice(4)]?.cupName || '杯赛冠军' };
  return { art: '🏅', name: '冠军' };
}

// ---------- 档案 / 图鉴 ----------
function archiveHTML() {
  const list = E.loadArchive();
  return shell(`
    <div class="page-head">
      <button class="btn btn-ghost" onclick="BL.backHome()">← 返回</button>
      <h2>历史档案</h2>
      <span class="count">${list.length}</span>
    </div>
    <div class="scroll">
      ${list.length ? list.map(a => {
        const t = a.totals;
        const titles = (a.titles || []).map(x => TITLES.find(t2 => t2.id === x.id)).filter(Boolean);
        return `
        <button class="archive-item" onclick="BL.viewArchive(${list.indexOf(a)})">
          <div class="row1">
            <span class="n">${esc(a.player.name)} <span class="muted" style="font-weight:600">${esc(a.player.positionEn)} · ${esc(a.player.nationality)}</span></span>
            <span class="peak num">巅峰 ${a.maxOverall}</span>
          </div>
          <div class="row2">
            <span class="chip chip-zinc">${a.seasonsCount} 季</span>
            <span class="chip chip-zinc num">${E.fmtInt(t.pts)} 分</span>
            <span class="chip chip-amber num">${E.fmtMoney(a.totalIncome)}</span>
            ${titles.slice(0, 2).map(x => `<span class="chip chip-green">${x.art} ${x.name}</span>`).join('')}
          </div>
          <div class="when">${timeAgo(a.savedAt)} · ${esc(a.seed)}</div>
        </button>`;
      }).join('') : `<div class="empty">还没有踢完的生涯<br/>去开启第一局吧</div>`}
    </div>
  `);
}

function archiveDetailHTML() {
  const a = app.archiveDetail;
  if (!a) return archiveHTML();
  const t = a.totals;
  const nat = a.national;
  const trophyCounts = {};
  const allTrophies = a.clubs.flatMap(c => c.trophies || []);
  allTrophies.forEach(id => trophyCounts[id] = (trophyCounts[id] || 0) + 1);
  const trophies = Object.entries(trophyCounts).slice(0, 10);
  const titles = (a.titles || []).map(x => TITLES.find(t2 => t2.id === x.id)).filter(Boolean);
  return shell(`
    <div class="page-head">
      <button class="btn btn-ghost" onclick="BL.backArchive()">← 返回</button>
      <h2>档案详情</h2>
    </div>
    <div class="scroll summary-scroll">
      <div class="sum-hero">
        <div class="sum-hero-top">
          <div class="rating-ring" style="width:58px;height:58px">
            <svg viewBox="0 0 46 46">
              <circle cx="23" cy="23" r="20" fill="none" stroke="#27272a" stroke-width="3.5"/>
              <circle cx="23" cy="23" r="20" fill="none" stroke="#fbbf24" stroke-width="3.5"
                stroke-linecap="round" stroke-dasharray="125.66" stroke-dashoffset="${125.66 * (1 - a.maxOverall / 99)}"/>
            </svg>
            <span class="val" style="font-size:16px">${a.maxOverall}</span>
          </div>
          <div>
            <div class="name">${esc(a.player.name)}</div>
            <div class="tags">
              <span class="chip chip-zinc">${esc(a.player.nationality)}</span>
              <span class="chip chip-green">#${a.player.number} ${esc(a.player.positionEn)} ${esc(a.player.position)}</span>
            </div>
          </div>
          <div class="sum-value">
            <div class="k">巅峰身价</div>
            <div class="v num">${E.fmtMoney(a.peakValue)}</div>
            <div class="k" style="margin-top:8px">总收入</div>
            <div class="v sub num">${E.fmtMoney(a.totalIncome)}</div>
          </div>
        </div>
        <div class="sum-ending">
          <div class="k">生涯结局</div>
          <div class="t">${esc(titles[0]?.name || '完整的一生')}</div>
          <div class="p">超过了 ${a.percentile}% 的球员</div>
          <div class="e">${esc(a.epitaph)}</div>
        </div>
      </div>
      <div class="sum-block">
        <div class="sum-stats">
          <div><div class="k">出场</div><div class="v num">${E.fmtInt(t.apps)}</div></div>
          <div><div class="k">总得分</div><div class="v num">${E.fmtInt(t.pts)}</div></div>
          <div><div class="k">总篮板</div><div class="v num">${E.fmtInt(t.reb)}</div></div>
          <div><div class="k">总助攻</div><div class="v num">${E.fmtInt(t.ast)}</div></div>
          <div><div class="k">抢断</div><div class="v num">${E.fmtInt(t.stl)}</div></div>
          <div><div class="k">盖帽</div><div class="v num">${E.fmtInt(t.blk)}</div></div>
        </div>
      </div>
      ${nat ? `<div class="sum-block"><h4>国家队</h4><div class="card" style="padding:12px;text-align:center"><div class="muted-2" style="font-weight:800">${esc(a.player.nationality)} · 大赛 ${nat.games} 场 · ${nat.pts} 分 · 冠军 ${nat.golds} 次</div></div></div>` : ''}
      ${a.highlights?.length ? `
      <div class="sum-block">
        <h4>生涯高光</h4>
        <div class="card" style="padding:14px">
          ${a.highlights.map(h => `<div class="sum-legacy" style="margin-top:6px">· ${h.age}岁 ${esc(h.text)}</div>`).join('')}
        </div>
      </div>` : ''}
      ${a.keyGames?.length ? `
      <div class="sum-block">
        <h4>关键之战</h4>
        <div class="card" style="padding:14px">
          ${a.keyGames.map(g => `<div class="sum-legacy" style="margin-top:6px">· ${g.age}岁 ${esc(g.label)} ${g.won ? '🏆 胜' : '💔 负'} ${g.score} vs ${esc(g.opp)}（我 ${g.myPts}分 ${g.myReb}板 ${g.myAst}助）</div>`).join('')}
        </div>
      </div>` : ''}
      ${a.rival ? `
      <div class="sum-block">
        <h4>一生之敌</h4>
        <div class="card" style="padding:14px">
          <div style="text-align:center;font-weight:900;font-size:15px">${esc(a.rival.name)} <span class="muted-2" style="font-weight:700">· ${esc(a.rival.nationality)} · 巅峰 ${a.rival.peak}</span></div>
          <div style="margin-top:10px">
            <div class="banner-row"><span class="k">交手赛季</span><span class="v num">${a.rival.series}</span></div>
            <div class="banner-row"><span class="k">总得分</span><span class="v num">你 ${E.fmtInt(a.rival.myPts)} : ${E.fmtInt(a.rival.rivalPts)} 他</span></div>
            <div class="banner-row"><span class="k">联赛冠军</span><span class="v num">你 ${a.rival.myChamps} : ${a.rival.rivalChamps} 他</span></div>
            <div class="banner-row"><span class="k">常规赛MVP</span><span class="v num">你 ${a.rival.myMvp} : ${a.rival.rivalMvp} 他</span></div>
          </div>
          <div style="margin-top:10px;text-align:center;font-size:12px;color:var(--amber);font-weight:800">
            ${a.rival.myPts >= a.rival.rivalPts ? `总得分，你赢了。` : `总得分，他赢了。`}
            ${a.rival.myChamps > a.rival.rivalChamps ? '冠军数，你也赢了。' : a.rival.myChamps === a.rival.rivalChamps ? '冠军数，打了个平手。' : '冠军数，他赢了。'}
          </div>
        </div>
      </div>` : ''}
      <div class="sum-block">
        <h4>荣誉室</h4>
        <div class="card" style="padding:12px 8px">
          ${trophies.length ? `<div class="sum-trophies">${trophies.map(([id, n]) => {
            const meta = trophyMeta(id);
            return `<div class="trophy-item"><div class="art">${meta.art}</div><div class="name">${meta.name}</div>${n > 1 ? `<div class="count">×${n}</div>` : ''}</div>`;
          }).join('')}</div>` : `<div class="muted" style="text-align:center;padding:14px">还没有奖杯</div>`}
        </div>
      </div>
      ${titles.length ? `<div class="sum-block"><h4>称号</h4><div class="sum-titles">${titles.map(x => `<span class="sum-title">${x.art} ${esc(x.name)}</span>`).join('')}</div></div>` : ''}
      <div class="sum-block">
        <h4>效力过的球队</h4>
        ${a.clubs.map(c => `
          <div class="sum-club">
            <div class="crest" style="background:${c.color};color:${c.isLight ? '#09090b' : '#fff'}">${esc(c.abbr)}</div>
            <div class="info"><div class="n">${esc(c.name)}</div><div class="s">${c.seasons} 个赛季 · ${c.games} 场 · ${c.trophies?.length || 0} 座奖杯</div></div>
            <div class="stat"><b class="num">${c.pts}</b>分</div>
          </div>`).join('')}
      </div>
      <div class="sum-foot">${APP_TITLE} · ${a.seasonsCount} 个赛季<br/>本局编号 ${esc(a.seed)}</div>
    </div>
  `);
}

function galleryHTML() {
  const g = E.galleryState();
  return shell(`
    <div class="page-head">
      <button class="btn btn-ghost" onclick="BL.backHome()">← 返回</button>
      <h2>称号图鉴</h2>
      <span class="count">${g.unlocked.size}/${g.total}</span>
    </div>
    <div class="scroll" style="padding:0">
      <div class="gallery-grid">
        ${TITLES.map(t => {
          const q = g.unlocked.get(t.id);
          return q
            ? `<div class="gallery-cell"><div class="art">${t.art}</div><div class="name">${esc(t.name)}</div><div class="q">${esc(q)}</div></div>`
            : `<div class="gallery-cell locked"><div class="art">${t.art}</div><div class="name">${esc(t.name)}</div><div class="q">${esc(t.hint)}</div></div>`;
        }).join('')}
      </div>
    </div>
  `);
}

// ---------- 建球员（13次锁属性） ----------
function buildHTML() {
  const b = app.build;
  const id = app.identity;
  if (!b) { app.view = 'identity'; render(); return ''; }
  const done = b.step >= ATTR_LIST.length;
  const pct = buildProgress(b);
  const posZh = POS_ZH[id.position];

  if (done) {
    const rv = app.buildResult;
    const attrsHTML = ATTR_LIST.map(({ key, zh, icon }) => {
      const rec = rv.record[key];
      return `<div class="attr-cell">
        <div class="a-icon">${icon}</div>
        <div class="a-zh">${zh}</div>
        <div class="a-val num">${rec ? rec.v : '—'}</div>
        <div class="a-from">${rec ? esc(rec.from) + ' · ' + esc(rec.team) : ''}</div>
      </div>`;
    }).join('');
    return shell(`
      <div class="page-head">
        <button class="btn btn-ghost" onclick="BL.backBuild()">← 返回</button>
        <h2>揭晓我的球员</h2>
      </div>
      <div class="scroll">
        <div class="reveal-card">
          <div class="reveal-name">${esc(id.name)}</div>
          <div class="reveal-pos">${posZh} · ${esc(COUNTRIES[id.nationality].flag)} ${esc(COUNTRIES[id.nationality].zh)}</div>
          <div class="reveal-ovr">
            <div class="ro-label">综合能力</div>
            <div class="ro-val num">${rv.ovr}</div>
            <div class="ro-pot num">潜力 ${rv.potential}</div>
          </div>
          ${rv.similar ? `<div class="reveal-similar">模板：${esc(rv.similar.name)}（${esc(rv.similar.team)}）</div>` : ''}
        </div>
        <div class="label" style="margin-top:16px">属性构成</div>
        <div class="attr-grid">${attrsHTML}</div>
        <div style="height:14px"></div>
        <button class="btn btn-primary btn-lg btn-block" onclick="BL.startCareer()">开始生涯</button>
        <div style="height:24px"></div>
      </div>
    `);
  }

  // 进行中：进度 + 老虎机球队 + 球员卡
  const teamName = b.currentTeam ? (b.currentTeam) : null;
  const selected = b.selectedPlayer;
  const hasTeam = !!teamName;
  const rosterCards = b.drawn.length
    ? b.drawn.map((p, i) => `
        <button class="player-card ${p.historical ? 'historical' : ''} ${selected && selected.name === p.name ? 'active' : ''}" onclick="BL.pickBuildPlayer(${i})">
          ${playerPortraitHTML(p)}
          <div class="pc-pos">${p.pos}</div>
          <div class="pc-name">${esc(p.cname || p.name)}</div>
          ${p.historical ? `<span class="pc-badge ${p.tier === 'hall-of-fame' ? 'hof' : 'astar'}">${p.tier === 'hall-of-fame' ? '🏆 名人堂' : '⭐ 全明星'}${p.era ? ' · ' + p.era : ''}</span>` : ''}
          <div class="pc-ovr num">${p.ovr}</div>
          <div class="pc-type">${esc(p.type || '')}</div>
        </button>`).join('')
    : '<div class="empty">点击下方按钮随机抽一支球队</div>';

  const lockedList = ATTR_LIST.map(({ key, zh, icon }) => {
    const l = b.attrs[key];
    // 选中球员后：未锁定属性槽显示该球员该属性的预览值（带跨位置衰减）
    let preview = null;
    let clickable = false;
    if (!l && selected) {
      const val = attrPreview(b, selected, key);
      if (val != null) { preview = val; clickable = true; }
    }
    return `<div class="attr-lock ${l ? 'done' : clickable ? 'pickable' : 'todo'}" ${clickable ? `onclick="BL.lockIt('${key}')"` : ''}>
      <span class="al-icon">${icon}</span>
      <span class="al-zh">${zh}</span>
      <span class="al-val num">${l ? l.value : (preview != null ? preview : '·')}</span>
      ${l ? `<span class="al-from">${esc(l.from)}</span>` : ''}
    </div>`;
  }).join('');

  // 按钮状态
  const spinBtn = hasTeam
    ? `<button class="btn btn-block btn-disabled" disabled>🎲 本队已锁定，先选球员锁定属性</button>`
    : `<button class="btn btn-primary btn-block" onclick="BL.spinTeam()">🎲 随机球队</button>`;
  const rerollBtn = hasTeam && !selected && b.rerollsLeft > 0
    ? `<button class="btn btn-outline btn-block" onclick="BL.rerollPlayers()">换 5 张球员卡（剩 ${b.rerollsLeft}）</button>`
    : '';

  return shell(`
    <div class="page-head">
      <button class="btn btn-ghost" onclick="BL.backBuild()">← 返回</button>
      <h2>抽取属性 · ${id.position.toUpperCase()}</h2>
    </div>
    <div class="scroll">
      <div class="build-progress"><div class="bp-bar" style="width:${pct}%"></div><span class="bp-txt">${b.step}/${ATTR_LIST.length}</span></div>
      <div class="label">第 ${b.step + 1}/${ATTR_LIST.length} 步 · ${selected ? `已选中 ${esc(selected.cname || selected.name)}，点下方任一属性锁定` : (hasTeam ? '在下方球员卡中选一人' : '抽球队、选球员')}</div>

      <div class="attr-lock-grid">${lockedList}</div>

      <div class="team-slot" id="team-slot">
        ${hasTeam ? `<div class="ts-team">🎰 抽中：${esc(teamName)}</div>` : '<div class="ts-empty">待抽取</div>'}
      </div>
      <div class="roster-area">${rosterCards}</div>

      <div class="build-actions">
        ${spinBtn}
        ${rerollBtn}
      </div>
      <div class="build-hint">抽到球队后不可再换队，只能换球员卡；选中球员并锁定一项属性后，自动进入下一支球队。</div>
      <div style="height:24px"></div>
    </div>
  `);
}

// 球员头像（现役在 assets/img/player/，历史名宿在 assets/img/historical/；无头像用首字占位）
function playerPortraitHTML(p) {
  if (p.nbaId) {
    const idStr = String(p.nbaId);
    let img = null;
    if (idStr.startsWith('h:')) {
      img = `assets/img/historical/${idStr.slice(2)}.png`;
    } else if (idStr.startsWith('slug:')) {
      img = `assets/img/player/${idStr.slice(5)}.png`;
    } else {
      img = `assets/img/player/${idStr}.png`;
    }
    return `<div class="pc-portrait"><img src="${img}" alt="" loading="lazy" onerror="this.style.display='none';this.parentNode.classList.add('ph');this.parentNode.querySelector('.ph-txt').style.display='flex'"/><span class="ph-txt" style="display:none">${esc((p.cname || p.name || '?').slice(0, 1))}</span></div>`;
  }
  return `<div class="pc-portrait ph"><span class="ph-txt">${esc((p.cname || p.name || '?').slice(0, 1))}</span></div>`;
}

// 计算选中球员某属性的预览值（含跨位置衰减）
function attrPreview(state, player, attrKey) {
  const src = srcPosKey(player.pos);
  const penalty = posPenalty(state.userPos, src);
  const raw = player[attrKey];
  if (raw == null) return null;
  return Math.round(Math.max(25, Math.min(99, raw * penalty)));
}

// ---------- 单场模拟 ----------
function gameHTML() {
  if (app.gameView === 'pre') return gamePreHTML();
  if (app.gameView === 'result') return gameResultHTML();
  return gamePlayHTML();
}

function gamePreHTML() {
  const ctx = app.gameCtx;
  if (!ctx) return '<div class="empty">无比赛</div>';
  return shell(`
    <div class="page-head"><h2>🏀 ${esc(ctx.label)}</h2></div>
    <div class="scroll">
      <div class="game-pre-card">
        <div class="gp-home"><div class="gp-name">${esc(ctx.homeTeam.zh)}</div><div class="gp-abbr">${esc(ctx.homeTeam.abbr)}</div></div>
        <div class="gp-vs">VS</div>
        <div class="gp-away"><div class="gp-name">${esc(ctx.awayTeam.zh)}</div><div class="gp-abbr">${esc(ctx.awayTeam.abbr)}</div></div>
      </div>
      <button class="btn btn-primary btn-lg btn-block" style="margin-top:16px" onclick="BL.playGame()">▶ 开始比赛</button>
      <button class="btn btn-outline btn-block" style="margin-top:8px" onclick="BL.skipGame()">跳过，用赛果直接判定</button>
      <div style="height:24px"></div>
    </div>
  `);
}

function gamePlayHTML() {
  const g = app.game;
  if (!g) return '<div class="empty">无比赛</div>';
  const visible = Math.min(app.gameStep, g.log.length);
  const logs = g.log.slice(0, visible).map((l, i) => {
    const [hs, as] = g.scoreSnap[i] || [0, 0];
    return `<div class="pbl-row"><span class="pbl-s">${hs}-${as}</span><span class="pbl-l">${l}</span></div>`;
  }).join('');
  const playing = visible < g.log.length;
  const hName = g.home.zh;
  const aName = g.away.zh;
  const cur = g.scoreSnap[visible - 1] || [0, 0];
  return shell(`
    <div class="game-top">
      <div class="game-score">
        <div class="gs-team"><div class="t">${esc(hName)}</div><div class="s num">${cur[0]}</div></div>
        <div class="gs-mid"><div class="gs-q">第 ${(app.gameQ || 1)} 节</div><div class="gs-t">LIVE</div></div>
        <div class="gs-team right"><div class="s num">${cur[1]}</div><div class="t">${esc(aName)}</div></div>
      </div>
      <div class="game-ctrl">
        ${playing ? `<button class="btn btn-outline" onclick="BL.gameFaster()">⏩ ${app.gameSpeed}x</button><button class="btn btn-primary" onclick="BL.gamePause()">⏸ 暂停</button>` : `<button class="btn btn-primary" onclick="BL.gameReplay()">🔁 重看</button>`}
      </div>
    </div>
    <div class="pbl-list">${logs || '<div class="empty">开场…</div>'}</div>
    ${!playing ? `<button class="btn btn-primary btn-block" style="margin:12px 0 24px" onclick="BL.gameNext()">查看技术统计 →</button>` : ''}
  `);
}

function gameResultHTML() {
  const g = app.game;
  if (!g) return '<div class="empty">无比赛</div>';
  const won = g.winner === 'home';
  const boxHome = g.box.home.players;
  const boxAway = g.box.away.players;
  const rows = (players) => players.map(p => `
    <tr class="${p.isMe ? 'me' : ''}">
      <td>${p.isMe ? '⭐ ' : ''}${esc(p.name)}</td>
      <td>${p.pos}</td>
      <td>${p.pts}</td><td>${p.reb}</td><td>${p.ast}</td><td>${p.stl}</td><td>${p.blk}</td>
    </tr>`).join('');
  const myLine = g.meBox;
  return shell(`
    <div class="game-result ${won ? 'win' : 'lose'}">
      <div class="gr-badge">${won ? '🏆 胜利' : '💔 失利'}</div>
      <div class="gr-score num">${g.homeScore} : ${g.awayScore}</div>
      <div class="gr-teams">${esc(g.home.zh)} vs ${esc(g.away.zh)}</div>
    </div>
    <div class="scroll" style="padding-top:10px">
      ${myLine ? `<div class="me-line">
        <div><div class="num">${myLine.pts}</div><div class="lab">得分</div></div>
        <div><div class="num">${myLine.reb}</div><div class="lab">篮板</div></div>
        <div><div class="num">${myLine.ast}</div><div class="lab">助攻</div></div>
        <div><div class="num">${myLine.stl}</div><div class="lab">抢断</div></div>
        <div><div class="num">${myLine.blk}</div><div class="lab">盖帽</div></div>
      </div>` : ''}
      <div class="label" style="margin-top:12px">${esc(g.home.zh)}</div>
      <table class="box-table"><thead><tr><th>球员</th><th>位</th><th>分</th><th>板</th><th>助</th><th>抢</th><th>帽</th></tr></thead>
      <tbody>${rows(boxHome)}</tbody></table>
      <div class="label" style="margin-top:12px">${esc(g.away.zh)}</div>
      <table class="box-table"><thead><tr><th>球员</th><th>位</th><th>分</th><th>板</th><th>助</th><th>抢</th><th>帽</th></tr></thead>
      <tbody>${rows(boxAway)}</tbody></table>
      <button class="btn btn-primary btn-lg btn-block" style="margin:16px 0 24px" onclick="BL.finishGame()">${won ? '拿下胜利，继续生涯 →' : '接受结果，继续生涯 →'}</button>
    </div>
  `);
}

// ---------- 弹层 ----------
function modalHTML() {
  const m = app.modal;
  if (!m) return '';
  if (m.type === 'gamebox' || m.type === 'mate') {
    return m.html || '';
  }
  if (m.type === 'updates') {
    return `<div class="modal-mask" onclick="if(event.target===this)BL.closeModal()">
      <div class="modal">
        <div class="close-row"><button class="btn btn-ghost" onclick="BL.closeModal()">✕</button></div>
        <h3>更新记录</h3>
        ${[...UPDATES].reverse().map(u => `
          <div class="update-item">
            <div class="ver">v${u.version}</div>
            <div class="ti">${esc(u.title)}</div>
            <ul>${u.items.map(i => `<li>${esc(i)}</li>`).join('')}</ul>
          </div>`).join('')}
      </div>
    </div>`;
  }
  if (m.type === 'share') {
    return `<div class="modal-mask" onclick="if(event.target===this)BL.closeModal()">
      <div class="modal">
        <div class="close-row"><button class="btn btn-ghost" onclick="BL.closeModal()">✕</button></div>
        <h3>分享战绩卡</h3>
        ${app.shareDataUrl ? `<img class="share-preview" src="${app.shareDataUrl}" alt="生涯战绩卡"/>` : '<div class="empty">正在生成…</div>'}
        <div style="margin-top:12px;display:grid;grid-template-columns:1fr 1fr;gap:8px">
          <button class="btn btn-outline" onclick="BL.copyCode()">复制编号</button>
          <button class="btn btn-primary" onclick="BL.downloadShare()">下载图片</button>
        </div>
        <div class="share-hint">长按上方图片保存，或直接发给朋友<br/>别人输入你的编号，就能看到同一段生涯</div>
      </div>
    </div>`;
  }
  return '';
}

// ---------- 分享图 ----------
function drawShare(sum) {
  const W = 1080, H = 1600;
  const cv = document.createElement('canvas');
  cv.width = W; cv.height = H;
  const ctx = cv.getContext('2d');
  const font = '"PingFang SC","Microsoft YaHei",-apple-system,sans-serif';
  const F = (size, weight = 400) => `${weight} ${size}px ${font}`;

  // 背景
  ctx.fillStyle = '#09090b';
  ctx.fillRect(0, 0, W, H);
  // 球场线装饰
  ctx.strokeStyle = 'rgba(255,255,255,0.045)';
  ctx.lineWidth = 4;
  ctx.beginPath();
  ctx.arc(W / 2, 250, 210, 0, Math.PI * 2);
  ctx.stroke();
  ctx.beginPath();
  ctx.arc(W / 2, H - 120, 320, Math.PI, Math.PI * 2);
  ctx.stroke();

  // 顶部
  const ringR = 74, cx = 150, cy = 205;
  ctx.strokeStyle = '#27272a';
  ctx.lineWidth = 12;
  ctx.beginPath(); ctx.arc(cx, cy, ringR, 0, Math.PI * 2); ctx.stroke();
  ctx.strokeStyle = '#fbbf24';
  ctx.beginPath(); ctx.arc(cx, cy, ringR, -Math.PI / 2, -Math.PI / 2 + Math.PI * 2 * sum.maxOverall / 99); ctx.stroke();
  ctx.fillStyle = '#fafafa';
  ctx.font = F(44, 900);
  ctx.textAlign = 'center'; ctx.textBaseline = 'middle';
  ctx.fillText(sum.maxOverall, cx, cy);
  ctx.font = F(17, 700);
  ctx.fillStyle = '#71717a';
  ctx.fillText('生涯最高', cx, cy + 58);

  ctx.textAlign = 'left';
  ctx.fillStyle = '#fafafa';
  ctx.font = F(58, 900);
  ctx.fillText(sum.player.name, 252, 178);
  const tags = `${sum.player.nationality}  ·  #${sum.player.number} ${sum.player.positionEn} ${sum.player.position}  ·  ${sum.player.hand}手`;
  ctx.font = F(23, 700);
  ctx.fillStyle = '#a1a1aa';
  ctx.fillText(tags, 254, 232);

  ctx.textAlign = 'right';
  ctx.fillStyle = '#71717a';
  ctx.font = F(18, 700);
  ctx.fillText('巅峰身价', W - 60, 168);
  ctx.fillStyle = '#fbbf24';
  ctx.font = F(34, 900);
  ctx.fillText(E.fmtMoney(sum.peakValue), W - 60, 208);
  ctx.fillStyle = '#71717a';
  ctx.font = F(18, 700);
  ctx.fillText('生涯总收入', W - 60, 252);
  ctx.fillStyle = 'rgba(251,191,36,0.85)';
  ctx.font = F(26, 900);
  ctx.fillText(E.fmtMoney(sum.totalIncome), W - 60, 288);

  // 结局条
  roundRect(ctx, 56, 330, W - 112, 158, 22);
  ctx.fillStyle = 'rgba(251,191,36,0.07)';
  ctx.fill();
  ctx.strokeStyle = 'rgba(251,191,36,0.4)';
  ctx.stroke();
  const topTitle = TITLES.find(t => t.id === sum.titles[0]?.id);
  ctx.textAlign = 'center';
  ctx.fillStyle = 'rgba(251,191,36,0.85)';
  ctx.font = F(18, 800);
  ctx.fillText('生涯结局', W / 2, 368);
  ctx.fillStyle = '#fbbf24';
  ctx.font = F(40, 900);
  ctx.fillText(topTitle ? topTitle.name : '完整的一生', W / 2, 414);
  ctx.fillStyle = '#34d399';
  ctx.font = F(22, 900);
  ctx.fillText(`超过了 ${sum.percentile}% 的球员`, W / 2, 456);

  // 数据
  const t = sum.totals;
  const stats = [[E.fmtInt(t.apps), '出场'], [E.fmtInt(t.pts), '总得分'], [E.fmtInt(t.reb), '总篮板'], [E.fmtInt(t.ast), '总助攻'], [E.fmtInt(t.stl), '抢断'], [E.fmtInt(t.blk), '盖帽']];
  const cellW = (W - 112) / 6;
  ctx.textAlign = 'center';
  stats.forEach(([v, k], i) => {
    const x = 56 + cellW * i + cellW / 2;
    ctx.fillStyle = '#fafafa';
    ctx.font = F(28, 900);
    ctx.fillText(v, x, 545);
    ctx.fillStyle = '#71717a';
    ctx.font = F(15, 700);
    ctx.fillText(k, x, 578);
  });

  let y = 630;
  ctx.textAlign = 'left';

  // 国家队
  if (sum.national) {
    ctx.fillStyle = '#a1a1aa';
    ctx.font = F(22, 700);
    ctx.fillText(`${sum.player.nationality} 国家队：大赛 ${sum.national.games} 场 · ${sum.national.pts} 分 · ${sum.national.reb} 板 · ${sum.national.ast} 助 · 冠军 ${sum.national.golds} 次`, 56, y);
    y += 52;
  }
  if (sum.rival) {
    ctx.fillStyle = '#a1a1aa';
    ctx.font = F(22, 700);
    ctx.fillText(`一生之敌 ${sum.rival.name}：总得分 ${sum.rival.myPts}:${sum.rival.rivalPts} · 冠军 ${sum.rival.myChamps}:${sum.rival.rivalChamps}`, 56, y);
    y += 52;
  }

  // 荣誉
  const trophies = sum.clubs.flatMap(c => (c.trophies || []).map(x => x));
  const tcount = {};
  trophies.forEach(x => tcount[x] = (tcount[x] || 0) + 1);
  const entries = Object.entries(tcount);
  if (entries.length) {
    ctx.fillStyle = '#71717a';
    ctx.font = F(17, 800);
    ctx.fillText('荣 誉 室', 56, y);
    y += 14;
    let tx = 56;
    const per = 170;
    entries.slice(0, 6).forEach(([id, n]) => {
      const meta = trophyMeta(id);
      ctx.font = F(34);
      ctx.textAlign = 'center';
      ctx.fillText(meta.art, tx + 45, y + 28);
      ctx.font = F(17, 800);
      ctx.fillStyle = '#d4d4d8';
      ctx.fillText(`${meta.name}${n > 1 ? ' ×' + n : ''}`, tx + 100, y + 33);
      ctx.textAlign = 'left';
      tx += per;
      if (tx > W - 120) { tx = 56; y += 56; }
    });
    y += 60;
  }

  // 球队
  ctx.fillStyle = '#71717a';
  ctx.font = F(17, 800);
  ctx.fillText('效 力 球 队', 56, y);
  y += 12;
  sum.clubs.slice(0, 5).forEach(c => {
    y += 44;
    ctx.fillStyle = c.color;
    ctx.beginPath();
    ctx.arc(56 + 19, y - 16, 19, 0, Math.PI * 2);
    ctx.fill();
    ctx.fillStyle = c.isLight ? '#09090b' : '#fff';
    ctx.font = F(13, 900);
    ctx.textAlign = 'center';
    ctx.fillText(c.abbr.slice(0, 3), 56 + 19, y - 12);
    ctx.textAlign = 'left';
    ctx.fillStyle = '#e4e4e7';
    ctx.font = F(20, 800);
    ctx.fillText(c.name, 104, y - 10);
    ctx.fillStyle = '#71717a';
    ctx.font = F(16, 700);
    ctx.fillText(`${c.seasons} 季 · ${c.games} 场 · ${c.pts} 分 · ${c.trophies?.length || 0} 冠`, 104, y + 16);
  });
  y += 44;

  // 称号
  if (sum.titles.length) {
    ctx.fillStyle = '#71717a';
    ctx.font = F(17, 800);
    ctx.fillText('称 号', 56, y);
    y += 12;
    let tx = 56;
    sum.titles.slice(0, 8).forEach(tid => {
      const meta = TITLES.find(x => x.id === tid.id);
      if (!meta) return;
      const text = `${meta.art} ${meta.name}`;
      ctx.font = F(18, 800);
      const w = ctx.measureText(text).width + 30;
      if (tx + w > W - 56) { tx = 56; y += 44; }
      roundRect(ctx, tx, y, w, 34, 17);
      ctx.fillStyle = 'rgba(251,191,36,0.10)';
      ctx.fill();
      ctx.strokeStyle = 'rgba(251,191,36,0.35)';
      ctx.stroke();
      ctx.fillStyle = '#fbbf24';
      ctx.textAlign = 'center';
      ctx.fillText(text, tx + w / 2, y + 22);
      ctx.textAlign = 'left';
      tx += w + 10;
    });
    y += 54;
  }

  // 结尾
  ctx.strokeStyle = '#27272a';
  ctx.beginPath(); ctx.moveTo(56, H - 108); ctx.lineTo(W - 56, H - 108); ctx.stroke();
  ctx.textAlign = 'center';
  ctx.fillStyle = '#71717a';
  ctx.font = F(17, 700);
  ctx.fillText(`${APP_TITLE} · 从青训到传奇`, W / 2, H - 72);
  ctx.fillStyle = '#3f3f46';
  ctx.font = F(15, 700);
  ctx.fillText(`编号 ${sum.seed}`, W / 2, H - 42);

  app.shareDataUrl = cv.toDataURL('image/png');
  return app.shareDataUrl;
}

function roundRect(ctx, x, y, w, h, r) {
  ctx.beginPath();
  ctx.moveTo(x + r, y);
  ctx.arcTo(x + w, y, x + w, y + h, r);
  ctx.arcTo(x + w, y + h, x, y + h, r);
  ctx.arcTo(x, y + h, x, y, r);
  ctx.arcTo(x, y, x + w, y, r);
  ctx.closePath();
}

// ---------- 渲染 ----------
function render() {
  const scrollEl = document.querySelector('.app > .scroll');
  const prevScroll = scrollEl ? scrollEl.scrollTop : 0;
  const prevView = app.view;
  if (app.view === 'home') root.innerHTML = homeHTML();
  else if (app.view === 'identity') root.innerHTML = identityHTML();
  else if (app.view === 'build') root.innerHTML = buildHTML();
  else if (app.view === 'game') root.innerHTML = gameHTML();
  else if (app.view === 'career') root.innerHTML = careerHTML();
  else if (app.view === 'summary') root.innerHTML = summaryHTML();
  else if (app.view === 'archive') root.innerHTML = archiveHTML();
  else if (app.view === 'archive-detail') root.innerHTML = archiveDetailHTML();
  else if (app.view === 'gallery') root.innerHTML = galleryHTML();
  else if (app.view === 'upgrade') root.innerHTML = upgradeHTML();
  else if (app.view === 'offseason') root.innerHTML = offseasonHTML();
  if (app.view === prevView) {
    requestAnimationFrame(() => {
      const el = document.querySelector('.app > .scroll');
      if (el) el.scrollTop = prevScroll;
    });
  }
}

// ---------- 事件 ----------
window.BL = {
  setMode(m) { app.mode = m; render(); },
  start() {
    app.seed = null;
    app.identity = { ...app.identity, name: '', number: String(8 + Math.floor(Math.random() * 20)), domesticDream: '', foreignDream: '' };
    app.view = 'identity';
    render();
  },
  resume() {
    if (!app.seed) {
      // 找最近一次的存档
      const key = Object.keys(localStorage).filter(k => k.startsWith('bl-save:')).sort().pop();
      if (key) app.seed = key.slice(8);
    }
    if (!app.seed) { toast('没有可继续的存档'); return; }
    const st = E.loadState(app.seed);
    if (!st) { toast('存档已清理'); return; }
    app.state = st;
    app.archived = false;
    app.view = st.phase === 'summary' ? 'summary' : 'career';
    app.receipt = false;
    app.pendingBanner = false;
    app.lastBanner = null;
    // 赛季进行中：直接显示赛季页（careerHTML 处理）
    if (st.phase === 'career' && st.season && !st.season.done) {
      render();
      return;
    }
    // 季后赛进行中
    if (st.phase === 'career' && st.playoffs && !st.playoffs.done) {
      render();
      return;
    }
    // 若停在季后赛关键战前，恢复该赛季的 banner 以保留模拟入口
    if (st.phase === 'career' && st.pendingGame && st.pendingGame.type === 'playoff_key' && st.pendingGame.seasonIndex != null) {
      app.lastBanner = st.seasons[st.pendingGame.seasonIndex] || null;
      render();
      return;
    }
    if (st.phase === 'career' && !st.currentEvent) {
      BL.tick();
      return;
    }
    render();
  },
  backHome() { app.view = 'home'; app.modal = null; render(); },
  openArchive() { app.view = 'archive'; render(); },
  openGallery() { app.view = 'gallery'; render(); },
  openUpdates() { app.modal = { type: 'updates' }; render(); },
  closeModal() { app.modal = null; render(); },
  setInvite(v) { app.invite = v; },
  useInvite() {
    const code = app.invite.trim();
    if (!code) { toast('输入一个编号'); return; }
    app.seed = code;
    const st = E.loadState(code);
    if (st) {
      app.state = st;
      app.archived = false;
      app.view = st.phase === 'summary' ? 'summary' : 'career';
      render();
      return;
    }
    app.view = 'identity';
    render();
  },
  setName(v) { app.identity.name = v; },
  setNationality(c) { app.identity.nationality = c; app.identity.domesticDream = ''; render(); },
  setPosition(p) { app.identity.position = p; render(); },
  setHand(h) { app.identity.hand = h; render(); },
  setNumber(v) {
    let n = parseInt(v, 10);
    if (isNaN(n)) n = 0;
    app.identity.number = String(Math.max(0, Math.min(99, n)));
  },
  setDream(kind, val) {
    if (kind === 'domestic') app.identity.domesticDream = val;
    else app.identity.foreignDream = val;
  },
  // ---------- 建球员 ----------
  backBuild() { app.view = 'identity'; app.build = null; render(); },
  async spinTeam() {
    if (!app.poolLoaded) { toast('球员池加载中…'); return; }
    const b = app.build;
    if (!b) return;
    // 已有球队时不允许重抽（锁定属性后自动进入下一队），只能换球员卡
    if (b.currentTeam) {
      toast('先锁定一项属性，才能抽下一支球队');
      return;
    }
    b.currentTeam = randomTeam();
    b.drawn = drawPlayers(b.currentTeam, 5);
    b.selectedPlayer = null;
    render();
  },
  rerollPlayers() {
    const b = app.build;
    if (!b || !b.currentTeam) { toast('先抽取一支球队'); return; }
    if (b.selectedPlayer) { toast('已选中球员，先锁定属性'); return; }
    if (b.rerollsLeft <= 0) { toast('换人次数用完了'); return; }
    b.rerollsLeft -= 1;
    b.drawn = drawPlayers(b.currentTeam, 5);
    b.selectedPlayer = null;
    render();
  },
  pickBuildPlayer(i) {
    const b = app.build;
    if (!b || !b.drawn[i]) return;
    b.selectedPlayer = b.drawn[i];
    render();
  },
  lockIt(key) {
    const b = app.build;
    if (!b || !b.selectedPlayer) { toast('先选一名球员'); return; }
    if (b.attrs[key]) return;
    lockAttr(b, key);
    if (b.step >= ATTR_LIST.length) {
      app.buildResult = reveal(b);
    }
    render();
  },
  startCareer() {
    const id = app.identity;
    const rv = app.buildResult;
    if (!rv) return;
    const seed = app.seed || E.genSeed();
    const st = E.newGame({
      seed,
      mode: app.mode,
      name: id.name.trim(),
      nationality: id.nationality,
      position: id.position,
      hand: id.hand,
      number: parseInt(id.number, 10) || 8,
      domesticDreamTeamId: id.domesticDream || null,
      foreignDreamTeamId: id.foreignDream || null,
      built: rv,
    });
    app.seed = seed;
    app.state = st;
    app.archived = false;
    app.receipt = false;
    app.pendingBanner = false;
    app.lastBanner = null;
    app.view = 'career';
    E.saveState(st);
    BL.tick();
  },
  // ---------- 单场模拟 ----------
  enterGame() {
    const st = app.state;
    if (!st) return;
    const pg = st.pendingGame;
    if (!pg) { toast('没有可模拟的比赛'); return; }
    // showdown 事件优先；否则 playoff_key
    const kind = st.currentEvent && st.currentEvent.type === 'showdown'
      ? st.currentEvent.showdownKey
      : (pg.type || 'playoff_key');
    app.gameCtx = E.makeGameContext(st, kind);
    app.gameCtx.kind = kind;
    app.game = null;
    app.gameStep = 0;
    app.gameView = 'pre';
    app.gameSpeed = 4;
    app.gameTimer = null;
    app.gameQ = 1;
    app.view = 'game';
    render();
  },
  playGame() {
    const ctx = app.gameCtx;
    if (!ctx) return;
    const st = app.state;
    // 准备名单
    const pool = app.pool || {};
    const homePool = pool[ctx.homeTeam.abbr.toUpperCase()] || null;
    const awayPool = pool[ctx.awayTeam.abbr.toUpperCase()] || null;
    const homeNames = surnamesFor(ctx.homeTeam);
    const awayNames = surnamesFor(ctx.awayTeam);
    const homeRoster = makeRoster(ctx.homeTeam, homeNames, homePool, { total: 10 });
    const awayRoster = makeRoster(ctx.awayTeam, awayNames, awayPool, { total: 10 });
    const playerPos = POSITIONS[st.player.position]?.en || 'SF';
    const g = simulateGame(ctx.homeTeam, ctx.awayTeam, homeRoster, awayRoster, {
      isPlayoff: ctx.kind !== 'last_shot' && ctx.kind !== 'free_throw',
      seed: ctx.seed,
      myPlayer: {
        name: st.player.name,
        pos: playerPos,
        overall: st.player.overall,
        attrs: st.player.attrs || fallbackAttrs(st.player.overall),
      },
    });
    g.kind = ctx.kind;
    g.stage = ctx.stage;
    g.label = ctx.label;
    g.awayName = ctx.awayTeam.zh;
    g.tournamentAge = ctx.tournamentAge != null ? ctx.tournamentAge : (st.pendingGame ? st.pendingGame.tournamentAge : null);
    app.game = g;
    app.gameStep = 0;
    app.gameView = 'play';
    app.gameTimer = setInterval(BL.gameTick, 80);
    render();
  },
  gameTick() {
    if (!app.game) { clearInterval(app.gameTimer); return; }
    const speed = app.gameSpeed || 4;
    app.gameStep = Math.min(app.gameStep + speed, app.game.log.length);
    const cur = app.game.scoreSnap[app.gameStep - 1] || [0, 0];
    // 简单节次推断（共4节+加时）
    const totalPlays = app.game.log.length;
    const pct = app.gameStep / totalPlays;
    app.gameQ = Math.min(4 + (app.game.ot || 0), 1 + Math.floor(pct * (4 + (app.game.ot || 0))));
    if (app.gameStep >= app.game.log.length) {
      clearInterval(app.gameTimer);
      app.gameTimer = null;
    }
    const el = document.getElementById('root');
    if (el) { el.innerHTML = gamePlayHTML(); el.querySelector('.pbl-list')?.scrollTo(0, 1e9); }
  },
  gameFaster() {
    app.gameSpeed = Math.min(32, (app.gameSpeed || 4) * 2);
    render();
  },
  gamePause() {
    if (app.gameTimer) { clearInterval(app.gameTimer); app.gameTimer = null; }
    else { app.gameTimer = setInterval(BL.gameTick, 80); }
  },
  gameReplay() {
    if (app.gameTimer) { clearInterval(app.gameTimer); app.gameTimer = null; }
    app.gameStep = 0;
    app.gameView = 'play';
    app.gameTimer = setInterval(BL.gameTick, 80);
    render();
  },
  gameNext() {
    app.gameView = 'result';
    render();
  },
  skipGame() {
    // 跳过：直接按原概率判定（如果有关键事件就交给 choose；否则就按原赛果继续）
    const st = app.state;
    if (st && st.currentEvent && st.currentEvent.type === 'showdown') {
      app.game = null; app.gameView = null; app.gameCtx = null;
      app.view = 'career';
      render();
      return; // 交给玩家在事件卡里选选项判定
    }
    if (st && st.pendingGame) st.pendingGame = null;
    app.game = null; app.gameView = null; app.gameCtx = null;
    app.view = 'career';
    E.saveState(st);
    render();
  },
  finishGame() {
    if (app.gameTimer) { clearInterval(app.gameTimer); app.gameTimer = null; }
    const st = app.state;
    const g = app.game;
    if (st && g) {
      E.applyGameResult(st, g);
      E.saveState(st);
    }
    app.game = null;
    app.gameView = null;
    app.gameCtx = null;
    app.view = 'career';
    app.pendingBanner = false;
    app.receipt = false;
    render();
  },
  confirmIdentity() {
    const id = app.identity;
    if (!id.name.trim()) { toast('先给自己起个名字'); return; }
    // 建档 → 进入"抽取属性"建球员流程
    app.view = 'build';
    app.build = createBuildState(id.position);
    render();
  },
  tick() {
    if (!app.state) return;
    // 玩家直接推进而未模拟季后赛关键战时，视为按原赛果跳过
    if (app.state.pendingGame && app.state.pendingGame.type === 'playoff_key') {
      app.state.pendingGame = null;
    }
    const { state, screen, snapshot } = E.step(app.state);
    app.state = state;
    app.receipt = false;
    if (screen === 'banner') {
      app.lastBanner = snapshot;
      // 若这个横幅后面还跟着事件，先显示横幅，点一下再进事件
      app.pendingBanner = !!state.currentEvent;
    }
    if (screen === 'summary') { app.view = 'summary'; app.archived = false; }
    E.saveState(state);
    render();
  },
  dismissSeasonSummary() {
    // 赛季总结关闭后：若在职业联赛且 NBA，展示赛季后交易/自由市场 + 明星退役
    if (app.state && app.state.currentTeamId && !app.state.season) {
      const team = TEAMS[app.state.currentTeamId];
      if (team && team.league === 'nba') {
        const trades = E.seasonOffseason(app.state);
        const retirements = E.starRetirements(app.state);
        app.offseason = { trades: trades ? trades.trades : [], retirements };
        if (app.offseason.trades.length || app.offseason.retirements.length) {
          app.view = 'offseason';
          app.seasonSummary = null;
          E.saveState(app.state);
          render();
          return;
        }
      }
    }
    app.seasonSummary = null;
    render();
  },
  dismissOffseason() {
    app.offseason = null;
    app.view = 'career';
    render();
  },
  tryNBAJump() {
    if (!app.state) return;
    const res = E.tryNBAJump(app.state);
    if (!res.ok) {
      if (res.reason === 'chance_failed') toast('冲击 NBA 失败，继续努力');
      else toast(res.reason || '失败');
    } else {
      toast(`🏀 成功！加盟 ${res.team.zh}`);
      // 刷新总结的 NBA 状态
      app.seasonSummary.nbaJump = E.nbaJumpInfo(app.state);
    }
    E.saveState(app.state);
    render();
  },
  viewGameBox(idx) {
    if (!app.state || !app.state.season) return;
    const game = app.state.season.games[idx];
    if (!game) return;
    const teamZh = app.state.season.kind === 'ncaa'
      ? (NCAA_TEAMS[app.state.season.teamId]?.zh || '')
      : (TEAMS[app.state.season.teamId]?.zh || '');
    app.modal = { type: 'gamebox', html: gameBoxHTML(game, teamZh) };
    render();
  },
  viewMate(i) {
    if (!app.state) return;
    const mates = E.getMyRoster(app.state).filter(p => !p.isMe);
    const mate = mates[i];
    if (!mate) return;
    app.modal = { type: 'mate', html: mateDetailHTML(mate) };
    render();
  },
  openUpgrade() {
    app.view = 'upgrade';
    render();
  },
  closeUpgrade() {
    app.view = 'career';
    app.seasonSummary = null;
    render();
  },
  upgradeAttr(key) {
    if (!app.state) return;
    const res = E.upgradeAttr(app.state, key, 1);
    if (!res.ok) { toast(res.reason); return; }
    E.saveState(app.state);
    render();
  },
  // ---------- 逐场赛季 ----------
  simGames(n) {
    if (!app.state || !app.state.season) return;
    E.simNextGames(app.state, n);
    // 赛季打完则推进
    if (app.state.season && app.state.season.done) {
      BL.advanceAfterRegularSeason();
      return;
    }
    E.saveState(app.state);
    render();
  },
  simAll() {
    if (!app.state || !app.state.season) return;
    const remain = app.state.season.totalGames - app.state.season.played;
    E.simNextGames(app.state, remain);
    if (app.state.season && app.state.season.done) {
      BL.advanceAfterRegularSeason();
      return;
    }
    E.saveState(app.state);
    render();
  },
  // 常规赛打完：让 step 决定进季后赛还是直接结算
  advanceAfterRegularSeason() {
    if (!app.state) return;
    const { state, screen, snapshot } = E.step(app.state);
    app.state = state;
    if (screen === 'playoffs') {
      E.saveState(state);
      render();
      return;
    }
    // 直接结算（未进季后赛）
    if (state.lastSeasonSummary) app.seasonSummary = state.lastSeasonSummary;
    if (screen === 'banner') {
      app.lastBanner = snapshot;
      app.pendingBanner = !!state.currentEvent;
    }
    if (screen === 'summary') { app.view = 'summary'; app.archived = false; }
    E.saveState(state);
    render();
  },
  // ---------- 季后赛 ----------
  simPlayoff(n) {
    if (!app.state || !app.state.playoffs) return;
    E.simPlayoffGames(app.state, n);
    if (app.state.playoffs && app.state.playoffs.done) {
      BL.finishPlayoffs();
      return;
    }
    E.saveState(app.state);
    render();
  },
  simPlayoffAll() {
    if (!app.state || !app.state.playoffs) return;
    E.simPlayoffGames(app.state, 7);
    if (app.state.playoffs && app.state.playoffs.done) {
      BL.finishPlayoffs();
      return;
    }
    E.saveState(app.state);
    render();
  },
  requestTrade() {
    if (!app.state) return;
    const res = E.requestTrade(app.state);
    if (!res.ok) {
      if (res.reason === 'chance') toast(`交易未能达成（成功率约 ${res.pct}%）`);
      else toast(res.reason);
    } else {
      toast(`🔄 交易成功！你被送往 ${res.team.zh}`);
    }
    E.saveState(app.state);
    render();
  },
  finishPlayoffs() {
    if (!app.state) return;
    app.receipt = false;
    app.pendingBanner = false;
    app.lastBanner = null;
    const { state, screen, snapshot } = E.step(app.state);
    app.state = state;
    if (state.lastSeasonSummary) app.seasonSummary = state.lastSeasonSummary;
    if (screen === 'banner') {
      app.lastBanner = snapshot;
      app.pendingBanner = !!state.currentEvent;
    }
    if (screen === 'summary') { app.view = 'summary'; app.archived = false; }
    E.saveState(state);
    render();
  },
  finishSeasonNow() {
    if (!app.state) return;
    // 赛季已打完：清掉 pending banner，调用 step 完成结算
    app.receipt = false;
    app.pendingBanner = false;
    app.lastBanner = null;
    const { state, screen, snapshot } = E.step(app.state);
    app.state = state;
    // 展示赛季总结
    if (state.lastSeasonSummary) {
      app.seasonSummary = state.lastSeasonSummary;
    }
    if (screen === 'banner') {
      app.lastBanner = snapshot;
      app.pendingBanner = !!state.currentEvent;
    }
    if (screen === 'summary') { app.view = 'summary'; app.archived = false; }
    E.saveState(state);
    render();
  },
  choose(optionId) {
    if (!app.state || !app.state.currentEvent) return;
    if (navigator.vibrate) navigator.vibrate(12);
    const res = E.decide(app.state, optionId);
    const state = res.state;
    app.state = state;
    app.lastBanner = null;
    app.pendingBanner = false;
    if (res.screen === 'summary') {
      app.view = 'summary';
      app.archived = false;
      app.receipt = false;
    } else {
      app.receipt = true;
    }
    E.saveState(state);
    render();
  },
  next() {
    if (app.pendingBanner) {
      app.pendingBanner = false;
      render();
    } else if (app.state && app.state.currentEvent) {
      app.receipt = false;
      render();
    } else {
      BL.tick();
    }
  },
  replay() {
    app.state = null;
    app.seed = null;
    app.modal = null;
    app.shareDataUrl = null;
    app.view = 'home';
    render();
  },
  copyCode() {
    const sum = app.state ? E.buildSummary(app.state) : app.archiveDetail;
    const code = sum?.seed || app.seed || '';
    const text = `${APP_TITLE}｜${sum?.player.name || ''} ${sum?.player.positionEn || ''}｜巅峰 ${sum?.maxOverall || '?'}｜总收入 ${sum ? E.fmtMoney(sum.totalIncome) : ''}｜编号 ${code}`;
    if (navigator.clipboard?.writeText) {
      navigator.clipboard.writeText(text).then(() => toast('已复制')).catch(() => fallbackCopy(text));
    } else fallbackCopy(text);
  },
  openShare() {
    app.modal = { type: 'share' };
    app.shareDataUrl = null;
    render();
    setTimeout(() => {
      if (app.modal?.type === 'share' && app.state) {
        drawShare(E.buildSummary(app.state));
        render();
      }
    }, 30);
  },
  downloadShare() {
    if (!app.shareDataUrl) return;
    const a = document.createElement('a');
    a.href = app.shareDataUrl;
    a.download = `${app.state?.player?.name || '生涯'}-生涯战绩卡.png`;
    a.click();
  },
  viewArchive(i) {
    const list = E.loadArchive();
    app.archiveDetail = list[i];
    app.view = 'archive-detail';
    render();
  },
  backArchive() { app.view = 'archive'; render(); },
};

// ---------- 休赛期（赛季后交易 + 明星退役） ----------
function offseasonHTML() {
  const o = app.offseason;
  if (!o) { app.view = 'career'; render(); return ''; }
  const s = app.state;
  const team = s.currentTeamId ? TEAMS[s.currentTeamId] : null;
  const tradesHTML = (o.trades || []).map(t => `
    <div class="trade-item notable">
      <span class="ti-text">${esc(t.teamA)} ↔ ${esc(t.teamB)}</span>
      <span class="ti-detail">${esc(t.fromAToB)}（${t.ovrA}）↔ ${esc(t.fromBToA)}（${t.ovrB}）</span>
    </div>`).join('') || '<div class="empty">休赛期暂无重磅交易</div>';
  const retHTML = (o.retirements || []).map(r => `
    <div class="retire-item">
      <span class="ri-name">${esc(r.name)}</span>
      <span class="ri-ovr">OVR ${r.ovr}</span>
      <span class="ri-text">宣布退役，告别赛场</span>
    </div>`).join('') || '<div class="empty">没有明星退役</div>';
  return shell(`
    <div class="page-head">
      <h2>🏖️ 休赛期</h2>
    </div>
    <div class="scroll">
      <div class="offseason-hero">
        <div class="oh-team">${team ? esc(team.zh) : ''}</div>
        <div class="oh-sub">赛季结束 · 交易窗口与退役公告</div>
      </div>
      <div class="label" style="margin-top:14px">📢 休赛期交易</div>
      <div class="trade-list">${tradesHTML}</div>
      <div class="label" style="margin-top:14px">👋 明星退役</div>
      <div class="retire-list">${retHTML}</div>
      ${(o.rookies || []).length ? `
      <div class="label" style="margin-top:14px">🎓 选秀新秀</div>
      <div class="rookie-list">
        ${o.rookies.map(r => `
          <div class="rookie-item ${r.isStar ? 'star' : ''}">
            <span class="rk-pick">#${r.pick}</span>
            <span class="rk-name">${esc(r.name)}</span>
            <span class="rk-pos">${r.pos}</span>
            <span class="rk-ovr num">${r.ovr}</span>
            ${r.isStar ? '<span class="rk-star">🌟 天才</span>' : ''}
          </div>`).join('')}
      </div>` : ''}
      <div style="height:14px"></div>
      <button class="btn btn-primary btn-lg btn-block" onclick="BL.dismissOffseason()">进入新赛季 →</button>
      <div style="height:24px"></div>
    </div>
  `);
}

function fallbackCopy(text) {
  const ta = document.createElement('textarea');
  ta.value = text;
  ta.setAttribute('readonly', '');
  ta.style.position = 'fixed';
  ta.style.top = '-9999px';
  document.body.appendChild(ta);
  ta.select();
  try { document.execCommand('copy'); toast('已复制'); } catch { toast('复制失败'); }
  document.body.removeChild(ta);
}

// 键盘继续：空格/回车轻触继续（赛季页不自动跳，需玩家手动模拟比赛）
document.addEventListener('keydown', (e) => {
  if ((e.key === ' ' || e.key === 'Enter') && app.view === 'career' && !app.modal) {
    if (app.state?.season && !app.state.season.done) return;
    e.preventDefault();
    if (app.state?.currentEvent && !app.receipt) return;
    BL.next();
  }
});

render();

// 启动时预加载球员池（供建球员与单场模拟）
window.__poolDebug = { state: 'loading' };
loadPool().then((pool) => {
  app.pool = pool;
  app.poolLoaded = true;
  E.setNbaPool(pool);
  window.__poolDebug = { state: 'loaded', teams: Object.keys(pool || {}).length };
}).catch((e) => {
  app.poolLoaded = false;
  window.__poolDebug = { state: 'failed', err: String(e && e.message || e) };
});

// 调试钩子（供自动化测试使用）
window.__testState = () => ({
  view: app.view,
  receipt: app.receipt,
  pendingBanner: app.pendingBanner,
  lastBanner: !!app.lastBanner,
  phase: app.state ? app.state.phase : null,
  stage: app.state ? app.state.stage : null,
  age: app.state ? app.state.player.age : null,
  currentEvent: app.state && app.state.currentEvent ? app.state.currentEvent.type : null,
  eventOptions: app.state && app.state.currentEvent ? app.state.currentEvent.options.length : 0,
  lastOutcome: app.state && app.state.lastEventOutcome ? app.state.lastEventOutcome.text : null,
});
  })();
})();

/* end of bundle */